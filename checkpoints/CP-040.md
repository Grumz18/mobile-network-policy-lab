# CP-040

## CHECKPOINT_ID
CP-040

## TITLE
Define the smallest bounded post-focus resumed-task verification probe

## CURRENT_STATE
CP-039 is complete.
The cleared `libcore` metadata bridge remains only an already-validated prerequisite from CP-014.
The persisted `android/sing-box` baseline remains the CP-017 prerequisite on branch `cp017-local-baseline` at `aed32ee3066cdbc7d471e3e0415c5134088962df`.

CP-039 execution confirms bounded focus/window verification success with required lines:
- `mCurrentFocus=... moe.nb4a.debug/io.nekohasekai.sagernet.ui.MainActivity`
- `EXIT_CODE: 0`

The first exact downstream surface after successful focus/window verification is bounded resumed-task verification.

## TARGET_STATE
The repository has a bounded checkpoint definition for probing only the first exact downstream surface after successful CP-039 focus/window verification.

After CP-040 execution, a future session must be able to answer:
- whether one bounded resumed-task probe confirms `moe.nb4a.debug` is resumed with explicit task identity
- what exact device/package/process/focus prerequisites were true before probe entry
- what exact first meaningful outcome appears when that single probe is entered
- whether the first outcome is a prerequisite failure, resumed-task-check failure, or resumed-task success
- how resumed-task verification is explicitly separated from UI interaction, network activity, and runtime debugging

CP-040 does not perform UI interaction.
CP-040 does not perform network actions.
CP-040 does not perform runtime debugging.
CP-040 does not implement product features.

## IN_SCOPE
- identify the first exact post-focus surface as bounded resumed-task verification
- define one bounded resumed-task probe:
  - `& "$env:ANDROID_HOME\\platform-tools\\adb.exe" -s <serial> shell "dumpsys activity activities | grep -m 1 -E 'topResumedActivity=.* t[0-9]+'"`
- define exact prerequisite checks before probe entry:
  - `android/sing-box` remains on branch `cp017-local-baseline`
  - `android/sing-box` `HEAD` remains `aed32ee3066cdbc7d471e3e0415c5134088962df`
  - CP-039 execution artifact exists:
    - `docs/android/ANDROID_POST_FOCUS_BOUNDARY_CORRECTION.md`
  - CP-039 prerequisite/probe evidence exists:
    - `docs/android/evidence/cp039_prereq_checks.log`
    - `docs/android/evidence/cp039_focus_probe.clean.log`
  - CP-038/CP-037/CP-036/CP-035 continuity artifacts exist:
    - `docs/android/ANDROID_POST_FOREGROUND_STATE_CONTINUATION.md`
    - `docs/android/ANDROID_POST_PROCESS_VERIFICATION_CONTINUATION.md`
    - `docs/android/ANDROID_POST_LAUNCH_PROCESS_VERIFICATION.md`
    - `docs/android/ANDROID_POST_INSTALL_VERIFICATION_CONTINUATION.md`
  - target APK/output identity remains available:
    - `android/fork/app/build/outputs/apk/oss/debug/output-metadata.json`
    - `android/fork/app/build/outputs/apk/oss/debug/NekoBox-1.4.2-x86_64-debug.apk`
  - package id expectation remains:
    - `applicationId` equals `moe.nb4a.debug`
  - Android SDK tool path is resolvable:
    - `"$env:ANDROID_HOME\\platform-tools\\adb.exe"` exists
    - or an equivalent explicitly captured adb path is resolvable
  - exactly one online adb target is available
  - target adb device ABI is `x86_64`
  - installed package check passes before resumed-task probe:
    - `adb shell pm path moe.nb4a.debug` returns a package path
  - process check passes before resumed-task probe:
    - `adb shell pidof moe.nb4a.debug` returns numeric PID output
  - focus continuity check passes before resumed-task probe:
    - `adb shell "dumpsys window | grep -m 1 -E 'mCurrentFocus|mFocusedApp'"` contains `moe.nb4a.debug` and `MainActivity`
- define exact expected resumed-task success signals:
  - probe output is non-empty
  - output contains `topResumedActivity=`
  - output contains `moe.nb4a.debug`
  - output contains `io.nekohasekai.sagernet.ui.MainActivity` or `.ui.MainActivity`
  - output contains task token matching `t[0-9]+`
  - `EXIT_CODE: 0`
- define exact out-of-scope actions that must remain unperformed:
  - no UI interaction commands (`adb shell input`, `adb shell monkey`, UI automation)
  - no network actions or traffic probes from device shell
  - no app relaunch commands (`adb shell am start*`)
  - no runtime-debugging capture (`adb logcat`, profiler, traces)
  - no broad diagnostics beyond the single bounded probe and defined fallbacks
  - no `assemble*` continuation
  - no release signing/distribution work
- define exact fallback evidence sources if direct shell output is incomplete:
  - direct shell transcript of the bounded resumed-task probe
  - captured clean transcript file for the same command
  - `adb devices -l` snapshot captured in the same run
  - fallback focus/window check:
    - `adb shell "dumpsys window | grep -m 1 -E 'mCurrentFocus|mFocusedApp'"`
  - fallback process continuity check:
    - `adb shell pidof moe.nb4a.debug`
  - fallback package continuity check:
    - `adb shell pm path moe.nb4a.debug`
- define exact cleanup expectations:
  - do not regenerate `libcore.aar`
  - do not create disposable gomobile/GOPATH workspaces
  - remove only temporary CP-040 scratch files if any are created outside `docs/android/evidence/`
  - retain canonical evidence files under `docs/android/evidence/`
  - do not uninstall package as part of CP-040
  - do not force-stop or kill app process as part of CP-040
  - leave upstream source trees unchanged
- create `docs/android/ANDROID_POST_FOCUS_VERIFICATION_CONTINUATION.md` during later CP-040 execution
- update `PROJECT_STATE.md` and `checkpoints/CP-040.md` during later CP-040 execution

## OUT_OF_SCOPE
- executing CP-040
- revisiting any previous build-chain checkpoints
- revisiting or re-solving the cleared `libcore` metadata bridge
- revisiting or re-testing the already-validated `android/sing-box` baseline except as a stated prerequisite
- modifying upstream source under `android/fork/`
- applying broad patches to build logic, dependency trees, or generated workspaces
- UI interaction or user-flow testing
- network actions from device shell
- runtime debugging, profiling, or diagnostic deep dives
- Android product feature implementation
- per-app routing implementation
- transport or proxy logic
- changes to `android/libneko/` or `android/sing-box/` baselines
- server artifacts or server implementation, except `PROJECT_STATE.md` for continuity
- CI/CD changes

## REQUIRED_ARTIFACTS
- `checkpoints/CP-040.md`
- `PROJECT_STATE.md` (updated for CP-040 definition state only)
- `docs/android/ANDROID_POST_FOCUS_VERIFICATION_CONTINUATION.md` (execution artifact for later CP-040 run)

## EXECUTION_NOTES
CP-040 is a resumed-task-verification definition checkpoint.
It must remain narrow and explicitly separate state verification from UI interaction, network activity, and runtime debugging.

Why the next surface is resumed-task verification:
- CP-039 already proved deterministic window focus ownership
- the smallest deterministic downstream gate is resumed activity with explicit task token (`tN`)
- one bounded `dumpsys activity activities` probe is sufficient for this gate

Execution priority for later CP-040 run:
1. verify all CP-040 prerequisites exactly
2. run the single bounded resumed-task probe once
3. capture first exact meaningful outcome and stop
4. do not continue into UI interaction, network activity, or runtime-debugging workflows

First meaningful outcome rules for CP-040:
- if prerequisite checks fail, the first failed prerequisite line is the checkpoint outcome
- if resumed-task probe runs but does not show required resumed-task/package/activity signal, the first exact failure line is the checkpoint outcome
- if resumed-task probe returns required resumed-task/package/activity signal, success signal lines are the checkpoint outcome
- stop immediately once one of those outcomes is captured

Toolchain and environment bounds for later CP-040 run:
- preserve persisted `android/sing-box` baseline at `cp017-local-baseline` / `aed32ee3066cdbc7d471e3e0415c5134088962df`
- preserve Android SDK/platform-tools baseline already used in CP-039
- do not intentionally upgrade Gradle/AGP/Kotlin/Java/Go/Android dependencies during probe
- if adb path/device identity differs from prior checkpoints, capture exact evidence and stop at first meaningful outcome

## COMPLETION_TEST
CP-040 is complete only if:
- `docs/android/ANDROID_POST_FOCUS_VERIFICATION_CONTINUATION.md` exists after execution
- the report confirms exact CP-040 prerequisites listed in `IN_SCOPE`
- the report documents exact bounded resumed-task probe command:
  - `& "$env:ANDROID_HOME\\platform-tools\\adb.exe" -s <serial> shell "dumpsys activity activities | grep -m 1 -E 'topResumedActivity=.* t[0-9]+'"`
- the report captures first exact outcome from that one probe and stops
- if probe succeeds, the report includes:
  - non-empty output line containing `topResumedActivity=`
  - output line containing `moe.nb4a.debug`
  - output line containing `io.nekohasekai.sagernet.ui.MainActivity` or `.ui.MainActivity`
  - output line containing task token `t[0-9]+`
  - `EXIT_CODE: 0`
- the report confirms out-of-scope actions in `IN_SCOPE` remain unperformed
- the report identifies fallback evidence used if direct shell output is incomplete
- the report confirms cleanup expectations were followed
- no upstream source files under `android/fork/` are modified
- no UI-interaction/network/runtime-debugging/full-assemble continuation is attempted
- `PROJECT_STATE.md` is updated after execution

## RESULT
complete

## WHAT_CHANGED
- CP-040 execution was performed in bounded scope after device recovery verification passed (`emulator-5554`, `device`, ABI `x86_64`).
- Exact prerequisites were revalidated and captured in:
  - `docs/android/evidence/cp040_prereq_checks.log`
  - `docs/android/evidence/cp040_adb_devices.log`
- The single bounded resumed-task probe was executed exactly once:
  - `adb shell "dumpsys activity activities | grep -m 1 -E 'topResumedActivity=.* t[0-9]+'"`
- First exact meaningful outcome captured:
  - `topResumedActivity=ActivityRecord{71196b7 u0 moe.nb4a.debug/io.nekohasekai.sagernet.ui.MainActivity t11}`
- Required success signals were satisfied:
  - non-empty probe output
  - `topResumedActivity=` present
  - `moe.nb4a.debug` present
  - `io.nekohasekai.sagernet.ui.MainActivity` present
  - task token matched `t[0-9]+` (`t11`)
  - `EXIT_CODE: 0`
- Probe transcript and outcome were captured in:
  - `docs/android/evidence/cp040_probe.log`
- Execution report was updated:
  - `docs/android/ANDROID_POST_FOCUS_VERIFICATION_CONTINUATION.md`
- Out-of-scope actions remained unperformed (no UI interaction, no network actions, no relaunch, no runtime-debugging capture, no feature implementation).
- Cleanup expectations remained satisfied (no `libcore` regeneration, no disposable gomobile/GOPATH workspaces, no upstream source modifications).
- `PROJECT_STATE.md` was updated to reflect CP-040 execution completion.

## REMAINING_GAPS
None within CP-040 scope.

## NEXT_CHECKPOINT
Define CP-041 as the next bounded post-resumed-task continuation definition, explicitly separated from UI interaction, network activity, runtime debugging, transport logic, and per-app routing implementation.

## HANDOFF
Current checkpoint status: complete
Safe next action: create CP-041 only for the next bounded post-resumed-task continuation definition
Artifacts created or updated:
- checkpoints/CP-040.md
- docs/android/ANDROID_POST_FOCUS_VERIFICATION_CONTINUATION.md
- docs/android/evidence/cp040_prereq_checks.log
- docs/android/evidence/cp040_adb_devices.log
- docs/android/evidence/cp040_probe.log
- PROJECT_STATE.md
Known risks:
- scope drift into UI/network/runtime/feature work if CP-041 is not strictly bounded
- device/process state drift between sessions
Resume from:
- checkpoints/CP-040.md
- docs/android/ANDROID_POST_FOCUS_BOUNDARY_CORRECTION.md
- docs/android/ANDROID_POST_FOCUS_VERIFICATION_CONTINUATION.md
- docs/android/evidence/cp040_prereq_checks.log
- docs/android/evidence/cp040_adb_devices.log
- docs/android/evidence/cp040_probe.log
- PROJECT_STATE.md
