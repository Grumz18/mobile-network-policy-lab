# CP-007

## CHECKPOINT_ID
CP-007

## TITLE
Execute fork intake — materialize upstream NekoBox snapshot into android/fork/

## CURRENT_STATE
CP-006 is complete.
The repository has a fully documented fork intake procedure (`docs/android/ANDROID_FORK_INTAKE.md`) and patch workflow (`docs/android/ANDROID_PATCH_WORKFLOW.md`).
The chosen intake method is manual snapshot (archive download and explicit commit).
The upstream source is `MatsuriDayo/NekoBoxForAndroid` on GitHub, primary branch `main`.
`android/fork/` exists but contains only `README.md` — no upstream code has been materialized.
`android/fork/UPSTREAM_TRACKING.md` does not yet exist.
No Android product features, per-app routing, or transport logic have been implemented.

## TARGET_STATE
`android/fork/` contains the full NekoBox source tree at a specific upstream commit.
`android/fork/UPSTREAM_TRACKING.md` records the exact upstream commit SHA, branch, snapshot date, and intake checkpoint (CP-007).
`android/fork/README.md` is updated to reflect the materialized state.
The fork intake verification script (`android/scripts/verify-fork-intake.ps1`) passes with the materialized fork detected.
No local patches have been applied — the fork content is a clean upstream snapshot.

## IN_SCOPE
- confirm the upstream repository URL and primary branch are accessible
- identify the target upstream commit SHA on the primary branch at execution time
- download the upstream source archive for that commit
- extract the archive contents into `android/fork/`
- remove temporary archive and extraction artifacts
- create `android/fork/UPSTREAM_TRACKING.md` with upstream commit SHA, branch, snapshot date, and checkpoint reference
- update `android/fork/README.md` to reflect the materialized post-intake state
- run the fork intake verification script and confirm it passes
- update `PROJECT_STATE.md` to reflect CP-007 completion

## OUT_OF_SCOPE
- modifying any upstream source code
- applying local patches (deferred to a later checkpoint)
- Android application code changes or Gradle module implementation
- per-app routing implementation
- transport or proxy logic
- server artifacts or server implementation
- CI/CD pipeline changes
- signing configuration
- emulator or device automation
- building the Android project

## REQUIRED_ARTIFACTS
- `android/fork/` populated with the upstream NekoBox source tree (clean snapshot, no local modifications)
- `android/fork/UPSTREAM_TRACKING.md` (created)
- `android/fork/README.md` (updated to reflect materialized state)
- `PROJECT_STATE.md` (updated)

## EXECUTION_NOTES
Follow the intake procedure defined in `docs/android/ANDROID_FORK_INTAKE.md` exactly.
The upstream repository URL is `https://github.com/MatsuriDayo/NekoBoxForAndroid`.
The primary branch is assumed to be `main`; if the repository uses a different default branch at execution time, document the actual branch used.
The target commit SHA must be recorded as a full 40-character hash.
The archive download uses the GitHub archive URL format: `https://github.com/MatsuriDayo/NekoBoxForAndroid/archive/<full-sha>.zip`.
After extraction, temporary files (`upstream-snapshot.zip`, `tmp-extract/`) must be removed before committing.
The `.gitignore` in `android/` should be reviewed to ensure it does not accidentally exclude upstream fork content that needs to be tracked.
This checkpoint produces a large file-count commit — this is expected and intentional for a fork snapshot.
If the download or extraction fails, mark the checkpoint as blocked with the exact error and do not leave partial state in `android/fork/`.

## COMPLETION_TEST
This checkpoint is complete only if:
- `android/fork/` contains the upstream NekoBox source tree (not just placeholder files)
- `android/fork/UPSTREAM_TRACKING.md` exists with the upstream commit SHA, branch name, snapshot date, and checkpoint reference (CP-007)
- `android/fork/README.md` reflects the materialized state
- no temporary archive or extraction artifacts remain in `android/fork/`
- no upstream source code has been modified
- no local patches have been applied
- `android/scripts/verify-fork-intake.ps1` passes (fork detected, tracking metadata present)
- `PROJECT_STATE.md` is updated
- no Android product features, per-app routing, or transport logic are introduced

## RESULT
complete

## WHAT_CHANGED
- Upstream repository confirmed accessible, not archived, default branch `main`.
- Target upstream commit identified: `5768494d8ae3c74a057bb6d46c0f8dc071b0d821` (latest on `main` at 2026-04-15).
- Source archive downloaded from GitHub archive URL and extracted into `android/fork/`.
- Temporary artifacts (`upstream-snapshot.zip`, `tmp-extract/`) removed.
- `android/fork/UPSTREAM_TRACKING.md` created with upstream commit SHA, branch, snapshot date, and checkpoint reference.
- `android/fork/README.md` updated to reflect the materialized post-intake state, referencing intake and patch workflow documents.
- `android/.gitignore` reviewed — no upstream tracked content is excluded (no `build/` directories exist in the upstream source tree).
- Fork intake verification script passed (18/18 checks, 0 failures, 2 expected warnings for materialized state).
- `PROJECT_STATE.md` updated to reflect CP-007 completion.
- `checkpoints/CP-007.md` updated with execution results.

Upstream source tree contents at intake:
- `app/` — Android application module
- `libcore/` — core library module
- `buildSrc/`, `buildScript/` — build support
- `gradle/` — Gradle wrapper
- `build.gradle.kts`, `settings.gradle.kts`, `repositories.gradle.kts` — Gradle configuration
- `gradle.properties`, `nb4a.properties` — project properties
- `gradlew`, `gradlew.bat` — Gradle wrapper scripts
- `run/` — run configuration
- `lint.xml` — lint configuration
- `release.keystore` — upstream release keystore (not used by this project)
- `AUTHORS`, `LICENSE` — upstream metadata

No upstream source code was modified. No local patches were applied.

## REMAINING_GAPS
No remaining gaps inside CP-007.
Note: the verification script emits 2 WARN-level messages after intake (UPSTREAM_TRACKING.md detected, unexpected files in fork/) — these are expected post-intake behavior from a script designed for pre-intake baseline checking. A future checkpoint may update the script to distinguish pre-intake and post-intake states.

## NEXT_CHECKPOINT
CP-008 - Initial Android build verification (confirm the materialized fork compiles or identify build prerequisites)

## HANDOFF
Current checkpoint status: complete
Safe next action: define CP-008 to verify whether the materialized fork compiles and to identify any missing build prerequisites (SDK, NDK, Go toolchain, submodules)
Artifacts created or updated:
- android/fork/ (populated with upstream NekoBox source tree)
- android/fork/UPSTREAM_TRACKING.md
- android/fork/README.md
- PROJECT_STATE.md
- checkpoints/CP-007.md
Known risks:
- the upstream source likely requires git submodules to be initialized (libcore depends on external repos) — build will fail without them
- upstream build may require specific Android SDK, NDK, and Go toolchain versions not yet documented
- the `release.keystore` from upstream is present but not usable for this project's builds
- verification script warnings are informational but could confuse a future session unfamiliar with the post-intake state
Resume from:
- PROJECT_STATE.md
- checkpoints/CP-007.md
