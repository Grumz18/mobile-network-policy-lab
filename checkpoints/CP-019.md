# CP-019

## CHECKPOINT_ID
CP-019

## TITLE
Define the smallest bounded post-Kotlin continuation probe

## CURRENT_STATE
CP-018 is complete.
The generated `libcore` metadata bridge is already repaired and validated in `docs/android/ANDROID_LIBCORE_METADATA_BRIDGE_REPAIR.md`.
That earlier checkpoint proved:
- the generated fallback `go.mod` can preserve `replace libcore`
- the generated fallback can preserve the local replacement mappings for `github.com/matsuridayo/libneko` and `github.com/sagernet/sing-box`
- the generated fallback can preserve the original `golang.org/x/mobile` baseline (`v0.0.0-20231108233038-35478a0c49da`)
- the exact bare `libcore` import failure from CP-013 is cleared

CP-017 then intentionally persisted the validated `android/sing-box` alignment in `docs/android/ANDROID_SINGBOX_BASELINE_PERSIST.md`.
That checkpoint proved:
- the local `android/sing-box` baseline is intentionally tracked on branch `cp017-local-baseline`
- that persisted baseline points to commit `aed32ee3066cdbc7d471e3e0415c5134088962df`
- the same bounded `android/fork/libcore/build.sh` path still proceeds through transient `android/fork/app/libs/libcore.aar` production under that persisted baseline

CP-018 then executed the first exact downstream app consumer probe in `docs/android/ANDROID_POST_LIBCORE_CONTINUATION.md`.
That checkpoint proved:
- the exact post-libcore handoff artifact remains `android/fork/app/libs/libcore.aar`
- the exact first downstream app consumer command is `.\gradlew.bat :app:compileOssDebugKotlin --stacktrace`
- the bounded Kotlin compile consumer surface succeeds under the persisted `android/sing-box` baseline
- no blocker appears at that Kotlin compile surface

The next downstream surface is now identifiable from the repository and from CP-018 evidence:
- `android/fork/app/src/main/java/` still contains real Java sources, including:
  - `android/fork/app/src/main/java/moe/matsuri/nb4a/SingBoxOptions.java`
  - `android/fork/app/src/main/java/io/nekohasekai/sagernet/database/SubscriptionBean.java`
  - `android/fork/app/src/main/java/io/nekohasekai/sagernet/fmt/AbstractBean.java`
- CP-018 left Kotlin/KSP outputs under:
  - `android/fork/app/build/tmp/kotlin-classes/ossDebug/`
  - `android/fork/app/build/generated/ksp/ossDebug/kotlin/`
- no Java compile outputs were observed after CP-018 under:
  - `android/fork/app/build/intermediates/javac/ossDebug/compileOssDebugJavaWithJavac/classes`
  - `android/fork/app/build/tmp/compileOssDebugJavaWithJavac`
  - `android/fork/app/build/intermediates/compile_app_classes_jar/ossDebug`
  - `android/fork/app/build/intermediates/project_dex_archive/ossDebug`
- the CP-018 Gradle daemon log showed `BUILD SUCCESSFUL` for the Kotlin probe but no `compileOssDebugJavaWithJavac` task evidence

No upstream source under `android/fork/` has been modified.
The default installed `gomobile-matsuri` and `gobind-matsuri` binaries remain unchanged.

## TARGET_STATE
The repository has a bounded checkpoint definition for probing only the first exact downstream build surface after successful `.\gradlew.bat :app:compileOssDebugKotlin --stacktrace`.

After CP-019 execution, a future session must be able to answer:
- whether `.\gradlew.bat :app:compileOssDebugJavaWithJavac --stacktrace` is the correct next exact downstream consumer surface after successful Kotlin compilation
- what exact prerequisite artifact and prerequisite baseline must still be true before entering that Java compile surface
- what exact first meaningful outcome appears when that single Java compile probe is entered
- whether the first new outcome is:
  - a task-graph failure before Java compilation starts
  - a Java compile failure inside `:app:compileOssDebugJavaWithJavac`
  - or a successful Java compile continuation signal
- how that probe is kept separate from broader build continuation such as class jar merge, dexing, packaging, or assemble work

CP-019 does not need to fix Java compile failures.
It must only define the smallest bounded continuation probe after successful Kotlin compilation and stop at the first exact downstream Java compile result.

## IN_SCOPE
- select the exact downstream build surface after successful `:app:compileOssDebugKotlin`
- document why `.\gradlew.bat :app:compileOssDebugJavaWithJavac --stacktrace` is the correct next bounded surface:
  - real Java source files remain under `android/fork/app/src/main/java/`
  - Kotlin/KSP outputs exist from CP-018
  - javac outputs do not yet exist
  - no class-jar, dex, packaging, or assemble outputs need to be touched to isolate the next outcome
- define the exact entry command for the probe from `android/fork/`:
  - `.\gradlew.bat :app:compileOssDebugJavaWithJavac --stacktrace`
- define the preconditions that must remain unchanged before the probe:
  - `android/sing-box` stays on branch `cp017-local-baseline`
  - `android/sing-box` stays at `aed32ee3066cdbc7d471e3e0415c5134088962df`
  - the cleared metadata bridge remains only an existing prerequisite reused through the bounded `libcore` path
  - transient `android/fork/app/libs/libcore.aar` is reproduced again before Gradle entry because CP-018 cleanup removed it
- define the exact evidence to capture during execution:
  - confirmation that the persisted `android/sing-box` prerequisite state is unchanged
  - the command and output that re-establish transient `android/fork/app/libs/libcore.aar`
  - the exact `.\gradlew.bat :app:compileOssDebugJavaWithJavac --stacktrace` invocation
  - the first task-graph failure before javac, if one appears
  - the first exact Java compile failure line, if javac starts and fails
  - or the exact success signal for `:app:compileOssDebugJavaWithJavac` plus evidence that javac outputs now exist
- define what counts as the first meaningful outcome:
  - first failing task before javac starts
  - first javac error if javac starts
  - or exact success of `:app:compileOssDebugJavaWithJavac`
- define toolchain and dependency drift bounds for that probe
- define rollback and cleanup expectations for transient `libcore.aar`, `android/fork/libcore/.build/`, and any disposable gomobile workspace recreated only to regenerate the transient AAR
- create `docs/android/ANDROID_POST_KOTLIN_CONTINUATION.md`
- update `PROJECT_STATE.md` and `checkpoints/CP-019.md` with execution results

## OUT_OF_SCOPE
- revisiting or re-solving the cleared `libcore` metadata bridge
- revisiting or re-testing the already-validated `android/sing-box` baseline except as a stated prerequisite
- modifying upstream source under `android/fork/`, including Gradle files, app code, Java sources, Kotlin sources, or `libcore/`
- broad patches to `gomobile-matsuri`, `gobind-matsuri`, Gradle wiring, generated workspace layout, or dependency trees
- broad Gradle or app build work beyond the single Java compile consumer surface
- `compileOssDebugKotlin` as a new probe surface, except if Gradle reruns it automatically as a prerequisite while entering `compileOssDebugJavaWithJavac`
- `assemble*`, `bundle*`, install, lint, test, class-jar merge, dexing, resource packaging, signing, or APK generation
- Android product features
- per-app routing implementation
- transport or proxy logic
- dependency upgrades or repairs outside the first observed Java compile continuation blocker
- changes to `android/libneko/` or `android/sing-box/` baselines
- server artifacts or server implementation, except `PROJECT_STATE.md` if needed for continuity
- CI/CD changes

## REQUIRED_ARTIFACTS
- `docs/android/ANDROID_POST_KOTLIN_CONTINUATION.md`
- `PROJECT_STATE.md` (updated after CP-019 execution)
- `checkpoints/CP-019.md` (updated after CP-019 execution)

## EXECUTION_NOTES
CP-019 is a continuation-definition checkpoint.
It must not turn into broader Gradle continuation or repair work.

Why the next surface is `:app:compileOssDebugJavaWithJavac`:
- CP-018 proved that Kotlin compilation is no longer the active downstream boundary
- real Java sources still remain in `android/fork/app/src/main/java/`
- CP-018 left Kotlin/KSP outputs but no javac outputs
- Java compilation is narrower than class jar merge, dexing, packaging, or any `assemble*` task

Execution priority for the later CP-019 run:
1. confirm the persisted `android/sing-box` baseline still matches branch `cp017-local-baseline` at `aed32ee3066cdbc7d471e3e0415c5134088962df`
2. rerun only the already-bounded `android/fork/libcore/build.sh` path until it installs transient `android/fork/app/libs/libcore.aar`
3. record that transient `android/fork/app/libs/libcore.aar` is again present before Gradle entry
4. enter Gradle exactly once with `.\gradlew.bat :app:compileOssDebugJavaWithJavac --stacktrace`
5. capture the first exact downstream outcome and stop
6. do not repair the observed blocker inside CP-019

Exact entry markers CP-019 must record during execution:
- artifact marker: `android/fork/app/libs/libcore.aar`
- command marker: `.\gradlew.bat :app:compileOssDebugJavaWithJavac --stacktrace`

First meaningful outcome rules for CP-019:
- if the Gradle task graph fails before `:app:compileOssDebugJavaWithJavac` starts, the first failing task is the checkpoint outcome
- if `:app:compileOssDebugJavaWithJavac` starts and emits compiler errors, the first exact javac error is the checkpoint outcome
- if `:app:compileOssDebugJavaWithJavac` succeeds, that success plus created javac output directories is the checkpoint outcome
- once one of those outcomes is captured conclusively, stop without continuing into class jar merge, dex, or assemble work

Toolchain and dependency drift bounds for CP-019 execution:
- preserve the persisted `android/sing-box` baseline on `cp017-local-baseline` at `aed32ee3066cdbc7d471e3e0415c5134088962df`
- preserve the exact disposable `gomobile-matsuri` source revision used in CP-014 through CP-018 (`17d6af34f6bd6d7e1e428e0c652c8b54a46bda4f`) if a disposable rebuild is still required to regenerate transient `libcore.aar`
- preserve the generated workspace `golang.org/x/mobile v0.0.0-20231108233038-35478a0c49da` baseline already validated in CP-014 through CP-018
- preserve the host generated-workspace Go baseline (`go1.23.6`) unless mere observation proves auto-selection cannot be avoided
- preserve the existing Gradle wrapper baseline (`8.10.2`)
- preserve the existing Android Gradle Plugin baseline (`8.8.1`)
- preserve the existing Kotlin and KSP baselines (`2.0.21` and `2.0.21-1.0.27`)
- preserve the existing JDK 17 and local SDK/NDK baseline already documented in project state
- do not intentionally upgrade Gradle plugins, Kotlin plugins, Java toolchains, Go dependencies, or Android dependencies during the probe
- if Gradle, Kotlin, Java, KSP, or Go auto-selects a different version, capture the exact version and trigger as evidence and do not treat that drift as the repair

Allowed probe depth:
- one transient `libcore.aar` regeneration path
- one exact Gradle Java compile-surface entry
- one captured first failure or success outcome
- if Gradle reruns upstream prerequisite tasks automatically while entering `compileOssDebugJavaWithJavac`, record them only as prerequisite activity and keep the checkpoint focused on the first new downstream outcome
- if `:app:compileOssDebugJavaWithJavac` succeeds, stop and name the next exact downstream task without continuing into it

Not allowed:
- rerunning metadata-bridge experimentation
- rerunning sing-box alignment experiments beyond confirming the persisted prerequisite state
- entering `assemble*`, `bundle*`, install, lint, test, dex, packaging, or APK publication work
- editing `android/fork/` to chase the first Java compile-side failure
- broad cache deletion or environment reconfiguration beyond what is required to reproduce the bounded entry command

Rollback expectations if the probe fails or is inconclusive:
- leave `android/sing-box` on its persisted CP-017 baseline unless the run itself changed it unexpectedly
- remove any disposable temporary gomobile source workspace created for CP-019
- remove any disposable temporary GOPATH or patched binaries created for CP-019
- remove `android/fork/libcore/.build/` after evidence capture unless the report explicitly preserves it for the first downstream failure evidence
- remove transient copied `android/fork/app/libs/libcore.aar` after evidence capture unless the report explicitly preserves it for the first downstream failure evidence
- leave the default installed `gomobile-matsuri` and `gobind-matsuri` binaries unchanged
- leave all upstream source trees unchanged
- stop without continuing into class jar merge, dexing, packaging, or broader build continuation
- document the exact stopping point and cleanup result in `docs/android/ANDROID_POST_KOTLIN_CONTINUATION.md`

## COMPLETION_TEST
CP-019 is complete only if:
- `docs/android/ANDROID_POST_KOTLIN_CONTINUATION.md` exists
- the report documents why `.\gradlew.bat :app:compileOssDebugJavaWithJavac --stacktrace` is the correct next exact downstream surface
- the report identifies `android/fork/app/libs/libcore.aar` as the prerequisite entry artifact before Gradle entry
- the report documents the exact Java-compile entry command `.\gradlew.bat :app:compileOssDebugJavaWithJavac --stacktrace`
- the report confirms the persisted `android/sing-box` prerequisite state before Gradle entry
- the report explicitly states that the cleared metadata bridge was not revisited as a new repair surface
- the report captures the first exact task-graph failure, first exact javac failure, or the exact success of `:app:compileOssDebugJavaWithJavac`
- the report explicitly separates Java-compile probing from broader Gradle continuation such as class jar merge, dexing, packaging, or assemble work
- the report documents the exact commands, exact outputs, and exact toolchain/dependency versions observed
- no upstream source files under `android/fork/` are modified
- no broad patches are applied
- no `assemble*`, `bundle*`, install, lint, test, dex, or APK build is attempted
- `PROJECT_STATE.md` is updated after execution

## RESULT
complete

## WHAT_CHANGED
- `docs/android/ANDROID_POST_KOTLIN_CONTINUATION.md` was created with the exact pre-probe Java print, prerequisite `android/sing-box` confirmation, transient `libcore.aar` reproduction command, exact Java-compile entry command, direct shell transcript, javac output evidence, and cleanup record.
- CP-019 was executed using the same disposable gomobile prerequisite path already validated by CP-014, CP-017, and CP-018, without reopening metadata-bridge repair as a new work surface.
- The first exact downstream Java compile consumer surface was execution-validated:
  - prerequisite artifact: `android/fork/app/libs/libcore.aar`
  - entry command: `.\gradlew.bat :app:compileOssDebugJavaWithJavac --stacktrace`
  - first meaningful downstream outcome: success, not a blocker

## REMAINING_GAPS
- No gaps remain inside CP-019.
- The next downstream surface after successful Java compilation remains intentionally unexecuted.

## NEXT_CHECKPOINT
Create CP-020 only to define the smallest bounded continuation checkpoint after successful `:app:compileOssDebugJavaWithJavac`, most likely targeting the first class-jar merge surface immediately downstream of javac, without broadening into dexing, packaging, or assemble work.

## HANDOFF
Current checkpoint status: complete
Safe next action: create CP-020 only, scoped to defining the first exact downstream surface after successful `:app:compileOssDebugJavaWithJavac`
Artifacts created or updated:
- checkpoints/CP-019.md
- docs/android/ANDROID_POST_KOTLIN_CONTINUATION.md
- PROJECT_STATE.md
Known risks:
- the next session may incorrectly jump from successful Java compilation into dexing, packaging, or `assemble*` instead of defining the next bounded surface
- the persisted `android/sing-box` baseline on `cp017-local-baseline` remains a hard prerequisite for continuity
- future Gradle probes may stop producing complete direct shell transcripts, in which case daemon-log evidence should be preserved again
Resume from:
- docs/android/ANDROID_POST_KOTLIN_CONTINUATION.md
- checkpoints/CP-019.md
- PROJECT_STATE.md
