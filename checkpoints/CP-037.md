# CP-037

## CHECKPOINT_ID
CP-037

## TITLE
Define the smallest bounded post-process foreground-state verification probe

## CURRENT_STATE
CP-036 is complete.
The cleared `libcore` metadata bridge remains only an already-validated prerequisite from CP-014.
The persisted `android/sing-box` baseline remains the CP-017 prerequisite on branch `cp017-local-baseline` at `aed32ee3066cdbc7d471e3e0415c5134088962df`.

CP-036 execution confirms bounded process-alive verification with required success lines:
- numeric `pidof` output for `moe.nb4a.debug`
- `EXIT_CODE: 0`

The first exact downstream surface after successful process verification is bounded foreground activity/state verification.

## TARGET_STATE
The repository has a bounded checkpoint definition for probing only the first exact downstream surface after successful CP-036 process verification.

After CP-037 execution, a future session must be able to answer:
- whether one bounded foreground-state probe confirms `moe.nb4a.debug` is the resumed/foreground activity owner
- what exact device/package/process prerequisites were true before probe entry
- what exact first meaningful outcome appears when that single probe is entered
- whether the first outcome is a prerequisite failure, state-check failure, or foreground-state success
- how state verification is explicitly separated from UI interaction and runtime debugging

CP-037 does not perform UI interaction.
CP-037 does not perform runtime debugging.
CP-037 does not implement product features.

## IN_SCOPE
- identify the first exact post-process surface as bounded foreground activity/state verification
- define one bounded state-verification probe:
  - `& "$env:ANDROID_HOME\\platform-tools\\adb.exe" -s <serial> shell "dumpsys activity activities | grep -m 1 -E 'mResumedActivity|topResumedActivity'"`
- define exact prerequisite checks before probe entry:
  - `android/sing-box` remains on branch `cp017-local-baseline`
  - `android/sing-box` `HEAD` remains `aed32ee3066cdbc7d471e3e0415c5134088962df`
  - CP-036 execution artifact exists:
    - `docs/android/ANDROID_POST_LAUNCH_PROCESS_VERIFICATION.md`
  - CP-036 prerequisite/probe evidence exists:
    - `docs/android/evidence/cp036_prereq_checks.log`
    - `docs/android/evidence/cp036_pidof_probe.clean.log`
  - target APK/output identity remains available:
    - `android/fork/app/build/outputs/apk/oss/debug/output-metadata.json`
    - `android/fork/app/build/outputs/apk/oss/debug/NekoBox-1.4.2-x86_64-debug.apk`
  - package id expectation remains:
    - `applicationId` equals `moe.nb4a.debug`
  - Android SDK tool path is resolvable:
    - `"$env:ANDROID_HOME\\platform-tools\\adb.exe"` exists (or equivalent resolved path explicitly captured)
  - exactly one online adb target is available
  - target adb device ABI is `x86_64`
  - installed package check passes before state probe:
    - `adb shell pm path moe.nb4a.debug` returns a package path
  - process check passes before state probe:
    - `adb shell pidof moe.nb4a.debug` returns numeric PID output
- define exact expected foreground-state success signals:
  - probe output is non-empty
  - probe output contains `moe.nb4a.debug`
  - probe output contains one exact activity identity signal:
    - `io.nekohasekai.sagernet.ui.MainActivity`
    - or `.ui.MainActivity`
  - `EXIT_CODE: 0`
- define exact out-of-scope actions that must remain unperformed:
  - no UI interaction commands (`adb shell input`, `adb shell monkey`, UI automation)
  - no network actions or traffic probes from device shell
  - no app relaunch commands (`adb shell am start*`)
  - no runtime-debugging capture (`adb logcat`, profiler, traces)
  - no broad diagnostics beyond the single bounded probe and defined fallbacks
  - no `assemble*` continuation
  - no release signing/distribution work
- define exact fallback evidence sources if direct shell output is incomplete:
  - direct shell transcript of the bounded `dumpsys activity activities` probe
  - captured clean transcript file for the same command
  - `adb devices -l` snapshot captured in the same run
  - fallback window-focus check:
    - `adb shell "dumpsys window windows | grep -m 1 -E 'mCurrentFocus|mFocusedApp'"`
  - fallback process continuity check:
    - `adb shell pidof moe.nb4a.debug`
  - fallback package continuity check:
    - `adb shell pm path moe.nb4a.debug`
- define exact cleanup expectations:
  - do not regenerate `libcore.aar`
  - do not create disposable gomobile/GOPATH workspaces
  - remove only temporary CP-037 scratch files if any are created outside `docs/android/evidence/`
  - retain canonical evidence files under `docs/android/evidence/`
  - do not uninstall package as part of CP-037
  - do not force-stop or kill app process as part of CP-037
  - leave upstream source trees unchanged
- create `docs/android/ANDROID_POST_PROCESS_VERIFICATION_CONTINUATION.md` during later CP-037 execution
- update `PROJECT_STATE.md` and `checkpoints/CP-037.md` during later CP-037 execution

## OUT_OF_SCOPE
- executing CP-037
- revisiting any previous build-chain checkpoints
- revisiting or re-solving the cleared `libcore` metadata bridge
- revisiting or re-testing the already-validated `android/sing-box` baseline except as a stated prerequisite
- modifying upstream source under `android/fork/`
- applying broad patches to build logic, dependency trees, or generated workspaces
- UI interaction or user-flow testing
- runtime debugging, profiling, or diagnostic deep dives
- network actions from device shell
- Android product feature implementation
- per-app routing implementation
- transport or proxy logic
- changes to `android/libneko/` or `android/sing-box/` baselines
- server artifacts or server implementation, except `PROJECT_STATE.md` for continuity
- CI/CD changes

## REQUIRED_ARTIFACTS
- `checkpoints/CP-037.md`
- `PROJECT_STATE.md` (updated for CP-037 definition state only)
- `docs/android/ANDROID_POST_PROCESS_VERIFICATION_CONTINUATION.md` (execution artifact for later CP-037 run)

## EXECUTION_NOTES
CP-037 is a foreground-state-verification definition checkpoint.
It must remain narrow and explicitly separate state verification from UI interaction and runtime debugging.

Why the next surface is foreground-state verification:
- CP-036 already proved process liveness
- the smallest deterministic downstream gate is whether the app owns resumed/foreground state
- one bounded `dumpsys activity activities` probe is sufficient for this gate

Execution priority for later CP-037 run:
1. verify all CP-037 prerequisites exactly
2. run the single bounded foreground-state probe once
3. capture first exact meaningful outcome and stop
4. do not continue into UI interaction or runtime-debugging workflows

First meaningful outcome rules for CP-037:
- if prerequisite checks fail, the first failed prerequisite line is the checkpoint outcome
- if state probe runs but does not show required app/activity signal, the first exact failure line is the checkpoint outcome
- if state probe returns required app/activity signal, success signal lines are the checkpoint outcome
- stop immediately once one of those outcomes is captured

Toolchain and environment bounds for later CP-037 run:
- preserve persisted `android/sing-box` baseline at `cp017-local-baseline` / `aed32ee3066cdbc7d471e3e0415c5134088962df`
- preserve Android SDK/platform-tools baseline already used in CP-036
- do not intentionally upgrade Gradle/AGP/Kotlin/Java/Go/Android dependencies during probe
- if adb path/device identity differs from prior checkpoints, capture exact evidence and stop at first meaningful outcome

## COMPLETION_TEST
CP-037 is complete only if:
- `docs/android/ANDROID_POST_PROCESS_VERIFICATION_CONTINUATION.md` exists after execution
- the report confirms exact CP-037 prerequisites listed in `IN_SCOPE`
- the report documents exact bounded probe command:
  - `& "$env:ANDROID_HOME\\platform-tools\\adb.exe" -s <serial> shell "dumpsys activity activities | grep -m 1 -E 'mResumedActivity|topResumedActivity'"`
- the report captures first exact outcome from that one probe and stops
- if probe succeeds, the report includes:
  - non-empty output line containing `moe.nb4a.debug`
  - output line containing `io.nekohasekai.sagernet.ui.MainActivity` or `.ui.MainActivity`
  - `EXIT_CODE: 0`
- the report confirms out-of-scope actions in `IN_SCOPE` remain unperformed
- the report identifies fallback evidence used if direct shell output is incomplete
- the report confirms cleanup expectations were followed
- no upstream source files under `android/fork/` are modified
- no UI-interaction/runtime-debugging/full-assemble continuation is attempted
- `PROJECT_STATE.md` is updated after execution

## RESULT
pending

## WHAT_CHANGED
- `checkpoints/CP-037.md` was authored to define the smallest bounded continuation after successful CP-036 process verification.
- The checkpoint now defines one bounded downstream probe as foreground activity/state verification only.
- `PROJECT_STATE.md` was updated so the next eligible work is executing CP-037 only.

## REMAINING_GAPS
- CP-037 has not been executed yet.
- `docs/android/ANDROID_POST_PROCESS_VERIFICATION_CONTINUATION.md` does not exist yet and must be created during CP-037 execution.
- Exact success or failure outcome of the bounded foreground-state probe is still unknown.

## NEXT_CHECKPOINT
Execute CP-037 only, exactly as defined, to run one bounded foreground-state verification probe after successful CP-036 process verification, without entering UI interaction, runtime debugging, network actions, or feature work.

## HANDOFF
Current checkpoint status: pending
Safe next action: execute CP-037 only using the single bounded foreground-state probe defined above
Artifacts created or updated:
- checkpoints/CP-037.md
- PROJECT_STATE.md
Known risks:
- scope will drift if UI interaction, runtime-debugging, network actions, or broader continuation starts before CP-037 execution is captured
- device state can drift between sessions; prerequisite device identity/ABI checks must pass before probe entry
- foreground state can drift quickly between sessions and must be captured as first meaningful outcome evidence
Resume from:
- checkpoints/CP-037.md
- PROJECT_STATE.md
- docs/android/ANDROID_POST_LAUNCH_PROCESS_VERIFICATION.md
