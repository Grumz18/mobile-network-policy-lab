# CP-005

## CHECKPOINT_ID
CP-005

## TITLE
Establish Android workspace skeleton and local build baseline

## CURRENT_STATE
CP-004 is complete.
The repository has a documented Android fork integration baseline with placeholder workspace boundaries.
The `android/` directory contains baseline documentation and verification helpers, but it does not yet contain a local build baseline or a concrete workspace skeleton for future Android implementation.
No Android product features have been implemented.
No per-app routing logic has been implemented.
No transport logic has been implemented.

## TARGET_STATE
The repository contains a minimal Android workspace skeleton and local build baseline.
After this checkpoint is executed, a future session should be able to identify:
- the reserved Android workspace layout for future code, local build files, and imported fork material
- the non-secret local build placeholders required before Android implementation begins
- the safe commands used to verify the local Android build baseline without building product code
- the distinction between local build preparation and actual Android feature work

## IN_SCOPE
- create the initial Android workspace skeleton directories and placeholder files
- create Android local build baseline documentation
- create non-secret Android local build template files
- add a safe Android local build verification script
- document the exact verification flow for the Android workspace skeleton

## OUT_OF_SCOPE
- Android application code
- per-app routing implementation
- transport or proxy logic
- fork acquisition or synchronization
- Gradle module implementation
- signing configuration
- server implementation
- CI/CD implementation

## REQUIRED_ARTIFACTS
- `docs/android/ANDROID_LOCAL_BUILD_BASELINE.md`
- `android/workspace/README.md`
- `android/build/README.md`
- `android/local.properties.example`
- `android/gradle.properties.example`
- `android/scripts/verify-local-build.ps1`

## EXECUTION_NOTES
This checkpoint should establish only the Android workspace skeleton and local build baseline.
It should not create Android source code, fetch fork content, or introduce Gradle modules.
If the exact local Android toolchain assumptions remain uncertain at execution time, document them explicitly in `docs/android/ANDROID_LOCAL_BUILD_BASELINE.md` before creating workflow-specific placeholders.
The local build verification script must remain non-destructive and must not trigger downloads, builds, or emulator actions.
The checkpoint should prepare the workspace for future implementation rather than starting product development.

## COMPLETION_TEST
This checkpoint is complete only if:
- `docs/android/ANDROID_LOCAL_BUILD_BASELINE.md` defines the local build assumptions, workspace skeleton boundaries, and verification flow
- `android/workspace/README.md` and `android/build/README.md` explain the responsibility of each Android workspace area
- `android/local.properties.example` and `android/gradle.properties.example` exist as non-secret local build templates
- `android/scripts/verify-local-build.ps1` exists and validates the Android local build baseline without building product code
- no Android application code, per-app routing logic, transport logic, or server implementation is introduced

## RESULT
complete

## WHAT_CHANGED
- `docs/android/ANDROID_LOCAL_BUILD_BASELINE.md` was created to define the Android local build assumptions, workspace skeleton boundaries, and verification flow.
- `android/workspace/README.md` and `android/build/README.md` were created to define the reserved Android workspace and build-support boundaries.
- `android/local.properties.example` and `android/gradle.properties.example` were created as non-secret local build template files.
- `android/scripts/verify-local-build.ps1` was created as a safe local verification script for the Android local build baseline.
- `PROJECT_STATE.md` was updated to reflect CP-005 completion and the new Android local build baseline.
- `checkpoints/CP-005.md` was updated with execution results, remaining gaps, and handoff data.

## REMAINING_GAPS
No remaining gaps inside CP-005.

## NEXT_CHECKPOINT
CP-006 - Define Android fork intake and patch workflow baseline

## HANDOFF
Current checkpoint status: complete
Safe next action: author CP-006 as the next bounded checkpoint for Android fork intake and patch workflow baseline
Artifacts created or updated:
- docs/android/ANDROID_LOCAL_BUILD_BASELINE.md
- android/workspace/README.md
- android/build/README.md
- android/local.properties.example
- android/gradle.properties.example
- android/scripts/verify-local-build.ps1
- PROJECT_STATE.md
- checkpoints/CP-005.md
Known risks:
- future work may prematurely commit to a fork intake workflow before a dedicated implementation checkpoint
- scope drift into Android product code, per-app routing, or transport logic before CP-006 is defined
Resume from:
- PROJECT_STATE.md
- checkpoints/CP-005.md
