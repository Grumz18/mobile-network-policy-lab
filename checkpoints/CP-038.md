# CP-038

## CHECKPOINT_ID
CP-038

## TITLE
Define the smallest bounded post-foreground-state window-focus verification probe

## CURRENT_STATE
CP-037 is complete.
The cleared `libcore` metadata bridge remains only an already-validated prerequisite from CP-014.
The persisted `android/sing-box` baseline remains the CP-017 prerequisite on branch `cp017-local-baseline` at `aed32ee3066cdbc7d471e3e0415c5134088962df`.

CP-037 execution confirms bounded foreground-state verification with required success lines:
- output contains `topResumedActivity`
- output contains `moe.nb4a.debug/io.nekohasekai.sagernet.ui.MainActivity`
- `EXIT_CODE: 0`

The first exact downstream surface after successful foreground-state verification is bounded window-focus verification.

## TARGET_STATE
The repository has a bounded checkpoint definition for probing only the first exact downstream surface after successful CP-037 foreground-state verification.

After CP-038 execution, a future session must be able to answer:
- whether one bounded window-focus probe confirms `moe.nb4a.debug` owns focused window/app state
- what exact device/package/process/state prerequisites were true before probe entry
- what exact first meaningful outcome appears when that single probe is entered
- whether the first outcome is a prerequisite failure, focus-check failure, or focus-state success
- how focus verification is explicitly separated from UI interaction and runtime debugging

CP-038 does not perform UI interaction.
CP-038 does not perform runtime debugging.
CP-038 does not implement product features.

## IN_SCOPE
- identify the first exact post-foreground-state surface as bounded window-focus verification
- define one bounded focus-verification probe:
  - `& "$env:ANDROID_HOME\\platform-tools\\adb.exe" -s <serial> shell "dumpsys window windows | grep -m 1 -E 'mCurrentFocus|mFocusedApp'"`
- define exact prerequisite checks before probe entry:
  - `android/sing-box` remains on branch `cp017-local-baseline`
  - `android/sing-box` `HEAD` remains `aed32ee3066cdbc7d471e3e0415c5134088962df`
  - CP-037 execution artifact exists:
    - `docs/android/ANDROID_POST_PROCESS_VERIFICATION_CONTINUATION.md`
  - CP-037 prerequisite/probe evidence exists:
    - `docs/android/evidence/cp037_prereq_checks.log`
    - `docs/android/evidence/cp037_state_probe.clean.log`
  - CP-036 and CP-035 continuity artifacts exist:
    - `docs/android/ANDROID_POST_LAUNCH_PROCESS_VERIFICATION.md`
    - `docs/android/ANDROID_POST_INSTALL_VERIFICATION_CONTINUATION.md`
  - target APK/output identity remains available:
    - `android/fork/app/build/outputs/apk/oss/debug/output-metadata.json`
    - `android/fork/app/build/outputs/apk/oss/debug/NekoBox-1.4.2-x86_64-debug.apk`
  - package id expectation remains:
    - `applicationId` equals `moe.nb4a.debug`
  - Android SDK tool path is resolvable:
    - `"$env:ANDROID_HOME\\platform-tools\\adb.exe"` exists (or equivalent resolved path explicitly captured)
  - exactly one online adb target is available
  - target adb device ABI is `x86_64`
  - installed package check passes before focus probe:
    - `adb shell pm path moe.nb4a.debug` returns a package path
  - process check passes before focus probe:
    - `adb shell pidof moe.nb4a.debug` returns numeric PID output
  - foreground-state continuity check passes before focus probe:
    - `adb shell "dumpsys activity activities | grep -m 1 -E 'mResumedActivity|topResumedActivity'"` contains `moe.nb4a.debug` and `MainActivity`
- define exact expected focus-state success signals:
  - probe output is non-empty
  - probe output contains one focus marker:
    - `mCurrentFocus`
    - or `mFocusedApp`
  - probe output contains `moe.nb4a.debug`
  - probe output contains one exact activity identity signal:
    - `io.nekohasekai.sagernet.ui.MainActivity`
    - or `.ui.MainActivity`
  - `EXIT_CODE: 0`
- define exact out-of-scope actions that must remain unperformed:
  - no UI interaction commands (`adb shell input`, `adb shell monkey`, UI automation)
  - no network actions or traffic probes from device shell
  - no app relaunch commands (`adb shell am start*`)
  - no runtime-debugging capture (`adb logcat`, profiler, traces)
  - no broad diagnostics beyond the single bounded probe and defined fallbacks
  - no `assemble*` continuation
  - no release signing/distribution work
- define exact fallback evidence sources if direct shell output is incomplete:
  - direct shell transcript of the bounded `dumpsys window windows` probe
  - captured clean transcript file for the same command
  - `adb devices -l` snapshot captured in the same run
  - fallback foreground-state check:
    - `adb shell "dumpsys activity activities | grep -m 1 -E 'mResumedActivity|topResumedActivity'"`
  - fallback process continuity check:
    - `adb shell pidof moe.nb4a.debug`
  - fallback package continuity check:
    - `adb shell pm path moe.nb4a.debug`
- define exact cleanup expectations:
  - do not regenerate `libcore.aar`
  - do not create disposable gomobile/GOPATH workspaces
  - remove only temporary CP-038 scratch files if any are created outside `docs/android/evidence/`
  - retain canonical evidence files under `docs/android/evidence/`
  - do not uninstall package as part of CP-038
  - do not force-stop or kill app process as part of CP-038
  - leave upstream source trees unchanged
- create `docs/android/ANDROID_POST_FOREGROUND_STATE_CONTINUATION.md` during later CP-038 execution
- update `PROJECT_STATE.md` and `checkpoints/CP-038.md` during later CP-038 execution

## OUT_OF_SCOPE
- executing CP-038
- revisiting any previous build-chain checkpoints
- revisiting or re-solving the cleared `libcore` metadata bridge
- revisiting or re-testing the already-validated `android/sing-box` baseline except as a stated prerequisite
- modifying upstream source under `android/fork/`
- applying broad patches to build logic, dependency trees, or generated workspaces
- UI interaction or user-flow testing
- runtime debugging, profiling, or diagnostic deep dives
- network actions from device shell
- Android product feature implementation
- per-app routing implementation
- transport or proxy logic
- changes to `android/libneko/` or `android/sing-box/` baselines
- server artifacts or server implementation, except `PROJECT_STATE.md` for continuity
- CI/CD changes

## REQUIRED_ARTIFACTS
- `checkpoints/CP-038.md`
- `PROJECT_STATE.md` (updated for CP-038 definition state only)
- `docs/android/ANDROID_POST_FOREGROUND_STATE_CONTINUATION.md` (execution artifact for later CP-038 run)

## EXECUTION_NOTES
CP-038 is a window-focus-verification definition checkpoint.
It must remain narrow and explicitly separate focus-state verification from UI interaction and runtime debugging.

Why the next surface is window-focus verification:
- CP-037 already proved resumed foreground activity identity
- the smallest deterministic downstream gate is window-manager focus ownership by the same package/activity
- one bounded `dumpsys window windows` probe is sufficient for this gate

Execution priority for later CP-038 run:
1. verify all CP-038 prerequisites exactly
2. run the single bounded focus-state probe once
3. capture first exact meaningful outcome and stop
4. do not continue into UI interaction or runtime-debugging workflows

First meaningful outcome rules for CP-038:
- if prerequisite checks fail, the first failed prerequisite line is the checkpoint outcome
- if focus probe runs but does not show required focus/package/activity signal, the first exact failure line is the checkpoint outcome
- if focus probe returns required focus/package/activity signal, success signal lines are the checkpoint outcome
- stop immediately once one of those outcomes is captured

Toolchain and environment bounds for later CP-038 run:
- preserve persisted `android/sing-box` baseline at `cp017-local-baseline` / `aed32ee3066cdbc7d471e3e0415c5134088962df`
- preserve Android SDK/platform-tools baseline already used in CP-037
- do not intentionally upgrade Gradle/AGP/Kotlin/Java/Go/Android dependencies during probe
- if adb path/device identity differs from prior checkpoints, capture exact evidence and stop at first meaningful outcome

## COMPLETION_TEST
CP-038 is complete only if:
- `docs/android/ANDROID_POST_FOREGROUND_STATE_CONTINUATION.md` exists after execution
- the report confirms exact CP-038 prerequisites listed in `IN_SCOPE`
- the report documents exact bounded probe command:
  - `& "$env:ANDROID_HOME\\platform-tools\\adb.exe" -s <serial> shell "dumpsys window windows | grep -m 1 -E 'mCurrentFocus|mFocusedApp'"`
- the report captures first exact outcome from that one probe and stops
- if probe succeeds, the report includes:
  - non-empty output line containing `mCurrentFocus` or `mFocusedApp`
  - output line containing `moe.nb4a.debug`
  - output line containing `io.nekohasekai.sagernet.ui.MainActivity` or `.ui.MainActivity`
  - `EXIT_CODE: 0`
- the report confirms out-of-scope actions in `IN_SCOPE` remain unperformed
- the report identifies fallback evidence used if direct shell output is incomplete
- the report confirms cleanup expectations were followed
- no upstream source files under `android/fork/` are modified
- no UI-interaction/runtime-debugging/full-assemble continuation is attempted
- `PROJECT_STATE.md` is updated after execution

## RESULT
partial

## WHAT_CHANGED
- `docs/android/ANDROID_POST_FOREGROUND_STATE_CONTINUATION.md` was created with bounded CP-038 execution evidence.
- CP-038 prerequisite checks were executed and captured, including:
  - `android/sing-box` branch/commit confirmation (`cp017-local-baseline` / `aed32ee3066cdbc7d471e3e0415c5134088962df`)
  - CP-035, CP-036, and CP-037 evidence presence checks
  - APK/output identity checks and `applicationId` check (`moe.nb4a.debug`)
  - adb path resolution via explicit SDK path (`C:/Android/Sdk/platform-tools/adb.exe`) because `ANDROID_HOME` was empty in-session
  - exactly-one-online-device check
  - target ABI check (`x86_64`)
  - installed package presence check (`pm path moe.nb4a.debug`)
  - process continuity check (`pidof moe.nb4a.debug`)
  - foreground continuity check (`topResumedActivity ... MainActivity`)
- The single bounded focus/window-state probe was executed exactly once:
  - `& "C:/Android/Sdk/platform-tools/adb.exe" -s emulator-5554 shell "dumpsys window windows | grep -m 1 -E 'mCurrentFocus|mFocusedApp'"`
- First exact meaningful outcome from the probe was captured as failure:
  - output was empty
  - `EXIT_CODE: 1`
- Fallback evidence was captured (as allowed when direct output is incomplete):
  - `dumpsys activity activities` fallback still shows `topResumedActivity ... moe.nb4a.debug/...MainActivity`
  - `pidof` fallback still returns numeric PID
  - `pm path` fallback still returns package path
- Out-of-scope actions remained unperformed (no UI interaction, no relaunch, no network actions, no runtime-debugging commands).
- Cleanup expectations were followed (no `libcore` regeneration, no disposable gomobile/GOPATH workspaces, no upstream source modification).
- `PROJECT_STATE.md` was updated to reflect CP-038 execution as partial.

## REMAINING_GAPS
- CP-038 target success is not met: the bounded `dumpsys window windows | grep -m 1 -E 'mCurrentFocus|mFocusedApp'` probe did not return required focus/package/activity signals.
- Direct probe success signals are absent (`mCurrentFocus|mFocusedApp` line with `moe.nb4a.debug` + `MainActivity` and `EXIT_CODE: 0`).
- A bounded follow-up checkpoint is required to correct or redefine the focus verification surface from observed CP-038 evidence.

## NEXT_CHECKPOINT
Create CP-039 only to define the smallest bounded boundary-correction continuation after CP-038 partial execution, focused on obtaining a deterministic window-focus signal without entering UI interaction, network actions, runtime debugging, or feature work.

## HANDOFF
Current checkpoint status: partial
Safe next action: create CP-039 only for bounded focus-surface boundary correction
Artifacts created or updated:
- checkpoints/CP-038.md
- docs/android/ANDROID_POST_FOREGROUND_STATE_CONTINUATION.md
- docs/android/evidence/cp038_prereq_checks.log
- docs/android/evidence/cp038_adb_devices.log
- docs/android/evidence/cp038_focus_probe.log
- docs/android/evidence/cp038_focus_probe.clean.log
- docs/android/evidence/cp038_focus_fallback.log
- docs/android/evidence/cp038_out_of_scope_checks.log
- PROJECT_STATE.md
Known risks:
- scope will drift if UI interaction, runtime-debugging, network actions, or broader continuation starts before CP-039 is defined
- current window-focus probe pattern may be non-deterministic on this emulator image even when app process/activity is healthy
- shell output may contain non-fatal pipe warnings that must not be confused with true prerequisite/probe failures
Resume from:
- checkpoints/CP-038.md
- docs/android/ANDROID_POST_FOREGROUND_STATE_CONTINUATION.md
- docs/android/evidence/cp038_focus_probe.clean.log
- docs/android/evidence/cp038_focus_fallback.log
- PROJECT_STATE.md
