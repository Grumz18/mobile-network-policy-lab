# CP-035

## CHECKPOINT_ID
CP-035

## TITLE
Define the smallest bounded post-install launch-verification probe

## CURRENT_STATE
CP-034 is complete.
The cleared `libcore` metadata bridge remains only an already-validated prerequisite from CP-014.
The persisted `android/sing-box` baseline remains the CP-017 prerequisite on branch `cp017-local-baseline` at `aed32ee3066cdbc7d471e3e0415c5134088962df`.
CP-033 and CP-034 execution-validated these downstream surfaces:
- package boundary correction and APK verification at `android/fork/app/build/outputs/apk/oss/debug`
- bounded install verification for `android/fork/app/build/outputs/apk/oss/debug/NekoBox-1.4.2-x86_64-debug.apk`

CP-034 execution confirms:
- exactly one online adb target exists
- target ABI is `x86_64`
- bounded install output includes:
  - `Performing Streamed Install`
  - `Success`
  - `EXIT_CODE: 0`
- package `moe.nb4a.debug` is installed on device

APK manifest evidence identifies launcher activity:
- package id: `moe.nb4a.debug`
- launchable activity: `io.nekohasekai.sagernet.ui.MainActivity`

The first exact downstream surface after successful install verification is bounded launch verification.

## TARGET_STATE
The repository has a bounded checkpoint definition for probing only the first exact downstream surface after successful CP-034 install verification.

After CP-035 execution, a future session must be able to answer:
- whether one bounded launch-verification probe succeeds for the installed app
- what exact prerequisite device/package/activity conditions were true before probe entry
- what exact first meaningful outcome appears when that single probe is entered
- whether the first outcome is a prerequisite failure, a launch-command failure, or launch success
- how launch verification is explicitly separated from runtime debugging and interaction testing

CP-035 does not perform runtime debugging.
CP-035 does not perform interaction testing.
CP-035 does not implement product features.

## IN_SCOPE
- identify the first exact post-install surface as bounded launch verification
- define one bounded launch-verification probe from `android/fork/`:
  - `& "$env:ANDROID_HOME\\platform-tools\\adb.exe" -s <serial> shell am start -W -n "moe.nb4a.debug/io.nekohasekai.sagernet.ui.MainActivity"`
- define exact prerequisite checks before probe entry:
  - `android/sing-box` remains on branch `cp017-local-baseline`
  - `android/sing-box` `HEAD` remains `aed32ee3066cdbc7d471e3e0415c5134088962df`
  - CP-034 execution artifact exists:
    - `docs/android/ANDROID_POST_APK_VERIFICATION_CONTINUATION.md`
  - target APK/output identity remains available:
    - `android/fork/app/build/outputs/apk/oss/debug/output-metadata.json`
    - `android/fork/app/build/outputs/apk/oss/debug/NekoBox-1.4.2-x86_64-debug.apk`
  - package id expectation remains:
    - `applicationId` equals `moe.nb4a.debug`
  - launchable activity expectation remains:
    - `io.nekohasekai.sagernet.ui.MainActivity`
  - Android SDK tool path is resolvable:
    - `"$env:ANDROID_HOME\\platform-tools\\adb.exe"` exists (or equivalent resolved path explicitly captured)
  - exactly one online adb target is available
  - target adb device ABI is `x86_64`
  - installed package check passes before launch probe:
    - `adb shell pm path moe.nb4a.debug` returns a package path
- define exact expected launch-verification success signals:
  - `Starting: Intent { cmp=moe.nb4a.debug/io.nekohasekai.sagernet.ui.MainActivity`
  - `Status: ok`
  - `Activity: moe.nb4a.debug/io.nekohasekai.sagernet.ui.MainActivity`
  - `Complete`
  - `EXIT_CODE: 0`
- define exact out-of-scope actions that must remain unperformed:
  - no `adb logcat`
  - no `adb shell dumpsys*`
  - no input/interaction commands (`adb shell input`, `adb shell monkey`, UI automation)
  - no feature validation beyond launch confirmation
  - no `assemble*` continuation
  - no release signing/distribution work
- define exact fallback evidence sources if direct shell output is incomplete:
  - direct shell transcript of the `adb shell am start -W` command
  - captured clean transcript file for the same command
  - `adb devices -l` snapshot captured in the same run
  - fallback package-presence check (`adb shell pm path moe.nb4a.debug`) only if launch output is incomplete
  - fallback process check (`adb shell pidof moe.nb4a.debug`) only if launch output is incomplete
- define exact cleanup expectations:
  - do not regenerate `libcore.aar`
  - do not create disposable gomobile/GOPATH workspaces
  - remove only temporary CP-035 transcript scratch files if any are created outside `docs/android/evidence/`
  - retain canonical evidence files under `docs/android/evidence/`
  - do not uninstall package as part of CP-035
  - leave upstream source trees unchanged
- create `docs/android/ANDROID_POST_INSTALL_VERIFICATION_CONTINUATION.md` during later CP-035 execution
- update `PROJECT_STATE.md` and `checkpoints/CP-035.md` during later CP-035 execution

## OUT_OF_SCOPE
- executing CP-035
- revisiting or re-solving the cleared `libcore` metadata bridge
- revisiting or re-testing the already-validated `android/sing-box` baseline except as a stated prerequisite
- modifying upstream source under `android/fork/`
- applying broad patches to build logic, dependency trees, or generated workspaces
- runtime debugging, profiling, or diagnostic deep dives
- interaction testing or user-flow testing beyond launch verification
- Android product feature implementation
- per-app routing implementation
- transport or proxy logic
- changes to `android/libneko/` or `android/sing-box/` baselines
- server artifacts or server implementation, except `PROJECT_STATE.md` for continuity
- CI/CD changes

## REQUIRED_ARTIFACTS
- `checkpoints/CP-035.md`
- `PROJECT_STATE.md` (updated for CP-035 definition state only)
- `docs/android/ANDROID_POST_INSTALL_VERIFICATION_CONTINUATION.md` (execution artifact for later CP-035 run)

## EXECUTION_NOTES
CP-035 is a launch-verification definition checkpoint.
It must remain narrow and separate launch verification from runtime debugging and interaction testing.

Why the next surface is launch verification:
- CP-034 already proved bounded install verification success
- the next deterministic downstream gate is whether the installed app can be launched once via explicit component
- one `am start -W` probe is the smallest bounded launch-confirmation surface

Execution priority for later CP-035 run:
1. verify all CP-035 prerequisites exactly
2. run the single bounded `adb shell am start -W` probe once
3. capture first exact meaningful outcome and stop
4. do not continue into runtime debugging or interaction testing

First meaningful outcome rules for CP-035:
- if prerequisite checks fail, the first failed prerequisite line is the checkpoint outcome
- if launch command starts and fails, the first exact failure line is the checkpoint outcome
- if launch command succeeds, success signal lines are the checkpoint outcome
- stop immediately once one of those outcomes is captured

Toolchain and environment bounds for later CP-035 run:
- preserve persisted `android/sing-box` baseline at `cp017-local-baseline` / `aed32ee3066cdbc7d471e3e0415c5134088962df`
- preserve existing Android SDK / platform-tools baseline already used in CP-034
- do not intentionally upgrade Gradle/AGP/Kotlin/Java/Go/Android dependencies during probe
- if adb path/device identity differs from prior checkpoints, capture exact evidence and stop at first meaningful outcome

## COMPLETION_TEST
CP-035 is complete only if:
- `docs/android/ANDROID_POST_INSTALL_VERIFICATION_CONTINUATION.md` exists after execution
- the report confirms exact CP-035 prerequisites listed in `IN_SCOPE`
- the report confirms continuity of:
  - `android/fork/app/build/outputs/apk/oss/debug`
  - `applicationId` = `moe.nb4a.debug`
  - launchable activity = `io.nekohasekai.sagernet.ui.MainActivity`
- the report documents exact bounded probe command:
  - `& "$env:ANDROID_HOME\\platform-tools\\adb.exe" -s <serial> shell am start -W -n "moe.nb4a.debug/io.nekohasekai.sagernet.ui.MainActivity"`
- the report captures first exact outcome from that one probe and stops
- if probe succeeds, the report includes:
  - `Starting: Intent { cmp=moe.nb4a.debug/io.nekohasekai.sagernet.ui.MainActivity`
  - `Status: ok`
  - `Activity: moe.nb4a.debug/io.nekohasekai.sagernet.ui.MainActivity`
  - `Complete`
  - `EXIT_CODE: 0`
- the report confirms out-of-scope actions in `IN_SCOPE` remain unperformed
- the report identifies fallback evidence used if direct shell output is incomplete
- the report confirms cleanup expectations were followed
- no upstream source files under `android/fork/` are modified
- no runtime-debugging/interaction/full-assemble continuation is attempted
- `PROJECT_STATE.md` is updated after execution

## RESULT
pending

## WHAT_CHANGED
- `checkpoints/CP-035.md` was authored to define the smallest bounded continuation after successful CP-034 install verification.
- The checkpoint now defines one bounded downstream probe as launch verification only.
- `PROJECT_STATE.md` was updated so the next eligible work is executing CP-035 only.

## REMAINING_GAPS
- CP-035 has not been executed yet.
- `docs/android/ANDROID_POST_INSTALL_VERIFICATION_CONTINUATION.md` does not exist yet and must be created during CP-035 execution.
- Exact success or failure outcome of the bounded launch-verification probe is still unknown.

## NEXT_CHECKPOINT
Execute CP-035 only, exactly as defined, to run one bounded launch-verification probe after successful install verification, without entering runtime debugging, interaction testing, or feature work.

## HANDOFF
Current checkpoint status: pending
Safe next action: execute CP-035 only using the single bounded launch-verification probe defined above
Artifacts created or updated:
- checkpoints/CP-035.md
- PROJECT_STATE.md
Known risks:
- scope will drift if runtime-debugging, interaction testing, or broader assemble continuation starts before CP-035 execution is captured
- device state can drift between sessions; prerequisite device identity/ABI checks must pass before probe entry
- package/activity naming drift can invalidate the probe command and must be captured as first meaningful outcome evidence
Resume from:
- checkpoints/CP-035.md
- PROJECT_STATE.md
- docs/android/ANDROID_POST_APK_VERIFICATION_CONTINUATION.md
