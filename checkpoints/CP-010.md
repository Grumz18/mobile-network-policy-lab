# CP-010

## CHECKPOINT_ID
CP-010

## TITLE
First Android build attempt from bootstrapped prerequisites

## CURRENT_STATE
CP-009 is complete.
The workstation now has the documented Android build prerequisites and external source dependencies needed to attempt the first upstream fork build:
- JDK 17 is installed at `C:\Program Files\Eclipse Adoptium\jdk-17.0.18.8-hotspot`
- Android SDK root is `C:\Android\Sdk`
- Android NDK `25.0.8775105` is installed
- Go `1.23.6` is installed
- `gomobile-matsuri` and `gobind-matsuri` are installed under `%GOPATH%\bin`
- `android/libneko/` and `android/sing-box/` exist
- `android/fork/local.properties` exists with `sdk.dir=C:/Android/Sdk`
- Gradle wrapper bootstrap is confirmed through `android/fork/gradlew.bat`
The materialized upstream NekoBox fork in `android/fork/` is still unbuilt.
`android/fork/app/libs/libcore.aar` does not exist yet.
`JAVA_HOME` and `ANDROID_HOME` are not persisted to the Windows environment and must be set per-session.
The upstream fork still must remain unmodified.
No Android product features, per-app routing, transport logic, patches, or build repairs have been authorized.

## TARGET_STATE
The repository has a durable first-build-attempt report that records:
- the exact session-scoped Java and Android environment used for the attempt
- whether `libcore/build.sh` is the required first dependency step and whether it produced `android/fork/app/libs/libcore.aar`
- the first safe Gradle build entry attempted after prerequisite setup
- the exact build commands run and the exact stage reached
- any build-time blocker, failure, or incompatibility encountered
- an explicit separation between discovery of build failures and any later repair work

This checkpoint does not need to produce a successful APK.
It only needs to attempt the first safe build path and preserve precise failure evidence.

## IN_SCOPE
- set the correct session-scoped environment for the build attempt:
  - `JAVA_HOME`
  - `ANDROID_HOME` or `ANDROID_SDK_ROOT`
  - `ANDROID_NDK_HOME` if needed
  - `GOPATH`
  - `PATH` additions required for JDK, Go, Android SDK tools, and gomobile tools
- verify from upstream build inputs whether `libcore.aar` generation is the required first dependency step before Gradle app assembly
- if `libcore.aar` is required first, run the minimal upstream-supported command path to attempt generating it
- if `libcore.aar` generation succeeds, attempt the first safe Gradle build entry from `android/fork/`
- stop at the first meaningful blocker when further progress would become repair work
- capture exact commands, exact shell mode, exact working directories, and exact error output
- create a durable report documenting the build path taken, stage reached, blockers, and next repair boundary
- update `PROJECT_STATE.md` to reflect CP-010 execution results

## OUT_OF_SCOPE
- modifying any upstream source code under `android/fork/`
- applying patches or creating `android/patches/` content
- repairing build scripts, Gradle files, Go module files, or source dependencies
- changing dependency versions or pinning new revisions for `libneko` or `sing-box`
- installing new toolchains or SDK components unless a missing executable from CP-009 must be re-verified as absent or misconfigured
- repeated trial-and-error build attempts after the first meaningful blocker is identified
- Android product feature work
- per-app routing implementation
- transport or proxy logic
- server artifacts or server implementation
- CI/CD changes
- signing configuration changes

## REQUIRED_ARTIFACTS
- `docs/android/ANDROID_FIRST_BUILD_ATTEMPT.md`
- `PROJECT_STATE.md` (updated)
- `checkpoints/CP-010.md` (updated with execution result)

## EXECUTION_NOTES
This checkpoint is a build-attempt discovery step, not a repair step.
Use the prerequisites documented in `docs/android/ANDROID_BUILD_PREREQUISITES.md` as the source of truth for installed component paths.
The first execution action must be to set the session environment explicitly, even if the tools appear to work without it. The report must record the exact values used.
`android/fork/app/build.gradle.kts` consumes `fileTree("libs")`, and `android/fork/libcore/build.sh` copies `libcore.aar` into `android/fork/app/libs/`. Treat `libcore` generation as the likely first build dependency step and confirm that assumption during execution.
On this Windows host, invoke shell-based upstream scripts through Git Bash, not `C:\Windows\system32\bash.exe`.
Invoke Gradle through `android/fork/gradlew.bat` from PowerShell or `cmd.exe` directly in `android/fork/`.
Do not use a Bash-to-cmd bridge for the Gradle step unless the report proves that path is required and reliable.
If `libcore/build.sh` fails, record the failure and stop. Do not continue into Gradle app assembly without a valid `libcore.aar`.
If `libcore.aar` is generated successfully, use the least expansive Gradle command that still enters the first real build path. Prefer a single, bounded app build entry such as an assemble task over broad exploratory commands.
If a failure suggests missing environment configuration, version mismatch, unsupported host behavior, or dependency misalignment, document it and stop at the discovery boundary.
Preserve the distinction between:
- build discovery completed in CP-010
- build repair deferred to a later checkpoint

## COMPLETION_TEST
This checkpoint is complete only if:
- `docs/android/ANDROID_FIRST_BUILD_ATTEMPT.md` exists
- the report records the exact session environment values used for Java, Android SDK/NDK, Go, and PATH-sensitive tools
- the report states whether `libcore.aar` generation was required before Gradle app assembly
- the report records whether `android/fork/app/libs/libcore.aar` was generated
- the report records the first safe Gradle build command attempted, if reached
- the report captures exact commands, working directories, shell mode, and exact failure output or success boundary
- the report explicitly separates discovered blockers from any future repair work
- no upstream source files under `android/fork/` are modified
- no patches are applied
- `PROJECT_STATE.md` is updated

## RESULT
partial

## WHAT_CHANGED
- `checkpoints/CP-010.md` was created to define the first Android build-attempt checkpoint.
- No build commands were run.
- No upstream source, Android implementation, patch, or server artifact was changed as part of this checkpoint definition step.

## REMAINING_GAPS
CP-010 has not been executed yet.
`docs/android/ANDROID_FIRST_BUILD_ATTEMPT.md` does not exist yet.
The actual first build boundary, `libcore.aar` generation result, Gradle entry result, and build-time blockers remain to be discovered during CP-010 execution.

## NEXT_CHECKPOINT
CP-011 - Convert CP-010 build-attempt findings into a bounded build repair or dependency-alignment checkpoint

## HANDOFF
Current checkpoint status: partial
Safe next action: execute CP-010 exactly as written to attempt `libcore.aar` generation first, then enter the first safe Gradle build path, and stop at the first meaningful blocker without repair
Artifacts created or updated:
- checkpoints/CP-010.md
Known risks:
- `JAVA_HOME` and `ANDROID_HOME` are not persisted and must be set correctly per-session before any build command
- `libneko` and `sing-box` may be revision-misaligned with the upstream fork snapshot, which may surface during the first real build attempt
- the first blocker may occur in the Go/gomobile native bridge path before Gradle app assembly is reached
Resume from:
- docs/android/ANDROID_BUILD_PREREQUISITES.md
- checkpoints/CP-009.md
- checkpoints/CP-010.md
