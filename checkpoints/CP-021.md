# CP-021

## CHECKPOINT_ID
CP-021

## TITLE
Define the smallest bounded post-compile-jar continuation probe

## CURRENT_STATE
CP-020 is complete.
The generated `libcore` metadata bridge is already repaired and validated in `docs/android/ANDROID_LIBCORE_METADATA_BRIDGE_REPAIR.md`.
That earlier checkpoint proved:
- the generated fallback `go.mod` can preserve `replace libcore`
- the generated fallback can preserve the local replacement mappings for `github.com/matsuridayo/libneko` and `github.com/sagernet/sing-box`
- the generated fallback can preserve the original `golang.org/x/mobile` baseline (`v0.0.0-20231108233038-35478a0c49da`)
- the exact bare `libcore` import failure from CP-013 is cleared

CP-017 then intentionally persisted the validated `android/sing-box` alignment in `docs/android/ANDROID_SINGBOX_BASELINE_PERSIST.md`.
That checkpoint proved:
- the local `android/sing-box` baseline is intentionally tracked on branch `cp017-local-baseline`
- that persisted baseline points to commit `aed32ee3066cdbc7d471e3e0415c5134088962df`
- the same bounded `android/fork/libcore/build.sh` path still proceeds through transient `android/fork/app/libs/libcore.aar` production under that persisted baseline

CP-018 then executed the first exact downstream app consumer probe in `docs/android/ANDROID_POST_LIBCORE_CONTINUATION.md`.
That checkpoint proved:
- the exact post-libcore handoff artifact remains `android/fork/app/libs/libcore.aar`
- the exact first downstream app consumer command is `.\gradlew.bat :app:compileOssDebugKotlin --stacktrace`
- the bounded Kotlin compile consumer surface succeeds under the persisted `android/sing-box` baseline
- no blocker appears at that Kotlin compile surface

CP-019 then executed the next exact downstream app consumer probe in `docs/android/ANDROID_POST_KOTLIN_CONTINUATION.md`.
That checkpoint proved:
- the prerequisite artifact before the next downstream entry still remains `android/fork/app/libs/libcore.aar`
- the exact Java compile consumer command is `.\gradlew.bat :app:compileOssDebugJavaWithJavac --stacktrace`
- the bounded Java compile consumer surface succeeds under the persisted `android/sing-box` baseline
- javac outputs now exist under:
  - `android/fork/app/build/intermediates/javac/ossDebug/compileOssDebugJavaWithJavac/classes/`
  - `android/fork/app/build/tmp/compileOssDebugJavaWithJavac/`

CP-020 then executed the compile-jar continuation probe in `docs/android/ANDROID_POST_JAVAC_CONTINUATION.md`.
That checkpoint proved:
- the prerequisite artifact before compile-jar entry still remains `android/fork/app/libs/libcore.aar`
- the exact compile-jar consumer command is `.\gradlew.bat :app:bundleOssDebugClassesToCompileJar --stacktrace`
- the bounded compile-jar consumer surface succeeds under the persisted `android/sing-box` baseline
- compile-jar output now exists under:
  - `android/fork/app/build/intermediates/compile_app_classes_jar/ossDebug/bundleOssDebugClassesToCompileJar/classes.jar`
- broader downstream outputs still did not exist after CP-020:
  - `android/fork/app/build/intermediates/runtime_app_classes_jar/ossDebug/bundleOssDebugClassesToRuntimeJar/classes.jar`
  - `android/fork/app/build/intermediates/project_dex_archive/ossDebug`

The next downstream surface is now identifiable from local task metadata and CP-020 evidence:
- a local Gradle task listing from `android/fork/` shows the exact app-side jar tasks for this variant:
  - `bundleOssDebugClassesToCompileJar`
  - `bundleOssDebugClassesToRuntimeJar`
- `bundleOssDebugClassesToCompileJar` is already execution-validated by CP-020
- `bundleOssDebugClassesToRuntimeJar` is the next narrower downstream jar surface still not entered
- runtime-jar output remains absent while dex output also remains absent, so runtime-jar merge is still the next bounded continuation point before any dex or packaging work

No upstream source under `android/fork/` has been modified.
The default installed `gomobile-matsuri` and `gobind-matsuri` binaries remain unchanged.

## TARGET_STATE
The repository has a bounded checkpoint definition for probing only the first exact downstream build surface after successful `.\gradlew.bat :app:bundleOssDebugClassesToCompileJar --stacktrace`.

After CP-021 execution, a future session must be able to answer:
- whether `.\gradlew.bat :app:bundleOssDebugClassesToRuntimeJar --stacktrace` is the correct next exact downstream consumer surface after successful compile-jar merge
- what exact prerequisite artifact and prerequisite baseline must still be true before entering that runtime-jar surface
- what exact first meaningful outcome appears when that single runtime-jar probe is entered
- whether the first new outcome is:
  - a task-graph failure before runtime-jar merge starts
  - a failure inside `:app:bundleOssDebugClassesToRuntimeJar`
  - or a successful runtime-jar continuation signal
- how that probe is kept separate from dexing, packaging, assemble, or repair work

CP-021 does not need to fix runtime-jar merge failures.
It must only define the smallest bounded continuation probe after successful compile-jar merge and stop at the first exact downstream runtime-jar result.

## IN_SCOPE
- select the exact downstream build surface after successful `:app:bundleOssDebugClassesToCompileJar`
- document why `.\gradlew.bat :app:bundleOssDebugClassesToRuntimeJar --stacktrace` is the correct next bounded surface:
  - CP-020 already execution-validated compile-jar success
  - the next listed jar task for the same variant is `bundleOssDebugClassesToRuntimeJar`
  - `android/fork/app/build/intermediates/runtime_app_classes_jar/ossDebug/bundleOssDebugClassesToRuntimeJar/classes.jar` is still absent
  - dex output is also absent, so runtime-jar merge is still narrower than dexing, packaging, or any `assemble*` task
- define the exact entry command for the probe from `android/fork/`:
  - `.\gradlew.bat :app:bundleOssDebugClassesToRuntimeJar --stacktrace`
- define the preconditions that must remain unchanged before the probe:
  - `android/sing-box` stays on branch `cp017-local-baseline`
  - `android/sing-box` stays at `aed32ee3066cdbc7d471e3e0415c5134088962df`
  - the cleared metadata bridge remains only an existing prerequisite reused through the bounded `libcore` path
  - transient `android/fork/app/libs/libcore.aar` is reproduced again before Gradle entry because CP-020 cleanup removed it
  - existing compile-jar output remains present before runtime-jar entry at:
    - `android/fork/app/build/intermediates/compile_app_classes_jar/ossDebug/bundleOssDebugClassesToCompileJar/classes.jar`
- define the exact evidence to capture during execution:
  - confirmation that the persisted `android/sing-box` prerequisite state is unchanged
  - confirmation that the compile-jar success artifact still exists before runtime-jar entry
  - confirmation that runtime-jar and dex outputs are absent before runtime-jar entry
  - the command and output that re-establish transient `android/fork/app/libs/libcore.aar`
  - the exact `.\gradlew.bat :app:bundleOssDebugClassesToRuntimeJar --stacktrace` invocation
  - the first task-graph failure before runtime-jar merge, if one appears
  - the first exact failure line inside `:app:bundleOssDebugClassesToRuntimeJar`, if the task starts and fails
  - or the exact success signal for `:app:bundleOssDebugClassesToRuntimeJar` plus evidence that `android/fork/app/build/intermediates/runtime_app_classes_jar/ossDebug/bundleOssDebugClassesToRuntimeJar/classes.jar` now exists
- define what counts as the first meaningful outcome:
  - first failing task before runtime-jar merge starts
  - first exact failure inside `:app:bundleOssDebugClassesToRuntimeJar`
  - or exact success of `:app:bundleOssDebugClassesToRuntimeJar`
- define toolchain and dependency drift bounds for that probe
- define rollback and cleanup expectations for transient `libcore.aar`, `android/fork/libcore/.build/`, and any disposable gomobile workspace recreated only to regenerate the transient AAR
- create `docs/android/ANDROID_POST_COMPILE_JAR_CONTINUATION.md`
- update `PROJECT_STATE.md` and `checkpoints/CP-021.md` with execution results

## OUT_OF_SCOPE
- revisiting or re-solving the cleared `libcore` metadata bridge
- revisiting or re-testing the already-validated `android/sing-box` baseline except as a stated prerequisite
- modifying upstream source under `android/fork/`, including Gradle files, app code, Java sources, Kotlin sources, or `libcore/`
- broad patches to `gomobile-matsuri`, `gobind-matsuri`, Gradle wiring, generated workspace layout, or dependency trees
- broad Gradle or app build work beyond the single runtime-jar consumer surface
- `compileOssDebugKotlin`, `compileOssDebugJavaWithJavac`, or `bundleOssDebugClassesToCompileJar` as new probe surfaces, except if Gradle reruns them automatically as prerequisites while entering `bundleOssDebugClassesToRuntimeJar`
- dexing, `mergeExtDex*`, `mergeLibDex*`, `dexBuilder*`, packaging, install, signing, or any `assemble*` task
- Android product features
- per-app routing implementation
- transport or proxy logic
- dependency upgrades or repairs outside the first observed runtime-jar continuation blocker
- changes to `android/libneko/` or `android/sing-box/` baselines
- server artifacts or server implementation, except `PROJECT_STATE.md` if needed for continuity
- CI/CD changes

## REQUIRED_ARTIFACTS
- `docs/android/ANDROID_POST_COMPILE_JAR_CONTINUATION.md`
- `PROJECT_STATE.md` (updated after CP-021 execution)
- `checkpoints/CP-021.md` (updated after CP-021 execution)

## EXECUTION_NOTES
CP-021 is a continuation-definition checkpoint.
It must not turn into dexing, packaging, assemble, or repair work.

Why the next surface is `:app:bundleOssDebugClassesToRuntimeJar`:
- CP-020 proved that compile-jar merge is no longer the active downstream boundary
- the same local Gradle task listing that exposed the compile-jar task also exposes `bundleOssDebugClassesToRuntimeJar`
- compile-jar output already exists at `android/fork/app/build/intermediates/compile_app_classes_jar/ossDebug/bundleOssDebugClassesToCompileJar/classes.jar`
- runtime-jar output is still absent at `android/fork/app/build/intermediates/runtime_app_classes_jar/ossDebug/bundleOssDebugClassesToRuntimeJar/classes.jar`
- runtime-jar merge is still narrower than dexing, packaging, or any `assemble*` task

Execution priority for the later CP-021 run:
1. confirm the persisted `android/sing-box` baseline still matches branch `cp017-local-baseline` at `aed32ee3066cdbc7d471e3e0415c5134088962df`
2. confirm the prior compile-jar success artifact still exists at `android/fork/app/build/intermediates/compile_app_classes_jar/ossDebug/bundleOssDebugClassesToCompileJar/classes.jar`
3. confirm runtime-jar and dex outputs are absent before the probe:
   - `android/fork/app/build/intermediates/runtime_app_classes_jar/ossDebug/bundleOssDebugClassesToRuntimeJar/classes.jar`
   - `android/fork/app/build/intermediates/project_dex_archive/ossDebug`
4. rerun only the already-bounded `android/fork/libcore/build.sh` path until it installs transient `android/fork/app/libs/libcore.aar`
5. record that transient `android/fork/app/libs/libcore.aar` is again present before Gradle entry
6. enter Gradle exactly once with `.\gradlew.bat :app:bundleOssDebugClassesToRuntimeJar --stacktrace`
7. capture the first exact downstream outcome and stop
8. do not repair the observed blocker inside CP-021

Exact entry markers CP-021 must record during execution:
- artifact marker: `android/fork/app/libs/libcore.aar`
- command marker: `.\gradlew.bat :app:bundleOssDebugClassesToRuntimeJar --stacktrace`

First meaningful outcome rules for CP-021:
- if the Gradle task graph fails before `:app:bundleOssDebugClassesToRuntimeJar` starts, the first failing task is the checkpoint outcome
- if `:app:bundleOssDebugClassesToRuntimeJar` starts and emits a failure, the first exact failure line is the checkpoint outcome
- if `:app:bundleOssDebugClassesToRuntimeJar` succeeds, that success plus creation of `android/fork/app/build/intermediates/runtime_app_classes_jar/ossDebug/bundleOssDebugClassesToRuntimeJar/classes.jar` is the checkpoint outcome
- once one of those outcomes is captured conclusively, stop without continuing into dex, packaging, or assemble work

Evidence sources if shell output is incomplete during CP-021 execution:
- the direct shell transcript from the `.\gradlew.bat :app:bundleOssDebugClassesToRuntimeJar --stacktrace` invocation
- the active Gradle daemon log under `android/fork/.gradle/daemon/8.10.2/` or the current user Gradle daemon directory for wrapper version `8.10.2`
- direct filesystem evidence from:
  - `android/fork/app/build/intermediates/runtime_app_classes_jar/ossDebug/bundleOssDebugClassesToRuntimeJar/classes.jar`
  - `android/fork/app/build/intermediates/project_dex_archive/ossDebug`

Toolchain and dependency drift bounds for CP-021 execution:
- preserve the persisted `android/sing-box` baseline on `cp017-local-baseline` at `aed32ee3066cdbc7d471e3e0415c5134088962df`
- preserve the exact disposable `gomobile-matsuri` source revision used in CP-014 through CP-020 (`17d6af34f6bd6d7e1e428e0c652c8b54a46bda4f`) if a disposable rebuild is still required to regenerate transient `libcore.aar`
- preserve the generated workspace `golang.org/x/mobile v0.0.0-20231108233038-35478a0c49da` baseline already validated in CP-014 through CP-020
- preserve the host generated-workspace Go baseline (`go1.23.6`) unless mere observation proves auto-selection cannot be avoided
- preserve the existing Gradle wrapper baseline (`8.10.2`)
- preserve the existing Android Gradle Plugin baseline (`8.8.1`)
- preserve the existing Kotlin and KSP baselines (`2.0.21` and `2.0.21-1.0.27`)
- preserve the existing JDK 17 and local SDK/NDK baseline already documented in project state
- do not intentionally upgrade Gradle plugins, Kotlin plugins, Java toolchains, Go dependencies, or Android dependencies during the probe
- if Gradle, Kotlin, Java, KSP, or Go auto-selects a different version, capture the exact version and trigger as evidence and do not treat that drift as the repair

Cleanup expectations after evidence capture for the later CP-021 run:
- remove any disposable temporary gomobile source workspace created for CP-021
- remove any disposable temporary GOPATH or patched binaries created for CP-021
- remove `android/fork/libcore/.build/` after evidence capture unless the report explicitly preserves it for the first downstream failure evidence
- remove transient copied `android/fork/app/libs/libcore.aar` after evidence capture unless the report explicitly preserves it for the first downstream failure evidence
- leave the default installed `gomobile-matsuri` and `gobind-matsuri` binaries unchanged
- leave the compile-jar and runtime-jar outputs in `android/fork/app/build/intermediates/` as normal Gradle artifacts
- leave all upstream source trees unchanged
- stop without continuing into dexing, packaging, or broader build continuation
- document the exact stopping point and cleanup result in `docs/android/ANDROID_POST_COMPILE_JAR_CONTINUATION.md`

## COMPLETION_TEST
CP-021 is complete only if:
- `docs/android/ANDROID_POST_COMPILE_JAR_CONTINUATION.md` exists
- the report documents why `.\gradlew.bat :app:bundleOssDebugClassesToRuntimeJar --stacktrace` is the correct next exact downstream surface
- the report confirms the exact prerequisite state before execution:
  - `android/sing-box` is on branch `cp017-local-baseline`
  - `android/sing-box` is at `aed32ee3066cdbc7d471e3e0415c5134088962df`
  - `android/fork/app/build/intermediates/compile_app_classes_jar/ossDebug/bundleOssDebugClassesToCompileJar/classes.jar` exists before runtime-jar entry
  - `android/fork/app/build/intermediates/runtime_app_classes_jar/ossDebug/bundleOssDebugClassesToRuntimeJar/classes.jar` is absent before runtime-jar entry
  - `android/fork/app/build/intermediates/project_dex_archive/ossDebug` is absent before runtime-jar entry
  - transient `android/fork/app/libs/libcore.aar` is reproduced before Gradle entry
- the report documents the exact runtime-jar entry command `.\gradlew.bat :app:bundleOssDebugClassesToRuntimeJar --stacktrace`
- the report captures the first exact task-graph failure, first exact runtime-jar failure, or the exact success of `:app:bundleOssDebugClassesToRuntimeJar`
- if the task succeeds, the report confirms the exact success artifact path:
  - `android/fork/app/build/intermediates/runtime_app_classes_jar/ossDebug/bundleOssDebugClassesToRuntimeJar/classes.jar`
- the report confirms the following paths remain absent because they are out of scope:
  - `android/fork/app/build/intermediates/project_dex_archive/ossDebug`
  - any APK output under `android/fork/app/build/outputs/apk/`
- the report identifies the exact evidence source used if direct shell output is incomplete:
  - direct shell transcript
  - Gradle daemon log
  - filesystem artifact checks
- the report explicitly states that the cleared metadata bridge was not revisited as a new repair surface
- the report explicitly separates runtime-jar probing from dexing, packaging, or assemble work
- the report documents the exact cleanup expectations and actual cleanup result after evidence capture
- no upstream source files under `android/fork/` are modified
- no broad patches are applied
- no dex, install, packaging, or `assemble*` work is attempted
- `PROJECT_STATE.md` is updated after execution

## RESULT
pending

## WHAT_CHANGED
- `checkpoints/CP-021.md` defines the first exact bounded continuation after successful `.\gradlew.bat :app:bundleOssDebugClassesToCompileJar --stacktrace`.
- The next downstream surface is narrowed to the runtime-jar merge task `.\gradlew.bat :app:bundleOssDebugClassesToRuntimeJar --stacktrace`.
- The checkpoint includes exact prerequisite checks, exact success and absence artifact paths, exact fallback evidence sources, and exact cleanup expectations for the later execution run.

## REMAINING_GAPS
- CP-021 has not been executed.
- `docs/android/ANDROID_POST_COMPILE_JAR_CONTINUATION.md` does not exist yet.
- The first exact runtime-jar-side outcome after successful compile-jar merge is still unobserved.

## NEXT_CHECKPOINT
Execute CP-021 only to probe `.\gradlew.bat :app:bundleOssDebugClassesToRuntimeJar --stacktrace` and stop at the first exact runtime-jar-side outcome.

## HANDOFF
Current checkpoint status: pending
Safe next action: execute CP-021 exactly as written and stop at the first exact outcome from `.\gradlew.bat :app:bundleOssDebugClassesToRuntimeJar --stacktrace`
Artifacts expected on execution:
- docs/android/ANDROID_POST_COMPILE_JAR_CONTINUATION.md
- checkpoints/CP-021.md
- PROJECT_STATE.md
Known risks:
- the next session may incorrectly skip the runtime-jar surface and jump into dexing, packaging, or `assemble*`
- the persisted `android/sing-box` baseline on `cp017-local-baseline` remains a hard prerequisite for continuity
- future Gradle probes may again produce incomplete direct shell transcripts, in which case daemon-log evidence should be preserved
Resume from:
- docs/android/ANDROID_POST_JAVAC_CONTINUATION.md
- checkpoints/CP-021.md
- PROJECT_STATE.md
