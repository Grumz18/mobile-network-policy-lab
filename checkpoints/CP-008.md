# CP-008

## CHECKPOINT_ID
CP-008

## TITLE
Initial Android build-entry verification and dependency discovery for the materialized upstream fork

## CURRENT_STATE
CP-007 is complete.
`android/fork/` contains a clean upstream NekoBox snapshot at commit `5768494d8ae3c74a057bb6d46c0f8dc071b0d821` with no local patches applied.
No Android build has been attempted against the materialized fork.
Observed upstream build metadata already indicates likely prerequisites and dependency edges:
- the Gradle wrapper points to Gradle `8.10.2`
- `buildSrc/build.gradle.kts` declares Android Gradle Plugin `8.8.1` and Kotlin Gradle plugin `2.0.21`
- `buildSrc/src/main/kotlin/Helpers.kt` sets `buildToolsVersion = "35.0.1"`, `compileSdk = 35`, `targetSdk = 35`, and `minSdk = 21`
- `libcore/build.sh` expects Go, `gomobile-matsuri`, `gobind-matsuri`, Android NDK toolchains, and produces `app/libs/libcore.aar`
- `libcore/go.mod` declares `go 1.23.1`, `toolchain go1.23.6`, and local replace directives to `../../libneko` and `../../sing-box`
- `android/fork/libneko` and `android/fork/sing-box` are not present in the materialized snapshot
The repository has Android baseline and local-build baseline documentation, but no checkpoint has yet verified whether this fork can enter a build process on a local workstation.
No Android product features, per-app routing, transport logic, or repair work have been authorized.

## TARGET_STATE
The repository has an evidence-backed answer to whether the materialized upstream fork can enter a build process on the current workstation.
A durable verification report records:
- the build dependency chain from Gradle wrapper entry through Android app assembly prerequisites
- the required Android SDK, Build Tools, NDK, JDK, Go, Gradle, gomobile/gobind, Git, and related environment variables or local paths
- whether required submodules, sibling repositories, or other upstream-managed dependencies are missing
- the first blocking condition if build entry cannot proceed
- the farthest safe build boundary reached if build entry does proceed
- an explicit statement that no repair work or source modification was performed in this checkpoint

## IN_SCOPE
- inspect upstream build metadata and scripts under `android/fork/`
- inventory expected prerequisites from Gradle files, shell scripts, Go module definitions, and repository structure
- verify local presence and versions of JDK, Android SDK, Android Build Tools, Android NDK, Go, Gradle wrapper availability, `gomobile-matsuri`, `gobind-matsuri`, Git, and relevant environment variables
- determine whether missing submodules, sibling repositories, or generated artifacts block build entry
- run only the minimum safe verification commands needed to determine whether the fork can enter a build process and where it first blocks
- document the build dependency chain, missing dependencies, and blockers in a durable report
- update `PROJECT_STATE.md` to reflect CP-008 execution results

## OUT_OF_SCOPE
- modifying any upstream source code under `android/fork/`
- applying local patches or creating `android/patches/` content
- repairing build scripts or Gradle configuration
- installing, upgrading, downloading, or configuring missing SDK, NDK, JDK, Go, Gradle, gomobile, or other toolchain components
- cloning, fetching, or materializing missing sibling repositories, submodules, or external sources
- Android product feature work
- per-app routing implementation
- transport or proxy logic
- server artifacts or server implementation
- CI/CD changes
- signing configuration changes

## REQUIRED_ARTIFACTS
- `docs/android/ANDROID_INITIAL_BUILD_VERIFICATION.md`
- `PROJECT_STATE.md` (updated)
- `checkpoints/CP-008.md` (updated with execution result)

## EXECUTION_NOTES
This checkpoint is verification-and-discovery only. It must not repair anything.
Use repository-local evidence first: `gradle/wrapper/gradle-wrapper.properties`, `build.gradle.kts`, `settings.gradle.kts`, `buildSrc/`, `buildScript/init/`, `libcore/`, `nb4a.properties`, and directory presence checks.
Treat the currently observed upstream metadata as candidate prerequisites to be confirmed during execution, not as already-validated workstation facts.
`libcore/build.sh` and `libcore/init.sh` show that native Android library production depends on Go tooling, `gomobile-matsuri`, `gobind-matsuri`, Android NDK toolchains, and a generated `app/libs/libcore.aar` input for the Android app build.
`libcore/go.mod` local replace directives (`../../libneko`, `../../sing-box`) must be evaluated as dependency-chain requirements. If those sources are absent, record the exact blocker and stop short of repair.
If a verification step would install, download, clone, or otherwise repair a missing prerequisite, do not perform that step. Record the missing prerequisite or blocker instead.
Any command execution in this checkpoint must remain minimal, non-destructive, and limited to establishing build-entry readiness or the first blocker. The goal is to discover the dependency chain and stop at the first hard failure, not to force a successful build.
Generated logs or notes may be captured in repository-owned documentation, but upstream source files must remain unedited.

## COMPLETION_TEST
This checkpoint is complete only if:
- `docs/android/ANDROID_INITIAL_BUILD_VERIFICATION.md` exists
- the report documents the build dependency chain from Gradle wrapper entry to Android app build prerequisites, including `libcore` and any external source dependencies
- the report documents observed or required versions, paths, and presence state for Android SDK, Build Tools, NDK, JDK, Go, Gradle wrapper/distribution, gomobile/gobind, Git, and relevant environment variables
- the report states whether the materialized fork can enter a build process on the current workstation
- if build entry is blocked, the first blocking dependency or condition is recorded with exact evidence
- if missing submodules, sibling repositories, or generated artifacts prevent build entry, they are explicitly recorded as blockers
- no upstream source files are modified, no patches are applied, and no repair work is performed
- `PROJECT_STATE.md` is updated

## RESULT
partial

## WHAT_CHANGED
- `checkpoints/CP-008.md` was created to define the initial Android build-entry verification and dependency discovery checkpoint.
- No build commands were run.
- No upstream source, Android implementation, or server artifact changes were made as part of this checkpoint definition step.

## REMAINING_GAPS
CP-008 has not been executed yet.
`docs/android/ANDROID_INITIAL_BUILD_VERIFICATION.md` does not exist yet.
The actual workstation dependency state, build-entry result, and first blocker remain to be verified during CP-008 execution.

## NEXT_CHECKPOINT
CP-009 - Convert CP-008 findings into a bounded dependency-bootstrap or blocker-remediation checkpoint

## HANDOFF
Current checkpoint status: partial
Safe next action: execute CP-008 exactly as written and produce `docs/android/ANDROID_INITIAL_BUILD_VERIFICATION.md` without repairing missing prerequisites
Artifacts created or updated:
- checkpoints/CP-008.md
Known risks:
- the materialized snapshot already shows likely dependency gaps (`libneko`, `sing-box`, toolchain prerequisites), but they are not yet verified on the workstation
- actual build-entry behavior may expose additional blockers beyond the metadata already inspected
- Gradle, SDK, NDK, JDK, Go, or gomobile availability may differ from what the upstream files imply
Resume from:
- PROJECT_STATE.md
- checkpoints/CP-008.md
