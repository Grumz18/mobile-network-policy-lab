# CP-041

## CHECKPOINT_ID
CP-041

## TITLE
Define the smallest bounded post-resumed-task service-state verification probe

## CURRENT_STATE
CP-040 is complete.
CP-040 captured bounded resumed-task verification success:
- `topResumedActivity=ActivityRecord{71196b7 u0 moe.nb4a.debug/io.nekohasekai.sagernet.ui.MainActivity t11}`
- `EXIT_CODE: 0`

The first exact downstream surface after resumed-task success is bounded service-state verification.

## TARGET_STATE
The repository has a bounded checkpoint definition for verifying only the first exact post-resumed-task service-state surface.

After later CP-041 execution, a future session must be able to answer:
- whether one bounded service-state probe observes a running service record for `moe.nb4a.debug`
- what exact prerequisites were true before probe entry
- what exact first meaningful outcome appears from that one probe
- whether the first outcome is a prerequisite failure, service-state-check failure, or service-state success
- how service-state verification remains explicitly separated from UI interaction, network activity, and runtime debugging

CP-041 does not perform UI interaction.
CP-041 does not perform network actions.
CP-041 does not perform runtime debugging.
CP-041 does not implement product features.

## IN_SCOPE
- identify the first exact post-resumed-task surface as bounded service-state verification
- define one bounded service-state probe:
  - `& "$env:ANDROID_HOME\\platform-tools\\adb.exe" -s <serial> shell "dumpsys activity services | grep -m 1 -E 'ServiceRecord\\{.*moe\\.nb4a\\.debug/.+'"`
- define exact prerequisite checks before probe entry:
  - CP-040 execution artifact exists:
    - `docs/android/ANDROID_POST_FOCUS_VERIFICATION_CONTINUATION.md`
  - CP-040 probe evidence exists:
    - `docs/android/evidence/cp040_probe.log`
  - Android SDK tool path is resolvable:
    - `"$env:ANDROID_HOME\\platform-tools\\adb.exe"` exists
    - or an equivalent explicitly captured adb path is resolvable
  - exactly one online adb target is available (`device`)
  - installed package check passes:
    - `adb shell pm path moe.nb4a.debug` returns a package path
- define exact expected service-state success signals:
  - probe output is non-empty
  - output contains `ServiceRecord{`
  - output contains `moe.nb4a.debug`
  - output contains package/service component pattern `moe.nb4a.debug/<service>`
  - `EXIT_CODE: 0`
- define first meaningful outcome rules for later CP-041 execution:
  - if prerequisite checks fail, first failed prerequisite line is the checkpoint outcome
  - if probe runs but required service-state signals are missing, first exact failure line is the checkpoint outcome
  - if probe returns required signals, success signal lines are the checkpoint outcome
  - stop immediately once one of those outcomes is captured
- define exact out-of-scope actions that must remain unperformed:
  - no UI interaction commands (`adb shell input`, `adb shell monkey`, UI automation)
  - no network actions or traffic probes from device shell
  - no app relaunch commands (`adb shell am start*`)
  - no service-start commands (`adb shell am start-service*`, `adb shell cmd activity start-service*`)
  - no runtime-debugging capture (`adb logcat`, profiler, traces)
  - no broad diagnostics beyond the single bounded probe and defined fallbacks
  - no `assemble*` continuation
  - no release signing/distribution work
- define exact fallback evidence sources if direct probe output is incomplete:
  - direct shell transcript of the bounded service-state probe
  - captured clean transcript file for the same command
  - `adb devices -l` snapshot captured in the same run
  - fallback service listing probe:
    - `adb shell "dumpsys activity services | grep -m 3 -E 'moe\\.nb4a\\.debug|ServiceRecord\\{'"`
  - fallback package continuity check:
    - `adb shell pm path moe.nb4a.debug`
  - fallback process continuity check:
    - `adb shell pidof moe.nb4a.debug`
- define exact cleanup expectations:
  - do not regenerate `libcore.aar`
  - do not create disposable gomobile/GOPATH workspaces
  - remove only temporary CP-041 scratch files if any are created outside `docs/android/evidence/`
  - retain canonical evidence files under `docs/android/evidence/`
  - do not uninstall package as part of CP-041
  - do not force-stop or kill app process as part of CP-041
  - leave upstream source trees unchanged
- create `docs/android/ANDROID_POST_RESUMED_TASK_SERVICE_VERIFICATION.md` during later CP-041 execution
- update `PROJECT_STATE.md` and `checkpoints/CP-041.md` during later CP-041 execution

## OUT_OF_SCOPE
- executing CP-041
- revisiting any previous build-chain checkpoints
- modifying upstream source under `android/fork/`
- applying broad patches to build logic, dependency trees, or generated workspaces
- UI interaction or user-flow testing
- network actions from device shell
- runtime debugging, profiling, or diagnostic deep dives
- Android product feature implementation
- per-app routing implementation
- transport or proxy logic
- changes to `android/libneko/` or `android/sing-box/` baselines
- server artifacts or server implementation, except `PROJECT_STATE.md` for continuity
- CI/CD changes

## REQUIRED_ARTIFACTS
- `checkpoints/CP-041.md`
- `PROJECT_STATE.md` (updated for CP-041 definition state only)
- `docs/android/ANDROID_POST_RESUMED_TASK_SERVICE_VERIFICATION.md` (execution artifact for later CP-041 run)

## EXECUTION_NOTES
CP-041 is a service-state-verification definition checkpoint.
It must remain narrow and explicitly separate service-state verification from UI interaction, network activity, and runtime debugging.

Why the next surface is service-state verification:
- CP-040 already proved resumed-task ownership for `moe.nb4a.debug`
- the smallest deterministic downstream gate is whether a package service record is observable
- one bounded `dumpsys activity services` probe is sufficient for this gate

Execution priority for later CP-041 run:
1. verify all CP-041 prerequisites exactly
2. run the single bounded service-state probe once
3. capture first exact meaningful outcome and stop
4. do not continue into UI interaction, network activity, or runtime-debugging workflows

## COMPLETION_TEST
CP-041 definition is complete only if:
- `checkpoints/CP-041.md` exists and follows checkpoint structure
- the checkpoint defines one bounded post-resumed-task service-state probe
- prerequisites/success signals/out-of-scope/fallbacks/cleanup are explicit and testable
- `docs/android/ANDROID_POST_RESUMED_TASK_SERVICE_VERIFICATION.md` is named as the later execution artifact
- `PROJECT_STATE.md` is updated so the next eligible work is executing CP-041 only
- no CP-041 execution evidence is claimed in this definition-only step

## RESULT
pending

## WHAT_CHANGED
- Created `checkpoints/CP-041.md` as a definition-only checkpoint for bounded post-resumed-task service-state verification.
- Defined exact prerequisites, one bounded probe, expected success signals, out-of-scope actions, fallback evidence sources, and cleanup expectations.
- Reserved later execution artifact path:
  - `docs/android/ANDROID_POST_RESUMED_TASK_SERVICE_VERIFICATION.md`
- Updated `PROJECT_STATE.md` so the next eligible work is executing CP-041 only.

## REMAINING_GAPS
- CP-041 execution has not been performed yet (definition-only step).
- Service-state success/failure evidence is pending later CP-041 execution.

## NEXT_CHECKPOINT
Execute CP-041 only: run the single bounded service-state probe once after prerequisites, capture first exact meaningful outcome, and stop.

## HANDOFF
Current checkpoint status: pending
Safe next action: execute CP-041 only in bounded scope
Artifacts created or updated:
- checkpoints/CP-041.md
- PROJECT_STATE.md
Known risks:
- scope drift into UI/network/runtime/feature work before bounded CP-041 execution
- device/process drift between sessions may change service visibility
Resume from:
- checkpoints/CP-041.md
- checkpoints/CP-040.md
- docs/android/ANDROID_POST_FOCUS_VERIFICATION_CONTINUATION.md
- PROJECT_STATE.md
