# CP-003

## CHECKPOINT_ID
CP-003

## TITLE
Establish server-side skeleton and configuration baseline

## CURRENT_STATE
CP-002 is complete.
The repository has a documented execution surface and a Windows-first local bootstrap baseline.
The `server/` directory exists but does not yet contain a server skeleton, server configuration baseline, or server-specific documentation.
No transport logic has been implemented.
No Android logic has been implemented.

## TARGET_STATE
The repository contains a minimal, documented server-side skeleton and configuration baseline.
After this checkpoint is executed, a future session should be able to identify:
- the intended boundaries of the initial server surface
- the server folder layout for future implementation
- the non-secret configuration variables required for baseline local development
- the safe commands used to verify the server baseline without implementing transport behavior

## IN_SCOPE
- define the initial server-side folder and file layout
- create server-specific baseline documentation
- create non-secret server configuration templates
- add server-local helper scripts for baseline verification only
- document the exact verification flow for the server skeleton baseline

## OUT_OF_SCOPE
- transport or proxy logic
- Android implementation
- iOS implementation
- production deployment logic
- CI/CD implementation, unless a minimal reference is strictly required inside server baseline documentation
- authentication, persistence, or policy-routing implementation

## REQUIRED_ARTIFACTS
- `docs/server/SERVER_BASELINE.md`
- `server/README.md`
- `server/.env.example`
- `server/.gitignore`
- `server/src/README.md`
- `server/config/README.md`
- `server/tests/README.md`
- `server/scripts/verify-baseline.ps1`

## EXECUTION_NOTES
This checkpoint should create only the server skeleton and configuration baseline.
It should not commit the project to transport behavior, protocol selection, or Android integration details.
If the server runtime choice is still uncertain at execution time, document the assumption explicitly in `docs/server/SERVER_BASELINE.md` before creating any runtime-specific files.
Baseline verification must remain local and non-destructive.
CI/CD artifacts should not be created by default in this checkpoint.

## COMPLETION_TEST
This checkpoint is complete only if:
- `docs/server/SERVER_BASELINE.md` defines the server skeleton boundaries and baseline assumptions
- `server/README.md` explains the purpose and layout of the server workspace
- `server/.env.example` and `server/.gitignore` exist and are aligned with the documented server workflow
- `server/src/README.md`, `server/config/README.md`, and `server/tests/README.md` define the intended responsibility of each area
- `server/scripts/verify-baseline.ps1` exists and validates the server baseline without implementing transport behavior
- no transport logic, Android logic, or production deployment code is introduced

## RESULT
complete

## WHAT_CHANGED
- `docs/server/SERVER_BASELINE.md` was created to define the server skeleton boundaries, configuration assumptions, and verification flow.
- `server/README.md` was created to describe the server workspace purpose, layout, and current constraints.
- `server/.env.example` and `server/.gitignore` were created as the non-secret server configuration baseline.
- `server/src/README.md`, `server/config/README.md`, and `server/tests/README.md` were created to define the intended responsibility of each server area without adding implementation.
- `server/scripts/verify-baseline.ps1` was created as a safe local verification script for the server baseline artifact set.
- `PROJECT_STATE.md` was updated to reflect CP-003 completion and the new server baseline.
- `checkpoints/CP-003.md` was updated with execution results, remaining gaps, and handoff data.

## REMAINING_GAPS
No remaining gaps inside CP-003.

## NEXT_CHECKPOINT
CP-004 - Define Android fork integration baseline

## HANDOFF
Current checkpoint status: complete
Safe next action: author CP-004 as the next bounded checkpoint for Android fork integration baseline
Artifacts created or updated:
- docs/server/SERVER_BASELINE.md
- server/README.md
- server/.env.example
- server/.gitignore
- server/src/README.md
- server/config/README.md
- server/tests/README.md
- server/scripts/verify-baseline.ps1
- PROJECT_STATE.md
- checkpoints/CP-003.md
Known risks:
- future work may prematurely commit to a concrete server runtime before a dedicated implementation checkpoint
- scope drift into Android or transport implementation before CP-004 is defined
Resume from:
- PROJECT_STATE.md
- checkpoints/CP-003.md
