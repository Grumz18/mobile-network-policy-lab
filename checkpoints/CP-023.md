# CP-023

## CHECKPOINT_ID
CP-023

## TITLE
Define the smallest bounded post-dex-builder continuation probe

## CURRENT_STATE
CP-022 is complete.
The generated `libcore` metadata bridge is already repaired and validated in `docs/android/ANDROID_LIBCORE_METADATA_BRIDGE_REPAIR.md`.
That earlier checkpoint proved:
- the generated fallback `go.mod` can preserve `replace libcore`
- the generated fallback can preserve the local replacement mappings for `github.com/matsuridayo/libneko` and `github.com/sagernet/sing-box`
- the generated fallback can preserve the original `golang.org/x/mobile` baseline (`v0.0.0-20231108233038-35478a0c49da`)
- the exact bare `libcore` import failure from CP-013 is cleared

CP-017 then intentionally persisted the validated `android/sing-box` alignment in `docs/android/ANDROID_SINGBOX_BASELINE_PERSIST.md`.
That checkpoint proved:
- the local `android/sing-box` baseline is intentionally tracked on branch `cp017-local-baseline`
- that persisted baseline points to commit `aed32ee3066cdbc7d471e3e0415c5134088962df`
- the same bounded `android/fork/libcore/build.sh` path still proceeds through transient `android/fork/app/libs/libcore.aar` production under that persisted baseline

CP-018 then executed the first exact downstream app consumer probe in `docs/android/ANDROID_POST_LIBCORE_CONTINUATION.md`.
That checkpoint proved:
- the exact post-libcore handoff artifact remains `android/fork/app/libs/libcore.aar`
- the exact first downstream app consumer command is `.\gradlew.bat :app:compileOssDebugKotlin --stacktrace`
- the bounded Kotlin compile consumer surface succeeds under the persisted `android/sing-box` baseline

CP-019 then executed the next exact downstream app consumer probe in `docs/android/ANDROID_POST_KOTLIN_CONTINUATION.md`.
That checkpoint proved:
- the exact Java compile consumer command is `.\gradlew.bat :app:compileOssDebugJavaWithJavac --stacktrace`
- the bounded Java compile consumer surface succeeds under the persisted `android/sing-box` baseline
- javac outputs exist under:
  - `android/fork/app/build/intermediates/javac/ossDebug/compileOssDebugJavaWithJavac/classes/`
  - `android/fork/app/build/tmp/compileOssDebugJavaWithJavac/`

CP-020 then executed the compile-jar continuation probe in `docs/android/ANDROID_POST_JAVAC_CONTINUATION.md`.
That checkpoint proved:
- the exact compile-jar consumer command is `.\gradlew.bat :app:bundleOssDebugClassesToCompileJar --stacktrace`
- the bounded compile-jar consumer surface succeeds under the persisted `android/sing-box` baseline
- compile-jar output exists under:
  - `android/fork/app/build/intermediates/compile_app_classes_jar/ossDebug/bundleOssDebugClassesToCompileJar/classes.jar`

CP-021 then executed the runtime-jar continuation probe in `docs/android/ANDROID_POST_COMPILE_JAR_CONTINUATION.md`.
That checkpoint proved:
- the exact runtime-jar consumer command is `.\gradlew.bat :app:bundleOssDebugClassesToRuntimeJar --stacktrace`
- the bounded runtime-jar consumer surface succeeds under the persisted `android/sing-box` baseline
- runtime-jar output exists under:
  - `android/fork/app/build/intermediates/runtime_app_classes_jar/ossDebug/bundleOssDebugClassesToRuntimeJar/classes.jar`

CP-022 then executed the dex-builder continuation probe in `docs/android/ANDROID_POST_RUNTIME_JAR_CONTINUATION.md`.
That checkpoint proved:
- the prerequisite artifact before dex-builder entry still remains `android/fork/app/libs/libcore.aar`
- the exact dex-builder consumer command is `.\gradlew.bat :app:dexBuilderOssDebug --stacktrace`
- the bounded dex-builder consumer surface succeeds under the persisted `android/sing-box` baseline
- dex-builder output now exists under:
  - `android/fork/app/build/intermediates/project_dex_archive/ossDebug`
  - `android/fork/app/build/intermediates/project_dex_archive/ossDebug/dexBuilderOssDebug/out`
- broader downstream outputs still did not exist after CP-022:
  - `android/fork/app/build/intermediates/project_dex_archive/ossDebug/mergeProjectDexOssDebug`
  - `android/fork/app/build/intermediates/merged_project_dex/ossDebug`
  - `android/fork/app/build/intermediates/dex/ossDebug`
  - `android/fork/app/build/outputs/apk/`

The next downstream surface is now identifiable from local task metadata and CP-022 evidence:
- a local Gradle task listing from `android/fork/` shows the exact next dex-side task names for this variant:
  - `mergeProjectDexOssDebug`
  - `mergeExtDexOssDebug`
  - `mergeLibDexOssDebug`
- `dexBuilderOssDebug` is already execution-validated by CP-022
- `android/fork/app/build/intermediates/project_dex_archive/ossDebug/dexBuilderOssDebug` now exists
- `android/fork/app/build/intermediates/merged_project_dex/ossDebug` is still absent
- `mergeProjectDexOssDebug` is the narrowest next surface aligned with the still-absent merged project-dex output family, while `mergeExtDexOssDebug` and `mergeLibDexOssDebug` are separate merge surfaces and packaging is broader still

No upstream source under `android/fork/` has been modified.
The default installed `gomobile-matsuri` and `gobind-matsuri` binaries remain unchanged.

## TARGET_STATE
The repository has a bounded checkpoint definition for probing only the first exact downstream build surface after successful `.\gradlew.bat :app:dexBuilderOssDebug --stacktrace`.

After CP-023 execution, a future session must be able to answer:
- whether `.\gradlew.bat :app:mergeProjectDexOssDebug --stacktrace` is the correct next exact downstream consumer surface after successful dex-builder generation
- what exact prerequisite artifact and prerequisite baseline must still be true before entering that merge-project-dex surface
- what exact first meaningful outcome appears when that single merge-project-dex probe is entered
- whether the first new outcome is:
  - a task-graph failure before `:app:mergeProjectDexOssDebug` starts
  - a failure inside `:app:mergeProjectDexOssDebug`
  - or a successful merge-project-dex continuation signal
- how that probe is kept separate from `mergeExtDex*`, `mergeLibDex*`, packaging, install, assemble, or repair work

CP-023 does not need to fix merge-project-dex failures.
It must only define the smallest bounded continuation probe after successful dex-builder generation and stop at the first exact downstream merge-project-dex result.

## IN_SCOPE
- select the exact downstream build surface after successful `:app:dexBuilderOssDebug`
- document why `.\gradlew.bat :app:mergeProjectDexOssDebug --stacktrace` is the correct next bounded surface:
  - CP-022 already execution-validated dex-builder success
  - `android/fork/app/build/intermediates/project_dex_archive/ossDebug/dexBuilderOssDebug` now exists
  - `android/fork/app/build/intermediates/merged_project_dex/ossDebug` is still absent
  - the next locally listed merge task for project dex output is `mergeProjectDexOssDebug`
  - `mergeExtDexOssDebug` and `mergeLibDexOssDebug` are separate merge surfaces, and packaging is broader than the first merge-project-dex continuation point
- define the exact entry command for the probe from `android/fork/`:
  - `.\gradlew.bat :app:mergeProjectDexOssDebug --stacktrace`
- define the exact prerequisite checks that must remain unchanged before the probe:
  - `android/sing-box` stays on branch `cp017-local-baseline`
  - `android/sing-box` stays at `aed32ee3066cdbc7d471e3e0415c5134088962df`
  - the cleared metadata bridge remains only an existing prerequisite reused through the bounded `libcore` path
  - transient `android/fork/app/libs/libcore.aar` is reproduced again before Gradle entry because CP-022 cleanup removed it
  - existing compile-jar output remains present before merge-project-dex entry at:
    - `android/fork/app/build/intermediates/compile_app_classes_jar/ossDebug/bundleOssDebugClassesToCompileJar/classes.jar`
  - existing runtime-jar output remains present before merge-project-dex entry at:
    - `android/fork/app/build/intermediates/runtime_app_classes_jar/ossDebug/bundleOssDebugClassesToRuntimeJar/classes.jar`
  - existing dex-builder output remains present before merge-project-dex entry at:
    - `android/fork/app/build/intermediates/project_dex_archive/ossDebug`
    - `android/fork/app/build/intermediates/project_dex_archive/ossDebug/dexBuilderOssDebug/out`
  - merge-project-dex output remains absent before entry at:
    - `android/fork/app/build/intermediates/merged_project_dex/ossDebug`
  - final dex output remains absent before entry at:
    - `android/fork/app/build/intermediates/dex/ossDebug`
  - APK output remains absent before entry at:
    - `android/fork/app/build/outputs/apk/`
- define the exact expected success artifact path for the bounded probe:
  - `android/fork/app/build/intermediates/merged_project_dex/ossDebug`
- define the exact evidence to capture during execution:
  - confirmation that the persisted `android/sing-box` prerequisite state is unchanged
  - confirmation that compile-jar, runtime-jar, and dex-builder success artifacts still exist before merge-project-dex entry
  - confirmation that merged-project-dex, final dex, and APK outputs are absent before merge-project-dex entry
  - the command and output that re-establish transient `android/fork/app/libs/libcore.aar`
  - the exact `.\gradlew.bat :app:mergeProjectDexOssDebug --stacktrace` invocation
  - the first task-graph failure before merge-project-dex starts, if one appears
  - the first exact failure line inside `:app:mergeProjectDexOssDebug`, if the task starts and fails
  - or the exact success signal for `:app:mergeProjectDexOssDebug` plus evidence that `android/fork/app/build/intermediates/merged_project_dex/ossDebug` now exists
- define what counts as the first meaningful outcome:
  - first failing task before merge-project-dex starts
  - first exact failure inside `:app:mergeProjectDexOssDebug`
  - or exact success of `:app:mergeProjectDexOssDebug`
- define toolchain and dependency drift bounds for that probe
- define rollback and cleanup expectations for transient `libcore.aar`, `android/fork/libcore/.build/`, and any disposable gomobile workspace recreated only to regenerate the transient AAR
- create `docs/android/ANDROID_POST_DEX_CONTINUATION.md`
- update `PROJECT_STATE.md` and `checkpoints/CP-023.md` with execution results

## OUT_OF_SCOPE
- revisiting or re-solving the cleared `libcore` metadata bridge
- revisiting or re-testing the already-validated `android/sing-box` baseline except as a stated prerequisite
- modifying upstream source under `android/fork/`, including Gradle files, app code, Java sources, Kotlin sources, or `libcore/`
- broad patches to `gomobile-matsuri`, `gobind-matsuri`, Gradle wiring, generated workspace layout, or dependency trees
- broad Gradle or app build work beyond the single merge-project-dex consumer surface
- `dexBuilderOssDebug` as a new probe surface, except if Gradle reruns it automatically as a prerequisite while entering `mergeProjectDexOssDebug`
- `mergeExtDex*`, `mergeLibDex*`, packaging, install, signing, or any `assemble*` task
- Android product features
- per-app routing implementation
- transport or proxy logic
- dependency upgrades or repairs outside the first observed merge-project-dex continuation blocker
- changes to `android/libneko/` or `android/sing-box/` baselines
- server artifacts or server implementation, except `PROJECT_STATE.md` if needed for continuity
- CI/CD changes

## REQUIRED_ARTIFACTS
- `docs/android/ANDROID_POST_DEX_CONTINUATION.md`
- `PROJECT_STATE.md` (updated after CP-023 execution)
- `checkpoints/CP-023.md` (updated after CP-023 execution)

## EXECUTION_NOTES
CP-023 is a continuation-definition checkpoint.
It must not turn into `mergeExtDex*`, `mergeLibDex*`, packaging, install, assemble, or repair work.

Why the next surface is `:app:mergeProjectDexOssDebug`:
- CP-022 proved that dex-builder generation is no longer the active downstream boundary
- the same local Gradle task listing that exposed `dexBuilderOssDebug` also exposes `mergeProjectDexOssDebug`, `mergeExtDexOssDebug`, and `mergeLibDexOssDebug`
- dex-builder output already exists at `android/fork/app/build/intermediates/project_dex_archive/ossDebug/dexBuilderOssDebug/out`
- merged project-dex output is still absent at `android/fork/app/build/intermediates/merged_project_dex/ossDebug`
- `mergeProjectDexOssDebug` is narrower than packaging or assemble work and is the first exact downstream surface aligned with the absent merged project-dex output family

Execution priority for the later CP-023 run:
1. confirm the persisted `android/sing-box` baseline still matches branch `cp017-local-baseline` at `aed32ee3066cdbc7d471e3e0415c5134088962df`
2. confirm the prior compile-jar success artifact still exists at `android/fork/app/build/intermediates/compile_app_classes_jar/ossDebug/bundleOssDebugClassesToCompileJar/classes.jar`
3. confirm the prior runtime-jar success artifact still exists at `android/fork/app/build/intermediates/runtime_app_classes_jar/ossDebug/bundleOssDebugClassesToRuntimeJar/classes.jar`
4. confirm the prior dex-builder success artifacts still exist at:
   - `android/fork/app/build/intermediates/project_dex_archive/ossDebug`
   - `android/fork/app/build/intermediates/project_dex_archive/ossDebug/dexBuilderOssDebug/out`
5. confirm merged-project-dex, final dex, and APK outputs are absent before the probe:
   - `android/fork/app/build/intermediates/merged_project_dex/ossDebug`
   - `android/fork/app/build/intermediates/dex/ossDebug`
   - `android/fork/app/build/outputs/apk/`
6. rerun only the already-bounded `android/fork/libcore/build.sh` path until it installs transient `android/fork/app/libs/libcore.aar`
7. record that transient `android/fork/app/libs/libcore.aar` is again present before Gradle entry
8. enter Gradle exactly once with `.\gradlew.bat :app:mergeProjectDexOssDebug --stacktrace`
9. capture the first exact downstream outcome and stop
10. do not repair the observed blocker inside CP-023

Exact entry markers CP-023 must record during execution:
- artifact marker: `android/fork/app/libs/libcore.aar`
- command marker: `.\gradlew.bat :app:mergeProjectDexOssDebug --stacktrace`

First meaningful outcome rules for CP-023:
- if the Gradle task graph fails before `:app:mergeProjectDexOssDebug` starts, the first failing task is the checkpoint outcome
- if `:app:mergeProjectDexOssDebug` starts and emits a failure, the first exact failure line is the checkpoint outcome
- if `:app:mergeProjectDexOssDebug` succeeds, that success plus creation of `android/fork/app/build/intermediates/merged_project_dex/ossDebug` is the checkpoint outcome
- once one of those outcomes is captured conclusively, stop without continuing into `mergeExtDex*`, `mergeLibDex*`, packaging, install, or assemble work

Evidence sources if shell output is incomplete during CP-023 execution:
- the direct shell transcript from the `.\gradlew.bat :app:mergeProjectDexOssDebug --stacktrace` invocation
- the active Gradle daemon log under `android/fork/.gradle/daemon/8.10.2/` or the current user Gradle daemon directory for wrapper version `8.10.2`
- direct filesystem evidence from:
  - `android/fork/app/build/intermediates/merged_project_dex/ossDebug`
  - `android/fork/app/build/intermediates/dex/ossDebug`
  - `android/fork/app/build/outputs/apk/`

Toolchain and dependency drift bounds for CP-023 execution:
- preserve the persisted `android/sing-box` baseline on `cp017-local-baseline` at `aed32ee3066cdbc7d471e3e0415c5134088962df`
- preserve the exact disposable `gomobile-matsuri` source revision used in CP-014 through CP-022 (`17d6af34f6bd6d7e1e428e0c652c8b54a46bda4f`) if a disposable rebuild is still required to regenerate transient `libcore.aar`
- preserve the generated workspace `golang.org/x/mobile v0.0.0-20231108233038-35478a0c49da` baseline already validated in CP-014 through CP-022
- preserve the host generated-workspace Go baseline (`go1.23.6`) unless mere observation proves auto-selection cannot be avoided
- preserve the existing Gradle wrapper baseline (`8.10.2`)
- preserve the existing Android Gradle Plugin baseline (`8.8.1`)
- preserve the existing Kotlin and KSP baselines (`2.0.21` and `2.0.21-1.0.27`)
- preserve the existing JDK 17 and local SDK/NDK baseline already documented in project state
- do not intentionally upgrade Gradle plugins, Kotlin plugins, Java toolchains, Go dependencies, or Android dependencies during the probe
- if Gradle, Kotlin, Java, KSP, or Go auto-selects a different version, capture the exact version and trigger as evidence and do not treat that drift as the repair

Cleanup expectations after evidence capture for the later CP-023 run:
- remove any disposable temporary gomobile source workspace created for CP-023
- remove any disposable temporary GOPATH or patched binaries created for CP-023
- remove `android/fork/libcore/.build/` after evidence capture unless the report explicitly preserves it for the first downstream failure evidence
- remove transient copied `android/fork/app/libs/libcore.aar` after evidence capture unless the report explicitly preserves it for the first downstream failure evidence
- leave the default installed `gomobile-matsuri` and `gobind-matsuri` binaries unchanged
- leave the compile-jar, runtime-jar, dex-builder, and merged-project-dex outputs in `android/fork/app/build/intermediates/` as normal Gradle artifacts if Gradle creates them during the bounded probe
- leave all upstream source trees unchanged
- stop without continuing into `mergeExtDex*`, `mergeLibDex*`, packaging, install, or broader build continuation
- document the exact stopping point and cleanup result in `docs/android/ANDROID_POST_DEX_CONTINUATION.md`

## COMPLETION_TEST
CP-023 is complete only if:
- `docs/android/ANDROID_POST_DEX_CONTINUATION.md` exists
- the report documents why `.\gradlew.bat :app:mergeProjectDexOssDebug --stacktrace` is the correct next exact downstream surface
- the report confirms the exact prerequisite state before execution:
  - `android/sing-box` is on branch `cp017-local-baseline`
  - `android/sing-box` is at `aed32ee3066cdbc7d471e3e0415c5134088962df`
  - `android/fork/app/build/intermediates/compile_app_classes_jar/ossDebug/bundleOssDebugClassesToCompileJar/classes.jar` exists before merge-project-dex entry
  - `android/fork/app/build/intermediates/runtime_app_classes_jar/ossDebug/bundleOssDebugClassesToRuntimeJar/classes.jar` exists before merge-project-dex entry
  - `android/fork/app/build/intermediates/project_dex_archive/ossDebug` exists before merge-project-dex entry
  - `android/fork/app/build/intermediates/project_dex_archive/ossDebug/dexBuilderOssDebug/out` exists before merge-project-dex entry
  - `android/fork/app/build/intermediates/merged_project_dex/ossDebug` is absent before merge-project-dex entry
  - `android/fork/app/build/intermediates/dex/ossDebug` is absent before merge-project-dex entry
  - `android/fork/app/build/outputs/apk/` is absent before merge-project-dex entry
  - transient `android/fork/app/libs/libcore.aar` is reproduced before Gradle entry
- the report documents the exact merge-project-dex entry command `.\gradlew.bat :app:mergeProjectDexOssDebug --stacktrace`
- the report captures the first exact task-graph failure, first exact merge-project-dex failure, or the exact success of `:app:mergeProjectDexOssDebug`
- if the task succeeds, the report confirms the exact success artifact path:
  - `android/fork/app/build/intermediates/merged_project_dex/ossDebug`
- the report confirms the following paths remain absent because they are out of scope:
  - `android/fork/app/build/intermediates/dex/ossDebug`
  - any APK output under `android/fork/app/build/outputs/apk/`
- the report identifies the exact evidence source used if direct shell output is incomplete:
  - direct shell transcript
  - Gradle daemon log
  - filesystem artifact checks
- the report explicitly states that the cleared metadata bridge was not revisited as a new repair surface
- the report explicitly separates merge-project-dex probing from `mergeExtDex*`, `mergeLibDex*`, packaging, install, or assemble work
- the report documents the exact cleanup expectations and actual cleanup result after evidence capture
- no upstream source files under `android/fork/` are modified
- no broad patches are applied
- no `mergeExtDex*`, `mergeLibDex*`, packaging, install, or `assemble*` work is attempted
- `PROJECT_STATE.md` is updated after execution

## RESULT
pending

## WHAT_CHANGED
- `checkpoints/CP-023.md` was created to define only the first exact downstream continuation surface after successful `:app:dexBuilderOssDebug`.
- The next bounded probe was narrowed to:
  - prerequisite artifact: `android/fork/app/libs/libcore.aar`
  - entry command: `.\gradlew.bat :app:mergeProjectDexOssDebug --stacktrace`
- Exact prerequisite checks, expected success artifact, out-of-scope absence checks, fallback evidence sources, cleanup expectations, and completion assertions were defined without executing the probe.

## REMAINING_GAPS
- CP-023 has not been executed yet.
- `docs/android/ANDROID_POST_DEX_CONTINUATION.md` does not exist yet.
- The first exact merge-project-dex-side outcome after successful dex-builder generation remains unobserved.

## NEXT_CHECKPOINT
Execute CP-023 only to probe `.\gradlew.bat :app:mergeProjectDexOssDebug --stacktrace` and stop at the first exact merge-project-dex outcome without continuing into `mergeExtDex*`, `mergeLibDex*`, packaging, install, or assemble work.

## HANDOFF
Current checkpoint status: pending
Safe next action: execute CP-023 exactly as written, scoped only to `.\gradlew.bat :app:mergeProjectDexOssDebug --stacktrace`
Artifacts created or updated:
- checkpoints/CP-023.md
- PROJECT_STATE.md
Known risks:
- the next session may incorrectly jump from successful dex-builder generation into broader merge-dex, packaging, or `assemble*` work instead of probing the first bounded merge-project-dex surface
- the persisted `android/sing-box` baseline on `cp017-local-baseline` remains a hard prerequisite for continuity
- future Gradle probes may stop producing complete direct shell transcripts, in which case daemon-log evidence should be preserved
Resume from:
- checkpoints/CP-023.md
- docs/android/ANDROID_POST_RUNTIME_JAR_CONTINUATION.md
- PROJECT_STATE.md
