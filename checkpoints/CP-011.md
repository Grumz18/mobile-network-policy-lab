# CP-011

## CHECKPOINT_ID
CP-011

## TITLE
Diagnose and bound the libcore gomobile/go.mod blocker

## CURRENT_STATE
CP-010 is complete.
The first Android build attempt stopped during `android/fork/libcore/build.sh` before any Gradle app build step.
`docs/android/ANDROID_FIRST_BUILD_ATTEMPT.md` records the exact session environment, exact command, and exact failure output.
The first meaningful blocker observed in CP-010 was:
- `gomobile-matsuri bind` generated intermediate `.build/src-android-*` directories
- the Go tool then reported `go: error reading go.mod: missing module declaration`
- `gomobile-matsuri.exe` exited with `go mod tidy -v failed: exit status 1`
`android/fork/app/libs/libcore.aar` was not generated.
The temporary `.build/` directory created by the failed attempt was removed at the end of CP-010, so the intermediate generated files that triggered the error are not currently preserved.
No upstream source files under `android/fork/` have been modified.
No patches, Android product features, per-app routing, transport logic, or build repair work have been performed.

## TARGET_STATE
The repository has a durable diagnosis artifact that isolates and bounds the gomobile/go.mod blocker without repairing it.
After CP-011 execution, a future session must be able to answer:
- which exact generated module path or intermediate source tree first produces the invalid or incomplete `go.mod`
- whether the failure is attributable to:
  - upstream repository layout
  - generated temporary module state
  - `gomobile-matsuri` behavior
  - local dependency materialization or dependency resolution shape
- what exact evidence supports that attribution
- what the smallest safe next repair checkpoint should investigate

This checkpoint does not need to fix the blocker.
It only needs to localize it precisely and preserve the evidence.

## IN_SCOPE
- re-establish the same session-scoped environment used in CP-010
- re-run only the minimum command path needed to reproduce the `libcore` gomobile/go.mod blocker
- preserve the generated intermediate `.build/` state long enough to inspect it during the checkpoint
- identify which generated `src-android-*` tree or other temporary module path first contains the problematic `go.mod`
- inspect the generated `go.mod` files, relevant generated source layout, and immediate surrounding files needed to understand why the module declaration is missing
- inspect the inputs that can influence that generated state:
  - `android/fork/libcore/go.mod`
  - `android/fork/libcore/build.sh`
  - `android/fork/libcore/init.sh`
  - relevant `gomobile-matsuri` invocation details already used by the upstream script
  - the local materialized dependency paths `android/libneko/` and `android/sing-box/` only to the extent required to determine whether their module layout contributes to the failure
- document whether the blocker appears to originate from upstream layout, temporary generated module state, gomobile behavior, or local dependency materialization
- create the diagnostic report and update `PROJECT_STATE.md` with execution results

## OUT_OF_SCOPE
- repairing the blocker
- modifying any upstream source code under `android/fork/`
- applying patches or creating `android/patches/` content
- editing generated files as a workaround
- changing gomobile, Go, JDK, SDK, NDK, or dependency versions
- re-cloning or re-materializing dependencies unless a path must be re-verified as missing or inconsistent
- attempting Gradle app assembly
- Android product feature work
- per-app routing implementation
- transport or proxy logic
- server artifacts or server implementation
- CI/CD changes
- signing configuration changes

## REQUIRED_ARTIFACTS
- `docs/android/ANDROID_LIBCORE_GOMOBILE_BLOCKER_DIAGNOSIS.md`
- `PROJECT_STATE.md` (updated)
- `checkpoints/CP-011.md` (updated with execution result)

## EXECUTION_NOTES
This checkpoint is diagnosis-only. It must not cross into repair.
Use `docs/android/ANDROID_FIRST_BUILD_ATTEMPT.md` as the baseline for the exact failing command and environment.
The execution should reproduce the blocker with as little deviation from CP-010 as possible so the generated temporary state is comparable.
The diagnostic priority is:
1. determine the first exact generated path that yields the invalid `go.mod`
2. inspect the contents of that `go.mod`
3. inspect adjacent generated files or directory structure needed to explain the missing module declaration
4. map the failure back to the most likely origin category:
   - upstream layout
   - generated temporary module state
   - `gomobile-matsuri` behavior
   - local dependency materialization
If multiple candidate causes remain, document the ambiguity explicitly and rank them by evidence.
Do not delete the generated `.build/` directory until after the diagnostic report captures the needed evidence. If it is removed at the end of the checkpoint, the report must preserve the exact paths and file contents needed for future continuity.
Allowed diagnostic steps may include:
- listing generated `.build/` trees
- opening generated `go.mod` files and nearby files
- re-running the upstream command with the same flags
- using read-only commands to inspect module declarations in `android/libneko/` and `android/sing-box/`
- capturing verbose command output if the upstream invocation already emits it or if the added verbosity does not change behavior materially
Not allowed:
- editing generated or source files
- altering command flags as an attempted workaround
- substituting a different build path

## COMPLETION_TEST
This checkpoint is complete only if:
- `docs/android/ANDROID_LIBCORE_GOMOBILE_BLOCKER_DIAGNOSIS.md` exists
- the report identifies the first exact generated path involved in the `go.mod` failure
- the report includes the relevant `go.mod` contents or a precise summary of why the module declaration is missing
- the report states whether the blocker most likely comes from upstream layout, temporary generated module state, `gomobile-matsuri` behavior, or local dependency materialization
- the report documents the exact diagnostic commands and exact evidence used
- the report explicitly separates diagnosis from repair
- no upstream source files under `android/fork/` are modified
- no patches are applied
- no Gradle app build is attempted
- `PROJECT_STATE.md` is updated

## RESULT
partial

## WHAT_CHANGED
- `checkpoints/CP-011.md` was created to define the diagnosis-only checkpoint for the `libcore` gomobile/go.mod blocker.
- No diagnostic commands were run.
- No upstream source, patch, Android implementation, or server artifact was changed as part of this checkpoint definition step.

## REMAINING_GAPS
CP-011 has not been executed yet.
`docs/android/ANDROID_LIBCORE_GOMOBILE_BLOCKER_DIAGNOSIS.md` does not exist yet.
The exact generated failing path, blocker origin category, and evidence-backed attribution remain to be discovered during CP-011 execution.

## NEXT_CHECKPOINT
CP-012 - Apply the smallest validated repair for the libcore gomobile/go.mod blocker

## HANDOFF
Current checkpoint status: partial
Safe next action: execute CP-011 exactly as written to reproduce the gomobile/go.mod failure, inspect the generated `.build/` state, and isolate the blocker origin without attempting repair
Artifacts created or updated:
- checkpoints/CP-011.md
Known risks:
- the failing generated `.build/` state is transient and must be inspected before cleanup during CP-011 execution
- multiple plausible causes may exist across gomobile behavior and local dependency layout, so evidence ranking must stay explicit
- later blockers may remain hidden until this native bridge blocker is diagnosed and eventually repaired
Resume from:
- docs/android/ANDROID_FIRST_BUILD_ATTEMPT.md
- checkpoints/CP-010.md
- checkpoints/CP-011.md
