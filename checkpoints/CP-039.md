# CP-039

## CHECKPOINT_ID
CP-039

## TITLE
Define the smallest bounded focus-boundary correction after CP-038 partial window-state result

## CURRENT_STATE
CP-038 is partial.
The cleared `libcore` metadata bridge remains only an already-validated prerequisite from CP-014.
The persisted `android/sing-box` baseline remains the CP-017 prerequisite on branch `cp017-local-baseline` at `aed32ee3066cdbc7d471e3e0415c5134088962df`.

CP-038 execution confirms:
- all hard prerequisites passed
- bounded probe `dumpsys window windows | grep -m 1 -E 'mCurrentFocus|mFocusedApp'` produced empty output
- probe returned `EXIT_CODE: 1`
- fallback continuity evidence remained healthy:
  - `topResumedActivity ... moe.nb4a.debug/io.nekohasekai.sagernet.ui.MainActivity`
  - `pidof moe.nb4a.debug` returned numeric PID
  - `pm path moe.nb4a.debug` returned package path

The next exact bounded surface is correction of the focus/window signal boundary only.

## TARGET_STATE
The repository has a bounded checkpoint definition that corrects the post-CP-038 focus/window verification boundary using observed CP-038 evidence.

After CP-039 execution, a future session must be able to answer:
- whether one bounded replacement focus/window probe returns a deterministic focus signal for `moe.nb4a.debug`
- what exact prerequisites were true before entering the replacement probe
- what exact first meaningful outcome appears from that one replacement probe
- whether the outcome is a prerequisite failure, replacement-probe failure, or focus/window success
- how this boundary correction remains explicitly separate from UI interaction and runtime debugging

CP-039 does not perform UI interaction.
CP-039 does not perform runtime debugging.
CP-039 does not implement product features.

## IN_SCOPE
- correct the post-CP-038 focus boundary from the observed empty-output/`EXIT_CODE: 1` result
- determine the next deterministic focus/window signal using already-observed CP-038 fallback continuity evidence
- define one bounded replacement focus/window probe:
  - `& "$env:ANDROID_HOME\\platform-tools\\adb.exe" -s <serial> shell "dumpsys window | grep -m 1 -E 'mCurrentFocus|mFocusedApp'"`
- define exact prerequisite checks before probe entry:
  - `android/sing-box` remains on branch `cp017-local-baseline`
  - `android/sing-box` `HEAD` remains `aed32ee3066cdbc7d471e3e0415c5134088962df`
  - CP-038 execution artifact exists:
    - `docs/android/ANDROID_POST_FOREGROUND_STATE_CONTINUATION.md`
  - CP-038 probe/fallback evidence exists:
    - `docs/android/evidence/cp038_prereq_checks.log`
    - `docs/android/evidence/cp038_focus_probe.clean.log`
    - `docs/android/evidence/cp038_focus_fallback.log`
  - CP-037/CP-036/CP-035 continuity artifacts exist:
    - `docs/android/ANDROID_POST_PROCESS_VERIFICATION_CONTINUATION.md`
    - `docs/android/ANDROID_POST_LAUNCH_PROCESS_VERIFICATION.md`
    - `docs/android/ANDROID_POST_INSTALL_VERIFICATION_CONTINUATION.md`
  - target APK/output identity remains available:
    - `android/fork/app/build/outputs/apk/oss/debug/output-metadata.json`
    - `android/fork/app/build/outputs/apk/oss/debug/NekoBox-1.4.2-x86_64-debug.apk`
  - package id expectation remains:
    - `applicationId` equals `moe.nb4a.debug`
  - Android SDK tool path is resolvable:
    - `"$env:ANDROID_HOME\\platform-tools\\adb.exe"` exists
    - or an equivalent explicitly captured adb path is resolvable
  - exactly one online adb target is available
  - target adb device ABI is `x86_64`
  - installed package check passes before replacement probe:
    - `adb shell pm path moe.nb4a.debug` returns a package path
  - process check passes before replacement probe:
    - `adb shell pidof moe.nb4a.debug` returns numeric PID output
  - fallback continuity prerequisite from CP-038 remains true:
    - `adb shell "dumpsys activity activities | grep -m 1 -E 'mResumedActivity|topResumedActivity'"` contains `moe.nb4a.debug` and `MainActivity`
- define exact expected replacement-probe success signals:
  - probe output is non-empty
  - output contains `mCurrentFocus` or `mFocusedApp`
  - output contains `moe.nb4a.debug`
  - output contains `io.nekohasekai.sagernet.ui.MainActivity` or `.ui.MainActivity`
  - `EXIT_CODE: 0`
- define exact out-of-scope actions that must remain unperformed:
  - no UI interaction commands (`adb shell input`, `adb shell monkey`, UI automation)
  - no network actions or traffic probes from device shell
  - no app relaunch commands (`adb shell am start*`)
  - no runtime-debugging capture (`adb logcat`, profiler, traces)
  - no broad diagnostics beyond the single bounded replacement probe and defined fallbacks
  - no `assemble*` continuation
  - no release signing/distribution work
- define exact fallback evidence sources if direct shell output is incomplete:
  - direct shell transcript of the bounded replacement `dumpsys window` probe
  - captured clean transcript file for the same command
  - `adb devices -l` snapshot captured in the same run
  - fallback foreground-state check:
    - `adb shell "dumpsys activity activities | grep -m 1 -E 'mResumedActivity|topResumedActivity'"`
  - fallback process continuity check:
    - `adb shell pidof moe.nb4a.debug`
  - fallback package continuity check:
    - `adb shell pm path moe.nb4a.debug`
- define exact cleanup expectations:
  - do not regenerate `libcore.aar`
  - do not create disposable gomobile/GOPATH workspaces
  - remove only temporary CP-039 scratch files if any are created outside `docs/android/evidence/`
  - retain canonical evidence files under `docs/android/evidence/`
  - do not uninstall package as part of CP-039
  - do not force-stop or kill app process as part of CP-039
  - leave upstream source trees unchanged
- create `docs/android/ANDROID_POST_FOCUS_BOUNDARY_CORRECTION.md` during later CP-039 execution
- update `PROJECT_STATE.md` and `checkpoints/CP-039.md` during later CP-039 execution

## OUT_OF_SCOPE
- executing CP-039
- revisiting any previous build-chain checkpoints
- revisiting or re-solving the cleared `libcore` metadata bridge
- revisiting or re-testing the already-validated `android/sing-box` baseline except as a stated prerequisite
- modifying upstream source under `android/fork/`
- applying broad patches to build logic, dependency trees, or generated workspaces
- UI interaction or user-flow testing
- runtime debugging, profiling, or diagnostic deep dives
- network actions from device shell
- Android product feature implementation
- per-app routing implementation
- transport or proxy logic
- changes to `android/libneko/` or `android/sing-box/` baselines
- server artifacts or server implementation, except `PROJECT_STATE.md` for continuity
- CI/CD changes

## REQUIRED_ARTIFACTS
- `checkpoints/CP-039.md`
- `PROJECT_STATE.md` (updated for CP-039 definition state only)
- `docs/android/ANDROID_POST_FOCUS_BOUNDARY_CORRECTION.md` (execution artifact for later CP-039 run)

## EXECUTION_NOTES
CP-039 is a focus-boundary-correction definition checkpoint.
It must remain narrow and explicitly separate focus/window-state correction from UI interaction and runtime debugging.

Why boundary correction is required:
- CP-038 showed a reproducible direct-probe failure pattern (empty output + `EXIT_CODE: 1`)
- CP-038 fallback continuity evidence showed app process/activity health remained intact
- the smallest valid correction is replacing `dumpsys window windows` with `dumpsys window` while preserving the same focus markers

Execution priority for later CP-039 run:
1. verify all CP-039 prerequisites exactly
2. run the single bounded replacement focus/window probe once
3. capture first exact meaningful outcome and stop
4. do not continue into UI interaction or runtime-debugging workflows

First meaningful outcome rules for CP-039:
- if prerequisite checks fail, the first failed prerequisite line is the checkpoint outcome
- if replacement probe runs but does not show required focus/package/activity signal, the first exact failure line is the checkpoint outcome
- if replacement probe returns required focus/package/activity signal, success signal lines are the checkpoint outcome
- stop immediately once one of those outcomes is captured

Toolchain and environment bounds for later CP-039 run:
- preserve persisted `android/sing-box` baseline at `cp017-local-baseline` / `aed32ee3066cdbc7d471e3e0415c5134088962df`
- preserve Android SDK/platform-tools baseline already used in CP-038
- do not intentionally upgrade Gradle/AGP/Kotlin/Java/Go/Android dependencies during probe
- if adb path/device identity differs from prior checkpoints, capture exact evidence and stop at first meaningful outcome

## COMPLETION_TEST
CP-039 is complete only if:
- `docs/android/ANDROID_POST_FOCUS_BOUNDARY_CORRECTION.md` exists after execution
- the report confirms exact CP-039 prerequisites listed in `IN_SCOPE`
- the report documents exact bounded replacement probe command:
  - `& "$env:ANDROID_HOME\\platform-tools\\adb.exe" -s <serial> shell "dumpsys window | grep -m 1 -E 'mCurrentFocus|mFocusedApp'"`
- the report captures first exact outcome from that one replacement probe and stops
- if probe succeeds, the report includes:
  - non-empty output line containing `mCurrentFocus` or `mFocusedApp`
  - output line containing `moe.nb4a.debug`
  - output line containing `io.nekohasekai.sagernet.ui.MainActivity` or `.ui.MainActivity`
  - `EXIT_CODE: 0`
- the report confirms out-of-scope actions in `IN_SCOPE` remain unperformed
- the report identifies fallback evidence used if direct shell output is incomplete
- the report confirms cleanup expectations were followed
- no upstream source files under `android/fork/` are modified
- no UI-interaction/runtime-debugging/full-assemble continuation is attempted
- `PROJECT_STATE.md` is updated after execution

## RESULT
complete

## WHAT_CHANGED
- `docs/android/ANDROID_POST_FOCUS_BOUNDARY_CORRECTION.md` was created with bounded CP-039 execution evidence.
- CP-039 prerequisite checks were executed and captured, including:
  - `android/sing-box` branch/commit confirmation (`cp017-local-baseline` / `aed32ee3066cdbc7d471e3e0415c5134088962df`)
  - CP-035, CP-036, CP-037, and CP-038 evidence presence checks
  - APK/output identity checks and `applicationId` check (`moe.nb4a.debug`)
  - adb path resolution via explicit SDK path (`C:/Android/Sdk/platform-tools/adb.exe`) because `ANDROID_HOME` was empty in-session
  - exactly-one-online-device check
  - target ABI check (`x86_64`)
  - installed package presence check (`pm path moe.nb4a.debug`)
  - process continuity check (`pidof moe.nb4a.debug`)
  - foreground continuity check (`topResumedActivity ... MainActivity`)
- The single bounded replacement focus/window-state probe was executed exactly once:
  - `& "C:/Android/Sdk/platform-tools/adb.exe" -s emulator-5554 shell "dumpsys window | grep -m 1 -E 'mCurrentFocus|mFocusedApp'"`
- Required replacement-probe success signals were captured:
  - non-empty output containing `mCurrentFocus`
  - output containing `moe.nb4a.debug`
  - output containing `io.nekohasekai.sagernet.ui.MainActivity`
  - `EXIT_CODE: 0`
- Direct output was sufficient; fallback evidence collection was not required in this run.
- Out-of-scope actions remained unperformed (no UI interaction, no relaunch, no network actions, no runtime-debugging commands).
- Cleanup expectations were followed (no `libcore` regeneration, no disposable gomobile/GOPATH workspaces, no upstream source modification).
- `PROJECT_STATE.md` was updated to reflect CP-039 execution as complete.

## REMAINING_GAPS
- No gaps remain inside CP-039.

## NEXT_CHECKPOINT
Create CP-040 only to define the smallest bounded continuation after successful CP-039 focus-boundary correction verification, without entering UI interaction, network actions, runtime debugging, or feature work.

## HANDOFF
Current checkpoint status: complete
Safe next action: create CP-040 only for the next bounded post-focus-boundary-correction continuation definition
Artifacts created or updated:
- checkpoints/CP-039.md
- docs/android/ANDROID_POST_FOCUS_BOUNDARY_CORRECTION.md
- docs/android/evidence/cp039_prereq_checks.log
- docs/android/evidence/cp039_adb_devices.log
- docs/android/evidence/cp039_focus_probe.log
- docs/android/evidence/cp039_focus_probe.clean.log
- docs/android/evidence/cp039_out_of_scope_checks.log
- PROJECT_STATE.md
Known risks:
- scope will drift if UI interaction, runtime-debugging, network actions, or broader continuation starts before CP-040 is defined
- device/focus state can drift between sessions; future checkpoints must re-verify adb and focus prerequisites
- shell output can include non-fatal pipe warnings; continuation must distinguish those from failing success signals
Resume from:
- checkpoints/CP-039.md
- docs/android/ANDROID_POST_FOCUS_BOUNDARY_CORRECTION.md
- PROJECT_STATE.md
