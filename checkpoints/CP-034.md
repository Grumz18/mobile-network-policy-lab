# CP-034

## CHECKPOINT_ID
CP-034

## TITLE
Define the smallest bounded post-APK-verification install-verification probe

## CURRENT_STATE
CP-033 is complete.
The cleared `libcore` metadata bridge remains only an already-validated prerequisite from CP-014.
The persisted `android/sing-box` baseline remains the CP-017 prerequisite on branch `cp017-local-baseline` at `aed32ee3066cdbc7d471e3e0415c5134088962df`.
CP-032 and CP-033 established and validated the package/output boundary at:
- `android/fork/app/build/outputs/apk/oss/debug`

CP-033 execution proved:
- corrected package boundary is valid
- bounded APK verification succeeded for:
  - `android/fork/app/build/outputs/apk/oss/debug/NekoBox-1.4.2-x86_64-debug.apk`
- required verification success signals were present:
  - `Verifies`
  - `Verified using v1 scheme (JAR signing): true`
  - `Verified using v2 scheme (APK Signature Scheme v2): true`
  - `EXIT_CODE: 0`
- install/launch/runtime continuation was not performed

The first exact downstream surface after successful APK verification is bounded device-side install verification.

## TARGET_STATE
The repository has a bounded checkpoint definition for probing only the first exact downstream surface after successful CP-033 APK verification.

After CP-034 execution, a future session must be able to answer:
- whether a single bounded install-verification probe succeeds for the verified APK
- what exact prerequisite environment/device/package conditions were true before entry
- what exact first meaningful outcome appears when the probe is entered
- whether the first outcome is a prerequisite failure, an install failure, or install success
- how install verification is explicitly separated from launch/runtime continuation

CP-034 does not execute launch or runtime validation.
CP-034 does not implement product features.

## IN_SCOPE
- identify the first exact downstream surface after successful APK verification as bounded install verification
- define one bounded probe from `android/fork/`:
  - `& "$env:ANDROID_HOME\\platform-tools\\adb.exe" install -r ".\\app\\build\\outputs\\apk\\oss\\debug\\NekoBox-1.4.2-x86_64-debug.apk"`
- define exact prerequisite checks before probe entry:
  - `android/sing-box` remains on branch `cp017-local-baseline`
  - `android/sing-box` `HEAD` remains `aed32ee3066cdbc7d471e3e0415c5134088962df`
  - CP-033 execution artifacts exist:
    - `docs/android/ANDROID_POST_PACKAGE_BOUNDARY_CORRECTION.md`
    - `docs/android/evidence/cp033_apksigner_verify.log`
    - `docs/android/evidence/cp033_apksigner_verify.clean.log`
  - package metadata and target APK exist:
    - `android/fork/app/build/outputs/apk/oss/debug/output-metadata.json`
    - `android/fork/app/build/outputs/apk/oss/debug/NekoBox-1.4.2-x86_64-debug.apk`
  - package metadata continues to declare expected debug package id:
    - `applicationId` equals `moe.nb4a.debug`
  - Android SDK tool path is resolvable:
    - `"$env:ANDROID_HOME\\platform-tools\\adb.exe"` exists (or equivalent resolved path explicitly captured)
  - exactly one online adb target is available for bounded install verification
  - target adb device ABI is `x86_64` (aligned with selected verified APK)
- define exact expected install-verification success signals:
  - `Performing Streamed Install`
  - `Success`
  - `EXIT_CODE: 0`
- define exact out-of-scope actions that must remain unperformed:
  - no `adb shell am start` app launch
  - no runtime-debugging commands (`adb logcat`, `adb shell dumpsys`, profiler/trace capture)
  - no functional app interaction or feature validation
  - no `assemble*` continuation
  - no release signing/distribution work
- define exact fallback evidence sources if direct shell output is incomplete:
  - direct shell transcript of the `adb install -r` command
  - captured clean transcript file for the same command
  - `adb devices -l` snapshot captured in the same run
  - fallback package-manager presence check (`adb shell pm path moe.nb4a.debug`) only if direct install output is incomplete
  - `android/fork/app/build/outputs/apk/oss/debug/output-metadata.json` snapshot used to confirm package identity and artifact mapping
- define exact cleanup expectations:
  - do not regenerate `libcore.aar`
  - do not create disposable gomobile/GOPATH workspaces
  - remove only temporary CP-034 transcript scratch files if any are created outside `docs/android/evidence/`
  - retain canonical evidence files under `docs/android/evidence/`
  - do not uninstall package as part of CP-034 unless install command itself requires rollback for a failed partial session (if so, capture exact reason/command and stop)
  - leave upstream source trees unchanged
- create `docs/android/ANDROID_POST_APK_VERIFICATION_CONTINUATION.md` during later CP-034 execution
- update `PROJECT_STATE.md` and `checkpoints/CP-034.md` during later CP-034 execution

## OUT_OF_SCOPE
- executing CP-034
- revisiting or re-solving the cleared `libcore` metadata bridge
- revisiting or re-testing the already-validated `android/sing-box` baseline except as a stated prerequisite
- modifying upstream source under `android/fork/`
- applying broad patches to build logic, dependency trees, or generated workspaces
- launch, runtime debugging, or behavior verification beyond the single install-verification probe
- full assemble continuation or packaging redesign
- Android product feature implementation
- per-app routing implementation
- transport or proxy logic
- changes to `android/libneko/` or `android/sing-box/` baselines
- server artifacts or server implementation, except `PROJECT_STATE.md` for continuity
- CI/CD changes

## REQUIRED_ARTIFACTS
- `checkpoints/CP-034.md`
- `PROJECT_STATE.md` (updated for CP-034 definition state only)
- `docs/android/ANDROID_POST_APK_VERIFICATION_CONTINUATION.md` (execution artifact for later CP-034 run)

## EXECUTION_NOTES
CP-034 is an install-verification definition checkpoint.
It must remain narrow and separate install verification from launch/runtime continuation.

Why the next surface is install verification:
- package boundary and APK verification are already confirmed in CP-033
- install verification is the first deterministic downstream gate after APK verification
- install verification can be bounded to one command and one outcome

Execution priority for later CP-034 run:
1. verify all CP-034 prerequisites exactly
2. run the single bounded `adb install -r` probe once
3. capture first exact meaningful outcome and stop
4. do not launch app and do not perform runtime debugging

First meaningful outcome rules for CP-034:
- if prerequisite checks fail, the first failed prerequisite line is the checkpoint outcome
- if `adb install -r` starts and fails, the first exact failure line is the checkpoint outcome
- if `adb install -r` succeeds, success signal lines are the checkpoint outcome
- stop immediately once one of those outcomes is captured

Toolchain and environment bounds for later CP-034 run:
- preserve persisted `android/sing-box` baseline at `cp017-local-baseline` / `aed32ee3066cdbc7d471e3e0415c5134088962df`
- preserve existing Android SDK / platform-tools baseline already used in CP-033
- do not intentionally upgrade Gradle/AGP/Kotlin/Java/Go/Android dependencies during probe
- if adb or SDK path resolution differs from prior checkpoints, capture exact path evidence and stop at first meaningful outcome

## COMPLETION_TEST
CP-034 is complete only if:
- `docs/android/ANDROID_POST_APK_VERIFICATION_CONTINUATION.md` exists after execution
- the report confirms the exact CP-034 prerequisites listed in `IN_SCOPE`
- the report confirms CP-033 boundary continuity and artifact identity:
  - `android/fork/app/build/outputs/apk/oss/debug`
  - `applicationId` is `moe.nb4a.debug`
  - `NekoBox-1.4.2-x86_64-debug.apk` is present
- the report documents exact bounded probe command:
  - `& "$env:ANDROID_HOME\\platform-tools\\adb.exe" install -r ".\\app\\build\\outputs\\apk\\oss\\debug\\NekoBox-1.4.2-x86_64-debug.apk"`
- the report captures first exact outcome from that one probe and stops
- if probe succeeds, the report includes:
  - `Performing Streamed Install`
  - `Success`
  - `EXIT_CODE: 0`
- the report confirms out-of-scope actions in `IN_SCOPE` remain unperformed
- the report identifies fallback evidence used if direct shell output is incomplete
- the report confirms cleanup expectations were followed
- no upstream source files under `android/fork/` are modified
- no launch/runtime-debugging/full-assemble continuation is attempted
- `PROJECT_STATE.md` is updated after execution

## RESULT
partial

## WHAT_CHANGED
- `docs/android/ANDROID_POST_APK_VERIFICATION_CONTINUATION.md` was created with bounded CP-034 execution evidence.
- CP-034 prerequisite checks were executed exactly as authored, including:
  - `JAVA_HOME` and `java -version`
  - `android/sing-box` branch/commit confirmation (`cp017-local-baseline` / `aed32ee3066cdbc7d471e3e0415c5134088962df`)
  - CP-033 artifact presence checks
  - exact APK path check for `android/fork/app/build/outputs/apk/oss/debug/NekoBox-1.4.2-x86_64-debug.apk`
  - metadata `applicationId` check (`moe.nb4a.debug`)
  - adb tool-path resolution check
  - adb device-state checks for exact-single-target and ABI
- The first exact meaningful outcome was captured as prerequisite failure before probe entry:
  - `online adb target count=0`
  - `online adb exactly one => False`
- Out-of-scope launch/runtime/assemble actions remained unperformed.
- CP-034 cleanup expectations were met (no libcore regeneration, no disposable gomobile/GOPATH workspaces, no scratch artifacts outside `docs/android/evidence/`).
- `PROJECT_STATE.md` was updated to reflect CP-034 execution as partial and preserve bounded retry continuity.

## REMAINING_GAPS
- CP-034 bounded install-verification probe was not entered because hard adb prerequisites failed.
- Expected install success signals are still unobserved:
  - `Performing Streamed Install`
  - `Success`
  - `EXIT_CODE: 0`
- The checkpoint needs a retry when exactly one online adb target with ABI `x86_64` is available.

## NEXT_CHECKPOINT
Retry CP-034 execution only after satisfying the adb prerequisite gate (exactly one online `x86_64` target), then run the single bounded install-verification probe without entering launch/runtime-debugging or feature work.

## HANDOFF
Current checkpoint status: partial
Safe next action: re-execute CP-034 only once exactly one online `x86_64` adb target is available
Artifacts created or updated:
- checkpoints/CP-034.md
- docs/android/ANDROID_POST_APK_VERIFICATION_CONTINUATION.md
- PROJECT_STATE.md
Known risks:
- scope will drift if launch/runtime-debugging or broader assemble continuation starts before CP-034 retry completes
- adb state may keep failing the hard prerequisite gate when no online device is attached
- APK filename or ABI drift can invalidate the probe path on retry and must be captured as first meaningful outcome evidence
Resume from:
- checkpoints/CP-034.md
- PROJECT_STATE.md
- docs/android/ANDROID_POST_APK_VERIFICATION_CONTINUATION.md
