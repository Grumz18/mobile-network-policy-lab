# CP-016

## CHECKPOINT_ID
CP-016

## TITLE
Define the smallest bounded `android/sing-box` revision-alignment test

## CURRENT_STATE
CP-015 is complete.
The generated `libcore` metadata bridge is already repaired and validated in `docs/android/ANDROID_LIBCORE_METADATA_BRIDGE_REPAIR.md`.
That earlier checkpoint proved:
- the generated fallback `go.mod` can preserve `replace libcore`
- the generated fallback can preserve the local replacement mappings for `github.com/matsuridayo/libneko` and `github.com/sagernet/sing-box`
- the generated fallback can preserve the original `golang.org/x/mobile` baseline (`v0.0.0-20231108233038-35478a0c49da`)
- the exact bare `libcore` import failure from CP-013 is cleared

CP-015 then isolated the next blocker in `docs/android/ANDROID_POST_METADATA_DEPENDENCY_BLOCKER.md`.
That checkpoint proved:
- the first post-CP-014 generated-workspace dependency blocker is primarily a revision/layout mismatch in the local `android/sing-box/` checkout
- the fork build scripts expect `android/sing-box` commit `aed32ee3066cdbc7d471e3e0415c5134088962df`
- the current local checkout remains at `ab23e111dda5f9ee66fca2d49cb28f39d41192bb` on branch `def`
- the current local tree is missing many `github.com/sagernet/sing-box/...` package directories imported by `libcore`
- the current `github.com/sagernet/cloudflare-tls@v0.0.0-20221031050923-d70792f4c3a0` failure is real in the drifted local checkout, but that edge is absent from the fork-pinned `sing-box` snapshot

No upstream source under `android/fork/` has been modified.
The default installed `gomobile-matsuri` and `gobind-matsuri` binaries remain unchanged.

## TARGET_STATE
The repository has a bounded checkpoint definition for testing only whether aligning the local `android/sing-box/` checkout to the fork-expected commit `aed32ee3066cdbc7d471e3e0415c5134088962df` is sufficient to clear:
- the missing-package errors seen in generated `src-android-*`
- the current `github.com/sagernet/cloudflare-tls` failure

After CP-016 execution, a future session must be able to answer:
- whether a reversible local checkout alignment alone removes the missing-package errors
- whether that same alignment also removes the current `cloudflare-tls` failure
- whether any smaller residual blocker remains immediately after alignment
- how toolchain and dependency drift were bounded during the test

CP-016 does not need to complete the Android build.
It must only define the smallest bounded alignment test and keep it clearly separated from broader build continuation.

## IN_SCOPE
- perform only a reversible local checkout alignment test for `android/sing-box/`
- use the fork-declared commit `aed32ee3066cdbc7d471e3e0415c5134088962df` as the target revision
- recreate only the isolated tool-local workspace shape needed to re-run the already-cleared post-CP-014 generated-workspace validation
- preserve the already-cleared `libcore` metadata bridge behavior from CP-014 without redesigning or re-expanding that repair surface
- validate only whether the alignment removes:
  - the missing-package errors against `github.com/sagernet/sing-box/...`
  - the current `github.com/sagernet/cloudflare-tls@v0.0.0-20221031050923-d70792f4c3a0` failure
- use bounded validation such as:
  - `git -C android/sing-box checkout aed32ee3066cdbc7d471e3e0415c5134088962df`
  - `go list -e -json ./gobind`
  - `go mod tidy -v`
  - `go list -m -e -json github.com/sagernet/cloudflare-tls`
  - read-only inspection of generated `src-android-*`
- keep toolchain and dependency drift bounded and documented
- rollback the local `android/sing-box/` checkout to its pre-test state after evidence capture unless the report explicitly justifies a different temporary preservation for diagnosis
- create `docs/android/ANDROID_SINGBOX_ALIGNMENT_TEST.md`
- update `PROJECT_STATE.md` and `checkpoints/CP-016.md` with execution results

## OUT_OF_SCOPE
- revisiting or re-solving the cleared `libcore` metadata bridge
- modifying upstream source under `android/fork/`, including `android/fork/libcore/`
- editing Go source under `android/sing-box/`, `android/libneko/`, or any other dependency tree
- broad patches to `gomobile-matsuri`, `gobind-matsuri`, or generated workspace layout
- repairing `github.com/sagernet/cloudflare-tls` in isolation before the alignment test result is known
- generic dependency upgrades or version changes as a workaround
- silent dependency upgrades
- persisting patched binaries into the default user GOPATH
- keeping `android/sing-box/` at the aligned revision as a permanent repo change in this checkpoint
- Gradle app assembly or any Android build continuation beyond the point needed to answer the alignment question
- Android product features
- per-app routing implementation
- transport or proxy logic
- server artifacts or server implementation
- CI/CD changes

## REQUIRED_ARTIFACTS
- `docs/android/ANDROID_SINGBOX_ALIGNMENT_TEST.md`
- `PROJECT_STATE.md` (updated after CP-016 execution)
- `checkpoints/CP-016.md` (updated after CP-016 execution)

## EXECUTION_NOTES
CP-016 is a bounded alignment-test checkpoint.
It must not turn into broader dependency repair or Android build continuation.

The execution priority is:
1. capture the current local `android/sing-box/` state so rollback is exact
2. align `android/sing-box/` to `aed32ee3066cdbc7d471e3e0415c5134088962df` only
3. recreate the already-known generated-workspace validation path with the CP-014 metadata bridge behavior preserved
4. observe whether the missing-package errors disappear
5. observe whether the current `cloudflare-tls` failure also disappears
6. stop at the first residual meaningful blocker
7. rollback the local `android/sing-box/` checkout after evidence capture

The diagnostic questions CP-016 must answer explicitly are:
1. after checkout alignment to `aed32ee3066cdbc7d471e3e0415c5134088962df`, do the generated-workspace missing-package errors disappear
2. after that same alignment, does `github.com/sagernet/cloudflare-tls@v0.0.0-20221031050923-d70792f4c3a0` still fail
3. if both symptoms disappear, what is the first new residual blocker
4. if the `cloudflare-tls` error remains, is it still the same exact edge or a different dependency shape
5. what one exact next surface should be selected after the alignment result is known

CP-016 must not broaden beyond that question set.

Version-drift bounds for CP-016 execution:
- preserve the default host Go baseline observed in CP-015 (`go1.23.6`) unless mere observation proves auto-selection cannot be avoided
- preserve the exact `gomobile-matsuri` source revision used in CP-014 and CP-015 (`17d6af34f6bd6d7e1e428e0c652c8b54a46bda4f`)
- preserve the already-validated `golang.org/x/mobile v0.0.0-20231108233038-35478a0c49da` baseline in the generated workspace
- do not intentionally upgrade `github.com/sagernet/sing-box`, `github.com/sagernet/cloudflare-tls`, or related dependencies during the test
- do not intentionally change the target alignment commit away from `aed32ee3066cdbc7d471e3e0415c5134088962df`
- if `go list`, `go mod tidy`, or `go build` still auto-selects a newer toolchain or different dependency version, capture the exact trigger and exact version as evidence
- do not treat any new auto-selected version as the repair

Allowed alignment depth:
- one generated architecture root is sufficient if the evidence is conclusive
- additional roots may be inspected only if the first root is inconclusive
- the only mutable tree in scope is the local `android/sing-box/` checkout, and only for reversible checkout movement
- once the alignment result is known strongly enough to choose the next surface, stop

Not allowed:
- reworking the generated fallback `go.mod` again unless mere reproduction requires the same already-approved CP-014 local behavior
- editing tracked source files under `android/sing-box/`
- turning alignment into a permanent repo state change
- repairing downstream dependency edges before observing the effect of the alignment itself
- continuing into Gradle or app-level build work after the alignment answer is known

Rollback expectations if the alignment test fails or is inconclusive:
- restore `android/sing-box/` to its exact pre-test revision and branch attachment state
- remove any isolated temporary gomobile source workspace created for CP-016
- remove any isolated temporary GOPATH or patched binaries created for CP-016
- remove `android/fork/libcore/.build/` after evidence capture unless the report explicitly preserves it
- leave the default installed `gomobile-matsuri` and `gobind-matsuri` binaries unchanged
- leave all upstream source trees unchanged
- stop without continuing into Gradle or broader build continuation
- document the exact stopping point and any inconclusive evidence in `docs/android/ANDROID_SINGBOX_ALIGNMENT_TEST.md`

## COMPLETION_TEST
CP-016 is complete only if:
- `docs/android/ANDROID_SINGBOX_ALIGNMENT_TEST.md` exists
- the report documents the exact pre-test and aligned `android/sing-box/` revisions
- the report explicitly states whether alignment to `aed32ee3066cdbc7d471e3e0415c5134088962df` removes the missing-package errors
- the report explicitly states whether that same alignment removes the current `cloudflare-tls` failure
- the report identifies the exact first meaningful failure point after the alignment, or states that both prior symptoms cleared
- the report documents the exact commands and exact evidence used
- the report bounds Go toolchain and dependency version drift during the attempt
- the report selects one exact smallest next surface to test after the alignment result
- the report explicitly separates that next surface from broader build continuation
- no upstream source files under `android/fork/` are modified
- no broad patches are applied
- no Gradle app build is attempted
- `PROJECT_STATE.md` is updated after execution

## RESULT
complete

## WHAT_CHANGED
- Performed a reversible alignment of `android/sing-box` from `ab23e111dda5f9ee66fca2d49cb28f39d41192bb` on branch `def` to detached commit `aed32ee3066cdbc7d471e3e0415c5134088962df`.
- Recreated the same bounded generated-workspace validation surface from CP-015 with the already-cleared CP-014 metadata bridge behavior preserved in a disposable tool-local `gomobile-matsuri` clone.
- Created `docs/android/ANDROID_SINGBOX_ALIGNMENT_TEST.md` with the exact commands, aligned and restored checkout states, generated-workspace evidence, drift bounds, and explicit rollback.
- Proved that the alignment alone clears both the missing-package errors and the current `github.com/sagernet/cloudflare-tls` failure, with no new meaningful blocker appearing inside the bounded validation surface before transient `libcore.aar` production.
- Restored `android/sing-box` exactly to branch `def` at `ab23e111dda5f9ee66fca2d49cb28f39d41192bb` and removed all transient CP-016 workspaces, logs, `.build`, and the copied `libcore.aar`.

## REMAINING_GAPS
No gaps remain inside CP-016.
The alignment question is answered, but the aligned local dependency state was intentionally not persisted.
The next checkpoint still needs to decide how to establish that result as an approved local baseline without expanding into broader Android build continuation.

## NEXT_CHECKPOINT
Create CP-017 to define the smallest bounded checkpoint for establishing `android/sing-box` at the fork-expected commit `aed32ee3066cdbc7d471e3e0415c5134088962df` as the local dependency baseline and re-validating the transient `libcore` path, without entering Gradle or broader app build work.

## HANDOFF
Current checkpoint status: complete
Safe next action: create CP-017 only, scoped to a bounded local-baseline checkpoint for persisting the proven `android/sing-box` alignment result and re-validating the transient `libcore` path
Artifacts created or updated:
- docs/android/ANDROID_SINGBOX_ALIGNMENT_TEST.md
- checkpoints/CP-016.md
Known risks:
- the next session may revisit the already-cleared `libcore` metadata bridge instead of staying on the now-proven `android/sing-box` alignment result
- the current local checkout is intentionally rolled back to `def` at `ab23e111dda5f9ee66fca2d49cb28f39d41192bb`, so the cleared blocker will reappear until an approved baseline checkpoint persists the alignment
- broader dependency or Gradle work could still obscure the narrow result if the next checkpoint does not keep the persisted alignment step separate from build continuation
Resume from:
- docs/android/ANDROID_SINGBOX_ALIGNMENT_TEST.md
- docs/android/ANDROID_POST_METADATA_DEPENDENCY_BLOCKER.md
- docs/android/ANDROID_LIBCORE_METADATA_BRIDGE_REPAIR.md
- checkpoints/CP-016.md
- PROJECT_STATE.md
