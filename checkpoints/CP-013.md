# CP-013

## CHECKPOINT_ID
CP-013

## TITLE
Diagnose the generated `libcore` import/layout blocker and select the smallest next repair surface

## CURRENT_STATE
CP-012 is complete.
The zero-byte generated `go.mod` blocker in the libcore gomobile path was cleared only inside an isolated local `gomobile-matsuri` repair workspace during CP-012.
In that isolated run:
- each generated `android/fork/libcore/.build/src-android-*` `go.mod` became non-zero and structurally valid
- direct `go mod tidy -v` succeeded inside `android/fork/libcore/.build/src-android-386`
- `android/fork/libcore/build.sh` advanced beyond the old missing-module-declaration error
The first new meaningful blocker observed in CP-012 was:
- `gobind/go_libcoremain.go:19:2: package libcore is not in std (...)`
CP-012 preserved the following exact evidence:
- `gobind/go_libcoremain.go` imported `libcore`
- `android/fork/libcore/.build/src-android-386` contained `gobind/`, `go.mod`, and `go.sum`
- `android/fork/libcore/.build/src-android-386` did not contain a `libcore/` directory
The fallback generated `go.mod` observed in CP-012 was:
- `module gobind`
- `go 1.25.0`
- `require golang.org/x/mobile v0.0.0-20260410095206-2cfb76559b7b`
The upstream package layout under `android/fork/libcore/` remains unchanged and contains root-level Go files in `package libcore` with `module libcore` in `android/fork/libcore/go.mod`.
The isolated CP-012 tool workspace and generated `.build/` directory were removed after evidence capture.
The default installed `gomobile-matsuri` and `gobind-matsuri` binaries remain unchanged.
CP-012 also observed version drift that must now be bounded:
- host Go command: `go1.23.6`
- patched repair binary build toolchain: `go1.24.0`
- generated fallback module resolution pulled `golang.org/x/mobile v0.0.0-20260410095206-2cfb76559b7b`
- `go mod tidy` auto-selected `go1.25.0`

## TARGET_STATE
The repository has a durable diagnosis artifact that explains why the generated `src-android-*` workspace fails to resolve `libcore` after the zero-byte `go.mod` symptom is cleared.
After CP-013 execution, a future session must be able to answer:
- whether the missing `libcore` resolution comes primarily from:
  - `gomobile-matsuri` generation behavior
  - `libcore` package layout assumptions
  - the current bind invocation shape
  - temporary workspace/module structure
- whether the immediate failure is caused by:
  - missing copied source files in the generated workspace
  - missing module metadata or replace directives in the generated `go.mod`
  - an import-path mismatch between `gobind` output and the generated workspace
  - some combination of the above
- what the smallest next repair surface should be
- how version drift must be constrained before that repair is attempted

CP-013 does not need to repair the blocker.
It must isolate it precisely, preserve the evidence, and select one exact next repair surface.

## IN_SCOPE
- recreate only the isolated local tool workspace shape needed to reach the post-CP-012 `libcore` import/layout blocker
- use the same exact `gomobile-matsuri` revision identified in CP-012 (`17d6af34f6bd6d7e1e428e0c652c8b54a46bda4f`) when reproducing the generated workspace
- reapply only the minimum temporary tool-local change needed to clear the zero-byte `go.mod` symptom so the later `libcore` import/layout blocker can be inspected again
- preserve `android/fork/libcore/.build/` long enough to inspect:
  - `.build/src`
  - `.build/src-android-*`
  - generated `gobind/` sources
  - generated `go.mod` and `go.sum`
- inspect the generated workspace shape to determine where `libcore` source files are expected to exist versus where they actually exist
- inspect relevant `gomobile-matsuri` source files involved in:
  - generated module file construction
  - temporary workspace source copying
  - per-architecture `src-android-*` workspace creation
  - `gobind` invocation context
- inspect `android/fork/libcore/` package and module layout only to the extent required to compare it with the generated workspace
- determine whether the immediate blocker is best attributed to:
  - `gomobile-matsuri` generation behavior
  - `libcore` package layout assumptions
  - bind invocation shape
  - temporary workspace/module structure
- define one exact smallest repair surface to test next
- explicitly bound Go toolchain and `golang.org/x/mobile` version drift for the next repair attempt
- document rollback expectations
- create `docs/android/ANDROID_LIBCORE_LAYOUT_BLOCKER_DIAGNOSIS.md`
- update `PROJECT_STATE.md` and `checkpoints/CP-013.md` with execution results

## OUT_OF_SCOPE
- modifying upstream source under `android/fork/`, including `android/fork/libcore/`
- persisting any patched `gomobile-matsuri` or `gobind-matsuri` binaries into the default user GOPATH
- applying the next repair after diagnosis
- broad patches across gomobile, the Android fork, or dependencies
- changing Go, JDK, SDK, NDK, gomobile, gobind, or dependency versions as a repair step
- re-materializing `android/libneko/` or `android/sing-box/` unless an execution-time diagnostic step proves a required local path is missing
- Gradle app assembly or any Android build continuation beyond the point needed to reproduce the `libcore` import/layout blocker
- Android product features
- per-app routing implementation
- transport or proxy logic
- server artifacts or server implementation
- CI/CD changes

## REQUIRED_ARTIFACTS
- `docs/android/ANDROID_LIBCORE_LAYOUT_BLOCKER_DIAGNOSIS.md`
- `PROJECT_STATE.md` (updated after CP-013 execution)
- `checkpoints/CP-013.md` (updated after CP-013 execution)

## EXECUTION_NOTES
CP-013 is a diagnosis-and-bounding checkpoint.
It must not turn into broad repair work or broader build continuation.

The execution priority is:
1. reproduce the post-CP-012 `libcore` import/layout blocker in an isolated workspace
2. preserve the generated `.build/` tree long enough to inspect workspace layout
3. determine why `gobind/go_libcoremain.go` imports `libcore` while the generated workspace does not resolve that import
4. decide which single smallest repair surface should be tested next
5. record version-drift bounds and rollback expectations

The diagnostic questions CP-013 must answer explicitly are:
1. does `.build/src` contain the root `libcore` package files before `src-android-*` copying happens
2. if so, where do those files end up, or fail to end up, in `src-android-*`
3. if not, which gomobile generation step failed to stage them
4. is `gobind/go_libcoremain.go` importing `libcore` in a way that assumes:
   - module replacement back to `android/fork/libcore`
   - copied `libcore` source under the temporary workspace
   - a different bind invocation or package path
5. is the missing resolution primarily a workspace-layout problem or a generated-module-metadata problem

The candidate cause categories must be evaluated and ranked with evidence:
- `gomobile-matsuri` generation behavior
- `libcore` package layout assumptions
- bind invocation shape
- temporary workspace/module structure

The smallest next repair surface selected at the end of CP-013 must be exactly one of these classes:
- generated fallback `go.mod` metadata for the local `libcore` module and its local replacements
- temporary workspace source-copy/layout behavior for the `libcore` package
- a bind invocation adjustment that remains tool-local and does not edit upstream `android/fork/` content

CP-013 must choose one class only and explain why the other classes were not selected yet.

Version-drift bounds for CP-013 execution:
- preserve the default host Go baseline observed in CP-012 unless mere observation proves the toolchain auto-selection cannot be avoided
- preserve the exact `gomobile-matsuri` source revision from CP-012
- do not intentionally upgrade `golang.org/x/mobile`
- if `go mod tidy` or `go build` auto-selects a newer toolchain or resolves a newer `golang.org/x/mobile` version, capture the exact version and exact trigger as evidence
- do not treat auto-selected newer versions as the repair unless the evidence proves version drift is itself the blocker

Allowed diagnostic steps include:
- reproducing the isolated CP-012 tool-local `go.mod` fallback only as a temporary prerequisite to reach the new blocker
- running read-only inspections over `android/fork/libcore/` and generated `.build/` trees
- using `go env`, `go list`, and direct inspection commands in generated workspaces
- using extra verbosity only if it does not materially change behavior

Not allowed:
- editing upstream Android fork content
- persisting patched tool binaries into the default environment
- changing versions as a workaround
- continuing into Gradle or app build steps after the `libcore` import/layout blocker is reproduced and inspected

Rollback expectations if the diagnostic attempt fails:
- remove any isolated temporary gomobile source workspace created for CP-013
- remove any isolated temporary GOPATH or patched binaries created for CP-013
- remove `android/fork/libcore/.build/` after evidence capture unless the report explicitly preserves it
- leave the default installed `gomobile-matsuri` and `gobind-matsuri` binaries unchanged
- stop without changing `android/fork/` source and without continuing into Gradle or app-level build work
- document the exact failure point and missing evidence in `docs/android/ANDROID_LIBCORE_LAYOUT_BLOCKER_DIAGNOSIS.md`

## COMPLETION_TEST
CP-013 is complete only if:
- `docs/android/ANDROID_LIBCORE_LAYOUT_BLOCKER_DIAGNOSIS.md` exists
- the report identifies the exact generated path relationship that causes `libcore` import resolution to fail
- the report states whether the blocker most likely comes from gomobile generation behavior, `libcore` package layout assumptions, bind invocation shape, temporary workspace/module structure, or a ranked combination
- the report explains whether the critical missing piece is source-copy/layout, generated module metadata, import-path mismatch, or a ranked combination
- the report documents the exact commands and exact evidence used
- the report explicitly bounds Go toolchain and `golang.org/x/mobile` version drift for the next repair attempt
- the report selects one exact smallest repair surface to test next
- the report documents rollback expectations
- no upstream source files under `android/fork/` are modified
- no broad patches are applied
- no Gradle app build is attempted
- `PROJECT_STATE.md` is updated after execution

## RESULT
partial

## WHAT_CHANGED
- `checkpoints/CP-013.md` was created to define the smallest bounded diagnosis checkpoint for the generated `libcore` import/layout blocker exposed after CP-012.
- The checkpoint explicitly limits later execution to blocker diagnosis, version-drift bounding, and selection of one exact next repair surface.
- Execution was intentionally not performed in this session.

## REMAINING_GAPS
CP-013 has not been executed yet.
`docs/android/ANDROID_LIBCORE_LAYOUT_BLOCKER_DIAGNOSIS.md` does not exist yet because the diagnostic run is deferred.
The generated `libcore` import/layout blocker remains unresolved until CP-013 is executed.

## NEXT_CHECKPOINT
Execute CP-013 to reproduce the generated `libcore` import/layout blocker, preserve the temporary workspace, diagnose the exact cause category, and select one exact next repair surface without continuing into broader Android build work.

## HANDOFF
Current checkpoint status: partial
Safe next action: execute CP-013 in an isolated gomobile workspace and document the generated `libcore` import/layout diagnosis in `docs/android/ANDROID_LIBCORE_LAYOUT_BLOCKER_DIAGNOSIS.md`
Artifacts created or updated:
- checkpoints/CP-013.md
Known risks:
- the observed blocker may still combine temporary workspace layout and generated module-metadata issues, so CP-013 must avoid overcommitting to a repair surface before the preserved `.build/` tree is inspected
- version drift from CP-012 may obscure the real cause if `golang.org/x/mobile` or the Go toolchain are allowed to change during diagnosis
- broader Android native build blockers may still appear after the current generated `libcore` import/layout issue is isolated
Resume from:
- docs/android/ANDROID_LIBCORE_GOMOBILE_REPAIR_ATTEMPT.md
- PROJECT_STATE.md
- checkpoints/CP-013.md
