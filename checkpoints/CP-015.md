# CP-015

## CHECKPOINT_ID
CP-015

## TITLE
Isolate the first post-metadata dependency-resolution blocker in generated `src-android-*`

## CURRENT_STATE
CP-014 is complete.
The generated `libcore` metadata bridge is already repaired and validated in `docs/android/ANDROID_LIBCORE_METADATA_BRIDGE_REPAIR.md`.
That checkpoint proved:
- the generated fallback `go.mod` can preserve `replace libcore`
- the generated fallback can preserve the local replacement mappings for `github.com/matsuridayo/libneko` and `github.com/sagernet/sing-box`
- the generated fallback can preserve the original `golang.org/x/mobile` baseline (`v0.0.0-20231108233038-35478a0c49da`)
- the exact bare `libcore` import failure from CP-013 is cleared

The first new meaningful blocker observed immediately after that repair was in generated-workspace dependency resolution during `go mod tidy -v` and equivalent validation.
CP-014 captured two overlapping symptoms:
- many imports under `github.com/sagernet/sing-box/...` were reported as missing from the replaced local `android/sing-box` tree
- the dependency graph still hit `github.com/sagernet/cloudflare-tls@v0.0.0-20221031050923-d70792f4c3a0: invalid version: unknown revision d70792f4c3a0`

Representative failure lines observed in CP-014:
- `module github.com/sagernet/sing-box@latest found (v1.13.8, replaced by ...\android\sing-box), but does not contain package github.com/sagernet/sing-box/adapter/endpoint`
- `github.com/sagernet/cloudflare-tls@v0.0.0-20221031050923-d70792f4c3a0: invalid version: unknown revision d70792f4c3a0`

The local `android/sing-box` tree remains cloned at default branch HEAD and is not pinned to a revision matching the upstream fork snapshot.
No upstream source under `android/fork/` has been modified.
The default installed `gomobile-matsuri` and `gobind-matsuri` binaries remain unchanged.

## TARGET_STATE
The repository has a bounded checkpoint definition for isolating the first post-CP-014 generated-workspace dependency blocker without revisiting the cleared `libcore` metadata bridge.
After CP-015 execution, a future session must be able to answer:
- whether the current blocker is primarily caused by:
  - `android/sing-box` revision/layout mismatch
  - the unresolved `github.com/sagernet/cloudflare-tls` revision
  - an interaction between both
- which exact symptom occurs first in the generated workspace once the `libcore` bridge is already present
- what single smallest diagnostic or repair surface should be tested next
- how version drift must be constrained while answering those questions

CP-015 does not need to fix the blocker.
It must isolate the blocker precisely, preserve evidence, and select one exact next surface only.

## IN_SCOPE
- recreate only the isolated tool-local workspace shape needed to reach the post-CP-014 generated-workspace dependency blocker again
- preserve the already-cleared metadata bridge behavior from CP-014 without redesigning or re-expanding that repair surface
- inspect the generated `src-android-*` workspace only to the extent needed to determine which dependency symptom appears first after the metadata bridge is present
- inspect the local `android/sing-box/` module and package layout only to the extent needed to compare:
  - package paths imported by `libcore`
  - package paths actually present in `android/sing-box/`
  - module path and version declarations relevant to those imports
- inspect the unresolved `github.com/sagernet/cloudflare-tls` edge only to the extent needed to determine whether it blocks before, after, or together with the missing-package symptoms
- use bounded generated-workspace validation such as:
  - `go list -e -json ./gobind`
  - `go mod tidy -v`
  - `go list -m -json`
  - read-only file inspection under `android/sing-box/`
- classify the blocker as primarily:
  - sing-box revision/layout mismatch
  - cloudflare-tls invalid revision
  - interaction between the two
- define one exact smallest next surface to test after CP-015
- keep version drift bounded and documented
- create `docs/android/ANDROID_POST_METADATA_DEPENDENCY_BLOCKER.md`
- update `PROJECT_STATE.md` and `checkpoints/CP-015.md` with execution results

## OUT_OF_SCOPE
- revisiting or re-solving the cleared `libcore` metadata bridge
- modifying upstream source under `android/fork/`, including `android/fork/libcore/`
- broad changes to `gomobile-matsuri`, `gobind-matsuri`, or generated workspace layout
- persisting patched binaries into the default user GOPATH
- fixing both blocker classes at once
- broad dependency upgrades or version changes as a workaround
- silent dependency upgrades
- re-materializing external dependencies to different revisions as a repair step before the blocker is isolated
- Gradle app assembly or any Android build continuation beyond the point needed to classify the current blocker
- Android product features
- per-app routing implementation
- transport or proxy logic
- server artifacts or server implementation
- CI/CD changes

## REQUIRED_ARTIFACTS
- `docs/android/ANDROID_POST_METADATA_DEPENDENCY_BLOCKER.md`
- `PROJECT_STATE.md` (updated after CP-015 execution)
- `checkpoints/CP-015.md` (updated after CP-015 execution)

## EXECUTION_NOTES
CP-015 is a diagnosis-and-classification checkpoint.
It must not turn into repair of multiple downstream module problems.

The execution priority is:
1. recreate the isolated post-CP-014 generated workspace with the metadata bridge already present
2. identify whether generated-workspace failure first manifests as missing `android/sing-box` packages, the unresolved `cloudflare-tls` revision, or both together
3. determine which cause category is primary enough to justify the next checkpoint
4. choose one exact smallest next surface only
5. stop without continuing into broader build or dependency repair

The diagnostic questions CP-015 must answer explicitly are:
1. are the `github.com/sagernet/sing-box/...` imports referenced by `libcore` actually absent from the current local `android/sing-box/` tree
2. if they are absent, is that best explained by branch/revision mismatch with the upstream fork snapshot
3. does `github.com/sagernet/cloudflare-tls@v0.0.0-20221031050923-d70792f4c3a0` fail independently of the missing-package symptom
4. when both symptoms appear in the same validation run, which one is the primary next blocker for the smallest checkpoint
5. what single next surface should be tested first:
   - source/revision alignment for `android/sing-box`
   - dependency-edge handling for `github.com/sagernet/cloudflare-tls`
   - a narrowly justified interaction test if neither can be separated cleanly

CP-015 must choose one class only at the end and explain why the other classes were not selected yet.

Version-drift bounds for CP-015 execution:
- preserve the default host Go baseline observed in CP-014 (`go1.23.6`) unless mere observation proves auto-selection cannot be avoided
- preserve the exact `gomobile-matsuri` source revision used in CP-014 (`17d6af34f6bd6d7e1e428e0c652c8b54a46bda4f`)
- preserve the already-validated `golang.org/x/mobile v0.0.0-20231108233038-35478a0c49da` baseline in the generated workspace
- do not intentionally upgrade `github.com/sagernet/sing-box`, `github.com/sagernet/cloudflare-tls`, or related dependencies during diagnosis
- if `go list`, `go mod tidy`, or `go build` still auto-selects a newer toolchain or different dependency version, capture the exact trigger and exact version as evidence
- do not treat any new auto-selected version as the repair

Allowed diagnostic depth:
- one generated architecture root is sufficient if the evidence is conclusive
- additional roots may be inspected only if the first root is inconclusive
- read-only inspection of `android/sing-box/` should be limited to paths directly implicated by the failing imports
- once the blocker is classified and the next surface is selected, stop

Not allowed:
- reworking the generated fallback `go.mod` again unless mere reproduction requires the same already-approved CP-014 local behavior
- editing `android/sing-box/`, `android/libneko/`, or any other upstream source tree
- turning diagnosis into source alignment, vendoring, or dependency replacement work
- continuing into Gradle or app-level build work after the blocker classification is known

Rollback expectations if the diagnostic attempt fails or is inconclusive:
- remove any isolated temporary gomobile source workspace created for CP-015
- remove any isolated temporary GOPATH or patched binaries created for CP-015
- remove `android/fork/libcore/.build/` after evidence capture unless the report explicitly preserves it
- leave the default installed `gomobile-matsuri` and `gobind-matsuri` binaries unchanged
- leave all upstream source trees unchanged
- stop without continuing into Gradle or broader build continuation
- document the exact stopping point and missing evidence in `docs/android/ANDROID_POST_METADATA_DEPENDENCY_BLOCKER.md`

## COMPLETION_TEST
CP-015 is complete only if:
- `docs/android/ANDROID_POST_METADATA_DEPENDENCY_BLOCKER.md` exists
- the report explicitly states whether the blocker is primarily caused by sing-box revision/layout mismatch, cloudflare-tls invalid revision, or their interaction
- the report identifies the exact first meaningful failure point after the cleared `libcore` bridge
- the report documents the exact commands and exact evidence used
- the report bounds Go toolchain and dependency version drift during the attempt
- the report selects one exact smallest next surface to test
- the report explicitly separates that next surface from broader build continuation
- no upstream source files under `android/fork/` are modified
- no broad patches are applied
- no Gradle app build is attempted
- `PROJECT_STATE.md` is updated after execution

## RESULT
partial

## WHAT_CHANGED
- `checkpoints/CP-015.md` was created to define the smallest bounded checkpoint for the first post-CP-014 generated-workspace dependency-resolution blocker.
- The checkpoint explicitly limits later execution to classifying sing-box revision/layout mismatch versus the unresolved cloudflare-tls revision, or their interaction, without revisiting the cleared `libcore` bridge.
- Execution was intentionally not performed in this session.

## REMAINING_GAPS
CP-015 has not been executed yet.
`docs/android/ANDROID_POST_METADATA_DEPENDENCY_BLOCKER.md` does not exist yet because the diagnostic run is deferred.
The post-CP-014 dependency blocker remains unclassified until CP-015 is executed.

## NEXT_CHECKPOINT
Execute CP-015 to classify the first post-CP-014 dependency-resolution blocker and select one exact next surface without revisiting the cleared `libcore` metadata bridge or continuing into broader Android build work.

## HANDOFF
Current checkpoint status: partial
Safe next action: execute CP-015 in an isolated gomobile workspace and stop as soon as the post-bridge blocker is classified strongly enough to select one exact next surface
Artifacts created or updated:
- checkpoints/CP-015.md
Known risks:
- the next session may drift back into already-solved `libcore` metadata work instead of staying on the new post-bridge blocker
- the sing-box missing-package symptom and the unresolved cloudflare-tls revision may overlap closely enough to tempt broad repair instead of diagnosis
- the local `android/sing-box` tree is still branch HEAD rather than a fork-matched revision, so revision mismatch is plausible but not yet proven as the primary cause
Resume from:
- docs/android/ANDROID_LIBCORE_METADATA_BRIDGE_REPAIR.md
- checkpoints/CP-015.md
- PROJECT_STATE.md
