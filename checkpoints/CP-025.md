# CP-025

## CHECKPOINT_ID
CP-025

## TITLE
Define the smallest bounded post-merge-ext-dex continuation probe

## CURRENT_STATE
CP-024 is complete.
The generated `libcore` metadata bridge is already repaired and validated in `docs/android/ANDROID_LIBCORE_METADATA_BRIDGE_REPAIR.md`.
That earlier checkpoint proved:
- the generated fallback `go.mod` can preserve `replace libcore`
- the generated fallback can preserve the local replacement mappings for `github.com/matsuridayo/libneko` and `github.com/sagernet/sing-box`
- the generated fallback can preserve the original `golang.org/x/mobile` baseline (`v0.0.0-20231108233038-35478a0c49da`)
- the exact bare `libcore` import failure from CP-013 is cleared

CP-017 then intentionally persisted the validated `android/sing-box` alignment in `docs/android/ANDROID_SINGBOX_BASELINE_PERSIST.md`.
That checkpoint proved:
- the local `android/sing-box` baseline is intentionally tracked on branch `cp017-local-baseline`
- that persisted baseline points to commit `aed32ee3066cdbc7d471e3e0415c5134088962df`
- the same bounded `android/fork/libcore/build.sh` path still proceeds through transient `android/fork/app/libs/libcore.aar` production under that persisted baseline

CP-018 through CP-023 then execution-validated the exact downstream app consumer surfaces through:
- `.\gradlew.bat :app:compileOssDebugKotlin --stacktrace`
- `.\gradlew.bat :app:compileOssDebugJavaWithJavac --stacktrace`
- `.\gradlew.bat :app:bundleOssDebugClassesToCompileJar --stacktrace`
- `.\gradlew.bat :app:bundleOssDebugClassesToRuntimeJar --stacktrace`
- `.\gradlew.bat :app:dexBuilderOssDebug --stacktrace`
- `.\gradlew.bat :app:mergeProjectDexOssDebug --stacktrace`

Those checkpoints proved that the persisted sing-box baseline clears all of those surfaces and corrected the post-`mergeProjectDexOssDebug` boundary to:
- `android/fork/app/build/intermediates/dex/ossDebug/mergeProjectDexOssDebug`

CP-024 then executed the first bounded post-merge-project-dex merge-dex probe in `docs/android/ANDROID_POST_MERGE_PROJECT_DEX_CONTINUATION.md`.
That checkpoint proved:
- the prerequisite artifact before Gradle entry still remains `android/fork/app/libs/libcore.aar`
- the exact bounded command `.\gradlew.bat :app:mergeExtDexOssDebug --stacktrace` succeeds under the persisted sing-box baseline
- the exact expected success artifact path is now:
  - `android/fork/app/build/intermediates/dex/ossDebug/mergeExtDexOssDebug`
- the exact observed merge-ext-dex success artifacts now exist:
  - `android/fork/app/build/intermediates/dex/ossDebug/mergeExtDexOssDebug/classes.dex`
  - `android/fork/app/build/intermediates/dex/ossDebug/mergeExtDexOssDebug/classes2.dex`
- the following downstream merge-dex output still remains absent after CP-024:
  - `android/fork/app/build/intermediates/dex/ossDebug/mergeLibDexOssDebug`
- APK outputs still remain absent after CP-024:
  - `android/fork/app/build/outputs/apk/`

The next downstream surface is now identifiable from actual task and artifact flow:
- CP-024 already execution-validated `mergeExtDexOssDebug`
- the same local task family exposed:
  - `mergeProjectDexOssDebug`
  - `mergeExtDexOssDebug`
  - `mergeLibDexOssDebug`
- `android/fork/app/build/intermediates/dex/ossDebug/mergeProjectDexOssDebug` now exists
- `android/fork/app/build/intermediates/dex/ossDebug/mergeExtDexOssDebug` now exists
- `android/fork/app/build/intermediates/sub_project_dex_archive/ossDebug/dexBuilderOssDebug/out` exists
- `android/fork/app/build/intermediates/dex/ossDebug/mergeLibDexOssDebug` is still absent
- `mergeLibDexOssDebug` is therefore the smallest next still-unexecuted surface supported by actual task naming and still-absent output flow, while packaging and `assemble*` remain broader downstream work

No upstream source under `android/fork/` has been modified.
The default installed `gomobile-matsuri` and `gobind-matsuri` binaries remain unchanged.

## TARGET_STATE
The repository has a bounded checkpoint definition for probing only the first exact downstream build surface after the observed successful `.\gradlew.bat :app:mergeExtDexOssDebug --stacktrace` result.

After CP-025 execution, a future session must be able to answer:
- whether `.\gradlew.bat :app:mergeLibDexOssDebug --stacktrace` is the correct next exact downstream consumer surface after successful `mergeExtDexOssDebug`
- what exact prerequisite artifact and prerequisite baseline must still be true before entering that merge-lib-dex surface
- what exact first meaningful outcome appears when that single merge-lib-dex probe is entered
- whether the first new outcome is:
  - a task-graph failure before `:app:mergeLibDexOssDebug` starts
  - a failure inside `:app:mergeLibDexOssDebug`
  - or a successful merge-lib-dex continuation signal
- how that probe is kept separate from packaging, install, assemble, or repair work

CP-025 does not need to fix merge-lib-dex failures.
It must only define the smallest bounded continuation probe after the observed successful `mergeExtDexOssDebug` result and stop at the first exact downstream merge-lib-dex result.

## IN_SCOPE
- select the exact downstream build surface after successful `:app:mergeExtDexOssDebug`
- document why `.\gradlew.bat :app:mergeLibDexOssDebug --stacktrace` is the correct next bounded surface:
  - CP-024 already execution-validated `mergeExtDexOssDebug`
  - `android/fork/app/build/intermediates/dex/ossDebug/mergeExtDexOssDebug` now exists
  - `android/fork/app/build/intermediates/sub_project_dex_archive/ossDebug/dexBuilderOssDebug/out` now exists
  - `android/fork/app/build/intermediates/dex/ossDebug/mergeLibDexOssDebug` is still absent
  - packaging and `assemble*` are broader than the first post-merge-ext-dex continuation point
- define the exact entry command for the probe from `android/fork/`:
  - `.\gradlew.bat :app:mergeLibDexOssDebug --stacktrace`
- define the exact prerequisite checks that must remain unchanged before the probe:
  - `android/sing-box` stays on branch `cp017-local-baseline`
  - `android/sing-box` stays at `aed32ee3066cdbc7d471e3e0415c5134088962df`
  - the cleared metadata bridge remains only an existing prerequisite reused through the bounded `libcore` path
  - transient `android/fork/app/libs/libcore.aar` is reproduced again before Gradle entry because CP-024 cleanup removed it
  - existing compile-jar output remains present before merge-lib-dex entry at:
    - `android/fork/app/build/intermediates/compile_app_classes_jar/ossDebug/bundleOssDebugClassesToCompileJar/classes.jar`
  - existing runtime-jar output remains present before merge-lib-dex entry at:
    - `android/fork/app/build/intermediates/runtime_app_classes_jar/ossDebug/bundleOssDebugClassesToRuntimeJar/classes.jar`
  - existing project dex archive output remains present before merge-lib-dex entry at:
    - `android/fork/app/build/intermediates/project_dex_archive/ossDebug`
    - `android/fork/app/build/intermediates/project_dex_archive/ossDebug/dexBuilderOssDebug/out`
  - existing external libs dex archive output remains present before merge-lib-dex entry at:
    - `android/fork/app/build/intermediates/external_libs_dex_archive/ossDebug/dexBuilderOssDebug/out`
  - existing sub-project dex archive output remains present before merge-lib-dex entry at:
    - `android/fork/app/build/intermediates/sub_project_dex_archive/ossDebug/dexBuilderOssDebug/out`
  - existing merge-project-dex output remains present before merge-lib-dex entry at:
    - `android/fork/app/build/intermediates/dex/ossDebug/mergeProjectDexOssDebug`
  - existing merge-ext-dex output remains present before merge-lib-dex entry at:
    - `android/fork/app/build/intermediates/dex/ossDebug/mergeExtDexOssDebug`
  - merge-lib-dex output remains absent before entry at:
    - `android/fork/app/build/intermediates/dex/ossDebug/mergeLibDexOssDebug`
  - APK output remains absent before entry at:
    - `android/fork/app/build/outputs/apk/`
- define the exact expected success artifact path for the bounded probe:
  - `android/fork/app/build/intermediates/dex/ossDebug/mergeLibDexOssDebug`
- define the exact evidence to capture during execution:
  - confirmation that the persisted `android/sing-box` prerequisite state is unchanged
  - confirmation that compile-jar, runtime-jar, project dex archive, external libs dex archive, sub-project dex archive, merge-project-dex, and merge-ext-dex success artifacts still exist before merge-lib-dex entry
  - confirmation that merge-lib-dex and APK outputs are absent before merge-lib-dex entry
  - the command and output that re-establish transient `android/fork/app/libs/libcore.aar`
  - the exact `.\gradlew.bat :app:mergeLibDexOssDebug --stacktrace` invocation
  - the first task-graph failure before merge-lib-dex starts, if one appears
  - the first exact failure line inside `:app:mergeLibDexOssDebug`, if the task starts and fails
  - or the exact success signal for `:app:mergeLibDexOssDebug` plus evidence that `android/fork/app/build/intermediates/dex/ossDebug/mergeLibDexOssDebug` now exists
- define what counts as the first meaningful outcome:
  - first failing task before merge-lib-dex starts
  - first exact failure inside `:app:mergeLibDexOssDebug`
  - or exact success of `:app:mergeLibDexOssDebug`
- define toolchain and dependency drift bounds for that probe
- define rollback and cleanup expectations for transient `libcore.aar`, `android/fork/libcore/.build/`, and any disposable gomobile workspace recreated only to regenerate the transient AAR
- create `docs/android/ANDROID_POST_MERGE_EXT_DEX_CONTINUATION.md`
- update `PROJECT_STATE.md` and `checkpoints/CP-025.md` with execution results

## OUT_OF_SCOPE
- revisiting or re-solving the cleared `libcore` metadata bridge
- revisiting or re-testing the already-validated `android/sing-box` baseline except as a stated prerequisite
- modifying upstream source under `android/fork/`, including Gradle files, app code, Java sources, Kotlin sources, or `libcore/`
- broad patches to `gomobile-matsuri`, `gobind-matsuri`, Gradle wiring, generated workspace layout, or dependency trees
- broad Gradle or app build work beyond the single merge-lib-dex consumer surface
- rerunning `mergeProjectDexOssDebug` or `mergeExtDexOssDebug` as new probe surfaces, except if Gradle reruns them automatically as prerequisites while entering `mergeLibDexOssDebug`
- packaging, install, signing, or any `assemble*` task
- Android product features
- per-app routing implementation
- transport or proxy logic
- dependency upgrades or repairs outside the first observed merge-lib-dex continuation blocker
- changes to `android/libneko/` or `android/sing-box/` baselines
- server artifacts or server implementation, except `PROJECT_STATE.md` if needed for continuity
- CI/CD changes

## REQUIRED_ARTIFACTS
- `docs/android/ANDROID_POST_MERGE_EXT_DEX_CONTINUATION.md`
- `PROJECT_STATE.md` (updated after CP-025 execution)
- `checkpoints/CP-025.md` (updated after CP-025 execution)

## EXECUTION_NOTES
CP-025 is a continuation-definition checkpoint.
It must not turn into packaging, install, assemble, or repair work.

Why the next surface is `:app:mergeLibDexOssDebug`:
- CP-024 proved that `mergeExtDexOssDebug` already materializes output under `android/fork/app/build/intermediates/dex/ossDebug/mergeExtDexOssDebug`
- the same local task family still exposes `mergeLibDexOssDebug` as the next separate downstream merge surface before packaging
- `android/fork/app/build/intermediates/sub_project_dex_archive/ossDebug/dexBuilderOssDebug/out` already exists
- `android/fork/app/build/intermediates/dex/ossDebug/mergeLibDexOssDebug` is still absent
- `mergeLibDexOssDebug` is narrower than packaging or assemble work and is the first still-unexecuted downstream surface aligned with the absent library merge-dex output family

Execution priority for the later CP-025 run:
1. confirm the persisted `android/sing-box` baseline still matches branch `cp017-local-baseline` at `aed32ee3066cdbc7d471e3e0415c5134088962df`
2. confirm the prior compile-jar success artifact still exists at `android/fork/app/build/intermediates/compile_app_classes_jar/ossDebug/bundleOssDebugClassesToCompileJar/classes.jar`
3. confirm the prior runtime-jar success artifact still exists at `android/fork/app/build/intermediates/runtime_app_classes_jar/ossDebug/bundleOssDebugClassesToRuntimeJar/classes.jar`
4. confirm the prior project dex archive success artifacts still exist at:
   - `android/fork/app/build/intermediates/project_dex_archive/ossDebug`
   - `android/fork/app/build/intermediates/project_dex_archive/ossDebug/dexBuilderOssDebug/out`
5. confirm the prior external libs dex archive success artifact still exists at:
   - `android/fork/app/build/intermediates/external_libs_dex_archive/ossDebug/dexBuilderOssDebug/out`
6. confirm the prior sub-project dex archive success artifact still exists at:
   - `android/fork/app/build/intermediates/sub_project_dex_archive/ossDebug/dexBuilderOssDebug/out`
7. confirm the prior merge-project-dex success artifact still exists at:
   - `android/fork/app/build/intermediates/dex/ossDebug/mergeProjectDexOssDebug`
8. confirm the prior merge-ext-dex success artifact still exists at:
   - `android/fork/app/build/intermediates/dex/ossDebug/mergeExtDexOssDebug`
9. confirm merge-lib-dex and APK outputs are absent before the probe:
   - `android/fork/app/build/intermediates/dex/ossDebug/mergeLibDexOssDebug`
   - `android/fork/app/build/outputs/apk/`
10. rerun only the already-bounded `android/fork/libcore/build.sh` path until it installs transient `android/fork/app/libs/libcore.aar`
11. record that transient `android/fork/app/libs/libcore.aar` is again present before Gradle entry
12. enter Gradle exactly once with `.\gradlew.bat :app:mergeLibDexOssDebug --stacktrace`
13. capture the first exact downstream outcome and stop
14. do not repair the observed blocker inside CP-025

Exact entry markers CP-025 must record during execution:
- artifact marker: `android/fork/app/libs/libcore.aar`
- command marker: `.\gradlew.bat :app:mergeLibDexOssDebug --stacktrace`

First meaningful outcome rules for CP-025:
- if the Gradle task graph fails before `:app:mergeLibDexOssDebug` starts, the first failing task is the checkpoint outcome
- if `:app:mergeLibDexOssDebug` starts and emits a failure, the first exact failure line is the checkpoint outcome
- if `:app:mergeLibDexOssDebug` succeeds, that success plus creation of `android/fork/app/build/intermediates/dex/ossDebug/mergeLibDexOssDebug` is the checkpoint outcome
- once one of those outcomes is captured conclusively, stop without continuing into packaging, install, or assemble work

Evidence sources if shell output is incomplete during CP-025 execution:
- the direct shell transcript from the `.\gradlew.bat :app:mergeLibDexOssDebug --stacktrace` invocation
- the active Gradle daemon log under `android/fork/.gradle/daemon/8.10.2/` or the current user Gradle daemon directory for wrapper version `8.10.2`
- direct filesystem evidence from:
  - `android/fork/app/build/intermediates/dex/ossDebug/mergeProjectDexOssDebug`
  - `android/fork/app/build/intermediates/dex/ossDebug/mergeExtDexOssDebug`
  - `android/fork/app/build/intermediates/dex/ossDebug/mergeLibDexOssDebug`
  - `android/fork/app/build/outputs/apk/`

Toolchain and dependency drift bounds for CP-025 execution:
- preserve the persisted `android/sing-box` baseline on `cp017-local-baseline` at `aed32ee3066cdbc7d471e3e0415c5134088962df`
- preserve the exact disposable `gomobile-matsuri` source revision used in CP-014 through CP-024 (`17d6af34f6bd6d7e1e428e0c652c8b54a46bda4f`) if a disposable rebuild is still required to regenerate transient `libcore.aar`
- preserve the generated workspace `golang.org/x/mobile v0.0.0-20231108233038-35478a0c49da` baseline already validated in CP-014 through CP-024
- preserve the host generated-workspace Go baseline (`go1.23.6`) unless mere observation proves auto-selection cannot be avoided
- preserve the existing Gradle wrapper baseline (`8.10.2`)
- preserve the existing Android Gradle Plugin baseline (`8.8.1`)
- preserve the existing Kotlin and KSP baselines (`2.0.21` and `2.0.21-1.0.27`)
- preserve the existing JDK 17 and local SDK/NDK baseline already documented in project state
- do not intentionally upgrade Gradle plugins, Kotlin plugins, Java toolchains, Go dependencies, or Android dependencies during the probe
- if Gradle, Kotlin, Java, KSP, or Go auto-selects a different version, capture the exact version and trigger as evidence and do not treat that drift as the repair

Cleanup expectations after evidence capture for the later CP-025 run:
- remove any disposable temporary gomobile source workspace created for CP-025
- remove any disposable temporary GOPATH or patched binaries created for CP-025
- remove `android/fork/libcore/.build/` after evidence capture unless the report explicitly preserves it for the first downstream failure evidence
- remove transient copied `android/fork/app/libs/libcore.aar` after evidence capture unless the report explicitly preserves it for the first downstream failure evidence
- leave the default installed `gomobile-matsuri` and `gobind-matsuri` binaries unchanged
- leave the compile-jar, runtime-jar, project dex archive, merge-project-dex, merge-ext-dex, and merge-lib-dex outputs in `android/fork/app/build/intermediates/` as normal Gradle artifacts if Gradle creates them during the bounded probe
- leave all upstream source trees unchanged
- stop without continuing into packaging, install, or broader build continuation
- document the exact stopping point and cleanup result in `docs/android/ANDROID_POST_MERGE_EXT_DEX_CONTINUATION.md`

## COMPLETION_TEST
CP-025 is complete only if:
- `docs/android/ANDROID_POST_MERGE_EXT_DEX_CONTINUATION.md` exists
- the report documents why `.\gradlew.bat :app:mergeLibDexOssDebug --stacktrace` is the correct next exact downstream surface
- the report confirms the exact prerequisite state before execution:
  - `android/sing-box` is on branch `cp017-local-baseline`
  - `android/sing-box` is at `aed32ee3066cdbc7d471e3e0415c5134088962df`
  - `android/fork/app/build/intermediates/compile_app_classes_jar/ossDebug/bundleOssDebugClassesToCompileJar/classes.jar` exists before merge-lib-dex entry
  - `android/fork/app/build/intermediates/runtime_app_classes_jar/ossDebug/bundleOssDebugClassesToRuntimeJar/classes.jar` exists before merge-lib-dex entry
  - `android/fork/app/build/intermediates/project_dex_archive/ossDebug` exists before merge-lib-dex entry
  - `android/fork/app/build/intermediates/project_dex_archive/ossDebug/dexBuilderOssDebug/out` exists before merge-lib-dex entry
  - `android/fork/app/build/intermediates/external_libs_dex_archive/ossDebug/dexBuilderOssDebug/out` exists before merge-lib-dex entry
  - `android/fork/app/build/intermediates/sub_project_dex_archive/ossDebug/dexBuilderOssDebug/out` exists before merge-lib-dex entry
  - `android/fork/app/build/intermediates/dex/ossDebug/mergeProjectDexOssDebug` exists before merge-lib-dex entry
  - `android/fork/app/build/intermediates/dex/ossDebug/mergeExtDexOssDebug` exists before merge-lib-dex entry
  - `android/fork/app/build/intermediates/dex/ossDebug/mergeLibDexOssDebug` is absent before merge-lib-dex entry
  - `android/fork/app/build/outputs/apk/` is absent before merge-lib-dex entry
  - transient `android/fork/app/libs/libcore.aar` is reproduced before Gradle entry
- the report documents the exact merge-lib-dex entry command `.\gradlew.bat :app:mergeLibDexOssDebug --stacktrace`
- the report captures the first exact task-graph failure, first exact merge-lib-dex failure, or the exact success of `:app:mergeLibDexOssDebug`
- if the task succeeds, the report confirms the exact success artifact path:
  - `android/fork/app/build/intermediates/dex/ossDebug/mergeLibDexOssDebug`
- the report confirms the following paths remain absent because they are out of scope:
  - any APK output under `android/fork/app/build/outputs/apk/`
- the report identifies the exact evidence source used if direct shell output is incomplete:
  - direct shell transcript
  - Gradle daemon log
  - filesystem artifact checks
- the report explicitly states that the cleared metadata bridge was not revisited as a new repair surface
- the report explicitly separates merge-lib-dex probing from packaging, install, or assemble work
- the report documents the exact cleanup expectations and actual cleanup result after evidence capture
- no upstream source files under `android/fork/` are modified
- no broad patches are applied
- no packaging, install, or `assemble*` work is attempted
- `PROJECT_STATE.md` is updated after execution

## RESULT
pending

## WHAT_CHANGED
- `checkpoints/CP-025.md` defines only the first exact downstream continuation surface after the observed successful `:app:mergeExtDexOssDebug` result.
- The corrected bounded probe is narrowed to:
  - prerequisite artifact: `android/fork/app/libs/libcore.aar`
  - existing upstream success artifacts:
    - `android/fork/app/build/intermediates/dex/ossDebug/mergeProjectDexOssDebug`
    - `android/fork/app/build/intermediates/dex/ossDebug/mergeExtDexOssDebug`
  - entry command: `.\gradlew.bat :app:mergeLibDexOssDebug --stacktrace`
- Exact prerequisite checks, expected success artifact expectations, out-of-scope absence checks, fallback evidence sources, cleanup expectations, and completion assertions were defined without executing the probe.

## REMAINING_GAPS
- CP-025 has not been executed yet.
- `docs/android/ANDROID_POST_MERGE_EXT_DEX_CONTINUATION.md` does not exist yet.
- The first exact merge-lib-dex-side outcome after the observed successful `:app:mergeExtDexOssDebug` result remains unobserved.

## NEXT_CHECKPOINT
Execute CP-025 only to probe `.\gradlew.bat :app:mergeLibDexOssDebug --stacktrace` and stop at the first exact merge-lib-dex outcome without continuing into packaging, install, or assemble work.

## HANDOFF
Current checkpoint status: pending
Safe next action: execute CP-025 exactly as written, scoped only to `.\gradlew.bat :app:mergeLibDexOssDebug --stacktrace`
Artifacts created or updated:
- checkpoints/CP-025.md
- PROJECT_STATE.md
Known risks:
- the next session may incorrectly skip the still-absent merge-lib-dex surface and jump directly into packaging or `assemble*` work
- future Gradle probes may again stop returning complete direct shell transcripts, in which case daemon-log plus filesystem evidence should be preserved
- the persisted `android/sing-box` baseline on `cp017-local-baseline` remains a hard prerequisite for continuity
Resume from:
- checkpoints/CP-025.md
- docs/android/ANDROID_POST_MERGE_PROJECT_DEX_CONTINUATION.md
- PROJECT_STATE.md
