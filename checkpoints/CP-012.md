# CP-012

## CHECKPOINT_ID
CP-012

## TITLE
Attempt the smallest tool-local repair for the libcore zero-byte generated `go.mod` blocker

## CURRENT_STATE
CP-011 is complete.
The libcore gomobile blocker is localized to zero-byte generated `go.mod` files under `android/fork/libcore/.build/src-android-*`.
The first exact failing generated path is `android/fork/libcore/.build/src-android-386/go.mod`.
`android/fork/libcore/go.mod`, `android/libneko/go.mod`, and `android/sing-box/go.mod` were inspected in CP-011 and confirmed to contain valid module declarations.
The current evidence ranks the blocker origin as:
1. generated temporary module state
2. `gomobile-matsuri` behavior
3. upstream layout assumptions
4. local dependency materialization
No upstream source files under `android/fork/` have been modified.
No repair has been attempted yet.

## TARGET_STATE
The repository has one narrowly bounded repair attempt definition that tests only the smallest evidence-backed surface for the zero-byte generated `go.mod` issue in the libcore gomobile path.
That repair surface is limited to the local `gomobile-matsuri` generation path responsible for writing the temporary `src-android-*` module roots.
When CP-012 is later executed, it must determine whether a tool-local repair is sufficient to:
- stop generating zero-byte `go.mod` files under `android/fork/libcore/.build/src-android-*`
- allow `go mod tidy -v` to succeed inside the first generated module root
- allow `android/fork/libcore/build.sh` to produce `libcore.aar`
CP-012 must explicitly stop after blocker validation.
It must not expand into broader Android build continuation.

## IN_SCOPE
- create an isolated local repair workspace for the `gomobile-matsuri` source and binaries used by the libcore build path
- inspect and modify only the code path that writes or finalizes generated `go.mod` files for `src-android-*` temporary module roots during `gomobile-matsuri bind`
- rebuild only the local patched `gomobile-matsuri` and `gobind-matsuri` binaries needed for the repair attempt
- re-run only the minimum libcore build path needed to validate the repair, while preserving `.build/` long enough to inspect generated files
- validate whether each generated `src-android-*` `go.mod` is non-zero and contains a `module` declaration
- validate whether direct `go mod tidy -v` succeeds inside `android/fork/libcore/.build/src-android-386`
- validate whether `android/fork/libcore/build.sh` reaches `libcore.aar` generation
- document the repair attempt outcome in `docs/android/ANDROID_LIBCORE_GOMOBILE_REPAIR_ATTEMPT.md`
- update `PROJECT_STATE.md` and `checkpoints/CP-012.md` with execution results when CP-012 is later run

## OUT_OF_SCOPE
- modifying upstream source under `android/fork/`, including `android/fork/libcore/go.mod`, `android/fork/libcore/build.sh`, and `android/fork/libcore/init.sh`, during the first repair attempt
- applying broad patches across the Android fork or external dependencies
- changing Go, gomobile, gobind, JDK, SDK, NDK, or dependency versions as a first-line repair
- re-materializing `android/libneko/` or `android/sing-box/` unless an execution-time failure proves that a required local path is missing
- Gradle app assembly or any Android build continuation beyond `libcore.aar`
- Android product features
- per-app routing implementation
- transport or proxy logic
- server artifacts or server implementation
- CI/CD changes
- any second repair strategy if the first tool-local repair fails

## REQUIRED_ARTIFACTS
- `docs/android/ANDROID_LIBCORE_GOMOBILE_REPAIR_ATTEMPT.md`
- `PROJECT_STATE.md` (updated after CP-012 execution)
- `checkpoints/CP-012.md` (updated after CP-012 execution)

## EXECUTION_NOTES
The smallest repair surface selected for CP-012 is the local `gomobile-matsuri` code path that emits the temporary `src-android-*` `go.mod` files.
This surface is selected because CP-011 established that:
- the upstream `libcore`, `libneko`, and `sing-box` `go.mod` files are already valid
- the failure occurs inside generated `.build/src-android-*` module roots before `build.sh` regains control
- the generated `go.mod` files exist but are zero bytes long

The first repair attempt must therefore not start by editing `android/fork/` source files.
It must test whether fixing the tool-local generated-module write path is enough to clear the blocker.

The execution sequence for CP-012 must be:
1. preserve the same session-scoped environment shape used in CP-010 and CP-011
2. create or use an isolated local copy of the `gomobile-matsuri` source used for the repair attempt
3. patch only the generated-`go.mod` write/finalization path
4. rebuild the local patched `gomobile-matsuri` and `gobind-matsuri` binaries
5. re-run only the libcore gomobile path
6. preserve `.build/` long enough to inspect the generated `src-android-*` roots
7. stop after blocker validation and document the outcome

Validation during CP-012 execution must answer all of the following:
- are the generated `src-android-*` `go.mod` files still zero bytes
- do those files now contain a valid `module` declaration
- does `go mod tidy -v` succeed inside `src-android-386`
- does `android/fork/libcore/build.sh` emit `libcore.aar`
- did the repair require any source change outside the isolated `gomobile-matsuri` repair surface

Rollback expectations if the repair attempt fails:
- restore the original `gomobile-matsuri` and `gobind-matsuri` binaries or restore the original tool selection used before the attempt
- remove the isolated patched tool workspace if it was created only for the attempt
- remove generated `.build/` contents and any failed-attempt `libcore.aar` output unless they are intentionally preserved as evidence in the report
- document the exact failure point and exact evidence in `docs/android/ANDROID_LIBCORE_GOMOBILE_REPAIR_ATTEMPT.md`
- stop without changing `android/fork/` source and without continuing into Gradle or app-level build steps

If the tool-local repair is insufficient, CP-012 must end with a documented failure and a recommendation for a new checkpoint.
It must not expand into a second repair surface inside the same checkpoint.

## COMPLETION_TEST
CP-012 is complete only when all of the following are true during its later execution:
- `docs/android/ANDROID_LIBCORE_GOMOBILE_REPAIR_ATTEMPT.md` exists
- the report identifies the exact local `gomobile-matsuri` repair surface tested and explains why `android/fork/` source was intentionally left untouched
- the report records whether the generated `src-android-*` `go.mod` files remain zero-byte or were repaired to contain a valid `module` declaration
- the report records the result of a direct `go mod tidy -v` run inside `android/fork/libcore/.build/src-android-386`
- the report records whether `android/fork/libcore/build.sh` produced `libcore.aar`
- the report records rollback actions if the attempt failed
- no upstream source files under `android/fork/` are modified
- no broad patches are applied
- no Gradle app build is attempted
- `PROJECT_STATE.md` is updated after execution

## RESULT
partial

## WHAT_CHANGED
- `checkpoints/CP-012.md` was created to define the narrowest validated repair checkpoint for the libcore zero-byte generated `go.mod` blocker.
- The selected first repair surface is tool-local and limited to the `gomobile-matsuri` generated-module write path.
- Execution was intentionally not performed in this session.

## REMAINING_GAPS
CP-012 has not been executed yet.
`docs/android/ANDROID_LIBCORE_GOMOBILE_REPAIR_ATTEMPT.md` does not exist yet because the repair attempt is deferred.
The zero-byte generated `go.mod` blocker remains unresolved until the defined repair attempt is run.

## NEXT_CHECKPOINT
Execute CP-012 to attempt the isolated `gomobile-matsuri` repair, validate whether it resolves the zero-byte generated `go.mod` blocker, and stop before broader Android build continuation.

## HANDOFF
Current checkpoint status: partial
Safe next action: execute CP-012 using an isolated local `gomobile-matsuri` repair surface and document the outcome in `docs/android/ANDROID_LIBCORE_GOMOBILE_REPAIR_ATTEMPT.md`
Artifacts created or updated:
- checkpoints/CP-012.md
Known risks:
- the tool-local repair surface may still prove insufficient if the zero-byte `go.mod` output is triggered by a deeper upstream layout assumption
- broader Android build blockers may still appear after the current gomobile blocker is cleared
- mixing blocker repair with full build continuation would break checkpoint discipline and make rollback harder
Resume from:
- docs/android/ANDROID_LIBCORE_GOMOBILE_BLOCKER_DIAGNOSIS.md
- PROJECT_STATE.md
- checkpoints/CP-012.md
