# CP-042

## CHECKPOINT_ID
CP-042

## TITLE
Define the smallest bounded post-service interface-state verification probe

## CURRENT_STATE
CP-041 is complete.
CP-041 proved bounded service-state verification success for `moe.nb4a.debug` with service records including:
- `moe.nb4a.debug/androidx.room.MultiInstanceInvalidationService`
- `moe.nb4a.debug/io.nekohasekai.sagernet.bg.VpnService`
- `EXIT_CODE: 0`

The first exact downstream surface after service-state verification is bounded tunnel-interface presence verification.

## TARGET_STATE
The repository has a bounded CP-042 definition for verifying only the first exact post-service interface-state surface.

After later CP-042 execution, a future session must be able to answer:
- whether one bounded interface probe observes `tun` or `tap` interface presence
- what exact prerequisites were true before probe entry
- what exact first meaningful outcome appears from that one probe
- whether the first outcome is a prerequisite failure, interface-state failure, or interface-state success
- how interface-state verification remains explicitly separated from UI interaction, network activity, and runtime debugging

CP-042 does not execute interface-state verification.
CP-042 does not perform UI interaction.
CP-042 does not perform network actions.
CP-042 does not perform runtime debugging.
CP-042 does not implement product features.

## IN_SCOPE
- identify the first exact post-service-verification surface as bounded tunnel-interface presence verification
- define one bounded interface-state probe:
  - `adb shell "ip link show | grep -E 'tun|tap'"`
- define exact prerequisite checks before probe entry:
  - CP-041 execution artifact exists:
    - `docs/android/ANDROID_POST_RESUMED_TASK_SERVICE_VERIFICATION.md`
  - CP-041 success evidence exists:
    - `docs/android/evidence/cp041_reretry4_probe.log`
  - Android SDK tool path is resolvable:
    - `"$env:ANDROID_HOME\\platform-tools\\adb.exe"` exists
    - or an equivalent explicitly captured adb path is resolvable
  - exactly one online adb target is available (`device`)
  - target ABI is `x86_64`
  - installed package check passes:
    - `adb shell pm path moe.nb4a.debug` returns a package path
  - service continuity check passes:
    - `adb shell "dumpsys activity services | grep -m 1 -E 'moe\\.nb4a\\.debug/io\\.nekohasekai\\.sagernet\\.bg\\.VpnService'"`
- define exact expected interface-state success signals:
  - probe output is non-empty
  - output contains `tun` or `tap`
  - output contains state indicator `UP`
  - `EXIT_CODE: 0`
- define first meaningful outcome rules for later CP-042 execution:
  - if prerequisite checks fail, first failed prerequisite line is the checkpoint outcome
  - if probe runs but required interface-state signals are missing, first exact failure line is the checkpoint outcome
  - if probe returns required signals, success signal lines are the checkpoint outcome
  - stop immediately once one of those outcomes is captured
- define exact out-of-scope actions that must remain unperformed:
  - no UI interaction commands (`adb shell input`, `adb shell monkey`, UI automation)
  - no network actions or traffic probes from device shell
  - no connectivity checks (`ping`, `curl`, `wget`, `nc`)
  - no DNS resolution tests (`nslookup`, `getprop net.dns*`, resolver probing)
  - no app relaunch commands (`adb shell am start*`)
  - no runtime-debugging capture (`adb logcat`, profiler, traces)
  - no broad diagnostics beyond the single bounded probe and defined fallbacks
  - no `assemble*` continuation
  - no release signing/distribution work
- define exact fallback evidence sources if direct probe output is incomplete:
  - direct shell transcript of the bounded interface-state probe
  - captured clean transcript file for the same command
  - `adb devices -l` snapshot captured in the same run
  - fallback interface listing probe:
    - `adb shell "ip addr show | grep -E 'tun|tap'"`
  - fallback process continuity:
    - `adb shell pidof moe.nb4a.debug`
  - fallback package continuity:
    - `adb shell pm path moe.nb4a.debug`
- define exact cleanup expectations:
  - do not regenerate `libcore.aar`
  - do not create disposable gomobile/GOPATH workspaces
  - remove only temporary CP-042 scratch files if any are created outside `docs/android/evidence/`
  - retain canonical evidence files under `docs/android/evidence/`
  - do not uninstall package as part of CP-042
  - do not force-stop or kill app process as part of CP-042
  - leave upstream source trees unchanged
- create `docs/android/ANDROID_POST_SERVICE_INTERFACE_VERIFICATION.md` during later CP-042 execution
- update `PROJECT_STATE.md` and `checkpoints/CP-042.md` during later CP-042 execution

## OUT_OF_SCOPE
- executing CP-042
- revisiting any previous build-chain checkpoints
- modifying upstream source under `android/fork/`
- applying broad patches to build logic, dependency trees, or generated workspaces
- UI interaction or user-flow testing
- network actions from device shell
- runtime debugging, profiling, or diagnostic deep dives
- Android product feature implementation
- per-app routing implementation
- transport or proxy logic
- changes to `android/libneko/` or `android/sing-box/` baselines
- server artifacts or server implementation, except `PROJECT_STATE.md` for continuity
- CI/CD changes

## REQUIRED_ARTIFACTS
- `checkpoints/CP-042.md`
- `PROJECT_STATE.md` (updated for CP-042 definition state only)
- `docs/android/ANDROID_POST_SERVICE_INTERFACE_VERIFICATION.md` (execution artifact for later CP-042 run)

## EXECUTION_NOTES
CP-042 is an interface-state-verification definition checkpoint.
It must remain narrow and explicitly separate interface-state verification from UI interaction, network activity, and runtime debugging.

Why the next surface is interface-state verification:
- CP-041 already proved service continuity including `VpnService`
- the smallest deterministic downstream gate is tunnel-interface presence (`tun`/`tap`)
- one bounded `ip link show` probe is sufficient for this gate

Execution priority for later CP-042 run:
1. verify all CP-042 prerequisites exactly
2. run the single bounded interface-state probe once
3. capture first exact meaningful outcome and stop
4. do not continue into UI interaction, network activity, or runtime-debugging workflows

## COMPLETION_TEST
CP-042 definition is complete only if:
- `checkpoints/CP-042.md` exists and follows checkpoint structure
- the checkpoint defines one bounded post-service interface-state probe
- prerequisites/success signals/out-of-scope/fallbacks/cleanup are explicit and testable
- `docs/android/ANDROID_POST_SERVICE_INTERFACE_VERIFICATION.md` is named as the later execution artifact
- `PROJECT_STATE.md` is updated so the next eligible work is executing CP-042 only
- no CP-042 execution evidence is claimed in this definition-only step

## RESULT
blocked

## WHAT_CHANGED
- CP-042 retry execution was performed in bounded scope without entering UI/network/runtime-debugging/feature work.
- Explicit adb device-recovery verification was executed first and captured in:
  - `docs/android/evidence/cp042_retry_adb_devices.log`
- First exact meaningful outcome captured:
  - `CP-042 retry blocked: expected exactly one online adb target with state device, found 0`
- CP-042 prerequisite re-evaluation was not executed because the device-recovery gate failed.
- The bounded interface-state probe was not executed because device recovery did not pass.
- Execution report was created:
  - `docs/android/ANDROID_POST_SERVICE_INTERFACE_VERIFICATION.md`
- Cleanup expectations remained satisfied (no `libcore` regeneration, no disposable gomobile/GOPATH workspaces, no uninstall/force-stop/kill, no upstream source modifications).
- `PROJECT_STATE.md` was updated to reflect CP-042 retry result.

## REMAINING_GAPS
- CP-042 retry is blocked at explicit device-recovery gate (`expected exactly one online adb target with state device, found 0`).
- CP-042 prerequisite re-evaluation remains pending until the device-recovery gate passes.
- Required success signals remain unmet in this run:
  - non-empty `tun|tap` output (not evaluated)
  - `UP` lifecycle indicator (not evaluated)
  - `EXIT_CODE: 0` (not evaluated)

## NEXT_CHECKPOINT
Retry CP-042 execution only in the same bounded scope after exactly one online adb target with state `device` and ABI `x86_64` is restored; then re-run prerequisites, execute one bounded probe once, capture first exact outcome, and stop.

## HANDOFF
Current checkpoint status: blocked
Safe next action: retry CP-042 only with identical bounded scope after restoring exactly one online adb target (`device`) with ABI `x86_64`
Artifacts created or updated:
- checkpoints/CP-042.md
- docs/android/ANDROID_POST_SERVICE_INTERFACE_VERIFICATION.md
- docs/android/evidence/cp042_retry_adb_devices.log
- PROJECT_STATE.md
Known risks:
- scope drift into UI/network/runtime/feature work before bounded CP-042 completion
- device availability drift (zero online adb targets) can keep CP-042 retry blocked across sessions
- device/interface state drift between sessions can invalidate interface probe assumptions
Resume from:
- checkpoints/CP-042.md
- checkpoints/CP-041.md
- docs/android/ANDROID_POST_RESUMED_TASK_SERVICE_VERIFICATION.md
- docs/android/ANDROID_POST_SERVICE_INTERFACE_VERIFICATION.md
- docs/android/evidence/cp042_retry_adb_devices.log
- PROJECT_STATE.md
