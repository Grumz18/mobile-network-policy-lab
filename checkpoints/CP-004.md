# CP-004

## CHECKPOINT_ID
CP-004

## TITLE
Define Android fork integration baseline

## CURRENT_STATE
CP-003 is complete.
The repository has documented repository and server bootstrap baselines.
The `android/` directory exists but does not yet contain an Android integration baseline, fork workspace structure, or Android-specific guidance.
No Android code has been implemented.
No transport logic has been implemented.

## TARGET_STATE
The repository contains a minimal, documented Android fork integration baseline.
After this checkpoint is executed, a future session should be able to identify:
- the intended boundaries of the Android integration workspace
- the folder layout reserved for fork intake, local patches, and Android-specific documentation
- the non-secret local configuration placeholders required before Android implementation starts
- the safe commands used to verify the Android baseline without fetching or modifying fork code

## IN_SCOPE
- define the initial Android fork integration folder and file layout
- create Android-specific baseline documentation
- create non-secret Android configuration templates
- add Android-local helper scripts for baseline verification only
- document the exact verification flow for the Android fork baseline

## OUT_OF_SCOPE
- Android application code
- fork acquisition or synchronization
- transport or proxy logic
- server implementation
- CI/CD implementation
- emulator orchestration or device automation

## REQUIRED_ARTIFACTS
- `docs/android/ANDROID_FORK_BASELINE.md`
- `android/README.md`
- `android/.env.example`
- `android/.gitignore`
- `android/fork/README.md`
- `android/patches/README.md`
- `android/config/README.md`
- `android/scripts/verify-baseline.ps1`

## EXECUTION_NOTES
This checkpoint should define only the Android fork integration baseline.
It should not fetch a fork, create Gradle files, or add any Android source code.
If the exact fork intake workflow is still uncertain at execution time, document the assumption explicitly in `docs/android/ANDROID_FORK_BASELINE.md` before creating any workflow-specific files.
Baseline verification must remain local and non-destructive.
The checkpoint should prepare the next step, not commit the project to a build system change or app-level implementation.

## COMPLETION_TEST
This checkpoint is complete only if:
- `docs/android/ANDROID_FORK_BASELINE.md` defines the Android integration boundaries and baseline assumptions
- `android/README.md` explains the purpose and layout of the Android workspace
- `android/.env.example` and `android/.gitignore` exist and are aligned with the documented Android workflow
- `android/fork/README.md`, `android/patches/README.md`, and `android/config/README.md` define the intended responsibility of each Android area
- `android/scripts/verify-baseline.ps1` exists and validates the Android baseline without fetching or modifying fork code
- no Android code, transport logic, or server implementation is introduced

## RESULT
complete

## WHAT_CHANGED
- `docs/android/ANDROID_FORK_BASELINE.md` was created to define the Android integration boundaries, baseline assumptions, and safe verification flow.
- `android/README.md` was created to describe the Android workspace purpose, layout, and current constraints.
- `android/.env.example` and `android/.gitignore` were created as the non-secret Android configuration baseline.
- `android/fork/README.md`, `android/patches/README.md`, and `android/config/README.md` were created to define the intended responsibility of each Android area without adding implementation.
- `android/scripts/verify-baseline.ps1` was created as a safe local verification script for the Android baseline artifact set.
- `PROJECT_STATE.md` was updated to reflect CP-004 completion and the new Android baseline.
- `checkpoints/CP-004.md` was updated with execution results, remaining gaps, and handoff data.

## REMAINING_GAPS
No remaining gaps inside CP-004.

## NEXT_CHECKPOINT
CP-005 - Establish Android workspace skeleton and local build baseline

## HANDOFF
Current checkpoint status: complete
Safe next action: author CP-005 as the next bounded checkpoint for Android workspace skeleton and local build baseline
Artifacts created or updated:
- docs/android/ANDROID_FORK_BASELINE.md
- android/README.md
- android/.env.example
- android/.gitignore
- android/fork/README.md
- android/patches/README.md
- android/config/README.md
- android/scripts/verify-baseline.ps1
- PROJECT_STATE.md
- checkpoints/CP-004.md
Known risks:
- future work may prematurely commit to a fork intake workflow before a dedicated implementation checkpoint
- scope drift into Android code, per-app routing, or transport logic before CP-005 is defined
Resume from:
- PROJECT_STATE.md
- checkpoints/CP-004.md
