# CP-024

## CHECKPOINT_ID
CP-024

## TITLE
Define the smallest bounded post-merge-project-dex continuation probe

## CURRENT_STATE
CP-023 execution is partial, not complete.
The generated `libcore` metadata bridge is already repaired and validated in `docs/android/ANDROID_LIBCORE_METADATA_BRIDGE_REPAIR.md`.
That earlier checkpoint proved:
- the generated fallback `go.mod` can preserve `replace libcore`
- the generated fallback can preserve the local replacement mappings for `github.com/matsuridayo/libneko` and `github.com/sagernet/sing-box`
- the generated fallback can preserve the original `golang.org/x/mobile` baseline (`v0.0.0-20231108233038-35478a0c49da`)
- the exact bare `libcore` import failure from CP-013 is cleared

CP-017 then intentionally persisted the validated `android/sing-box` alignment in `docs/android/ANDROID_SINGBOX_BASELINE_PERSIST.md`.
That checkpoint proved:
- the local `android/sing-box` baseline is intentionally tracked on branch `cp017-local-baseline`
- that persisted baseline points to commit `aed32ee3066cdbc7d471e3e0415c5134088962df`
- the same bounded `android/fork/libcore/build.sh` path still proceeds through transient `android/fork/app/libs/libcore.aar` production under that persisted baseline

CP-018 through CP-022 then execution-validated the exact downstream app consumer surfaces through:
- `.\gradlew.bat :app:compileOssDebugKotlin --stacktrace`
- `.\gradlew.bat :app:compileOssDebugJavaWithJavac --stacktrace`
- `.\gradlew.bat :app:bundleOssDebugClassesToCompileJar --stacktrace`
- `.\gradlew.bat :app:bundleOssDebugClassesToRuntimeJar --stacktrace`
- `.\gradlew.bat :app:dexBuilderOssDebug --stacktrace`

Those checkpoints proved that the persisted sing-box baseline clears all of those surfaces and leaves dex-side continuation as the active boundary.

CP-023 then executed the first bounded post-dex-builder merge-dex probe in `docs/android/ANDROID_POST_DEX_CONTINUATION.md`.
That checkpoint proved:
- the prerequisite artifact before Gradle entry still remains `android/fork/app/libs/libcore.aar`
- the exact bounded command `.\gradlew.bat :app:mergeProjectDexOssDebug --stacktrace` succeeds under the persisted sing-box baseline
- the authored boundary assumption was wrong:
  - `android/fork/app/build/intermediates/merged_project_dex/ossDebug` was not created
  - `android/fork/app/build/intermediates/dex/ossDebug/mergeProjectDexOssDebug` was created instead
- the actual observed project-dex output family after CP-023 is now:
  - `android/fork/app/build/intermediates/dex/ossDebug/mergeProjectDexOssDebug`
- the following downstream merge-dex outputs still remain absent after CP-023:
  - `android/fork/app/build/intermediates/dex/ossDebug/mergeExtDexOssDebug`
  - `android/fork/app/build/intermediates/dex/ossDebug/mergeLibDexOssDebug`
- APK outputs still remain absent after CP-023:
  - `android/fork/app/build/outputs/apk/`

The next downstream surface is now identifiable from actual task and artifact flow:
- CP-023 already execution-validated `mergeProjectDexOssDebug`
- the same local task family exposed:
  - `mergeProjectDexOssDebug`
  - `mergeExtDexOssDebug`
  - `mergeLibDexOssDebug`
- `android/fork/app/build/intermediates/dex/ossDebug/mergeProjectDexOssDebug` now exists
- `android/fork/app/build/intermediates/external_libs_dex_archive/ossDebug/dexBuilderOssDebug/out` exists
- `android/fork/app/build/intermediates/dex/ossDebug/mergeExtDexOssDebug` is still absent
- `mergeExtDexOssDebug` is therefore the smallest next still-unexecuted surface supported by actual task naming and still-absent output flow, while `mergeLibDexOssDebug`, packaging, and `assemble*` remain broader downstream work

No upstream source under `android/fork/` has been modified.
The default installed `gomobile-matsuri` and `gobind-matsuri` binaries remain unchanged.

## TARGET_STATE
The repository has a bounded checkpoint definition for probing only the first exact downstream build surface after the observed successful `.\gradlew.bat :app:mergeProjectDexOssDebug --stacktrace` result.

After CP-024 execution, a future session must be able to answer:
- whether `.\gradlew.bat :app:mergeExtDexOssDebug --stacktrace` is the correct next exact downstream consumer surface after successful `mergeProjectDexOssDebug`
- what exact prerequisite artifact and prerequisite baseline must still be true before entering that merge-ext-dex surface
- what exact first meaningful outcome appears when that single merge-ext-dex probe is entered
- whether the first new outcome is:
  - a task-graph failure before `:app:mergeExtDexOssDebug` starts
  - a failure inside `:app:mergeExtDexOssDebug`
  - or a successful merge-ext-dex continuation signal
- how that probe is kept separate from `mergeLibDex*`, packaging, install, assemble, or repair work

CP-024 does not need to fix merge-ext-dex failures.
It must only define the smallest bounded continuation probe after the observed successful `mergeProjectDexOssDebug` result and stop at the first exact downstream merge-ext-dex result.

## IN_SCOPE
- correct the post-`mergeProjectDexOssDebug` boundary from the authored `merged_project_dex` assumption to the observed `intermediates/dex/ossDebug/mergeProjectDexOssDebug` reality
- select the exact downstream build surface after successful `:app:mergeProjectDexOssDebug`
- document why `.\gradlew.bat :app:mergeExtDexOssDebug --stacktrace` is the correct next bounded surface:
  - CP-023 already execution-validated `mergeProjectDexOssDebug`
  - `android/fork/app/build/intermediates/dex/ossDebug/mergeProjectDexOssDebug` now exists
  - `android/fork/app/build/intermediates/external_libs_dex_archive/ossDebug/dexBuilderOssDebug/out` now exists
  - `android/fork/app/build/intermediates/dex/ossDebug/mergeExtDexOssDebug` is still absent
  - `mergeLibDexOssDebug` is a separate still-unexecuted merge surface, and packaging is broader than the first post-merge-project-dex continuation point
- define the exact entry command for the probe from `android/fork/`:
  - `.\gradlew.bat :app:mergeExtDexOssDebug --stacktrace`
- define the exact prerequisite checks that must remain unchanged before the probe:
  - `android/sing-box` stays on branch `cp017-local-baseline`
  - `android/sing-box` stays at `aed32ee3066cdbc7d471e3e0415c5134088962df`
  - the cleared metadata bridge remains only an existing prerequisite reused through the bounded `libcore` path
  - transient `android/fork/app/libs/libcore.aar` is reproduced again before Gradle entry because CP-023 cleanup removed it
  - existing compile-jar output remains present before merge-ext-dex entry at:
    - `android/fork/app/build/intermediates/compile_app_classes_jar/ossDebug/bundleOssDebugClassesToCompileJar/classes.jar`
  - existing runtime-jar output remains present before merge-ext-dex entry at:
    - `android/fork/app/build/intermediates/runtime_app_classes_jar/ossDebug/bundleOssDebugClassesToRuntimeJar/classes.jar`
  - existing project dex archive output remains present before merge-ext-dex entry at:
    - `android/fork/app/build/intermediates/project_dex_archive/ossDebug`
    - `android/fork/app/build/intermediates/project_dex_archive/ossDebug/dexBuilderOssDebug/out`
  - existing external libs dex archive output remains present before merge-ext-dex entry at:
    - `android/fork/app/build/intermediates/external_libs_dex_archive/ossDebug/dexBuilderOssDebug/out`
  - existing merge-project-dex output remains present before merge-ext-dex entry at:
    - `android/fork/app/build/intermediates/dex/ossDebug/mergeProjectDexOssDebug`
  - merge-ext-dex output remains absent before entry at:
    - `android/fork/app/build/intermediates/dex/ossDebug/mergeExtDexOssDebug`
  - merge-lib-dex output remains absent before entry at:
    - `android/fork/app/build/intermediates/dex/ossDebug/mergeLibDexOssDebug`
  - APK output remains absent before entry at:
    - `android/fork/app/build/outputs/apk/`
- define the exact expected success artifact path for the bounded probe:
  - `android/fork/app/build/intermediates/dex/ossDebug/mergeExtDexOssDebug`
- define the exact evidence to capture during execution:
  - confirmation that the persisted `android/sing-box` prerequisite state is unchanged
  - confirmation that compile-jar, runtime-jar, project dex archive, external libs dex archive, and merge-project-dex success artifacts still exist before merge-ext-dex entry
  - confirmation that merge-ext-dex, merge-lib-dex, and APK outputs are absent before merge-ext-dex entry
  - the command and output that re-establish transient `android/fork/app/libs/libcore.aar`
  - the exact `.\gradlew.bat :app:mergeExtDexOssDebug --stacktrace` invocation
  - the first task-graph failure before merge-ext-dex starts, if one appears
  - the first exact failure line inside `:app:mergeExtDexOssDebug`, if the task starts and fails
  - or the exact success signal for `:app:mergeExtDexOssDebug` plus evidence that `android/fork/app/build/intermediates/dex/ossDebug/mergeExtDexOssDebug` now exists
- define what counts as the first meaningful outcome:
  - first failing task before merge-ext-dex starts
  - first exact failure inside `:app:mergeExtDexOssDebug`
  - or exact success of `:app:mergeExtDexOssDebug`
- define toolchain and dependency drift bounds for that probe
- define rollback and cleanup expectations for transient `libcore.aar`, `android/fork/libcore/.build/`, and any disposable gomobile workspace recreated only to regenerate the transient AAR
- create `docs/android/ANDROID_POST_MERGE_PROJECT_DEX_CONTINUATION.md`
- update `PROJECT_STATE.md` and `checkpoints/CP-024.md` with execution results

## OUT_OF_SCOPE
- revisiting or re-solving the cleared `libcore` metadata bridge
- revisiting or re-testing the already-validated `android/sing-box` baseline except as a stated prerequisite
- modifying upstream source under `android/fork/`, including Gradle files, app code, Java sources, Kotlin sources, or `libcore/`
- broad patches to `gomobile-matsuri`, `gobind-matsuri`, Gradle wiring, generated workspace layout, or dependency trees
- broad Gradle or app build work beyond the single merge-ext-dex consumer surface
- rerunning `mergeProjectDexOssDebug` as a new probe surface, except if Gradle reruns it automatically as a prerequisite while entering `mergeExtDexOssDebug`
- `mergeLibDex*`, packaging, install, signing, or any `assemble*` task
- Android product features
- per-app routing implementation
- transport or proxy logic
- dependency upgrades or repairs outside the first observed merge-ext-dex continuation blocker
- changes to `android/libneko/` or `android/sing-box/` baselines
- server artifacts or server implementation, except `PROJECT_STATE.md` if needed for continuity
- CI/CD changes

## REQUIRED_ARTIFACTS
- `docs/android/ANDROID_POST_MERGE_PROJECT_DEX_CONTINUATION.md`
- `PROJECT_STATE.md` (updated after CP-024 execution)
- `checkpoints/CP-024.md` (updated after CP-024 execution)

## EXECUTION_NOTES
CP-024 is a continuation-definition checkpoint.
It must not turn into `mergeLibDex*`, packaging, install, assemble, or repair work.

Why the next surface is `:app:mergeExtDexOssDebug`:
- CP-023 proved that the authored `merged_project_dex` boundary was wrong and that `mergeProjectDexOssDebug` already materializes output under `android/fork/app/build/intermediates/dex/ossDebug/mergeProjectDexOssDebug`
- the same local task family still exposes `mergeExtDexOssDebug` and `mergeLibDexOssDebug` as separate downstream merge surfaces
- `android/fork/app/build/intermediates/external_libs_dex_archive/ossDebug/dexBuilderOssDebug/out` already exists
- `android/fork/app/build/intermediates/dex/ossDebug/mergeExtDexOssDebug` is still absent
- `mergeExtDexOssDebug` is narrower than packaging or assemble work and is the first still-unexecuted downstream surface aligned with the absent external merge-dex output family

Execution priority for the later CP-024 run:
1. confirm the persisted `android/sing-box` baseline still matches branch `cp017-local-baseline` at `aed32ee3066cdbc7d471e3e0415c5134088962df`
2. confirm the prior compile-jar success artifact still exists at `android/fork/app/build/intermediates/compile_app_classes_jar/ossDebug/bundleOssDebugClassesToCompileJar/classes.jar`
3. confirm the prior runtime-jar success artifact still exists at `android/fork/app/build/intermediates/runtime_app_classes_jar/ossDebug/bundleOssDebugClassesToRuntimeJar/classes.jar`
4. confirm the prior project dex archive success artifacts still exist at:
   - `android/fork/app/build/intermediates/project_dex_archive/ossDebug`
   - `android/fork/app/build/intermediates/project_dex_archive/ossDebug/dexBuilderOssDebug/out`
5. confirm the prior external libs dex archive success artifact still exists at:
   - `android/fork/app/build/intermediates/external_libs_dex_archive/ossDebug/dexBuilderOssDebug/out`
6. confirm the prior merge-project-dex success artifact still exists at:
   - `android/fork/app/build/intermediates/dex/ossDebug/mergeProjectDexOssDebug`
7. confirm merge-ext-dex, merge-lib-dex, and APK outputs are absent before the probe:
   - `android/fork/app/build/intermediates/dex/ossDebug/mergeExtDexOssDebug`
   - `android/fork/app/build/intermediates/dex/ossDebug/mergeLibDexOssDebug`
   - `android/fork/app/build/outputs/apk/`
8. rerun only the already-bounded `android/fork/libcore/build.sh` path until it installs transient `android/fork/app/libs/libcore.aar`
9. record that transient `android/fork/app/libs/libcore.aar` is again present before Gradle entry
10. enter Gradle exactly once with `.\gradlew.bat :app:mergeExtDexOssDebug --stacktrace`
11. capture the first exact downstream outcome and stop
12. do not repair the observed blocker inside CP-024

Exact entry markers CP-024 must record during execution:
- artifact marker: `android/fork/app/libs/libcore.aar`
- command marker: `.\gradlew.bat :app:mergeExtDexOssDebug --stacktrace`

First meaningful outcome rules for CP-024:
- if the Gradle task graph fails before `:app:mergeExtDexOssDebug` starts, the first failing task is the checkpoint outcome
- if `:app:mergeExtDexOssDebug` starts and emits a failure, the first exact failure line is the checkpoint outcome
- if `:app:mergeExtDexOssDebug` succeeds, that success plus creation of `android/fork/app/build/intermediates/dex/ossDebug/mergeExtDexOssDebug` is the checkpoint outcome
- once one of those outcomes is captured conclusively, stop without continuing into `mergeLibDex*`, packaging, install, or assemble work

Evidence sources if shell output is incomplete during CP-024 execution:
- the direct shell transcript from the `.\gradlew.bat :app:mergeExtDexOssDebug --stacktrace` invocation
- the active Gradle daemon log under `android/fork/.gradle/daemon/8.10.2/` or the current user Gradle daemon directory for wrapper version `8.10.2`
- direct filesystem evidence from:
  - `android/fork/app/build/intermediates/dex/ossDebug/mergeProjectDexOssDebug`
  - `android/fork/app/build/intermediates/dex/ossDebug/mergeExtDexOssDebug`
  - `android/fork/app/build/intermediates/dex/ossDebug/mergeLibDexOssDebug`
  - `android/fork/app/build/outputs/apk/`

Toolchain and dependency drift bounds for CP-024 execution:
- preserve the persisted `android/sing-box` baseline on `cp017-local-baseline` at `aed32ee3066cdbc7d471e3e0415c5134088962df`
- preserve the exact disposable `gomobile-matsuri` source revision used in CP-014 through CP-023 (`17d6af34f6bd6d7e1e428e0c652c8b54a46bda4f`) if a disposable rebuild is still required to regenerate transient `libcore.aar`
- preserve the generated workspace `golang.org/x/mobile v0.0.0-20231108233038-35478a0c49da` baseline already validated in CP-014 through CP-023
- preserve the host generated-workspace Go baseline (`go1.23.6`) unless mere observation proves auto-selection cannot be avoided
- preserve the existing Gradle wrapper baseline (`8.10.2`)
- preserve the existing Android Gradle Plugin baseline (`8.8.1`)
- preserve the existing Kotlin and KSP baselines (`2.0.21` and `2.0.21-1.0.27`)
- preserve the existing JDK 17 and local SDK/NDK baseline already documented in project state
- do not intentionally upgrade Gradle plugins, Kotlin plugins, Java toolchains, Go dependencies, or Android dependencies during the probe
- if Gradle, Kotlin, Java, KSP, or Go auto-selects a different version, capture the exact version and trigger as evidence and do not treat that drift as the repair

Cleanup expectations after evidence capture for the later CP-024 run:
- remove any disposable temporary gomobile source workspace created for CP-024
- remove any disposable temporary GOPATH or patched binaries created for CP-024
- remove `android/fork/libcore/.build/` after evidence capture unless the report explicitly preserves it for the first downstream failure evidence
- remove transient copied `android/fork/app/libs/libcore.aar` after evidence capture unless the report explicitly preserves it for the first downstream failure evidence
- leave the default installed `gomobile-matsuri` and `gobind-matsuri` binaries unchanged
- leave the compile-jar, runtime-jar, project dex archive, merge-project-dex, and merge-ext-dex outputs in `android/fork/app/build/intermediates/` as normal Gradle artifacts if Gradle creates them during the bounded probe
- leave all upstream source trees unchanged
- stop without continuing into `mergeLibDex*`, packaging, install, or broader build continuation
- document the exact stopping point and cleanup result in `docs/android/ANDROID_POST_MERGE_PROJECT_DEX_CONTINUATION.md`

## COMPLETION_TEST
CP-024 is complete only if:
- `docs/android/ANDROID_POST_MERGE_PROJECT_DEX_CONTINUATION.md` exists
- the report documents why `.\gradlew.bat :app:mergeExtDexOssDebug --stacktrace` is the correct next exact downstream surface
- the report confirms the corrected post-`mergeProjectDexOssDebug` boundary:
  - `android/fork/app/build/intermediates/dex/ossDebug/mergeProjectDexOssDebug` exists
  - `android/fork/app/build/intermediates/merged_project_dex/ossDebug` is not treated as the success path for this continuation
- the report confirms the exact prerequisite state before execution:
  - `android/sing-box` is on branch `cp017-local-baseline`
  - `android/sing-box` is at `aed32ee3066cdbc7d471e3e0415c5134088962df`
  - `android/fork/app/build/intermediates/compile_app_classes_jar/ossDebug/bundleOssDebugClassesToCompileJar/classes.jar` exists before merge-ext-dex entry
  - `android/fork/app/build/intermediates/runtime_app_classes_jar/ossDebug/bundleOssDebugClassesToRuntimeJar/classes.jar` exists before merge-ext-dex entry
  - `android/fork/app/build/intermediates/project_dex_archive/ossDebug` exists before merge-ext-dex entry
  - `android/fork/app/build/intermediates/project_dex_archive/ossDebug/dexBuilderOssDebug/out` exists before merge-ext-dex entry
  - `android/fork/app/build/intermediates/external_libs_dex_archive/ossDebug/dexBuilderOssDebug/out` exists before merge-ext-dex entry
  - `android/fork/app/build/intermediates/dex/ossDebug/mergeProjectDexOssDebug` exists before merge-ext-dex entry
  - `android/fork/app/build/intermediates/dex/ossDebug/mergeExtDexOssDebug` is absent before merge-ext-dex entry
  - `android/fork/app/build/intermediates/dex/ossDebug/mergeLibDexOssDebug` is absent before merge-ext-dex entry
  - `android/fork/app/build/outputs/apk/` is absent before merge-ext-dex entry
  - transient `android/fork/app/libs/libcore.aar` is reproduced before Gradle entry
- the report documents the exact merge-ext-dex entry command `.\gradlew.bat :app:mergeExtDexOssDebug --stacktrace`
- the report captures the first exact task-graph failure, first exact merge-ext-dex failure, or the exact success of `:app:mergeExtDexOssDebug`
- if the task succeeds, the report confirms the exact success artifact path:
  - `android/fork/app/build/intermediates/dex/ossDebug/mergeExtDexOssDebug`
- the report confirms the following paths remain absent because they are out of scope:
  - `android/fork/app/build/intermediates/dex/ossDebug/mergeLibDexOssDebug`
  - any APK output under `android/fork/app/build/outputs/apk/`
- the report identifies the exact evidence source used if direct shell output is incomplete:
  - direct shell transcript
  - Gradle daemon log
  - filesystem artifact checks
- the report explicitly states that the cleared metadata bridge was not revisited as a new repair surface
- the report explicitly separates merge-ext-dex probing from `mergeLibDex*`, packaging, install, or assemble work
- the report documents the exact cleanup expectations and actual cleanup result after evidence capture
- no upstream source files under `android/fork/` are modified
- no broad patches are applied
- no `mergeLibDex*`, packaging, install, or `assemble*` work is attempted
- `PROJECT_STATE.md` is updated after execution

## RESULT
complete

## WHAT_CHANGED
- `docs/android/ANDROID_POST_MERGE_PROJECT_DEX_CONTINUATION.md` was created to record the bounded CP-024 execution.
- CP-024 was executed with the prerequisite artifact `android/fork/app/libs/libcore.aar` and the exact command `.\gradlew.bat :app:mergeExtDexOssDebug --stacktrace`.
- The corrected post-`mergeProjectDexOssDebug` boundary was preserved at `android/fork/app/build/intermediates/dex/ossDebug/mergeProjectDexOssDebug`.
- The first exact downstream outcome was a successful merge-ext-dex continuation signal, evidenced by:
  - daemon-log success for the build
  - creation of `android/fork/app/build/intermediates/dex/ossDebug/mergeExtDexOssDebug`
- Cleanup was completed for the disposable gomobile workspace, `android/fork/libcore/.build/`, and transient `android/fork/app/libs/libcore.aar`.

## REMAINING_GAPS
- No gaps remain inside CP-024.
- The next downstream continuation after successful `:app:mergeExtDexOssDebug` has not been defined yet.

## NEXT_CHECKPOINT
Create CP-025 only to define the smallest bounded continuation after successful `.\gradlew.bat :app:mergeExtDexOssDebug --stacktrace`, most likely around `.\gradlew.bat :app:mergeLibDexOssDebug --stacktrace`, without executing it or broadening into packaging, install, or assemble work.

## HANDOFF
Current checkpoint status: complete
Safe next action: create CP-025 only, scoped to redefining the next exact bounded continuation after the observed successful `.\gradlew.bat :app:mergeExtDexOssDebug --stacktrace` result
Artifacts created or updated:
- checkpoints/CP-024.md
- docs/android/ANDROID_POST_MERGE_PROJECT_DEX_CONTINUATION.md
- PROJECT_STATE.md
Known risks:
- the next session may incorrectly skip the still-absent merge-lib-dex surface and jump directly into packaging or `assemble*` work
- future direct shell transcripts may again be empty even when the task succeeds, in which case daemon-log plus filesystem evidence must be preserved
- the persisted `android/sing-box` baseline on `cp017-local-baseline` remains a hard prerequisite for continuity
Resume from:
- checkpoints/CP-024.md
- docs/android/ANDROID_POST_MERGE_PROJECT_DEX_CONTINUATION.md
- PROJECT_STATE.md
