# CP-036

## CHECKPOINT_ID
CP-036

## TITLE
Define the smallest bounded post-launch process-verification probe

## CURRENT_STATE
CP-035 is complete.
The cleared `libcore` metadata bridge remains only an already-validated prerequisite from CP-014.
The persisted `android/sing-box` baseline remains the CP-017 prerequisite on branch `cp017-local-baseline` at `aed32ee3066cdbc7d471e3e0415c5134088962df`.

CP-035 execution now confirms bounded CLI launch verification with required success lines:
- `Status: ok`
- `Activity: moe.nb4a.debug/io.nekohasekai.sagernet.ui.MainActivity`
- `Complete`
- `EXIT_CODE: 0`

The first exact downstream surface after successful launch verification is bounded process-alive verification.

## TARGET_STATE
The repository has a bounded checkpoint definition for probing only the first exact downstream surface after successful CP-035 launch verification.

After CP-036 execution, a future session must be able to answer:
- whether one bounded process-verification probe confirms that `moe.nb4a.debug` is alive
- what exact device/package prerequisites were true before probe entry
- what exact first meaningful outcome appears when that single probe is entered
- whether the first outcome is a prerequisite failure, process-check failure, or process-alive success
- how process verification is explicitly separated from runtime debugging and interaction testing

CP-036 does not perform runtime debugging.
CP-036 does not perform interaction testing.
CP-036 does not implement product features.

## IN_SCOPE
- identify the first exact post-launch surface as bounded process-alive verification
- define one bounded process-verification probe:
  - `& "$env:ANDROID_HOME\\platform-tools\\adb.exe" -s <serial> shell pidof moe.nb4a.debug`
- define exact prerequisite checks before probe entry:
  - `android/sing-box` remains on branch `cp017-local-baseline`
  - `android/sing-box` `HEAD` remains `aed32ee3066cdbc7d471e3e0415c5134088962df`
  - CP-035 execution artifact exists:
    - `docs/android/ANDROID_POST_INSTALL_VERIFICATION_CONTINUATION.md`
  - CP-035 prerequisite/launch evidence exists:
    - `docs/android/evidence/cp035_retry3_prereq_checks.log`
    - `docs/android/evidence/cp035_retry3_launch_probe.clean.log`
  - target APK/output identity remains available:
    - `android/fork/app/build/outputs/apk/oss/debug/output-metadata.json`
    - `android/fork/app/build/outputs/apk/oss/debug/NekoBox-1.4.2-x86_64-debug.apk`
  - package id expectation remains:
    - `applicationId` equals `moe.nb4a.debug`
  - Android SDK tool path is resolvable:
    - `"$env:ANDROID_HOME\\platform-tools\\adb.exe"` exists (or equivalent resolved path explicitly captured)
  - exactly one online adb target is available
  - target adb device ABI is `x86_64`
  - installed package check passes before process probe:
    - `adb shell pm path moe.nb4a.debug` returns a package path
- define exact expected process-alive success signals:
  - `pidof` output is non-empty
  - `pidof` output contains one or more numeric PID tokens for `moe.nb4a.debug`
  - `EXIT_CODE: 0`
- define exact out-of-scope actions that must remain unperformed:
  - no UI interaction commands (`adb shell input`, `adb shell monkey`, UI automation)
  - no network actions or traffic probes from device shell
  - no app relaunch commands (`adb shell am start*`)
  - no runtime-debugging capture (`adb logcat`, profiler, traces)
  - no broad diagnostics beyond the single bounded probe
  - no `assemble*` continuation
  - no release signing/distribution work
- define exact fallback evidence sources if direct shell output is incomplete:
  - direct shell transcript of `adb shell pidof moe.nb4a.debug`
  - captured clean transcript file for the same command
  - `adb devices -l` snapshot captured in the same run
  - fallback package-presence check (`adb shell pm path moe.nb4a.debug`) only if `pidof` output is incomplete
  - fallback process-list check (`adb shell ps -A | grep moe.nb4a.debug`) only if `pidof` output is incomplete
  - fallback activity-process check (`adb shell dumpsys activity processes | grep moe.nb4a.debug`) only if both prior outputs are incomplete
- define exact cleanup expectations:
  - do not regenerate `libcore.aar`
  - do not create disposable gomobile/GOPATH workspaces
  - remove only temporary CP-036 scratch files if any are created outside `docs/android/evidence/`
  - retain canonical evidence files under `docs/android/evidence/`
  - do not uninstall package as part of CP-036
  - do not force-stop or kill the app process as part of CP-036
  - leave upstream source trees unchanged
- create `docs/android/ANDROID_POST_LAUNCH_PROCESS_VERIFICATION.md` during later CP-036 execution
- update `PROJECT_STATE.md` and `checkpoints/CP-036.md` during later CP-036 execution

## OUT_OF_SCOPE
- executing CP-036
- revisiting any previous build-chain checkpoints
- revisiting or re-solving the cleared `libcore` metadata bridge
- revisiting or re-testing the already-validated `android/sing-box` baseline except as a stated prerequisite
- modifying upstream source under `android/fork/`
- applying broad patches to build logic, dependency trees, or generated workspaces
- runtime debugging, profiling, or diagnostic deep dives
- interaction testing or user-flow testing
- Android product feature implementation
- per-app routing implementation
- transport or proxy logic
- changes to `android/libneko/` or `android/sing-box/` baselines
- server artifacts or server implementation, except `PROJECT_STATE.md` for continuity
- CI/CD changes

## REQUIRED_ARTIFACTS
- `checkpoints/CP-036.md`
- `PROJECT_STATE.md` (updated for CP-036 definition state only)
- `docs/android/ANDROID_POST_LAUNCH_PROCESS_VERIFICATION.md` (execution artifact for later CP-036 run)

## EXECUTION_NOTES
CP-036 is a process-verification definition checkpoint.
It must remain narrow and separate process verification from runtime debugging and interaction testing.

Why the next surface is process verification:
- CP-035 already proved bounded CLI launch verification
- the smallest deterministic downstream gate is process liveness confirmation
- one `pidof` probe is the narrowest bounded surface for this gate

Execution priority for later CP-036 run:
1. verify all CP-036 prerequisites exactly
2. run the single bounded `adb shell pidof` probe once
3. capture first exact meaningful outcome and stop
4. do not continue into interaction or runtime-debugging workflows

First meaningful outcome rules for CP-036:
- if prerequisite checks fail, the first failed prerequisite line is the checkpoint outcome
- if `pidof` command runs but yields empty/non-process output, the first exact failure line is the checkpoint outcome
- if `pidof` command returns numeric PID output, success signal lines are the checkpoint outcome
- stop immediately once one of those outcomes is captured

Toolchain and environment bounds for later CP-036 run:
- preserve persisted `android/sing-box` baseline at `cp017-local-baseline` / `aed32ee3066cdbc7d471e3e0415c5134088962df`
- preserve Android SDK/platform-tools baseline already used in CP-035
- do not intentionally upgrade Gradle/AGP/Kotlin/Java/Go/Android dependencies during probe
- if adb path/device identity differs from prior checkpoints, capture exact evidence and stop at first meaningful outcome

## COMPLETION_TEST
CP-036 is complete only if:
- `docs/android/ANDROID_POST_LAUNCH_PROCESS_VERIFICATION.md` exists after execution
- the report confirms exact CP-036 prerequisites listed in `IN_SCOPE`
- the report documents exact bounded probe command:
  - `& "$env:ANDROID_HOME\\platform-tools\\adb.exe" -s <serial> shell pidof moe.nb4a.debug`
- the report captures first exact outcome from that one probe and stops
- if probe succeeds, the report includes:
  - non-empty numeric PID output for `moe.nb4a.debug`
  - `EXIT_CODE: 0`
- the report confirms out-of-scope actions in `IN_SCOPE` remain unperformed
- the report identifies fallback evidence used if direct shell output is incomplete
- the report confirms cleanup expectations were followed
- no upstream source files under `android/fork/` are modified
- no runtime-debugging/interaction/full-assemble continuation is attempted
- `PROJECT_STATE.md` is updated after execution

## RESULT
pending

## WHAT_CHANGED
- `checkpoints/CP-036.md` was authored to define the smallest bounded continuation after successful CP-035 launch verification.
- The checkpoint now defines one bounded downstream probe as process-alive verification only.
- `PROJECT_STATE.md` was updated so the next eligible work is executing CP-036 only.

## REMAINING_GAPS
- CP-036 has not been executed yet.
- `docs/android/ANDROID_POST_LAUNCH_PROCESS_VERIFICATION.md` does not exist yet and must be created during CP-036 execution.
- Exact success or failure outcome of the bounded process-verification probe is still unknown.

## NEXT_CHECKPOINT
Execute CP-036 only, exactly as defined, to run one bounded process-verification probe after successful launch verification, without entering runtime debugging, interaction testing, or feature work.

## HANDOFF
Current checkpoint status: pending
Safe next action: execute CP-036 only using the single bounded process-verification probe defined above
Artifacts created or updated:
- checkpoints/CP-036.md
- PROJECT_STATE.md
Known risks:
- scope will drift if runtime-debugging, interaction testing, or broader continuation starts before CP-036 execution is captured
- device state can drift between sessions; prerequisite device identity/ABI checks must pass before probe entry
- process identity can drift if package state changes between sessions and must be captured as first meaningful outcome evidence
Resume from:
- checkpoints/CP-036.md
- PROJECT_STATE.md
- docs/android/ANDROID_POST_INSTALL_VERIFICATION_CONTINUATION.md
