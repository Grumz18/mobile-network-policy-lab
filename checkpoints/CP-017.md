# CP-017

## CHECKPOINT_ID
CP-017

## TITLE
Define the smallest bounded `android/sing-box` baseline-persistence checkpoint

## CURRENT_STATE
CP-016 is complete.
The generated `libcore` metadata bridge is already repaired and validated in `docs/android/ANDROID_LIBCORE_METADATA_BRIDGE_REPAIR.md`.
That earlier checkpoint proved:
- the generated fallback `go.mod` can preserve `replace libcore`
- the generated fallback can preserve the local replacement mappings for `github.com/matsuridayo/libneko` and `github.com/sagernet/sing-box`
- the generated fallback can preserve the original `golang.org/x/mobile` baseline (`v0.0.0-20231108233038-35478a0c49da`)
- the exact bare `libcore` import failure from CP-013 is cleared

CP-015 then isolated the next blocker in `docs/android/ANDROID_POST_METADATA_DEPENDENCY_BLOCKER.md`.
That checkpoint proved:
- the first post-CP-014 generated-workspace dependency blocker is primarily a revision/layout mismatch in the local `android/sing-box/` checkout
- the fork build scripts expect `android/sing-box` commit `aed32ee3066cdbc7d471e3e0415c5134088962df`
- the drifted local checkout at `ab23e111dda5f9ee66fca2d49cb28f39d41192bb` on branch `def` carries the missing-package failures and the current `github.com/sagernet/cloudflare-tls` failure

CP-016 then proved in `docs/android/ANDROID_SINGBOX_ALIGNMENT_TEST.md` that:
- a reversible alignment of `android/sing-box` to `aed32ee3066cdbc7d471e3e0415c5134088962df` alone clears the generated-workspace missing-package errors
- that same alignment also clears the current `github.com/sagernet/cloudflare-tls` failure
- within the same bounded validation surface, no new meaningful blocker appeared before transient `libcore.aar` production

The local checkout was intentionally restored after CP-016:
- branch `def`
- commit `ab23e111dda5f9ee66fca2d49cb28f39d41192bb`

No upstream source under `android/fork/` has been modified.
The default installed `gomobile-matsuri` and `gobind-matsuri` binaries remain unchanged.

## TARGET_STATE
The repository has a bounded checkpoint definition for making the validated `android/sing-box` alignment intentional and durable as the new local dependency baseline, and only for that purpose.

After CP-017 execution, a future session must be able to answer:
- how the local `android/sing-box` baseline is intentionally moved from `ab23e111dda5f9ee66fca2d49cb28f39d41192bb` on `def` to `aed32ee3066cdbc7d471e3e0415c5134088962df`
- how that new baseline is recorded for continuity
- how the persisted baseline is revalidated only within the same bounded `libcore` validation surface already proven in CP-016
- how rollback is handled if the persistence step or the revalidation step fails

CP-017 does not need to continue into Gradle or app build work.
It must only define the smallest bounded baseline-persistence checkpoint and keep it clearly separated from broader build continuation.

## IN_SCOPE
- make the validated `android/sing-box` alignment intentional and durable as the new local dependency baseline
- define the exact local baseline transition:
  - from `def` at `ab23e111dda5f9ee66fca2d49cb28f39d41192bb`
  - to detached or branch-attached state at `aed32ee3066cdbc7d471e3e0415c5134088962df`
- define how that baseline change will be tracked in project continuity artifacts
- define only the same bounded revalidation surface already used in CP-016, such as:
  - reversible checkout capture before baseline persistence
  - `android/fork/libcore/build.sh` only through transient `libcore.aar` production
  - one generated root inspection such as `src-android-386`
  - `go list -e -json ./gobind`
  - `go mod tidy -v`
  - `go list -m -e -json github.com/sagernet/cloudflare-tls`
- define toolchain and dependency drift bounds for the persistence run
- define explicit rollback expectations if the baseline persistence or bounded revalidation fails
- create `docs/android/ANDROID_SINGBOX_BASELINE_PERSIST.md`
- update `PROJECT_STATE.md` and `checkpoints/CP-017.md` with execution results

## OUT_OF_SCOPE
- revisiting or re-solving the cleared `libcore` metadata bridge
- modifying upstream source under `android/fork/`, including `android/fork/libcore/`
- broad patches to `gomobile-matsuri`, `gobind-matsuri`, or generated workspace layout
- Gradle app assembly or any Android build continuation beyond the bounded `libcore` validation surface already proven in CP-016
- Android product features
- per-app routing implementation
- transport or proxy logic
- standalone dependency repair unrelated to the already-proven `android/sing-box` alignment
- changing `android/libneko/` baseline in this checkpoint
- server artifacts or server implementation
- CI/CD changes

## REQUIRED_ARTIFACTS
- `docs/android/ANDROID_SINGBOX_BASELINE_PERSIST.md`
- `PROJECT_STATE.md` (updated after CP-017 execution)
- `checkpoints/CP-017.md` (updated after CP-017 execution)

## EXECUTION_NOTES
CP-017 is a baseline-persistence checkpoint.
It must not turn into broader build continuation.

The execution priority is:
1. capture the current local `android/sing-box/` state for safety and audit
2. intentionally establish `aed32ee3066cdbc7d471e3e0415c5134088962df` as the new local baseline
3. record that persisted baseline explicitly in the checkpoint artifacts
4. rerun only the bounded `libcore` validation surface already proven in CP-016
5. stop after confirming that the persisted baseline still clears the previously diagnosed blocker

The persistence questions CP-017 must answer explicitly are:
1. what exact checkout state is being replaced as the previous local baseline
2. what exact checkout state becomes the new intentional local baseline
3. how the new baseline is recorded so future sessions do not need to rediscover it
4. whether the persisted baseline still clears the missing-package and `cloudflare-tls` symptoms under the same bounded validation surface
5. what one exact next surface should be selected after the baseline is intentionally established

Version-drift bounds for CP-017 execution:
- preserve the default host Go baseline observed in CP-016 (`go1.23.6`) unless mere observation proves auto-selection cannot be avoided
- preserve the exact `gomobile-matsuri` source revision used in CP-014, CP-015, and CP-016 (`17d6af34f6bd6d7e1e428e0c652c8b54a46bda4f`)
- preserve the already-validated `golang.org/x/mobile v0.0.0-20231108233038-35478a0c49da` baseline in the generated workspace
- do not intentionally upgrade `github.com/sagernet/sing-box`, `github.com/sagernet/cloudflare-tls`, or related dependencies during the persistence run
- do not intentionally change the target baseline commit away from `aed32ee3066cdbc7d471e3e0415c5134088962df`
- if `go list`, `go mod tidy`, or `go build` still auto-selects a newer toolchain or different dependency version, capture the exact trigger and exact version as evidence
- do not treat any new auto-selected version as the repair

Allowed persistence depth:
- the only mutable dependency tree in scope is `android/sing-box/`
- one generated architecture root is sufficient if the evidence is conclusive
- once the baseline is intentionally established and the same bounded revalidation succeeds or fails conclusively, stop

Not allowed:
- reworking the generated fallback `go.mod` again unless mere reproduction requires the same already-approved CP-014 local behavior
- turning the checkpoint into Gradle continuation because transient `libcore.aar` appears again
- broadening from local baseline persistence into application build, release, or feature work

Rollback expectations if the persistence attempt fails or is inconclusive:
- restore `android/sing-box/` to its exact pre-CP-017 state if the intended baseline cannot be established cleanly
- remove any isolated temporary gomobile source workspace created for CP-017
- remove any isolated temporary GOPATH or patched binaries created for CP-017
- remove `android/fork/libcore/.build/` after evidence capture unless the report explicitly preserves it
- remove transient copied `android/fork/app/libs/libcore.aar` if created during bounded revalidation
- leave the default installed `gomobile-matsuri` and `gobind-matsuri` binaries unchanged
- leave all upstream source trees unchanged
- stop without continuing into Gradle or broader build continuation
- document the exact stopping point and rollback result in `docs/android/ANDROID_SINGBOX_BASELINE_PERSIST.md`

## COMPLETION_TEST
CP-017 is complete only if:
- `docs/android/ANDROID_SINGBOX_BASELINE_PERSIST.md` exists
- the report documents the exact previous and persisted `android/sing-box` baseline states
- the report explicitly states how the new local baseline is being tracked for continuity
- the report explicitly states whether the persisted baseline still clears the missing-package and `cloudflare-tls` symptoms under the same bounded validation surface
- the report documents the exact commands and exact evidence used
- the report bounds Go toolchain and dependency version drift during the attempt
- the report selects one exact smallest next surface after baseline persistence
- the report explicitly separates that next surface from broader build continuation
- no upstream source files under `android/fork/` are modified
- no broad patches are applied
- no Gradle app build is attempted
- `PROJECT_STATE.md` is updated after execution

## RESULT
partial

## WHAT_CHANGED
- `checkpoints/CP-017.md` was created to define the smallest bounded checkpoint for intentionally persisting the validated `android/sing-box` alignment as the new local baseline.
- The checkpoint explicitly limits later execution to baseline persistence, continuity tracking, bounded rollback behavior, and the same narrow `libcore` validation surface already proven in CP-016.
- Execution was intentionally not performed in this session.

## REMAINING_GAPS
CP-017 has not been executed yet.
`docs/android/ANDROID_SINGBOX_BASELINE_PERSIST.md` does not exist yet because the baseline-persistence run is deferred.
The validated alignment result is still not persisted as the intentional local baseline until CP-017 is executed.

## NEXT_CHECKPOINT
Execute CP-017 to intentionally establish `android/sing-box` at `aed32ee3066cdbc7d471e3e0415c5134088962df` as the local dependency baseline and revalidate only the same bounded `libcore` path already proven in CP-016, without entering Gradle or broader app build work.

## HANDOFF
Current checkpoint status: partial
Safe next action: execute CP-017 only, scoped to persisting the proven `android/sing-box` baseline and revalidating only the same bounded `libcore` surface from CP-016
Artifacts created or updated:
- checkpoints/CP-017.md
Known risks:
- the next session may drift back into the already-cleared `libcore` metadata bridge instead of staying on baseline persistence
- broader build continuation could blur whether the checkpoint succeeded at baseline establishment alone
- if rollback is not described and executed carefully, the local dependency state could become ambiguous for future sessions
Resume from:
- docs/android/ANDROID_SINGBOX_ALIGNMENT_TEST.md
- docs/android/ANDROID_POST_METADATA_DEPENDENCY_BLOCKER.md
- docs/android/ANDROID_LIBCORE_METADATA_BRIDGE_REPAIR.md
- checkpoints/CP-017.md
- PROJECT_STATE.md
