# CP-043

## CHECKPOINT_ID
CP-043

## TITLE
Define persistence-with-reporting rule for bounded checkpoint execution

## CURRENT_STATE
CP-042 retry execution is blocked at explicit adb device-recovery gate in bounded scope:
- `expected exactly one online adb target with state device, found 0`

The project currently applies bounded retry discipline inside individual checkpoint definitions, but there is no single cross-checkpoint persistence-with-reporting rule artifact that standardizes:
- maximum recovery cycles
- per-cycle evidence naming
- blocked vs complete outcome behavior under transient environment failures
- mandatory completion narrative when eventual success is achieved

## TARGET_STATE
The repository has a bounded CP-043 definition that adds a persistence-with-reporting rule for checkpoint execution without changing existing checkpoint logic or project structure.

After later CP-043 execution, a future session must be able to answer:
- how transient environment failures are handled with bounded recovery and bounded retry
- how many recovery cycles are allowed per checkpoint execution
- what evidence files must be captured per cycle
- how blocked outcomes must be labeled when environment-limited
- what completion narrative must be appended when success occurs after recovery

CP-043 does not execute persistence logic.
CP-043 does not execute recovery cycles.
CP-043 does not execute probes.

## IN_SCOPE
- define a bounded persistence-with-reporting rule for checkpoint execution:
  - when a checkpoint probe fails due to transient environmental signals (for example: `adb devices` shows 0 targets, `Broken pipe`, timeout), the LLM must:
    - capture exact failure signal and evidence
    - attempt bounded recovery actions:
      - `adb kill-server`
      - `adb start-server`
      - wait 5 seconds
      - re-verify checkpoint prerequisites
    - retry the checkpoint probe exactly once per recovery cycle
    - repeat up to maximum 5 recovery cycles total for the same checkpoint
  - if all recovery cycles are exhausted without success:
    - mark `RESULT: blocked`
    - include explicit `environment-limited` flag in checkpoint execution report
  - if success is achieved at any cycle:
    - mark `RESULT: complete`
    - proceed without extra recovery cycles
- define mandatory completion narrative requirement upon `RESULT: complete`:
  - append `Completion Narrative` section to the checkpoint execution artifact
  - include:
    - which recovery actions were taken (if any)
    - which recovery cycle achieved success
    - exact final probe output that satisfied success signals
    - assumptions or environmental conditions that enabled success
- define exact fallback evidence sources:
  - per-cycle evidence logs:
    - `cpXXX_retry_N_adb_devices.log`
    - `cpXXX_retry_N_probe.log`
  - recovery action transcript:
    - `cpXXX_recovery_actions.log`
  - final completion narrative embedded in the main execution artifact
- define exact cleanup expectations:
  - retain all retry-cycle evidence under `docs/android/evidence/`
  - remove only temporary scratch files outside evidence directory
  - do not modify upstream source or baseline branches
- explicitly separate persistence logic from UI interaction, network activity, and runtime debugging
- define expected later execution artifact:
  - `docs/android/ANDROID_PERSISTENCE_WITH_REPORTING_RULE.md`
- update `PROJECT_STATE.md` and `checkpoints/CP-043.md` for definition-only authoring continuity
- explicitly mark that this rule applies prospectively to CP-043 and all future checkpoints only (no retroactive rewrite of prior checkpoint outcomes)

## OUT_OF_SCOPE
- executing CP-043
- executing recovery cycles or any checkpoint probe
- revisiting previous build-chain checkpoints
- modifying upstream source under `android/fork/`
- applying broad patches to build logic, dependency trees, or generated workspaces
- UI interaction commands or user-flow testing
- network actions or traffic probes
- runtime debugging, profiling, or diagnostic deep dives
- Android product feature implementation
- per-app routing implementation
- transport or proxy logic implementation
- server artifacts or server implementation, except `PROJECT_STATE.md` for continuity
- infinite retry loops
- automatic scope expansion to debug environment beyond defined bounded recovery actions
- modifying checkpoint success criteria to force completion
- suppressing failure evidence

## REQUIRED_ARTIFACTS
- `checkpoints/CP-043.md`
- `PROJECT_STATE.md` (updated for CP-043 definition state and next eligible work)
- `docs/android/ANDROID_PERSISTENCE_WITH_REPORTING_RULE.md` (execution artifact for later CP-043 execution)

## EXECUTION_NOTES
CP-043 is a definition-only checkpoint.
It standardizes persistence-with-reporting behavior while preserving existing checkpoint structures and success criteria.

The persistence rule is prospective only:
- applies to CP-043 execution and future checkpoints
- does not retroactively alter prior checkpoint outcomes or evidence

Bounded persistence model:
1. capture failure signal and evidence
2. run bounded recovery actions
3. re-verify prerequisites
4. retry probe once for that cycle
5. stop at success, or continue until max 5 cycles
6. mark blocked with explicit environment-limited flag if all cycles fail

## COMPLETION_TEST
CP-043 definition is complete only if:
- `checkpoints/CP-043.md` exists and follows checkpoint structure
- bounded persistence-with-reporting rule is explicit and testable
- retry ceiling is explicit (`max 5 recovery cycles`)
- blocked behavior with explicit `environment-limited` flag is defined
- completion narrative requirements are explicit for successful recovery outcomes
- evidence naming conventions for per-cycle and recovery logs are explicit
- prospective-only applicability is explicit (no retroactive changes)
- `docs/android/ANDROID_PERSISTENCE_WITH_REPORTING_RULE.md` is named as the later execution artifact
- `PROJECT_STATE.md` is updated so next eligible work is executing CP-043 only
- no CP-043 execution evidence is claimed in this definition-only step

## RESULT
pending

## WHAT_CHANGED
- Created CP-043 as a definition-only checkpoint for persistence-with-reporting execution discipline.
- Defined bounded recovery cycle behavior (max 5), blocked behavior with explicit environment-limited flag, and complete behavior with mandatory completion narrative.
- Defined required fallback evidence naming for per-cycle and recovery-action logs.
- Explicitly constrained scope to avoid retry-loop expansion, success-criteria drift, and evidence suppression.
- Updated `PROJECT_STATE.md` to set CP-043 execution as next eligible work.

## REMAINING_GAPS
- CP-043 execution is pending.
- Execution artifact `docs/android/ANDROID_PERSISTENCE_WITH_REPORTING_RULE.md` is not created in this definition-only step.

## NEXT_CHECKPOINT
Execute CP-043 only: apply the bounded persistence-with-reporting rule in one checkpoint execution flow, capture required evidence artifacts, and publish the execution artifact.

## HANDOFF
Current checkpoint status: pending
Safe next action: execute CP-043 only in bounded scope and create `docs/android/ANDROID_PERSISTENCE_WITH_REPORTING_RULE.md`
Artifacts created or updated:
- checkpoints/CP-043.md
- PROJECT_STATE.md
Known risks:
- scope drift into environment debugging beyond bounded recovery actions
- accidental retroactive rewriting of earlier checkpoint outcomes
- continuity loss if completion narrative is omitted after recovered success
Resume from:
- checkpoints/CP-043.md
- PROJECT_STATE.md
- checkpoints/CP-042.md
