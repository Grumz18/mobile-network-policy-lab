# CP-006

## CHECKPOINT_ID
CP-006

## TITLE
Define Android fork intake and patch workflow baseline

## CURRENT_STATE
CP-005 is complete.
The repository has a documented Android workspace skeleton, local build baseline, and fork/patch directory boundaries.
The following Android workspace areas exist but are empty placeholders:
- `android/fork/` — reserved for upstream fork intake, no code present
- `android/patches/` — reserved for local modifications, no patches present
- `android/config/` — reserved for non-secret configuration, placeholder only
The fork baseline document (`docs/android/ANDROID_FORK_BASELINE.md`) explicitly states that the fork intake workflow is intentionally undecided.
No fork has been fetched. No upstream repository has been materialized. No synchronization or patch workflow exists.
No Android product features, per-app routing, or transport logic have been implemented.

## TARGET_STATE
The repository contains a complete, documented fork intake and patch workflow baseline.
After this checkpoint is executed, a future session should be able to identify:
- the exact upstream fork source (NekoBox repository coordinates)
- the chosen intake method (subtree, submodule, or manual snapshot) with rationale
- the step-by-step procedure to acquire the fork into `android/fork/`
- the convention for tracking which upstream commit the local fork is based on
- the patch workflow: how local modifications in `android/patches/` are created, named, ordered, and applied against fork content
- the procedure to re-apply patches after an upstream sync
- the verification flow to confirm fork intake and patch state are consistent

## IN_SCOPE
- create `docs/android/ANDROID_FORK_INTAKE.md` defining the fork acquisition method, upstream coordinates, intake steps, and upstream tracking convention
- create `docs/android/ANDROID_PATCH_WORKFLOW.md` defining the patch creation, naming, ordering, application, and re-application workflow
- update `android/fork/README.md` to reference the intake document and describe the expected post-intake directory state
- update `android/patches/README.md` to reference the patch workflow document and describe patch file conventions
- create `android/scripts/verify-fork-intake.ps1` as a safe verification script that checks fork intake and patch baseline state without fetching code or running builds
- update `PROJECT_STATE.md` to reflect CP-006 completion

## OUT_OF_SCOPE
- executing the fork intake (no git clone, subtree add, or submodule init)
- fetching, downloading, or materializing any upstream code
- Android application code or Gradle module changes
- per-app routing implementation
- transport or proxy logic
- server artifacts or server implementation
- CI/CD pipeline changes
- signing configuration
- emulator or device automation
- local build execution

## REQUIRED_ARTIFACTS
- `docs/android/ANDROID_FORK_INTAKE.md`
- `docs/android/ANDROID_PATCH_WORKFLOW.md`
- `android/fork/README.md` (updated)
- `android/patches/README.md` (updated)
- `android/scripts/verify-fork-intake.ps1`
- `PROJECT_STATE.md` (updated)

## EXECUTION_NOTES
This checkpoint is documentation-and-workflow-definition only. It must not fetch, clone, or materialize any upstream code.
The fork intake document should commit to a specific intake method (subtree, submodule, or manual snapshot) and justify the choice in the context of this project's research-grade, checkpoint-driven workflow.
The patch workflow document should define a repeatable, auditable process that survives session handoff.
The verification script must be non-destructive: it checks file existence and consistency, not network or build state.
If the exact upstream NekoBox repository URL or branch cannot be confirmed at execution time, the document should state the assumed coordinates and flag them for owner confirmation.

## COMPLETION_TEST
This checkpoint is complete only if:
- `docs/android/ANDROID_FORK_INTAKE.md` defines the upstream source, chosen intake method with rationale, step-by-step intake procedure, and upstream tracking convention
- `docs/android/ANDROID_PATCH_WORKFLOW.md` defines patch creation, naming, ordering, application, and re-application procedures
- `android/fork/README.md` references the intake document and describes the expected post-intake state
- `android/patches/README.md` references the patch workflow document and describes patch file conventions
- `android/scripts/verify-fork-intake.ps1` exists and validates the fork intake and patch workflow baseline without fetching code or running builds
- `PROJECT_STATE.md` is updated to reflect CP-006 completion
- no upstream code is fetched, no Android application code is introduced, no per-app routing or transport logic is added

## RESULT
pending

## WHAT_CHANGED
Checkpoint defined. Not yet executed.

## REMAINING_GAPS
All work in this checkpoint is pending execution.

## NEXT_CHECKPOINT
CP-007 - Execute fork intake (materialize upstream NekoBox code into android/fork/)

## HANDOFF
Current checkpoint status: pending
Safe next action: execute CP-006 to create fork intake and patch workflow baseline documents and scripts
Artifacts created or updated:
- checkpoints/CP-006.md
- PROJECT_STATE.md
Known risks:
- upstream NekoBox repository coordinates may need owner confirmation at execution time
- chosen intake method (subtree vs submodule vs snapshot) may need owner review before execution of CP-007
Resume from:
- PROJECT_STATE.md
- checkpoints/CP-006.md
