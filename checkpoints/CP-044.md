# CP-044

## CHECKPOINT_ID
CP-044

## TITLE
Define bounded adb-environment recovery preconditions after CP-043 blocked state

## CURRENT_STATE
CP-043 retry is blocked with explicit environment limitation due persistent adb target absence at stabilization gate:
- `expected exactly one online target with device state, found 0`

CP-043 already validated bounded persistence-with-reporting behavior and captured complete retry evidence without entering out-of-scope activity.

The first exact downstream surface is host-side adb-environment recovery preconditions, explicitly separated from device-side checkpoint execution.

## TARGET_STATE
The repository has a bounded CP-044 definition for host-side adb-environment recovery preconditions only.

After later CP-044 execution, a future session must be able to answer:
- whether host-side emulator/USB readiness preconditions are present
- whether a single bounded host-side probe returns required readiness signal(s)
- what exact first meaningful outcome was observed
- how host-side verification remains separated from device-side checkpoint execution

CP-044 does not execute adb-environment recovery.
CP-044 does not launch/stop emulators.
CP-044 does not perform device-side probes.

## IN_SCOPE
- identify the first exact recovery surface as host-side emulator/USB device readiness
- define exact prerequisite checks before host-side probe entry:
  - CP-043 execution artifact exists:
    - `docs/android/ANDROID_PERSISTENCE_WITH_REPORTING_RULE.md`
  - `PROJECT_STATE.md` reflects CP-043 blocked state
  - host OS is Windows
  - Android SDK paths are resolvable:
    - `C:/Android/Sdk/emulator/emulator.exe`
    - `C:/Android/Sdk/platform-tools/adb.exe`
- select one bounded host-side verification probe (exactly one per CP-044 execution):
  - preferred probe:
    - `& "C:/Android/Sdk/emulator/emulator.exe" -list-avds`
  - allowed alternative when emulator path is unresolved:
    - `& "C:/Android/Sdk/platform-tools/adb.exe" devices -l`
- define exact expected success signals:
  - for emulator-list probe:
    - probe output non-empty
    - output contains `CP034_x86_64`
    - `EXIT_CODE: 0`
  - for adb-devices alternative probe:
    - output non-empty
    - output contains exactly one non-header `device` state line
    - target ABI is `x86_64` (via `adb shell getprop ro.product.cpu.abi` or explicitly available model/ABI metadata)
    - target serial is `emulator-5554` or explicitly captured valid alternative
    - `EXIT_CODE: 0`
- define first meaningful outcome rules for later CP-044 execution:
  - if prerequisite fails, first failed prerequisite line is checkpoint outcome
  - if probe runs but required readiness signals are missing, first exact failure line is checkpoint outcome
  - if probe returns required readiness signal(s), success line(s) are checkpoint outcome
  - stop immediately after first meaningful outcome
- define exact fallback evidence sources if direct shell output is incomplete:
  - host process list:
    - `Get-Process | Where-Object { $_.ProcessName -match 'emulator|adb' }`
  - emulator log snapshot (read-only capture only)
  - SDK path verification capture for emulator/adb executables
  - optional host screenshot evidence (read-only capture)
- define exact cleanup expectations:
  - no modification of emulator configs
  - no persistent host environment changes
  - remove only temporary scratch files outside `docs/android/evidence/`
  - retain canonical CP-044 evidence under `docs/android/evidence/`
- explicitly separate host-side environment verification from device-side checkpoint execution
- define expected later execution artifact:
  - `docs/android/ANDROID_ADB_ENVIRONMENT_RECOVERY_PRECONDITIONS.md`
- update `PROJECT_STATE.md` and `checkpoints/CP-044.md` for definition-only continuity

## OUT_OF_SCOPE
- executing CP-044
- revisiting previous build-chain checkpoints
- modifying upstream source under `android/fork/`
- applying broad patches to build logic, dependency trees, generated workspaces, or project structure
- emulator start/stop commands
- USB driver installation or host-driver mutation
- adb kill/start actions beyond observation-only host readiness probe execution
- device-side probes
- UI interaction commands or user-flow testing
- network actions or traffic probes
- runtime debugging, profiling, or diagnostic deep dives
- Android product feature implementation
- per-app routing implementation
- transport or proxy logic implementation
- server artifacts or server implementation, except `PROJECT_STATE.md` for continuity

## REQUIRED_ARTIFACTS
- `checkpoints/CP-044.md`
- `PROJECT_STATE.md` (updated for CP-044 definition status and next eligible work)
- `docs/android/ANDROID_ADB_ENVIRONMENT_RECOVERY_PRECONDITIONS.md` (execution artifact for later CP-044 run)

## EXECUTION_NOTES
CP-044 is a definition-only checkpoint.
It defines host-side readiness verification preconditions only and intentionally does not perform recovery operations.

Execution priority for later CP-044 run:
1. verify prerequisites (artifact/state/OS/path resolution)
2. execute exactly one bounded host-side readiness probe
3. capture first exact meaningful outcome and stop
4. avoid all device-side checkpoint execution surfaces

## COMPLETION_TEST
CP-044 definition is complete only if:
- `checkpoints/CP-044.md` exists and follows checkpoint structure
- host-side recovery-precondition surface is explicit and bounded
- exactly one bounded host-side probe choice is defined (`emulator -list-avds` or `adb devices -l`)
- expected success signals are explicit and testable
- out-of-scope host/device boundaries are explicit
- fallback evidence sources are explicit and host-side only
- cleanup expectations prohibit host config mutation
- `docs/android/ANDROID_ADB_ENVIRONMENT_RECOVERY_PRECONDITIONS.md` is named as later execution artifact
- `PROJECT_STATE.md` is updated so next eligible work is executing CP-044 only
- no CP-044 execution evidence is claimed in this definition-only step

## RESULT
pending

## WHAT_CHANGED
- Created CP-044 as a definition-only checkpoint for bounded host-side adb-environment recovery preconditions.
- Defined prerequisite checks (CP-043 artifact/state continuity, Windows host, SDK path resolution).
- Defined single bounded host-side probe selection and explicit success signals.
- Defined out-of-scope boundaries that prohibit emulator control, driver changes, adb restart actions beyond observation, and all device-side probes.
- Defined fallback evidence and cleanup expectations with no persistent host mutation.
- Updated `PROJECT_STATE.md` so next eligible work becomes executing CP-044 only.

## REMAINING_GAPS
- CP-044 execution is pending.
- Execution artifact `docs/android/ANDROID_ADB_ENVIRONMENT_RECOVERY_PRECONDITIONS.md` is not created in this definition-only step.

## NEXT_CHECKPOINT
Execute CP-044 only: verify host-side prerequisites, run one bounded host-side readiness probe, capture first exact meaningful outcome, and stop.

## HANDOFF
Current checkpoint status: pending
Safe next action: execute CP-044 only in bounded host-side scope and create `docs/android/ANDROID_ADB_ENVIRONMENT_RECOVERY_PRECONDITIONS.md`
Artifacts created or updated:
- checkpoints/CP-044.md
- PROJECT_STATE.md
Known risks:
- scope drift into emulator control or host-environment mutation beyond bounded observation
- accidental fallback to device-side probing before host readiness is validated
- continuity loss if host-side evidence is incomplete or non-deterministic
Resume from:
- checkpoints/CP-044.md
- checkpoints/CP-043.md
- docs/android/ANDROID_PERSISTENCE_WITH_REPORTING_RULE.md
- PROJECT_STATE.md
