# CP-014

## CHECKPOINT_ID
CP-014

## TITLE
Test the smallest tool-local generated `go.mod` metadata-bridge repair for `libcore`

## CURRENT_STATE
CP-013 is complete.
The current generated `libcore` blocker is diagnosed and bounded in `docs/android/ANDROID_LIBCORE_LAYOUT_BLOCKER_DIAGNOSIS.md`.
That diagnosis proved:
- `.build/src` and `.build/src-android-*` intentionally stage only generated `gobind` sources
- the generated per-architecture workspace becomes `module gobind`
- `gobind/go_libcoremain.go` imports bare `libcore`
- the immediate failure occurs because the generated fallback `go.mod` loses the metadata bridge back to the original `libcore` module and its local replacements
- the current fallback path also drifts from the original `golang.org/x/mobile` baseline to a newer version that triggers Go toolchain auto-selection into `go1.25.x`

The smallest next repair surface selected by CP-013 is:
- generated fallback `go.mod` metadata for the local `libcore` module and its local replacements

No upstream source under `android/fork/` has been modified.
The default installed `gomobile-matsuri` and `gobind-matsuri` binaries remain unchanged.

## TARGET_STATE
The repository has a bounded repair-attempt checkpoint that tests only whether a tool-local generated `go.mod` fallback can preserve the module bridge required for bare import path `libcore` in the generated per-architecture workspace.
After CP-014 execution, a future session must be able to answer:
- whether a tool-local fallback `go.mod` that preserves:
  - `replace libcore`
  - local replacement mappings for `github.com/matsuridayo/libneko` and `github.com/sagernet/sing-box`
  - the original `golang.org/x/mobile` baseline
  clears the unresolved bare `libcore` import in generated `src-android-*`
- whether that metadata-only repair is sufficient to advance past the exact CP-013 import-resolution failure
- what first new blocker appears immediately after that validation, if the `libcore` import is cleared
- whether version drift stayed bounded during the attempt

CP-014 does not need to complete the libcore build path.
It must only test the selected metadata-bridge repair surface and stop at the first conclusive result.

## IN_SCOPE
- recreate only the isolated local tool workspace shape needed to reproduce the post-CP-013 generated workspace
- use the same exact `gomobile-matsuri` revision identified in CP-012 and CP-013 (`17d6af34f6bd6d7e1e428e0c652c8b54a46bda4f`)
- apply only the minimum temporary tool-local change needed to the generated fallback `go.mod` path so that, when strict module enumeration fails, the fallback preserves:
  - `replace libcore => <absolute path to android/fork/libcore>`
  - `replace github.com/matsuridayo/libneko => <absolute path to android/libneko>`
  - `replace github.com/sagernet/sing-box => <absolute path to android/sing-box>`
  - the original `golang.org/x/mobile` baseline observed from `android/fork/libcore/go.mod` and CP-013 evidence (`v0.0.0-20231108233038-35478a0c49da`)
- preserve the generated `.build/` tree long enough to inspect:
  - generated `src-android-*` `go.mod`
  - generated `src-android-*` `go.sum`
  - generated `gobind/go_libcoremain.go`
- validate in at least one generated per-architecture workspace whether the unresolved bare `libcore` import is cleared
- use direct generated-workspace validation commands such as:
  - `go list -e -json ./gobind`
  - `go mod tidy -v`
  - one bounded `go build ./gobind` attempt only if needed to prove the exact CP-013 import error is gone
- stop immediately after one of these conditions is reached:
  - the exact unresolved `libcore` import is cleared
  - the same unresolved `libcore` import still fails
  - a first new blocker appears after the import is cleared
- capture exact version behavior for:
  - host Go
  - isolated rebuilt binary toolchain
  - generated `golang.org/x/mobile` resolution
  - any Go auto-toolchain selection
- create `docs/android/ANDROID_LIBCORE_METADATA_BRIDGE_REPAIR.md`
- update `PROJECT_STATE.md` and `checkpoints/CP-014.md` with execution results

## OUT_OF_SCOPE
- modifying upstream source under `android/fork/`, including `android/fork/libcore/`
- persisting any patched `gomobile-matsuri` or `gobind-matsuri` binaries into the default user GOPATH
- changing the source-copy/layout behavior of `.build/src` or `.build/src-android-*`
- changing bind invocation shape except if a read-only inspection proves current invocation arguments are not the same as the CP-013 diagnosis baseline
- fixing the unresolved `github.com/sagernet/cloudflare-tls` dependency in upstream modules
- changing Go, JDK, SDK, NDK, gomobile, gobind, or dependency versions as a workaround
- silent dependency upgrades
- broader gomobile repair beyond the selected metadata fallback surface
- Gradle app assembly or any Android build continuation beyond the point needed to prove whether the `libcore` import is cleared
- Android product features
- per-app routing implementation
- transport or proxy logic
- server artifacts or server implementation
- CI/CD changes

## REQUIRED_ARTIFACTS
- `docs/android/ANDROID_LIBCORE_METADATA_BRIDGE_REPAIR.md`
- `PROJECT_STATE.md` (updated after CP-014 execution)
- `checkpoints/CP-014.md` (updated after CP-014 execution)

## EXECUTION_NOTES
CP-014 is a narrow repair-validation checkpoint.
It exists only to test the exact repair surface selected by CP-013.

The execution priority is:
1. recreate the isolated tool-local workspace at the pinned `gomobile-matsuri` revision
2. patch only the generated fallback `go.mod` metadata path
3. reproduce the generated `src-android-*` workspace
4. inspect whether the generated fallback now preserves the required module bridge
5. validate whether the exact unresolved bare `libcore` import is cleared
6. stop at the first conclusive result without continuing into broader build work

The exact diagnostic/repair questions CP-014 must answer are:
1. does the generated fallback `go.mod` now preserve `replace libcore` and the local replacements for `libneko` and `sing-box`
2. does the generated fallback preserve the original `golang.org/x/mobile` baseline instead of drifting to `latest`
3. in generated `src-android-*`, does `go list -e -json ./gobind` or equivalent validation still report:
   - `gobind/go_libcoremain.go:19:2: package libcore is not in std (...)`
4. if that exact import error disappears, what first new blocker appears next
5. did the attempt avoid silent dependency or toolchain upgrades

The repair must remain local to the generated fallback metadata surface.
CP-014 must not switch to broader source-copy or workspace-layout edits if the selected metadata repair is inconclusive.

The repair intent is not to solve all module problems.
It is only to test whether preserving the missing metadata bridge is sufficient to clear the exact CP-013 import-resolution failure.

Version-drift bounds for CP-014 execution:
- preserve the default host Go baseline observed in CP-013 (`go1.23.6`) unless mere observation proves auto-selection cannot be avoided
- preserve the exact `gomobile-matsuri` source revision from CP-013
- do not intentionally upgrade `golang.org/x/mobile`
- preserve `golang.org/x/mobile v0.0.0-20231108233038-35478a0c49da` in the fallback metadata unless direct evidence proves that baseline cannot be used in the generated workspace
- if `go mod tidy`, `go list`, or `go build` still auto-selects a newer toolchain or resolves a newer dependency, capture the exact version and exact trigger as evidence
- do not treat any newer auto-selected dependency or toolchain as the repair

Allowed validation depth:
- one generated architecture root is sufficient if the evidence is conclusive
- additional architectures may be inspected only if the first root is inconclusive
- once the exact `libcore` import result is known, stop

Not allowed:
- continuing into Gradle or app-level build work after the metadata-bridge result is known
- editing upstream Android fork content
- patching unrelated gomobile behaviors
- upgrading or pinning unrelated dependencies as a workaround

Rollback expectations if the repair attempt fails or is inconclusive:
- remove any isolated temporary gomobile source workspace created for CP-014
- remove any isolated temporary GOPATH or patched binaries created for CP-014
- remove `android/fork/libcore/.build/` after evidence capture unless the report explicitly preserves it
- leave the default installed `gomobile-matsuri` and `gobind-matsuri` binaries unchanged
- leave all `android/fork/` source unchanged
- stop without continuing into Gradle or broader build continuation
- document the exact stopping point and evidence in `docs/android/ANDROID_LIBCORE_METADATA_BRIDGE_REPAIR.md`

## COMPLETION_TEST
CP-014 is complete only if:
- `docs/android/ANDROID_LIBCORE_METADATA_BRIDGE_REPAIR.md` exists
- the report documents the exact temporary tool-local metadata fallback change that was tested
- the report shows the generated fallback `go.mod` contents after the repair attempt
- the report explicitly confirms whether the generated fallback preserves:
  - `replace libcore`
  - local replacement mappings for `libneko` and `sing-box`
  - the original `golang.org/x/mobile` baseline
- the report explicitly states whether the exact unresolved bare `libcore` import was cleared in generated `src-android-*`
- if the import was cleared, the report identifies the first new blocker and stops there
- if the import was not cleared, the report identifies the exact remaining failure and why the metadata-only repair was insufficient
- the report documents exact commands and exact version evidence used
- the report explicitly records whether any toolchain or dependency drift still occurred
- the report documents rollback expectations and actual cleanup actions
- no upstream source files under `android/fork/` are modified
- no broad patches are applied
- no Gradle app build is attempted
- `PROJECT_STATE.md` is updated after execution

## RESULT
partial

## WHAT_CHANGED
- `checkpoints/CP-014.md` was created to define the smallest bounded repair checkpoint for the generated `go.mod` fallback metadata bridge in the libcore gomobile path.
- The checkpoint explicitly limits later execution to a tool-local metadata-fallback test for `libcore`, `libneko`, `sing-box`, and the original `golang.org/x/mobile` baseline.
- Execution was intentionally not performed in this session.

## REMAINING_GAPS
CP-014 has not been executed yet.
`docs/android/ANDROID_LIBCORE_METADATA_BRIDGE_REPAIR.md` does not exist yet because the repair attempt is deferred.
The generated metadata-bridge repair remains unvalidated until CP-014 is executed.

## NEXT_CHECKPOINT
Execute CP-014 to test the isolated generated `go.mod` metadata-bridge fallback and determine whether it clears the unresolved bare `libcore` import without continuing into broader Android build work.

## HANDOFF
Current checkpoint status: partial
Safe next action: execute CP-014 in an isolated gomobile workspace and stop as soon as the generated `libcore` import either clears or fails for a new exact reason
Artifacts created or updated:
- checkpoints/CP-014.md
Known risks:
- the next session may drift from metadata-only repair into broader source-copy or bind-shape changes unless CP-014 is followed literally
- fallback metadata that does not preserve the original `golang.org/x/mobile` baseline may reintroduce version drift and obscure the repair result
- additional native build blockers may appear immediately after the `libcore` import is cleared, so CP-014 must stop at the first new blocker
Resume from:
- docs/android/ANDROID_LIBCORE_LAYOUT_BLOCKER_DIAGNOSIS.md
- checkpoints/CP-014.md
- PROJECT_STATE.md
