# CP-002

## CHECKPOINT_ID
CP-002

## TITLE
Define repository execution surface and development environment bootstrap

## CURRENT_STATE
CP-001 is complete.
The repository is checkpoint-driven and has authoritative bootstrap guidance.
The top-level repository folders exist, but their execution boundaries are not yet documented.
There is no explicit development environment bootstrap document, root editor policy, ignore policy, or local setup helper script.
Server and Android implementation work has not started and must remain untouched in this checkpoint.

## TARGET_STATE
The repository execution surface is defined and the local development bootstrap is documented and scaffolded.
After this checkpoint is executed, a future session should be able to identify:
- which top-level directories are active execution surfaces
- which root-level files govern editing, ignores, and environment setup
- how to bootstrap the local development environment on the current Windows-first workstation
- what commands verify that the local environment is ready before server or Android work begins

## IN_SCOPE
- define the authoritative repository execution surface at the root level
- create repository-level developer bootstrap documentation
- create root-level environment and editor baseline files
- create a local bootstrap helper script for prerequisite checks and guidance
- document the exact verification flow for local environment readiness

## OUT_OF_SCOPE
- server implementation
- Android implementation
- iOS implementation
- CI/CD pipeline implementation
- Dockerized runtime services
- proxy or transport integration
- per-app routing implementation

## REQUIRED_ARTIFACTS
- `docs/repository/EXECUTION_SURFACE.md`
- `docs/development/ENVIRONMENT_BOOTSTRAP.md`
- `.editorconfig`
- `.gitignore`
- `.env.example`
- `scripts/bootstrap-dev.ps1`

## EXECUTION_NOTES
This checkpoint defines and bootstraps the repository surface only.
It should stay Windows-first because the current owner workstation is Windows-based.
The bootstrap script may validate prerequisites and print setup guidance, but it must not fetch infrastructure or start product services.
The environment bootstrap should name required tools conservatively and only for immediate repository work.
If a tool requirement is uncertain, document it as an assumption instead of silently enforcing it.

## COMPLETION_TEST
This checkpoint is complete only if:
- `docs/repository/EXECUTION_SURFACE.md` defines each top-level repository area and its intended use
- `docs/development/ENVIRONMENT_BOOTSTRAP.md` documents local setup prerequisites, environment variables, and verification commands
- `.editorconfig`, `.gitignore`, and `.env.example` exist and are aligned with the documented workflow
- `scripts/bootstrap-dev.ps1` exists and provides a safe local bootstrap or prerequisite-check path
- no server, Android, or transport implementation is introduced

## RESULT
complete

## WHAT_CHANGED
- `docs/repository/EXECUTION_SURFACE.md` was created to define active repository surfaces, reserved implementation areas, and root policy files.
- `docs/development/ENVIRONMENT_BOOTSTRAP.md` was created to document the Windows-first local bootstrap flow and verification commands.
- `.editorconfig`, `.gitignore`, and `.env.example` were created as root-level repository bootstrap baselines.
- `scripts/bootstrap-dev.ps1` was created as a safe prerequisite-check script that validates required commands and repository paths without installing or starting services.
- `PROJECT_STATE.md` was updated to reflect CP-002 completion and the new bootstrap baseline.
- `checkpoints/CP-002.md` was updated with execution results, remaining gaps, and handoff data.

## REMAINING_GAPS
No remaining gaps inside CP-002.

## NEXT_CHECKPOINT
CP-003 - Establish server-side skeleton and configuration baseline

## HANDOFF
Current checkpoint status: complete
Safe next action: author CP-003 as the next bounded checkpoint for server-side skeleton and configuration baseline
Artifacts created or updated:
- docs/repository/EXECUTION_SURFACE.md
- docs/development/ENVIRONMENT_BOOTSTRAP.md
- .editorconfig
- .gitignore
- .env.example
- scripts/bootstrap-dev.ps1
- PROJECT_STATE.md
- checkpoints/CP-002.md
Known risks:
- scope drift into server or Android implementation before CP-003 is defined
- optional Android tooling is still intentionally absent from the current bootstrap baseline
Resume from:
- PROJECT_STATE.md
- checkpoints/CP-002.md
