# CP-022

## CHECKPOINT_ID
CP-022

## TITLE
Define the smallest bounded post-runtime-jar continuation probe

## CURRENT_STATE
CP-021 is complete.
The generated `libcore` metadata bridge is already repaired and validated in `docs/android/ANDROID_LIBCORE_METADATA_BRIDGE_REPAIR.md`.
That earlier checkpoint proved:
- the generated fallback `go.mod` can preserve `replace libcore`
- the generated fallback can preserve the local replacement mappings for `github.com/matsuridayo/libneko` and `github.com/sagernet/sing-box`
- the generated fallback can preserve the original `golang.org/x/mobile` baseline (`v0.0.0-20231108233038-35478a0c49da`)
- the exact bare `libcore` import failure from CP-013 is cleared

CP-017 then intentionally persisted the validated `android/sing-box` alignment in `docs/android/ANDROID_SINGBOX_BASELINE_PERSIST.md`.
That checkpoint proved:
- the local `android/sing-box` baseline is intentionally tracked on branch `cp017-local-baseline`
- that persisted baseline points to commit `aed32ee3066cdbc7d471e3e0415c5134088962df`
- the same bounded `android/fork/libcore/build.sh` path still proceeds through transient `android/fork/app/libs/libcore.aar` production under that persisted baseline

CP-018 then executed the first exact downstream app consumer probe in `docs/android/ANDROID_POST_LIBCORE_CONTINUATION.md`.
That checkpoint proved:
- the exact post-libcore handoff artifact remains `android/fork/app/libs/libcore.aar`
- the exact first downstream app consumer command is `.\gradlew.bat :app:compileOssDebugKotlin --stacktrace`
- the bounded Kotlin compile consumer surface succeeds under the persisted `android/sing-box` baseline
- no blocker appears at that Kotlin compile surface

CP-019 then executed the next exact downstream app consumer probe in `docs/android/ANDROID_POST_KOTLIN_CONTINUATION.md`.
That checkpoint proved:
- the prerequisite artifact before the next downstream entry still remains `android/fork/app/libs/libcore.aar`
- the exact Java compile consumer command is `.\gradlew.bat :app:compileOssDebugJavaWithJavac --stacktrace`
- the bounded Java compile consumer surface succeeds under the persisted `android/sing-box` baseline
- javac outputs now exist under:
  - `android/fork/app/build/intermediates/javac/ossDebug/compileOssDebugJavaWithJavac/classes/`
  - `android/fork/app/build/tmp/compileOssDebugJavaWithJavac/`

CP-020 then executed the compile-jar continuation probe in `docs/android/ANDROID_POST_JAVAC_CONTINUATION.md`.
That checkpoint proved:
- the prerequisite artifact before compile-jar entry still remains `android/fork/app/libs/libcore.aar`
- the exact compile-jar consumer command is `.\gradlew.bat :app:bundleOssDebugClassesToCompileJar --stacktrace`
- the bounded compile-jar consumer surface succeeds under the persisted `android/sing-box` baseline
- compile-jar output now exists under:
  - `android/fork/app/build/intermediates/compile_app_classes_jar/ossDebug/bundleOssDebugClassesToCompileJar/classes.jar`

CP-021 then executed the runtime-jar continuation probe in `docs/android/ANDROID_POST_COMPILE_JAR_CONTINUATION.md`.
That checkpoint proved:
- the prerequisite artifact before runtime-jar entry still remains `android/fork/app/libs/libcore.aar`
- the exact runtime-jar consumer command is `.\gradlew.bat :app:bundleOssDebugClassesToRuntimeJar --stacktrace`
- the bounded runtime-jar consumer surface succeeds under the persisted `android/sing-box` baseline
- runtime-jar output now exists under:
  - `android/fork/app/build/intermediates/runtime_app_classes_jar/ossDebug/bundleOssDebugClassesToRuntimeJar/classes.jar`
- broader downstream outputs still did not exist after CP-021:
  - `android/fork/app/build/intermediates/project_dex_archive/ossDebug`
  - `android/fork/app/build/outputs/apk/`

The next downstream surface is now identifiable from local task metadata and CP-021 evidence:
- a local Gradle task listing from `android/fork/` shows the exact first dex-side task names for this variant:
  - `dexBuilderOssDebug`
  - `mergeExtDexOssDebug`
  - `mergeLibDexOssDebug`
  - `mergeProjectDexOssDebug`
- `bundleOssDebugClassesToRuntimeJar` is already execution-validated by CP-021
- `android/fork/app/build/intermediates/project_dex_archive/ossDebug` is still absent after CP-021
- `dexBuilderOssDebug` is the narrowest first dex-side surface aligned with that still-absent `project_dex_archive` output family, while the merge-dex tasks are downstream and broader

No upstream source under `android/fork/` has been modified.
The default installed `gomobile-matsuri` and `gobind-matsuri` binaries remain unchanged.

## TARGET_STATE
The repository has a bounded checkpoint definition for probing only the first exact downstream build surface after successful `.\gradlew.bat :app:bundleOssDebugClassesToRuntimeJar --stacktrace`.

After CP-022 execution, a future session must be able to answer:
- whether `.\gradlew.bat :app:dexBuilderOssDebug --stacktrace` is the correct next exact downstream consumer surface after successful runtime-jar merge
- what exact prerequisite artifact and prerequisite baseline must still be true before entering that dex-builder surface
- what exact first meaningful outcome appears when that single dex-builder probe is entered
- whether the first new outcome is:
  - a task-graph failure before `:app:dexBuilderOssDebug` starts
  - a failure inside `:app:dexBuilderOssDebug`
  - or a successful dex-builder continuation signal
- how that probe is kept separate from merge-dex, packaging, install, assemble, or repair work

CP-022 does not need to fix dex-builder failures.
It must only define the smallest bounded continuation probe after successful runtime-jar merge and stop at the first exact downstream dex-side result.

## IN_SCOPE
- select the exact downstream build surface after successful `:app:bundleOssDebugClassesToRuntimeJar`
- document why `.\gradlew.bat :app:dexBuilderOssDebug --stacktrace` is the correct next bounded surface:
  - CP-021 already execution-validated runtime-jar success
  - the next locally listed dex-side task aligned with the still-absent `project_dex_archive` output family is `dexBuilderOssDebug`
  - `android/fork/app/build/intermediates/project_dex_archive/ossDebug` is still absent
  - `mergeExtDexOssDebug`, `mergeLibDexOssDebug`, and `mergeProjectDexOssDebug` are downstream and broader than the initial dex-builder surface
- define the exact entry command for the probe from `android/fork/`:
  - `.\gradlew.bat :app:dexBuilderOssDebug --stacktrace`
- define the exact prerequisite checks that must remain unchanged before the probe:
  - `android/sing-box` stays on branch `cp017-local-baseline`
  - `android/sing-box` stays at `aed32ee3066cdbc7d471e3e0415c5134088962df`
  - the cleared metadata bridge remains only an existing prerequisite reused through the bounded `libcore` path
  - transient `android/fork/app/libs/libcore.aar` is reproduced again before Gradle entry because CP-021 cleanup removed it
  - existing compile-jar output remains present before dex-builder entry at:
    - `android/fork/app/build/intermediates/compile_app_classes_jar/ossDebug/bundleOssDebugClassesToCompileJar/classes.jar`
  - existing runtime-jar output remains present before dex-builder entry at:
    - `android/fork/app/build/intermediates/runtime_app_classes_jar/ossDebug/bundleOssDebugClassesToRuntimeJar/classes.jar`
  - dex output remains absent before dex-builder entry at:
    - `android/fork/app/build/intermediates/project_dex_archive/ossDebug`
  - APK output remains absent before dex-builder entry at:
    - `android/fork/app/build/outputs/apk/`
- define the exact expected success artifact path for the bounded probe:
  - `android/fork/app/build/intermediates/project_dex_archive/ossDebug`
- define the exact evidence to capture during execution:
  - confirmation that the persisted `android/sing-box` prerequisite state is unchanged
  - confirmation that compile-jar and runtime-jar success artifacts still exist before dex-builder entry
  - confirmation that dex and APK outputs are absent before dex-builder entry
  - the command and output that re-establish transient `android/fork/app/libs/libcore.aar`
  - the exact `.\gradlew.bat :app:dexBuilderOssDebug --stacktrace` invocation
  - the first task-graph failure before dex-builder starts, if one appears
  - the first exact failure line inside `:app:dexBuilderOssDebug`, if the task starts and fails
  - or the exact success signal for `:app:dexBuilderOssDebug` plus evidence that `android/fork/app/build/intermediates/project_dex_archive/ossDebug` now exists
- define what counts as the first meaningful outcome:
  - first failing task before dex-builder starts
  - first exact failure inside `:app:dexBuilderOssDebug`
  - or exact success of `:app:dexBuilderOssDebug`
- define toolchain and dependency drift bounds for that probe
- define rollback and cleanup expectations for transient `libcore.aar`, `android/fork/libcore/.build/`, and any disposable gomobile workspace recreated only to regenerate the transient AAR
- create `docs/android/ANDROID_POST_RUNTIME_JAR_CONTINUATION.md`
- update `PROJECT_STATE.md` and `checkpoints/CP-022.md` with execution results

## OUT_OF_SCOPE
- revisiting or re-solving the cleared `libcore` metadata bridge
- revisiting or re-testing the already-validated `android/sing-box` baseline except as a stated prerequisite
- modifying upstream source under `android/fork/`, including Gradle files, app code, Java sources, Kotlin sources, or `libcore/`
- broad patches to `gomobile-matsuri`, `gobind-matsuri`, Gradle wiring, generated workspace layout, or dependency trees
- broad Gradle or app build work beyond the single dex-builder consumer surface
- `bundleOssDebugClassesToCompileJar` or `bundleOssDebugClassesToRuntimeJar` as new probe surfaces, except if Gradle reruns them automatically as prerequisites while entering `dexBuilderOssDebug`
- `mergeExtDex*`, `mergeLibDex*`, `mergeProjectDex*`, packaging, install, signing, or any `assemble*` task
- Android product features
- per-app routing implementation
- transport or proxy logic
- dependency upgrades or repairs outside the first observed dex-builder continuation blocker
- changes to `android/libneko/` or `android/sing-box/` baselines
- server artifacts or server implementation, except `PROJECT_STATE.md` if needed for continuity
- CI/CD changes

## REQUIRED_ARTIFACTS
- `docs/android/ANDROID_POST_RUNTIME_JAR_CONTINUATION.md`
- `PROJECT_STATE.md` (updated after CP-022 execution)
- `checkpoints/CP-022.md` (updated after CP-022 execution)

## EXECUTION_NOTES
CP-022 is a continuation-definition checkpoint.
It must not turn into merge-dex, packaging, install, assemble, or repair work.

Why the next surface is `:app:dexBuilderOssDebug`:
- CP-021 proved that runtime-jar merge is no longer the active downstream boundary
- the same local Gradle task listing that exposed the runtime-jar task also exposes `dexBuilderOssDebug`, `mergeExtDexOssDebug`, `mergeLibDexOssDebug`, and `mergeProjectDexOssDebug`
- runtime-jar output already exists at `android/fork/app/build/intermediates/runtime_app_classes_jar/ossDebug/bundleOssDebugClassesToRuntimeJar/classes.jar`
- dex output is still absent at `android/fork/app/build/intermediates/project_dex_archive/ossDebug`
- `dexBuilderOssDebug` is narrower than the merge-dex tasks and is the first exact downstream surface aligned with the absent dex output family

Execution priority for the later CP-022 run:
1. confirm the persisted `android/sing-box` baseline still matches branch `cp017-local-baseline` at `aed32ee3066cdbc7d471e3e0415c5134088962df`
2. confirm the prior compile-jar success artifact still exists at `android/fork/app/build/intermediates/compile_app_classes_jar/ossDebug/bundleOssDebugClassesToCompileJar/classes.jar`
3. confirm the prior runtime-jar success artifact still exists at `android/fork/app/build/intermediates/runtime_app_classes_jar/ossDebug/bundleOssDebugClassesToRuntimeJar/classes.jar`
4. confirm dex and APK outputs are absent before the probe:
   - `android/fork/app/build/intermediates/project_dex_archive/ossDebug`
   - `android/fork/app/build/outputs/apk/`
5. rerun only the already-bounded `android/fork/libcore/build.sh` path until it installs transient `android/fork/app/libs/libcore.aar`
6. record that transient `android/fork/app/libs/libcore.aar` is again present before Gradle entry
7. enter Gradle exactly once with `.\gradlew.bat :app:dexBuilderOssDebug --stacktrace`
8. capture the first exact downstream outcome and stop
9. do not repair the observed blocker inside CP-022

Exact entry markers CP-022 must record during execution:
- artifact marker: `android/fork/app/libs/libcore.aar`
- command marker: `.\gradlew.bat :app:dexBuilderOssDebug --stacktrace`

First meaningful outcome rules for CP-022:
- if the Gradle task graph fails before `:app:dexBuilderOssDebug` starts, the first failing task is the checkpoint outcome
- if `:app:dexBuilderOssDebug` starts and emits a failure, the first exact failure line is the checkpoint outcome
- if `:app:dexBuilderOssDebug` succeeds, that success plus creation of `android/fork/app/build/intermediates/project_dex_archive/ossDebug` is the checkpoint outcome
- once one of those outcomes is captured conclusively, stop without continuing into merge-dex, packaging, install, or assemble work

Evidence sources if shell output is incomplete during CP-022 execution:
- the direct shell transcript from the `.\gradlew.bat :app:dexBuilderOssDebug --stacktrace` invocation
- the active Gradle daemon log under `android/fork/.gradle/daemon/8.10.2/` or the current user Gradle daemon directory for wrapper version `8.10.2`
- direct filesystem evidence from:
  - `android/fork/app/build/intermediates/project_dex_archive/ossDebug`
  - `android/fork/app/build/outputs/apk/`

Toolchain and dependency drift bounds for CP-022 execution:
- preserve the persisted `android/sing-box` baseline on `cp017-local-baseline` at `aed32ee3066cdbc7d471e3e0415c5134088962df`
- preserve the exact disposable `gomobile-matsuri` source revision used in CP-014 through CP-021 (`17d6af34f6bd6d7e1e428e0c652c8b54a46bda4f`) if a disposable rebuild is still required to regenerate transient `libcore.aar`
- preserve the generated workspace `golang.org/x/mobile v0.0.0-20231108233038-35478a0c49da` baseline already validated in CP-014 through CP-021
- preserve the host generated-workspace Go baseline (`go1.23.6`) unless mere observation proves auto-selection cannot be avoided
- preserve the existing Gradle wrapper baseline (`8.10.2`)
- preserve the existing Android Gradle Plugin baseline (`8.8.1`)
- preserve the existing Kotlin and KSP baselines (`2.0.21` and `2.0.21-1.0.27`)
- preserve the existing JDK 17 and local SDK/NDK baseline already documented in project state
- do not intentionally upgrade Gradle plugins, Kotlin plugins, Java toolchains, Go dependencies, or Android dependencies during the probe
- if Gradle, Kotlin, Java, KSP, or Go auto-selects a different version, capture the exact version and trigger as evidence and do not treat that drift as the repair

Cleanup expectations after evidence capture for the later CP-022 run:
- remove any disposable temporary gomobile source workspace created for CP-022
- remove any disposable temporary GOPATH or patched binaries created for CP-022
- remove `android/fork/libcore/.build/` after evidence capture unless the report explicitly preserves it for the first downstream failure evidence
- remove transient copied `android/fork/app/libs/libcore.aar` after evidence capture unless the report explicitly preserves it for the first downstream failure evidence
- leave the default installed `gomobile-matsuri` and `gobind-matsuri` binaries unchanged
- leave the compile-jar, runtime-jar, and dex outputs in `android/fork/app/build/intermediates/` as normal Gradle artifacts if Gradle creates them during the bounded probe
- leave all upstream source trees unchanged
- stop without continuing into merge-dex, packaging, install, or broader build continuation
- document the exact stopping point and cleanup result in `docs/android/ANDROID_POST_RUNTIME_JAR_CONTINUATION.md`

## COMPLETION_TEST
CP-022 is complete only if:
- `docs/android/ANDROID_POST_RUNTIME_JAR_CONTINUATION.md` exists
- the report documents why `.\gradlew.bat :app:dexBuilderOssDebug --stacktrace` is the correct next exact downstream surface
- the report confirms the exact prerequisite state before execution:
  - `android/sing-box` is on branch `cp017-local-baseline`
  - `android/sing-box` is at `aed32ee3066cdbc7d471e3e0415c5134088962df`
  - `android/fork/app/build/intermediates/compile_app_classes_jar/ossDebug/bundleOssDebugClassesToCompileJar/classes.jar` exists before dex-builder entry
  - `android/fork/app/build/intermediates/runtime_app_classes_jar/ossDebug/bundleOssDebugClassesToRuntimeJar/classes.jar` exists before dex-builder entry
  - `android/fork/app/build/intermediates/project_dex_archive/ossDebug` is absent before dex-builder entry
  - `android/fork/app/build/outputs/apk/` is absent before dex-builder entry
  - transient `android/fork/app/libs/libcore.aar` is reproduced before Gradle entry
- the report documents the exact dex-builder entry command `.\gradlew.bat :app:dexBuilderOssDebug --stacktrace`
- the report captures the first exact task-graph failure, first exact dex-builder failure, or the exact success of `:app:dexBuilderOssDebug`
- if the task succeeds, the report confirms the exact success artifact path:
  - `android/fork/app/build/intermediates/project_dex_archive/ossDebug`
- the report confirms the following paths remain absent because they are out of scope:
  - any APK output under `android/fork/app/build/outputs/apk/`
- the report identifies the exact evidence source used if direct shell output is incomplete:
  - direct shell transcript
  - Gradle daemon log
  - filesystem artifact checks
- the report explicitly states that the cleared metadata bridge was not revisited as a new repair surface
- the report explicitly separates dex-builder probing from merge-dex, packaging, install, or assemble work
- the report documents the exact cleanup expectations and actual cleanup result after evidence capture
- no upstream source files under `android/fork/` are modified
- no broad patches are applied
- no merge-dex, packaging, install, or `assemble*` work is attempted
- `PROJECT_STATE.md` is updated after execution

## RESULT
complete

## WHAT_CHANGED
- `docs/android/ANDROID_POST_RUNTIME_JAR_CONTINUATION.md` was created with the exact pre-probe Java print, prerequisite `android/sing-box` confirmation, exact prerequisite state checks, disposable tool rebuild commands, corrected transient `libcore.aar` reproduction command, exact dex-builder entry command, direct shell transcript, dex archive evidence, out-of-scope APK absence check, and cleanup record.
- CP-022 was executed using the same disposable gomobile prerequisite path already validated by CP-014 through CP-021, without reopening metadata-bridge repair as a new work surface.
- The first exact downstream dex-builder consumer surface was execution-validated:
  - prerequisite artifact: `android/fork/app/libs/libcore.aar`
  - entry command: `.\gradlew.bat :app:dexBuilderOssDebug --stacktrace`
  - first meaningful downstream outcome: success, not a blocker

## REMAINING_GAPS
- No gaps remain inside CP-022.
- The next downstream surface after successful dex-builder generation remains intentionally unexecuted.

## NEXT_CHECKPOINT
Create CP-023 only to define the smallest bounded continuation checkpoint after successful `:app:dexBuilderOssDebug`, most likely targeting `.\gradlew.bat :app:mergeProjectDexOssDebug --stacktrace` without broadening into other merge-dex, packaging, install, or assemble work.

## HANDOFF
Current checkpoint status: complete
Safe next action: create CP-023 only, scoped to defining the first exact downstream surface after successful `:app:dexBuilderOssDebug`
Artifacts created or updated:
- checkpoints/CP-022.md
- docs/android/ANDROID_POST_RUNTIME_JAR_CONTINUATION.md
- PROJECT_STATE.md
Known risks:
- the next session may incorrectly jump from successful dex-builder generation into broader merge-dex, packaging, or `assemble*` work instead of defining the next bounded surface
- the persisted `android/sing-box` baseline on `cp017-local-baseline` remains a hard prerequisite for continuity
- future Gradle probes may stop producing complete direct shell transcripts, in which case daemon-log evidence should be preserved again
Resume from:
- docs/android/ANDROID_POST_RUNTIME_JAR_CONTINUATION.md
- checkpoints/CP-022.md
- PROJECT_STATE.md
