# CP-032

## CHECKPOINT_ID
CP-032

## TITLE
Define the smallest bounded post-validate-signing continuation probe

## CURRENT_STATE
CP-031 is complete.
The cleared `libcore` metadata bridge remains only an already-validated prerequisite from CP-014.
The persisted `android/sing-box` baseline remains the CP-017 prerequisite on branch `cp017-local-baseline` at `aed32ee3066cdbc7d471e3e0415c5134088962df`.
CP-018 through CP-031 execution-validated these exact downstream surfaces:
- `.\gradlew.bat :app:compileOssDebugKotlin --stacktrace`
- `.\gradlew.bat :app:compileOssDebugJavaWithJavac --stacktrace`
- `.\gradlew.bat :app:bundleOssDebugClassesToCompileJar --stacktrace`
- `.\gradlew.bat :app:bundleOssDebugClassesToRuntimeJar --stacktrace`
- `.\gradlew.bat :app:dexBuilderOssDebug --stacktrace`
- `.\gradlew.bat :app:mergeProjectDexOssDebug --stacktrace`
- `.\gradlew.bat :app:mergeExtDexOssDebug --stacktrace`
- `.\gradlew.bat :app:mergeLibDexOssDebug --stacktrace`
- `.\gradlew.bat :app:processOssDebugJavaRes --stacktrace`
- `.\gradlew.bat :app:mergeOssDebugJavaResource --stacktrace`
- `.\gradlew.bat :app:mergeOssDebugJniLibFolders --stacktrace`
- `.\gradlew.bat :app:mergeOssDebugNativeLibs --stacktrace`
- `.\gradlew.bat :app:stripOssDebugDebugSymbols --stacktrace`
- `.\gradlew.bat :app:validateSigningOssDebug --stacktrace`

Those checkpoints proved these success artifact families exist:
- `android/fork/app/build/intermediates/compile_app_classes_jar/ossDebug/bundleOssDebugClassesToCompileJar/classes.jar`
- `android/fork/app/build/intermediates/runtime_app_classes_jar/ossDebug/bundleOssDebugClassesToRuntimeJar/classes.jar`
- `android/fork/app/build/intermediates/project_dex_archive/ossDebug`
- `android/fork/app/build/intermediates/dex/ossDebug/mergeProjectDexOssDebug`
- `android/fork/app/build/intermediates/dex/ossDebug/mergeExtDexOssDebug`
- `android/fork/app/build/intermediates/dex/ossDebug/mergeLibDexOssDebug`
- `android/fork/app/build/intermediates/java_res/ossDebug/processOssDebugJavaRes/out`
- `android/fork/app/build/intermediates/merged_java_res/ossDebug/mergeOssDebugJavaResource`
- `android/fork/app/build/intermediates/merged_jni_libs/ossDebug/mergeOssDebugJniLibFolders`
- `android/fork/app/build/intermediates/merged_native_libs/ossDebug/mergeOssDebugNativeLibs/out`
- `android/fork/app/build/intermediates/stripped_native_libs/ossDebug/stripOssDebugDebugSymbols/out`

Actual downstream task and artifact flow now shows:
- CP-031 proved `:app:validateSigningOssDebug` succeeds with the expected validation signal
- the first exact downstream continuation surface after successful validate-signing is the packaging-stage gate `:app:packageOssDebug`
- the first still-absent downstream output family after successful `validateSigningOssDebug` is:
  - `android/fork/app/build/intermediates/apk/oss/debug`
- broader downstream families still remain absent:
  - `android/fork/app/build/outputs/apk/oss/debug`

`packageOssDebug` is therefore the smallest next still-unexecuted surface supported by the current downstream ordering and still-absent output flow.

## TARGET_STATE
The repository has a bounded checkpoint definition for probing only the first exact downstream build surface after the observed successful `.\gradlew.bat :app:validateSigningOssDebug --stacktrace` result.

After CP-032 execution, a future session must be able to answer:
- whether `.\gradlew.bat :app:packageOssDebug --stacktrace` is the correct next exact downstream surface
- what exact prerequisite artifact and baseline must still be true before entry
- what exact first meaningful outcome appears when that single probe is entered
- whether the first new outcome is a task-graph failure before `:app:packageOssDebug`, a failure inside that task, or a successful continuation signal
- how that probe is kept separate from install/assemble continuation

CP-032 does not fix `packageOssDebug` failures.
It only defines the smallest bounded continuation probe after successful `validateSigningOssDebug`.

## IN_SCOPE
- select the exact downstream build surface after successful `:app:validateSigningOssDebug`
- document why `.\gradlew.bat :app:packageOssDebug --stacktrace` is the correct next bounded surface
- define the exact entry command from `android/fork/`:
  - `.\gradlew.bat :app:packageOssDebug --stacktrace`
- define the exact prerequisite checks before entry:
  - `android/sing-box` stays on `cp017-local-baseline` at `aed32ee3066cdbc7d471e3e0415c5134088962df`
  - the cleared metadata bridge remains only an existing prerequisite reused through the bounded `libcore` path
  - transient `android/fork/app/libs/libcore.aar` is reproduced before Gradle entry
  - these upstream success artifacts remain present:
    - `android/fork/app/build/intermediates/compile_app_classes_jar/ossDebug/bundleOssDebugClassesToCompileJar/classes.jar`
    - `android/fork/app/build/intermediates/runtime_app_classes_jar/ossDebug/bundleOssDebugClassesToRuntimeJar/classes.jar`
    - `android/fork/app/build/intermediates/project_dex_archive/ossDebug`
    - `android/fork/app/build/intermediates/project_dex_archive/ossDebug/dexBuilderOssDebug/out`
    - `android/fork/app/build/intermediates/external_libs_dex_archive/ossDebug/dexBuilderOssDebug/out`
    - `android/fork/app/build/intermediates/sub_project_dex_archive/ossDebug/dexBuilderOssDebug/out`
    - `android/fork/app/build/intermediates/dex/ossDebug/mergeProjectDexOssDebug`
    - `android/fork/app/build/intermediates/dex/ossDebug/mergeExtDexOssDebug`
    - `android/fork/app/build/intermediates/dex/ossDebug/mergeLibDexOssDebug`
    - `android/fork/app/build/intermediates/java_res/ossDebug/processOssDebugJavaRes/out`
    - `android/fork/app/build/intermediates/merged_java_res/ossDebug/mergeOssDebugJavaResource`
    - `android/fork/app/build/intermediates/merged_jni_libs/ossDebug/mergeOssDebugJniLibFolders`
    - `android/fork/app/build/intermediates/merged_native_libs/ossDebug/mergeOssDebugNativeLibs/out`
    - `android/fork/app/build/intermediates/stripped_native_libs/ossDebug/stripOssDebugDebugSymbols/out`
  - these downstream outputs remain absent before entry:
    - `android/fork/app/build/intermediates/apk/oss/debug`
    - `android/fork/app/build/outputs/apk/oss/debug`
- define the exact expected success artifact path and success signal for this packaging-stage probe:
  - expected success artifact path: `android/fork/app/build/intermediates/apk/oss/debug`
  - expected success signal lines:
    - `> Task :app:packageOssDebug`
    - `BUILD SUCCESSFUL`
    - `EXIT_CODE: 0`
- define the exact out-of-scope paths that must still remain absent after the bounded probe:
  - `android/fork/app/build/outputs/apk/oss/debug`
- define the exact fallback evidence sources if shell output is incomplete:
  - direct shell transcript
  - Gradle daemon log for wrapper `8.10.2`
  - filesystem checks for the success path and the still-absent out-of-scope path
- define the exact cleanup expectations:
  - remove any disposable gomobile source workspace used to regenerate transient `libcore.aar`
  - remove any disposable GOPATH or patched binaries used for that regeneration
  - remove `android/fork/libcore/.build/` after evidence capture unless explicitly preserved for first-failure evidence
  - remove transient copied `android/fork/app/libs/libcore.aar` after evidence capture unless explicitly preserved for first-failure evidence
  - leave upstream source trees unchanged
  - leave existing Gradle-produced intermediate outputs in place
- create `docs/android/ANDROID_POST_VALIDATE_SIGNING_CONTINUATION.md`
- update `PROJECT_STATE.md` and `checkpoints/CP-032.md` with execution results

## OUT_OF_SCOPE
- revisiting or re-solving the cleared `libcore` metadata bridge
- revisiting or re-testing the already-validated `android/sing-box` baseline except as a stated prerequisite
- modifying upstream source under `android/fork/`
- broad patches to gomobile, Gradle wiring, generated workspace layout, or dependency trees
- broad Gradle or app build work beyond the single `packageOssDebug` surface
- entering install, signing distribution, or any `assemble*` task as new probe surfaces
- Android product features
- per-app routing implementation
- transport or proxy logic
- dependency upgrades or repairs outside the first observed `packageOssDebug` blocker
- changes to `android/libneko/` or `android/sing-box/` baselines
- server artifacts or server implementation, except `PROJECT_STATE.md` if needed for continuity
- CI/CD changes

## REQUIRED_ARTIFACTS
- `docs/android/ANDROID_POST_VALIDATE_SIGNING_CONTINUATION.md`
- `PROJECT_STATE.md` (updated after CP-032 execution)
- `checkpoints/CP-032.md` (updated after CP-032 execution)

## EXECUTION_NOTES
CP-032 is a continuation-definition checkpoint.
It must not turn into install/assemble or repair work.

Why the next surface is `:app:packageOssDebug`:
- CP-031 proved that `validateSigningOssDebug` succeeds
- `android/fork/app/build/intermediates/apk/oss/debug` remains absent and is the first packaging-stage output family to probe
- `android/fork/app/build/outputs/apk/oss/debug` remains absent, so install/assemble continuation has not started
- `packageOssDebug` is narrower than install/assemble continuation

Execution priority for the later CP-032 run:
1. confirm the persisted `android/sing-box` baseline still matches `cp017-local-baseline` at `aed32ee3066cdbc7d471e3e0415c5134088962df`
2. confirm all prior success artifacts listed in `IN_SCOPE` still exist
3. confirm `android/fork/app/build/intermediates/apk/oss/debug` and `android/fork/app/build/outputs/apk/oss/debug` are absent before entry
4. rerun only the already-bounded `android/fork/libcore/build.sh` path until it installs transient `android/fork/app/libs/libcore.aar`
5. enter Gradle exactly once with `.\gradlew.bat :app:packageOssDebug --stacktrace`
6. capture the first exact downstream outcome and stop
7. do not repair the observed blocker inside CP-032

Exact entry markers CP-032 must record during execution:
- artifact marker: `android/fork/app/libs/libcore.aar`
- command marker: `.\gradlew.bat :app:packageOssDebug --stacktrace`

First meaningful outcome rules for CP-032:
- if the Gradle task graph fails before `:app:packageOssDebug` starts, the first failing task is the checkpoint outcome
- if `:app:packageOssDebug` starts and emits a failure, the first exact failure line is the checkpoint outcome
- if `:app:packageOssDebug` succeeds, that success plus creation of `android/fork/app/build/intermediates/apk/oss/debug` is the checkpoint outcome
- once one of those outcomes is captured conclusively, stop without continuing into install/assemble work

Evidence sources if shell output is incomplete during CP-032 execution:
- the direct shell transcript from the `.\gradlew.bat :app:packageOssDebug --stacktrace` invocation
- the active Gradle daemon log under `android/fork/.gradle/daemon/8.10.2/` or the current user Gradle daemon directory for wrapper version `8.10.2`
- direct filesystem evidence from:
  - `android/fork/app/build/intermediates/apk/oss/debug`
  - `android/fork/app/build/outputs/apk/oss/debug`

Toolchain and dependency drift bounds for CP-032 execution:
- preserve the persisted `android/sing-box` baseline on `cp017-local-baseline` at `aed32ee3066cdbc7d471e3e0415c5134088962df`
- preserve the exact disposable `gomobile-matsuri` source revision used in CP-014 through CP-031 (`17d6af34f6bd6d7e1e428e0c652c8b54a46bda4f`) if a disposable rebuild is still required to regenerate transient `libcore.aar`
- preserve the generated workspace `golang.org/x/mobile v0.0.0-20231108233038-35478a0c49da` baseline already validated in CP-014 through CP-031
- preserve the host generated-workspace Go baseline (`go1.23.6`) unless mere observation proves auto-selection cannot be avoided
- preserve the existing Gradle wrapper baseline (`8.10.2`)
- preserve the existing Android Gradle Plugin baseline (`8.8.1`)
- preserve the existing Kotlin and KSP baselines (`2.0.21` and `2.0.21-1.0.27`)
- preserve the existing JDK 17 and local SDK/NDK baseline already documented in project state
- do not intentionally upgrade Gradle plugins, Kotlin plugins, Java toolchains, Go dependencies, or Android dependencies during the probe
- if Gradle, Kotlin, Java, KSP, or Go auto-selects a different version, capture the exact version and trigger as evidence and do not treat that drift as the repair

Cleanup expectations after evidence capture for the later CP-032 run:
- remove any disposable temporary gomobile source workspace created for CP-032
- remove any disposable temporary GOPATH or patched binaries created for CP-032
- remove `android/fork/libcore/.build/` after evidence capture unless the report explicitly preserves it for first-downstream-failure evidence
- remove transient copied `android/fork/app/libs/libcore.aar` after evidence capture unless the report explicitly preserves it for first-downstream-failure evidence
- leave the default installed `gomobile-matsuri` and `gobind-matsuri` binaries unchanged
- leave the existing Gradle-produced intermediate outputs in place, including `stripOssDebugDebugSymbols`, `validateSigningOssDebug`, and `packageOssDebug` outputs if Gradle creates them during the bounded probe
- leave all upstream source trees unchanged
- stop without continuing into install/assemble or broader build continuation
- document the exact stopping point and cleanup result in `docs/android/ANDROID_POST_VALIDATE_SIGNING_CONTINUATION.md`

## COMPLETION_TEST
CP-032 is complete only if:
- `docs/android/ANDROID_POST_VALIDATE_SIGNING_CONTINUATION.md` exists
- the report documents why `.\gradlew.bat :app:packageOssDebug --stacktrace` is the correct next exact downstream surface
- the report confirms the exact prerequisite state before execution:
  - `android/sing-box` is on `cp017-local-baseline`
  - `android/sing-box` is at `aed32ee3066cdbc7d471e3e0415c5134088962df`
  - all prior success artifacts listed in `IN_SCOPE` exist before entry
  - `android/fork/app/build/intermediates/apk/oss/debug` is absent before entry
  - `android/fork/app/build/outputs/apk/oss/debug` is absent before entry
  - transient `android/fork/app/libs/libcore.aar` is reproduced before Gradle entry
- the report documents the exact entry command `.\gradlew.bat :app:packageOssDebug --stacktrace`
- the report captures the first exact task-graph failure, first exact `packageOssDebug` failure, or the exact success signal of `:app:packageOssDebug`
- if the task succeeds, the report confirms:
  - `android/fork/app/build/intermediates/apk/oss/debug` exists
  - success signal lines include `> Task :app:packageOssDebug`, `BUILD SUCCESSFUL`, and `EXIT_CODE: 0`
- the report confirms all listed out-of-scope paths still remain absent
- the report identifies the exact evidence source used if direct shell output is incomplete
- the report explicitly states that the cleared metadata bridge was not revisited as a new repair surface
- the report explicitly separates `packageOssDebug` probing from install/assemble work
- the report documents the exact cleanup expectations and actual cleanup result after evidence capture
- no upstream source files under `android/fork/` are modified
- no broad patches are applied
- no install/`assemble*` work is attempted beyond the single bounded probe
- `PROJECT_STATE.md` is updated after execution

## RESULT
pending

## WHAT_CHANGED
- `checkpoints/CP-032.md` defines only the first exact downstream continuation surface after the observed successful `:app:validateSigningOssDebug` result.
- The bounded probe is narrowed to:
  - prerequisite artifact: `android/fork/app/libs/libcore.aar`
  - existing upstream success artifacts through `android/fork/app/build/intermediates/stripped_native_libs/ossDebug/stripOssDebugDebugSymbols/out`
  - entry command: `.\gradlew.bat :app:packageOssDebug --stacktrace`
- Exact prerequisite checks, expected packaging-stage success artifact/signal expectations, out-of-scope absence checks, fallback evidence sources, cleanup expectations, and completion assertions were defined without executing the probe.

## REMAINING_GAPS
- CP-032 has not been executed yet.
- `docs/android/ANDROID_POST_VALIDATE_SIGNING_CONTINUATION.md` does not exist yet.
- The first exact `packageOssDebug` outcome after the observed successful `:app:validateSigningOssDebug` result remains unobserved.

## NEXT_CHECKPOINT
Execute CP-032 only to probe `.\gradlew.bat :app:packageOssDebug --stacktrace` and stop at the first exact `packageOssDebug` outcome without continuing into install/assemble work.

## HANDOFF
Current checkpoint status: pending
Safe next action: execute CP-032 exactly as written, scoped only to `.\gradlew.bat :app:packageOssDebug --stacktrace`
Artifacts created or updated:
- checkpoints/CP-032.md
- PROJECT_STATE.md
Known risks:
- the next session may incorrectly skip the bounded `packageOssDebug` surface and jump directly into install/`assemble*` work
- future Gradle probes may again return incomplete direct shell transcripts, in which case daemon-log plus filesystem evidence should be preserved
- the persisted `android/sing-box` baseline on `cp017-local-baseline` remains a hard prerequisite for continuity
Resume from:
- checkpoints/CP-032.md
- PROJECT_STATE.md
