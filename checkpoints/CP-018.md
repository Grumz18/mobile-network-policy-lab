# CP-018

## CHECKPOINT_ID
CP-018

## TITLE
Define the smallest bounded post-`libcore.aar` continuation probe

## CURRENT_STATE
CP-017 is complete.
The generated `libcore` metadata bridge is already repaired and validated in `docs/android/ANDROID_LIBCORE_METADATA_BRIDGE_REPAIR.md`.
That earlier checkpoint proved:
- the generated fallback `go.mod` can preserve `replace libcore`
- the generated fallback can preserve the local replacement mappings for `github.com/matsuridayo/libneko` and `github.com/sagernet/sing-box`
- the generated fallback can preserve the original `golang.org/x/mobile` baseline (`v0.0.0-20231108233038-35478a0c49da`)
- the exact bare `libcore` import failure from CP-013 is cleared

CP-017 then intentionally persisted the validated `android/sing-box` alignment in `docs/android/ANDROID_SINGBOX_BASELINE_PERSIST.md`.
That checkpoint proved:
- the local `android/sing-box` baseline is now intentionally tracked on branch `cp017-local-baseline`
- the persisted baseline points to commit `aed32ee3066cdbc7d471e3e0415c5134088962df`
- the same bounded `android/fork/libcore/build.sh` path still proceeds through transient `android/fork/app/libs/libcore.aar` production under that persisted baseline
- the previously diagnosed missing-package and `github.com/sagernet/cloudflare-tls` failures do not recur inside that bounded `libcore` path

The first downstream consumer surface after transient `libcore.aar` production is now identifiable from the fork itself:
- `android/fork/libcore/build.sh` copies `libcore.aar` into `android/fork/app/libs/`
- `android/fork/app/build.gradle.kts` declares `implementation(fileTree("libs"))`
- app sources import `libcore.*` and `go.Seq`, including:
  - `android/fork/app/src/main/java/moe/matsuri/nb4a/NativeInterface.kt`
  - `android/fork/app/src/main/java/io/nekohasekai/sagernet/SagerNet.kt`
  - `android/fork/app/src/main/java/io/nekohasekai/sagernet/bg/proto/BoxInstance.kt`

No Gradle task has yet been executed after transient `libcore.aar` production.
No upstream source under `android/fork/` has been modified.
The default installed `gomobile-matsuri` and `gobind-matsuri` binaries remain unchanged.

## TARGET_STATE
The repository has a bounded checkpoint definition for probing only the first exact consumer surface after transient `libcore.aar` production under the persisted `android/sing-box` baseline.

After CP-018 execution, a future session must be able to answer:
- what exact artifact marks the handoff from the bounded `libcore` path into the downstream Android app surface
- what exact single Gradle command enters that first downstream surface
- whether that first downstream surface accepts the generated `libcore.aar` on the app compile classpath or stops at a new blocker
- what exact first failing Gradle task or first successful compile outcome appears
- how that probe is kept separate from broader Gradle repair, app assembly, packaging, or feature work

CP-018 does not need to fix Gradle failures.
It must only define the smallest bounded continuation probe and stop at the first exact post-`libcore.aar` compile-surface result.

## IN_SCOPE
- define the exact post-`libcore.aar` handoff artifact as `android/fork/app/libs/libcore.aar`
- define the smallest safe next build surface as the app module compile consumer path rather than `assemble*`, `bundle*`, or APK work
- define the exact entry command for that probe from `android/fork/`:
  - `.\gradlew.bat :app:compileOssDebugKotlin --stacktrace`
- define the preconditions that must remain unchanged before the probe:
  - `android/sing-box` stays on branch `cp017-local-baseline`
  - `android/sing-box` stays at `aed32ee3066cdbc7d471e3e0415c5134088962df`
  - the already-cleared metadata bridge is reused only as an existing prerequisite inside the bounded `libcore` path
  - the bounded `libcore` path is rerun only until `android/fork/app/libs/libcore.aar` exists
- define the exact evidence to capture during execution:
  - the command and output that shows transient `libcore.aar` production
  - confirmation that `android/fork/app/libs/libcore.aar` exists before Gradle entry
  - the first Gradle task reached after entry
  - the first exact compile-surface failure or the exact success of `:app:compileOssDebugKotlin`
  - whether the failure is still before app source compilation or inside app source compilation against `libcore`
- define toolchain and dependency drift bounds for that probe
- define rollback and cleanup expectations for transient `libcore.aar`, `android/fork/libcore/.build/`, and any disposable gomobile workspace created only to regenerate the transient AAR
- create `docs/android/ANDROID_POST_LIBCORE_CONTINUATION.md`
- update `PROJECT_STATE.md` and `checkpoints/CP-018.md` with execution results

## OUT_OF_SCOPE
- revisiting or re-solving the cleared `libcore` metadata bridge
- revisiting or re-testing the already-validated `android/sing-box` baseline persistence except as a stated prerequisite
- modifying upstream source under `android/fork/`, including Gradle files, app code, or `libcore/`
- broad patches to `gomobile-matsuri`, `gobind-matsuri`, Gradle wiring, generated workspace layout, or dependency trees
- broad Gradle or app build work beyond the first exact compile consumer surface
- `assembleOssDebug`, `assembleOssRelease`, `assemblePreviewRelease`, `bundle*`, signing, shrinking, packaging, or APK generation
- Android product features
- per-app routing implementation
- transport or proxy logic
- dependency upgrades or repairs outside the first observed continuation blocker
- changes to `android/libneko/` or `android/sing-box/` baselines
- server artifacts or server implementation, except `PROJECT_STATE.md` if needed for continuity
- CI/CD changes

## REQUIRED_ARTIFACTS
- `docs/android/ANDROID_POST_LIBCORE_CONTINUATION.md`
- `PROJECT_STATE.md` (updated after CP-018 execution)
- `checkpoints/CP-018.md` (updated after CP-018 execution)

## EXECUTION_NOTES
CP-018 is a continuation-definition checkpoint.
It must not turn into Gradle repair or broader Android build continuation.

Why the next surface is `:app:compileOssDebugKotlin`:
- `android/fork/app/build.gradle.kts` consumes `app/libs/*` through `implementation(fileTree("libs"))`
- app Kotlin sources directly import generated `libcore` and `go` symbols
- the `ossDebug` variant exists from the declared `oss` product flavor plus the standard `debug` build type
- `compileOssDebugKotlin` is narrower than any `assemble*` or `bundle*` task and avoids release signing, shrink, and packaging surfaces

Execution priority for the later CP-018 run:
1. confirm the persisted `android/sing-box` baseline still matches branch `cp017-local-baseline` at `aed32ee3066cdbc7d471e3e0415c5134088962df`
2. rerun only the already-bounded `android/fork/libcore/build.sh` path until it installs transient `android/fork/app/libs/libcore.aar`
3. record that `android/fork/app/libs/libcore.aar` is the exact entry artifact into the downstream app surface
4. enter Gradle exactly once with `.\gradlew.bat :app:compileOssDebugKotlin --stacktrace`
5. capture the first exact Gradle-side outcome and stop
6. do not repair the observed blocker inside CP-018

Exact entry markers CP-018 must record during execution:
- artifact marker: `android/fork/app/libs/libcore.aar`
- command marker: `.\gradlew.bat :app:compileOssDebugKotlin --stacktrace`

Toolchain and dependency drift bounds for CP-018 execution:
- preserve the persisted `android/sing-box` baseline on `cp017-local-baseline` at `aed32ee3066cdbc7d471e3e0415c5134088962df`
- preserve the exact disposable `gomobile-matsuri` source revision used in CP-014 through CP-017 (`17d6af34f6bd6d7e1e428e0c652c8b54a46bda4f`) if a disposable rebuild is still required to regenerate transient `libcore.aar`
- preserve the generated workspace `golang.org/x/mobile v0.0.0-20231108233038-35478a0c49da` baseline already validated in CP-014 through CP-017
- preserve the host generated-workspace Go baseline (`go1.23.6`) unless mere observation proves auto-selection cannot be avoided
- preserve the existing Gradle wrapper baseline (`8.10.2`)
- preserve the existing Android Gradle Plugin baseline (`8.8.1`)
- preserve the existing Kotlin and KSP baselines (`2.0.21` and `2.0.21-1.0.27`)
- preserve the existing JDK 17 and local SDK/NDK baseline already documented in project state
- do not intentionally upgrade Gradle plugins, Kotlin plugins, Go dependencies, or Android dependencies during the probe
- if Gradle, Kotlin, KSP, or Go auto-selects a different version, capture the exact version and trigger as evidence and do not treat that drift as the repair

Allowed probe depth:
- one transient `libcore.aar` regeneration path
- one exact Gradle compile-surface entry
- one captured first failure or success outcome
- if the task graph fails before `:app:compileOssDebugKotlin` executes, capture the first exact failing task and stop
- if `:app:compileOssDebugKotlin` succeeds, stop and name the next exact downstream task without continuing into it

Not allowed:
- rerunning metadata-bridge experimentation
- rerunning sing-box alignment experiments beyond confirming the persisted prerequisite state
- entering `assemble*`, `bundle*`, install, lint, test, or APK publication work
- editing `android/fork/` to chase the first Gradle-side failure
- broad cache deletion or environment reconfiguration beyond what is required to reproduce the bounded entry command

Rollback expectations if the probe fails or is inconclusive:
- leave `android/sing-box` on its persisted CP-017 baseline unless the run itself changed it unexpectedly
- remove any disposable temporary gomobile source workspace created for CP-018
- remove any disposable temporary GOPATH or patched binaries created for CP-018
- remove `android/fork/libcore/.build/` after evidence capture unless the report explicitly preserves it for the first Gradle-side failure evidence
- remove transient copied `android/fork/app/libs/libcore.aar` after evidence capture unless the report explicitly preserves it for the first Gradle-side failure evidence
- leave the default installed `gomobile-matsuri` and `gobind-matsuri` binaries unchanged
- leave all upstream source trees unchanged
- stop without continuing into broader Gradle repair or app build continuation
- document the exact stopping point and cleanup result in `docs/android/ANDROID_POST_LIBCORE_CONTINUATION.md`

## COMPLETION_TEST
CP-018 is complete only if:
- `docs/android/ANDROID_POST_LIBCORE_CONTINUATION.md` exists
- the report identifies `android/fork/app/libs/libcore.aar` as the exact post-`libcore` entry artifact
- the report documents the exact entry command `.\gradlew.bat :app:compileOssDebugKotlin --stacktrace`
- the report confirms the persisted `android/sing-box` prerequisite state before Gradle entry
- the report explicitly states that the cleared metadata bridge was not revisited as a new repair surface
- the report captures the first exact Gradle task failure or the exact success of `:app:compileOssDebugKotlin`
- the report explicitly separates compile-surface probing from broader Gradle repair, assembly, packaging, or feature work
- the report documents the exact commands, exact outputs, and exact toolchain/dependency versions observed
- no upstream source files under `android/fork/` are modified
- no broad patches are applied
- no `assemble*`, `bundle*`, or APK build is attempted
- `PROJECT_STATE.md` is updated after execution

## RESULT
complete

## WHAT_CHANGED
- `docs/android/ANDROID_POST_LIBCORE_CONTINUATION.md` was created with the exact pre-probe environment print, prerequisite `android/sing-box` confirmation, transient `libcore.aar` reproduction command, exact Gradle entry command, direct shell result, daemon-log evidence, sample compiled outputs, and cleanup record.
- CP-018 was executed using the same disposable gomobile prerequisite path already validated by CP-014 and CP-017, without reopening metadata-bridge repair as a new work surface.
- The first exact downstream app consumer surface was execution-validated:
  - entry artifact: `android/fork/app/libs/libcore.aar`
  - entry command: `.\gradlew.bat :app:compileOssDebugKotlin --stacktrace`
  - first meaningful Gradle-side outcome: success, not a blocker

## REMAINING_GAPS
- No gaps remain inside CP-018.
- The next downstream surface after successful Kotlin compilation remains intentionally unexecuted.

## NEXT_CHECKPOINT
Create CP-019 only to define the smallest bounded continuation checkpoint after successful `:app:compileOssDebugKotlin`, most likely targeting `.\gradlew.bat :app:compileOssDebugJavaWithJavac --stacktrace` without broadening into assemble, packaging, or repair work.

## HANDOFF
Current checkpoint status: complete
Safe next action: create CP-019 only, scoped to defining the first exact downstream surface after successful `:app:compileOssDebugKotlin`
Artifacts created or updated:
- checkpoints/CP-018.md
- docs/android/ANDROID_POST_LIBCORE_CONTINUATION.md
- PROJECT_STATE.md
Known risks:
- the next session may incorrectly jump from successful Kotlin compilation into `assemble*`, packaging, or repair work instead of defining the next bounded surface
- the shell adapter returned an empty direct transcript for the Gradle command, so future sessions should preserve both the direct command result and daemon-log evidence if they need exact output continuity
- the persisted `android/sing-box` baseline on `cp017-local-baseline` remains a hard prerequisite for continuity
Resume from:
- docs/android/ANDROID_POST_LIBCORE_CONTINUATION.md
- checkpoints/CP-018.md
- PROJECT_STATE.md
