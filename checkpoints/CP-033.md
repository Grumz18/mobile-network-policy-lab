# CP-033

## CHECKPOINT_ID
CP-033

## TITLE
Correct the post-package boundary and define the smallest APK verification continuation

## CURRENT_STATE
CP-032 definition is complete and CP-032 execution is partial.
The cleared `libcore` metadata bridge remains only an already-validated prerequisite from CP-014.
The persisted `android/sing-box` baseline remains the CP-017 prerequisite on branch `cp017-local-baseline` at `aed32ee3066cdbc7d471e3e0415c5134088962df`.
CP-032 execution proved:
- `.\gradlew.bat :app:packageOssDebug --stacktrace` completed successfully
- success signal lines were present:
  - `> Task :app:packageOssDebug`
  - `BUILD SUCCESSFUL`
  - `EXIT_CODE: 0`
- authored CP-032 success-boundary path did not materialize:
  - `android/fork/app/build/intermediates/apk/oss/debug` remained absent
- actual APK outputs materialized at:
  - `android/fork/app/build/outputs/apk/oss/debug`
- observed artifacts at that path were:
  - `android/fork/app/build/outputs/apk/oss/debug/NekoBox-1.4.2-arm64-v8a-debug.apk`
  - `android/fork/app/build/outputs/apk/oss/debug/NekoBox-1.4.2-armeabi-v7a-debug.apk`
  - `android/fork/app/build/outputs/apk/oss/debug/NekoBox-1.4.2-x86-debug.apk`
  - `android/fork/app/build/outputs/apk/oss/debug/NekoBox-1.4.2-x86_64-debug.apk`
  - `android/fork/app/build/outputs/apk/oss/debug/output-metadata.json`

CP-033 exists only to correct this package-stage boundary and define one bounded continuation immediately after package success.

## TARGET_STATE
The repository has a bounded checkpoint definition that:
- explicitly corrects the package-stage success boundary to `android/fork/app/build/outputs/apk/oss/debug`
- explicitly states that CP-032 already reached the first APK materialization boundary
- defines exactly one next bounded continuation step after package success:
  - APK verification only (no install/launch/runtime continuation)
- defines exact prerequisites, success artifacts/signals, out-of-scope absent checks, fallback evidence sources, and cleanup expectations for that single probe

CP-033 does not execute the probe.
CP-033 does not continue into install, launch, runtime debugging, assemble, or feature work.

## IN_SCOPE
- correct the CP-032 package success boundary to the observed output location:
  - `android/fork/app/build/outputs/apk/oss/debug`
- explicitly record that CP-032 already crossed the first APK materialization boundary
- define one bounded next continuation probe as APK verification only:
  - from `android/fork/`, run:
    - `& "$env:ANDROID_HOME\\build-tools\\35.0.1\\apksigner.bat" verify --verbose --print-certs ".\\app\\build\\outputs\\apk\\oss\\debug\\NekoBox-1.4.2-x86_64-debug.apk"`
- define exact prerequisite checks before that probe:
  - `android/sing-box` remains on branch `cp017-local-baseline`
  - `android/sing-box` `HEAD` remains `aed32ee3066cdbc7d471e3e0415c5134088962df`
  - CP-032 evidence artifacts exist:
    - `docs/android/ANDROID_POST_VALIDATE_SIGNING_CONTINUATION.md`
    - `docs/android/evidence/cp032_packageOssDebug.log`
    - `docs/android/evidence/cp032_packageOssDebug.clean.log`
  - package output artifacts exist:
    - `android/fork/app/build/outputs/apk/oss/debug/output-metadata.json`
    - `android/fork/app/build/outputs/apk/oss/debug/NekoBox-1.4.2-arm64-v8a-debug.apk`
    - `android/fork/app/build/outputs/apk/oss/debug/NekoBox-1.4.2-armeabi-v7a-debug.apk`
    - `android/fork/app/build/outputs/apk/oss/debug/NekoBox-1.4.2-x86-debug.apk`
    - `android/fork/app/build/outputs/apk/oss/debug/NekoBox-1.4.2-x86_64-debug.apk`
- define exact expected success artifact/signal checks for this bounded verification:
  - artifact paths remain present:
    - `android/fork/app/build/outputs/apk/oss/debug/output-metadata.json`
    - `android/fork/app/build/outputs/apk/oss/debug/NekoBox-1.4.2-x86_64-debug.apk`
  - expected command success signal lines include:
    - `Verifies`
    - `Verified using v1 scheme (JAR signing): true`
    - `Verified using v2 scheme (APK Signature Scheme v2): true`
    - `EXIT_CODE: 0`
- define exact out-of-scope paths that must remain absent after the probe:
  - `android/fork/app/build/intermediates/apk/oss/debug`
  - `android/fork/app/build/outputs/apk/oss/release`
  - `android/fork/app/build/outputs/bundle/ossDebug`
  - `android/fork/app/build/outputs/apk/androidTest/oss/debug`
- define exact fallback evidence sources if shell output is incomplete:
  - direct shell transcript from the `apksigner verify` command
  - `docs/android/evidence/cp032_packageOssDebug.log`
  - `docs/android/evidence/cp032_packageOssDebug.clean.log`
  - directory listing and timestamps for `android/fork/app/build/outputs/apk/oss/debug`
  - content snapshot for `android/fork/app/build/outputs/apk/oss/debug/output-metadata.json`
- define exact cleanup expectations for the later CP-033 execution:
  - do not regenerate `libcore.aar`
  - do not create disposable gomobile/GOPATH workspaces
  - remove only temporary verification scratch files if created for CP-033 evidence capture
  - keep produced APK outputs and metadata in place
  - leave upstream source trees unchanged
- create `docs/android/ANDROID_POST_PACKAGE_BOUNDARY_CORRECTION.md` during later execution
- update `PROJECT_STATE.md` and `checkpoints/CP-033.md` during later execution

## OUT_OF_SCOPE
- executing CP-033
- revisiting or re-solving the cleared `libcore` metadata bridge
- revisiting or re-testing the already-validated `android/sing-box` baseline except as a stated prerequisite
- modifying upstream source under `android/fork/`
- applying broad patches to build logic, dependency trees, or generated workspaces
- entering install, launch, runtime debugging, or feature work
- entering broad `assemble*` continuation
- Android product feature implementation
- per-app routing implementation
- transport or proxy logic
- changes to `android/libneko/` or `android/sing-box/` baselines
- server artifacts or server implementation, except `PROJECT_STATE.md` for continuity
- CI/CD changes

## REQUIRED_ARTIFACTS
- `checkpoints/CP-033.md`
- `PROJECT_STATE.md` (updated for CP-033 definition state only)
- `docs/android/ANDROID_POST_PACKAGE_BOUNDARY_CORRECTION.md` (execution artifact for later CP-033 run)

## EXECUTION_NOTES
CP-033 is a boundary-correction definition checkpoint.
It must stay narrow and separate boundary correction from runtime continuation.

Boundary correction from CP-032:
- CP-032 already reached package success and first APK materialization.
- The correct package output boundary is `android/fork/app/build/outputs/apk/oss/debug`.
- `android/fork/app/build/intermediates/apk/oss/debug` must no longer be treated as the package success boundary.

Why the next bounded step is APK verification and not install:
- package outputs already exist and can be verified without device-side state
- install/launch adds new runtime variables and is broader than this correction checkpoint
- `apksigner verify` is the smallest deterministic post-package proof surface

Execution priority for later CP-033 run:
1. verify prerequisites and corrected boundary paths exactly
2. run the single bounded `apksigner verify` probe once
3. capture first exact meaningful outcome and stop
4. do not start install/launch/runtime or broader assemble continuation

First meaningful outcome rules for CP-033:
- if prerequisite checks fail, the first failed prerequisite line is the outcome
- if `apksigner verify` runs and fails, the first exact failure line is the outcome
- if `apksigner verify` succeeds, the exact success signal lines are the outcome
- stop immediately once one of the above is captured

Toolchain drift bounds for later CP-033 run:
- preserve persisted `android/sing-box` baseline at `cp017-local-baseline` / `aed32ee3066cdbc7d471e3e0415c5134088962df`
- preserve existing Android SDK Build Tools baseline `35.0.1`
- do not intentionally upgrade Gradle, AGP, Kotlin, Java, Go, or Android dependencies
- if tooling auto-selects a different path/version, capture exact evidence and stop at first meaningful outcome

## COMPLETION_TEST
CP-033 is complete only if:
- `docs/android/ANDROID_POST_PACKAGE_BOUNDARY_CORRECTION.md` exists after execution
- the report explicitly states CP-032 already reached first APK materialization
- the report explicitly confirms corrected package boundary path:
  - `android/fork/app/build/outputs/apk/oss/debug`
- the report confirms prerequisite checks exactly as defined in `IN_SCOPE`
- the report documents the exact bounded probe command:
  - `& "$env:ANDROID_HOME\\build-tools\\35.0.1\\apksigner.bat" verify --verbose --print-certs ".\\app\\build\\outputs\\apk\\oss\\debug\\NekoBox-1.4.2-x86_64-debug.apk"`
- the report captures first exact outcome from that one probe and stops
- if probe succeeds, the report includes success lines:
  - `Verifies`
  - `Verified using v1 scheme (JAR signing): true`
  - `Verified using v2 scheme (APK Signature Scheme v2): true`
  - `EXIT_CODE: 0`
- the report confirms out-of-scope absent paths listed in `IN_SCOPE` remain absent
- the report identifies fallback evidence used if shell output is incomplete
- the report confirms cleanup expectations were followed
- no upstream source files under `android/fork/` are modified
- no install/launch/runtime or broad assemble continuation is attempted
- `PROJECT_STATE.md` is updated after execution

## RESULT
complete

## WHAT_CHANGED
- `docs/android/ANDROID_POST_PACKAGE_BOUNDARY_CORRECTION.md` was created with bounded CP-033 execution evidence.
- CP-033 prerequisite checks were executed exactly as authored, including:
  - `JAVA_HOME` and `java -version`
  - `android/sing-box` branch/commit confirmation (`cp017-local-baseline` / `aed32ee3066cdbc7d471e3e0415c5134088962df`)
  - CP-032 evidence artifact presence checks
  - corrected output-boundary checks at `android/fork/app/build/outputs/apk/oss/debug`
- CP-032 first APK materialization at the corrected boundary was explicitly re-validated from report evidence.
- The bounded probe command was executed once:
  - `& "$env:ANDROID_HOME\\build-tools\\35.0.1\\apksigner.bat" verify --verbose --print-certs ".\\app\\build\\outputs\\apk\\oss\\debug\\NekoBox-1.4.2-x86_64-debug.apk"`
- The first exact meaningful outcome was probe success with required lines:
  - `Verifies`
  - `Verified using v1 scheme (JAR signing): true`
  - `Verified using v2 scheme (APK Signature Scheme v2): true`
  - `EXIT_CODE: 0`
- Expected success artifacts remained present and out-of-scope paths remained absent.
- CP-033 cleanup expectations were satisfied with no disposable gomobile/GOPATH or scratch workspace creation.
- `PROJECT_STATE.md` was updated so the next eligible work is defining CP-034 only.

## REMAINING_GAPS
- No gaps remain inside CP-033.
- The next bounded continuation after successful APK verification has not been defined yet.

## NEXT_CHECKPOINT
Create CP-034 only to define the smallest bounded continuation after successful CP-033 APK verification, most likely a single bounded install-verification probe, without entering launch/runtime-debugging/feature work.

## HANDOFF
Current checkpoint status: complete
Safe next action: create CP-034 only, scoped to defining one bounded post-APK-verification continuation step
Artifacts created or updated:
- checkpoints/CP-033.md
- docs/android/ANDROID_POST_PACKAGE_BOUNDARY_CORRECTION.md
- PROJECT_STATE.md
Known risks:
- scope will drift if install/launch/runtime-debugging or broader assemble continuation starts before CP-034 is defined
- versioned APK filenames may drift between checkpoints and could invalidate hardcoded probe paths
- shell sessions may not have `ANDROID_HOME` set by default; future checkpoints should verify tool path resolution before execution
Resume from:
- checkpoints/CP-033.md
- docs/android/ANDROID_POST_PACKAGE_BOUNDARY_CORRECTION.md
- PROJECT_STATE.md
