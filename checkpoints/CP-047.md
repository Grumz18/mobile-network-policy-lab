# CP-047

## CHECKPOINT_ID
CP-047

## TITLE
Define bounded filesystem-based interface verification after CP-045 `ip` permission failures

## CURRENT_STATE
CP-045 interface verification failed at probe stage with persistent `ip` utility transport/permission error:
- `request send failed: Permission denied`

CP-046 then verified minimal adb shell transport stability (`complete`), so the next exact bounded continuation is to verify interface existence through a non-`ip` surface.

CP-046 execution artifact exists:
- `docs/android/ANDROID_ADB_SHELL_TRANSPORT_STABILITY.md`

## TARGET_STATE
The repository has a bounded CP-047 definition for interface verification via filesystem visibility, bypassing the restricted `ip` binary.

Single bounded probe for later execution:
- `adb shell "ls /sys/class/net/"`

After later CP-047 execution, a future session must be able to answer:
- whether `/sys/class/net/` is readable from adb shell
- whether tun/tap-related interface entries are visible without `ip`/`ifconfig`
- what first exact meaningful outcome was observed

CP-047 does not execute verification in this step.

## IN_SCOPE
- identify new bounded interface-verification surface that bypasses failing `ip` binary:
  - hypothesis: `ip` utility is restricted for shell user, while `/sys/class/net/` filesystem state may remain readable
- define one bounded probe for later CP-047 execution:
  - `adb shell "ls /sys/class/net/"`
- define exact prerequisite checks before probe entry:
  - CP-046 execution artifact exists:
    - `docs/android/ANDROID_ADB_SHELL_TRANSPORT_STABILITY.md`
  - `adb devices -l` shows exactly one online target with `device` state
- define exact expected success signals:
  - probe output is non-empty
  - output contains one of: `tun0`, `tun`, `tap`, `tap0`
  - `EXIT_CODE: 0`
  - no `Permission denied` errors
- define first meaningful outcome rules for later CP-047 execution:
  - if prerequisite fails: record first failed prerequisite line as outcome
  - if probe returns transport/permission failure: record exact failure line as outcome
  - if probe runs but expected interface signal is absent: record first exact failure line as outcome
  - if probe returns required signal(s): record success signal lines as outcome
  - stop immediately after first meaningful outcome
- define exact fallback evidence sources if direct shell output is incomplete:
  - direct shell transcript of `ls /sys/class/net/`
  - captured clean transcript file
- define exact cleanup expectations:
  - retain canonical evidence files
  - do not modify upstream source
- explicitly separate filesystem verification from tool execution (`ip`/`ifconfig`)
- define expected later execution artifact:
  - `docs/android/ANDROID_FILESYSTEM_INTERFACE_VERIFICATION.md`
- update `PROJECT_STATE.md` and `checkpoints/CP-047.md` for definition-only continuity

## OUT_OF_SCOPE
- executing CP-047
- revisiting previous build-chain checkpoints
- modifying upstream source under `android/fork/`
- applying broad patches
- re-execution of `ip link show` or `ifconfig`
- `ping`, `curl`, or traffic generation
- `su` or privilege escalation attempts
- UI interaction commands
- network actions
- runtime debugging
- Android product feature implementation
- per-app routing implementation
- transport/proxy logic implementation
- server artifact changes except `PROJECT_STATE.md` for continuity

## REQUIRED_ARTIFACTS
- `checkpoints/CP-047.md`
- `PROJECT_STATE.md` (updated for CP-047 definition status and next eligible work)
- `docs/android/ANDROID_FILESYSTEM_INTERFACE_VERIFICATION.md` (execution artifact for later CP-047 run)

## EXECUTION_NOTES
CP-047 is definition-only.
It defines a filesystem-read surface for interface existence verification and intentionally avoids restricted network tooling.

Execution priority for later CP-047 run:
1. verify prerequisites
2. run exactly one bounded filesystem probe
3. capture first exact meaningful outcome and stop
4. avoid all out-of-scope actions

## COMPLETION_TEST
CP-047 definition is complete only if:
- `checkpoints/CP-047.md` exists and follows checkpoint structure
- new bounded surface is explicit (`ls /sys/class/net/`) and separated from `ip`/`ifconfig`
- prerequisites are explicit and minimal (CP-046 artifact + single online adb device state)
- expected success signals are explicit (`non-empty`, tun/tap indicator, `EXIT_CODE: 0`, no `Permission denied`)
- out-of-scope boundaries prohibit `ip/ifconfig` reruns, escalation, network/UI/debugging scope
- fallback evidence sources are explicit (direct + clean transcript)
- cleanup expectations are conservative and explicit
- `docs/android/ANDROID_FILESYSTEM_INTERFACE_VERIFICATION.md` is named as later execution artifact
- `PROJECT_STATE.md` is updated so next eligible work is executing CP-047 only
- `RESULT` remains `pending` because CP-047 is authored only and not executed

## RESULT
pending

## WHAT_CHANGED
- Authored CP-047 as definition-only checkpoint for filesystem-based interface verification after `ip` permission failures.
- Defined single bounded probe (`ls /sys/class/net/`), exact prerequisites, expected success signals, first-outcome stop rule, fallback evidence, and cleanup boundaries.
- Preserved strict separation from `ip`/`ifconfig`, network activity, UI interaction, runtime debugging, and implementation scope.
- Declared expected later execution artifact:
  - `docs/android/ANDROID_FILESYSTEM_INTERFACE_VERIFICATION.md`
- No CP-047 execution was performed.

## REMAINING_GAPS
- CP-047 execution is pending.
- No CP-047 execution evidence exists yet because this step is authoring-only.

## NEXT_CHECKPOINT
Execute CP-047 only in bounded scope as defined in this file.

## HANDOFF
Current checkpoint status: pending
Safe next action: execute CP-047 only for bounded filesystem-based interface verification
Artifacts created or updated:
- checkpoints/CP-047.md
- PROJECT_STATE.md
Known risks:
- ADB transport stability can regress between sessions and invalidate assumptions
- filesystem view may omit tun/tap entries depending on current VPN/service state
- scope drift if work expands to tool/runtime debugging instead of bounded verification
Resume from:
- checkpoints/CP-047.md
- checkpoints/CP-046.md
- docs/android/ANDROID_ADB_SHELL_TRANSPORT_STABILITY.md
- docs/android/evidence/cp045_retry_probe.log
- PROJECT_STATE.md
