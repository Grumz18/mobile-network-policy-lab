# CP-020

## CHECKPOINT_ID
CP-020

## TITLE
Define the smallest bounded post-javac continuation probe

## CURRENT_STATE
CP-019 is complete.
The generated `libcore` metadata bridge is already repaired and validated in `docs/android/ANDROID_LIBCORE_METADATA_BRIDGE_REPAIR.md`.
That earlier checkpoint proved:
- the generated fallback `go.mod` can preserve `replace libcore`
- the generated fallback can preserve the local replacement mappings for `github.com/matsuridayo/libneko` and `github.com/sagernet/sing-box`
- the generated fallback can preserve the original `golang.org/x/mobile` baseline (`v0.0.0-20231108233038-35478a0c49da`)
- the exact bare `libcore` import failure from CP-013 is cleared

CP-017 then intentionally persisted the validated `android/sing-box` alignment in `docs/android/ANDROID_SINGBOX_BASELINE_PERSIST.md`.
That checkpoint proved:
- the local `android/sing-box` baseline is intentionally tracked on branch `cp017-local-baseline`
- that persisted baseline points to commit `aed32ee3066cdbc7d471e3e0415c5134088962df`
- the same bounded `android/fork/libcore/build.sh` path still proceeds through transient `android/fork/app/libs/libcore.aar` production under that persisted baseline

CP-018 then executed the first exact downstream app consumer probe in `docs/android/ANDROID_POST_LIBCORE_CONTINUATION.md`.
That checkpoint proved:
- the exact post-libcore handoff artifact remains `android/fork/app/libs/libcore.aar`
- the exact first downstream app consumer command is `.\gradlew.bat :app:compileOssDebugKotlin --stacktrace`
- the bounded Kotlin compile consumer surface succeeds under the persisted `android/sing-box` baseline
- no blocker appears at that Kotlin compile surface

CP-019 then executed the next exact downstream app consumer probe in `docs/android/ANDROID_POST_KOTLIN_CONTINUATION.md`.
That checkpoint proved:
- the prerequisite artifact before the next downstream entry still remains `android/fork/app/libs/libcore.aar`
- the exact Java compile consumer command is `.\gradlew.bat :app:compileOssDebugJavaWithJavac --stacktrace`
- the bounded Java compile consumer surface succeeds under the persisted `android/sing-box` baseline
- javac outputs now exist under:
  - `android/fork/app/build/intermediates/javac/ossDebug/compileOssDebugJavaWithJavac/classes/`
  - `android/fork/app/build/tmp/compileOssDebugJavaWithJavac/`
- broader downstream outputs still did not exist after CP-019:
  - `android/fork/app/build/intermediates/compile_app_classes_jar/ossDebug`
  - `android/fork/app/build/intermediates/project_dex_archive/ossDebug`

The next downstream surface is now identifiable from local task metadata and CP-019 evidence:
- the first absent downstream output family after javac is `android/fork/app/build/intermediates/compile_app_classes_jar/ossDebug`
- a local Gradle task listing from `android/fork/` shows the exact app-side jar tasks for this variant:
  - `bundleOssDebugClassesToCompileJar`
  - `bundleOssDebugClassesToRuntimeJar`
- `bundleOssDebugClassesToCompileJar` is the narrower first continuation surface because it aligns to the absent `compile_app_classes_jar/ossDebug` output family observed after CP-019
- `bundleOssDebugClassesToRuntimeJar` is downstream of that and must remain out of scope for this checkpoint

No upstream source under `android/fork/` has been modified.
The default installed `gomobile-matsuri` and `gobind-matsuri` binaries remain unchanged.

## TARGET_STATE
The repository has a bounded checkpoint definition for probing only the first exact downstream build surface after successful `.\gradlew.bat :app:compileOssDebugJavaWithJavac --stacktrace`.

After CP-020 execution, a future session must be able to answer:
- whether `.\gradlew.bat :app:bundleOssDebugClassesToCompileJar --stacktrace` is the correct next exact downstream consumer surface after successful javac
- what exact prerequisite artifact and prerequisite baseline must still be true before entering that class-jar merge surface
- what exact first meaningful outcome appears when that single class-jar probe is entered
- whether the first new outcome is:
  - a task-graph failure before compile-jar merge starts
  - a failure inside `:app:bundleOssDebugClassesToCompileJar`
  - or a successful compile-jar continuation signal
- how that probe is kept separate from runtime-jar merge, dexing, packaging, assemble, or repair work

CP-020 does not need to fix class-jar merge failures.
It must only define the smallest bounded continuation probe after successful javac and stop at the first exact downstream compile-jar result.

## IN_SCOPE
- select the exact downstream build surface after successful `:app:compileOssDebugJavaWithJavac`
- document why `.\gradlew.bat :app:bundleOssDebugClassesToCompileJar --stacktrace` is the correct next bounded surface:
  - CP-019 already execution-validated javac success
  - the first absent downstream output family after CP-019 is `android/fork/app/build/intermediates/compile_app_classes_jar/ossDebug`
  - the local Gradle task listing exposes `bundleOssDebugClassesToCompileJar` as the matching variant-specific compile-jar task
  - this surface is narrower than `bundleOssDebugClassesToRuntimeJar`, dexing, packaging, or any `assemble*` task
- define the exact entry command for the probe from `android/fork/`:
  - `.\gradlew.bat :app:bundleOssDebugClassesToCompileJar --stacktrace`
- define the preconditions that must remain unchanged before the probe:
  - `android/sing-box` stays on branch `cp017-local-baseline`
  - `android/sing-box` stays at `aed32ee3066cdbc7d471e3e0415c5134088962df`
  - the cleared metadata bridge remains only an existing prerequisite reused through the bounded `libcore` path
  - transient `android/fork/app/libs/libcore.aar` is reproduced again before Gradle entry because CP-019 cleanup removed it
- define the exact evidence to capture during execution:
  - confirmation that the persisted `android/sing-box` prerequisite state is unchanged
  - the command and output that re-establish transient `android/fork/app/libs/libcore.aar`
  - the exact `.\gradlew.bat :app:bundleOssDebugClassesToCompileJar --stacktrace` invocation
  - the first task-graph failure before compile-jar merge, if one appears
  - the first exact failure line inside `:app:bundleOssDebugClassesToCompileJar`, if the task starts and fails
  - or the exact success signal for `:app:bundleOssDebugClassesToCompileJar` plus evidence that `android/fork/app/build/intermediates/compile_app_classes_jar/ossDebug` now exists
- define what counts as the first meaningful outcome:
  - first failing task before compile-jar merge starts
  - first exact failure inside `:app:bundleOssDebugClassesToCompileJar`
  - or exact success of `:app:bundleOssDebugClassesToCompileJar`
- define toolchain and dependency drift bounds for that probe
- define rollback and cleanup expectations for transient `libcore.aar`, `android/fork/libcore/.build/`, and any disposable gomobile workspace recreated only to regenerate the transient AAR
- create `docs/android/ANDROID_POST_JAVAC_CONTINUATION.md`
- update `PROJECT_STATE.md` and `checkpoints/CP-020.md` with execution results

## OUT_OF_SCOPE
- revisiting or re-solving the cleared `libcore` metadata bridge
- revisiting or re-testing the already-validated `android/sing-box` baseline except as a stated prerequisite
- modifying upstream source under `android/fork/`, including Gradle files, app code, Java sources, Kotlin sources, or `libcore/`
- broad patches to `gomobile-matsuri`, `gobind-matsuri`, Gradle wiring, generated workspace layout, or dependency trees
- broad Gradle or app build work beyond the single compile-jar consumer surface
- `compileOssDebugKotlin` or `compileOssDebugJavaWithJavac` as new probe surfaces, except if Gradle reruns them automatically as prerequisites while entering `bundleOssDebugClassesToCompileJar`
- `bundleOssDebugClassesToRuntimeJar` as a probe surface in CP-020
- `assemble*`, `bundle*` targets other than the exact compile-jar task above, install, lint, test, dexing, resource packaging, signing, or APK generation
- Android product features
- per-app routing implementation
- transport or proxy logic
- dependency upgrades or repairs outside the first observed compile-jar continuation blocker
- changes to `android/libneko/` or `android/sing-box/` baselines
- server artifacts or server implementation, except `PROJECT_STATE.md` if needed for continuity
- CI/CD changes

## REQUIRED_ARTIFACTS
- `docs/android/ANDROID_POST_JAVAC_CONTINUATION.md`
- `PROJECT_STATE.md` (updated after CP-020 execution)
- `checkpoints/CP-020.md` (updated after CP-020 execution)

## EXECUTION_NOTES
CP-020 is a continuation-definition checkpoint.
It must not turn into runtime-jar continuation, dexing, packaging, assemble, or repair work.

Why the next surface is `:app:bundleOssDebugClassesToCompileJar`:
- CP-019 proved that Java compilation is no longer the active downstream boundary
- the first absent downstream output family after CP-019 is `android/fork/app/build/intermediates/compile_app_classes_jar/ossDebug`
- the local Gradle task listing exposes `bundleOssDebugClassesToCompileJar` as the exact variant-specific compile-jar task
- compile-jar merge is narrower than runtime-jar merge, dexing, packaging, or any `assemble*` task

Execution priority for the later CP-020 run:
1. confirm the persisted `android/sing-box` baseline still matches branch `cp017-local-baseline` at `aed32ee3066cdbc7d471e3e0415c5134088962df`
2. rerun only the already-bounded `android/fork/libcore/build.sh` path until it installs transient `android/fork/app/libs/libcore.aar`
3. record that transient `android/fork/app/libs/libcore.aar` is again present before Gradle entry
4. enter Gradle exactly once with `.\gradlew.bat :app:bundleOssDebugClassesToCompileJar --stacktrace`
5. capture the first exact downstream outcome and stop
6. do not repair the observed blocker inside CP-020

Exact entry markers CP-020 must record during execution:
- artifact marker: `android/fork/app/libs/libcore.aar`
- command marker: `.\gradlew.bat :app:bundleOssDebugClassesToCompileJar --stacktrace`

First meaningful outcome rules for CP-020:
- if the Gradle task graph fails before `:app:bundleOssDebugClassesToCompileJar` starts, the first failing task is the checkpoint outcome
- if `:app:bundleOssDebugClassesToCompileJar` starts and emits a failure, the first exact failure line is the checkpoint outcome
- if `:app:bundleOssDebugClassesToCompileJar` succeeds, that success plus creation of `android/fork/app/build/intermediates/compile_app_classes_jar/ossDebug` is the checkpoint outcome
- once one of those outcomes is captured conclusively, stop without continuing into runtime-jar merge, dex, or assemble work

Toolchain and dependency drift bounds for CP-020 execution:
- preserve the persisted `android/sing-box` baseline on `cp017-local-baseline` at `aed32ee3066cdbc7d471e3e0415c5134088962df`
- preserve the exact disposable `gomobile-matsuri` source revision used in CP-014 through CP-019 (`17d6af34f6bd6d7e1e428e0c652c8b54a46bda4f`) if a disposable rebuild is still required to regenerate transient `libcore.aar`
- preserve the generated workspace `golang.org/x/mobile v0.0.0-20231108233038-35478a0c49da` baseline already validated in CP-014 through CP-019
- preserve the host generated-workspace Go baseline (`go1.23.6`) unless mere observation proves auto-selection cannot be avoided
- preserve the existing Gradle wrapper baseline (`8.10.2`)
- preserve the existing Android Gradle Plugin baseline (`8.8.1`)
- preserve the existing Kotlin and KSP baselines (`2.0.21` and `2.0.21-1.0.27`)
- preserve the existing JDK 17 and local SDK/NDK baseline already documented in project state
- do not intentionally upgrade Gradle plugins, Kotlin plugins, Java toolchains, Go dependencies, or Android dependencies during the probe
- if Gradle, Kotlin, Java, KSP, or Go auto-selects a different version, capture the exact version and trigger as evidence and do not treat that drift as the repair

Allowed probe depth:
- one transient `libcore.aar` regeneration path
- one exact Gradle compile-jar entry
- one captured first failure or success outcome
- if Gradle reruns upstream prerequisite tasks automatically while entering `bundleOssDebugClassesToCompileJar`, record them only as prerequisite activity and keep the checkpoint focused on the first new downstream outcome
- if `:app:bundleOssDebugClassesToCompileJar` succeeds, stop and name the next exact downstream task without continuing into it

Not allowed:
- rerunning metadata-bridge experimentation
- rerunning sing-box alignment experiments beyond confirming the persisted prerequisite state
- entering `bundleOssDebugClassesToRuntimeJar`, dex, packaging, `assemble*`, install, lint, or test work
- editing `android/fork/` to chase the first compile-jar-side failure
- broad cache deletion or environment reconfiguration beyond what is required to reproduce the bounded entry command

Rollback expectations if the probe fails or is inconclusive:
- leave `android/sing-box` on its persisted CP-017 baseline unless the run itself changed it unexpectedly
- remove any disposable temporary gomobile source workspace created for CP-020
- remove any disposable temporary GOPATH or patched binaries created for CP-020
- remove `android/fork/libcore/.build/` after evidence capture unless the report explicitly preserves it for the first downstream failure evidence
- remove transient copied `android/fork/app/libs/libcore.aar` after evidence capture unless the report explicitly preserves it for the first downstream failure evidence
- leave the default installed `gomobile-matsuri` and `gobind-matsuri` binaries unchanged
- leave all upstream source trees unchanged
- stop without continuing into runtime-jar merge, dexing, packaging, or broader build continuation
- document the exact stopping point and cleanup result in `docs/android/ANDROID_POST_JAVAC_CONTINUATION.md`

## COMPLETION_TEST
CP-020 is complete only if:
- `docs/android/ANDROID_POST_JAVAC_CONTINUATION.md` exists
- the report documents why `.\gradlew.bat :app:bundleOssDebugClassesToCompileJar --stacktrace` is the correct next exact downstream surface
- the report identifies `android/fork/app/libs/libcore.aar` as the prerequisite entry artifact before Gradle entry
- the report documents the exact compile-jar entry command `.\gradlew.bat :app:bundleOssDebugClassesToCompileJar --stacktrace`
- the report confirms the persisted `android/sing-box` prerequisite state before Gradle entry
- the report explicitly states that the cleared metadata bridge was not revisited as a new repair surface
- the report captures the first exact task-graph failure, first exact compile-jar failure, or the exact success of `:app:bundleOssDebugClassesToCompileJar`
- the report explicitly separates compile-jar probing from runtime-jar merge, dexing, packaging, or assemble work
- the report documents the exact commands, exact outputs, and exact toolchain/dependency versions observed
- no upstream source files under `android/fork/` are modified
- no broad patches are applied
- no runtime-jar, dex, install, packaging, or `assemble*` work is attempted
- `PROJECT_STATE.md` is updated after execution

## RESULT
partial

## WHAT_CHANGED
- `checkpoints/CP-020.md` defines the first exact bounded continuation after successful `.\gradlew.bat :app:compileOssDebugJavaWithJavac --stacktrace`.
- The next downstream surface is narrowed to the compile-jar merge task `.\gradlew.bat :app:bundleOssDebugClassesToCompileJar --stacktrace`.
- The checkpoint explicitly separates compile-jar probing from runtime-jar merge, dexing, packaging, assemble, and repair work.

## REMAINING_GAPS
- CP-020 has not been executed.
- `docs/android/ANDROID_POST_JAVAC_CONTINUATION.md` does not exist yet.
- The first exact compile-jar-side outcome after successful javac is still unobserved.

## NEXT_CHECKPOINT
Execute CP-020 only to probe `.\gradlew.bat :app:bundleOssDebugClassesToCompileJar --stacktrace` and stop at the first exact compile-jar-side outcome.

## HANDOFF
Current checkpoint status: defined only, not executed
Safe next action: execute CP-020 exactly as written and stop at the first exact outcome from `.\gradlew.bat :app:bundleOssDebugClassesToCompileJar --stacktrace`
Artifacts expected on execution:
- docs/android/ANDROID_POST_JAVAC_CONTINUATION.md
- checkpoints/CP-020.md
- PROJECT_STATE.md
Known risks:
- the next session may incorrectly skip the compile-jar surface and jump into runtime-jar merge, dexing, packaging, or `assemble*`
- the persisted `android/sing-box` baseline on `cp017-local-baseline` remains a hard prerequisite for continuity
- future Gradle probes may again produce incomplete direct shell transcripts, in which case daemon-log evidence should be preserved
Resume from:
- docs/android/ANDROID_POST_KOTLIN_CONTINUATION.md
- checkpoints/CP-020.md
- PROJECT_STATE.md
