# Android Config Boundary

`android/config/` is reserved for future Android integration configuration assets.

At the current checkpoint:
- no secret material belongs here
- no SDK-specific configuration is committed
- no build-system or signing configuration is defined

Future checkpoints may add structured configuration files here after the Android workspace baseline is accepted.
