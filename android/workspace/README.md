# Android Workspace Skeleton

`android/workspace/` is reserved for the future Android project structure.

At the current checkpoint:
- no application modules are created
- no Android source code is added
- no manifest, resource, or Gradle module files are added

Future checkpoints may materialize the actual Android workspace here only after the local build baseline is accepted and implementation is explicitly authorized.
