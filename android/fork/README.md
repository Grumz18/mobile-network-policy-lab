# Android Fork Boundary

`android/fork/` is reserved for future upstream Android fork intake.

At the current checkpoint:
- no fork has been fetched
- no upstream repository has been materialized here
- no synchronization workflow is implemented

Future checkpoints may populate this area only after the fork intake process is explicitly authorized and documented.
