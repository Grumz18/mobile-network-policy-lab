# Android Patches Boundary

`android/patches/` is reserved for repository-owned Android modifications and patch workflow notes.

At the current checkpoint:
- no patch files exist
- no local Android modifications are defined
- no per-app routing or transport changes are implemented

Future checkpoints may add patch structure here only after fork intake and workspace layout are established.
