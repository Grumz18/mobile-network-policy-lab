# Server Config Boundary

`server/config/` is reserved for future server configuration assets.

At the current checkpoint:
- no secret material belongs here
- no production deployment configuration is defined
- no transport-specific configuration is defined

Future checkpoints may add structured configuration files after the server runtime and control-surface assumptions are documented.
