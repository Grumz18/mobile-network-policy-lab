# Server Source Boundary

`server/src/` is reserved for future server application code.

At the current checkpoint:
- no source files are implemented
- no transport behavior is defined
- no framework-specific entrypoint is selected

Future implementation checkpoints should place only authorized server code here.
